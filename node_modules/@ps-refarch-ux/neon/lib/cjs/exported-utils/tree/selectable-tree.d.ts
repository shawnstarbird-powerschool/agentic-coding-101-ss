import { type TreeNode } from '../../interfaces/tree-node';
export declare class SelectableTree<T extends TreeNode> {
    treeNodes: Array<T>;
    parentNodeStatus: Record<string, boolean | 'indeterminate'>;
    leafNodeStatus: Record<string, boolean>;
    treeNodeParents: Record<string, T>;
    disabledItems: Record<string, boolean>;
    constructor(tree: Array<T>, disabledItems?: Record<string, boolean>, leafNodeStatus?: Record<string, boolean>);
    setTreeNodes(treeNodes: Array<T>): void;
    setDisabledItems(value: Record<string, boolean>): void;
    /**
     * Sets and individual status of the tree.
     * @param treeNode
     * @param isChecked
     */
    setStatus(treeNode: T, isChecked: boolean): void;
    setStatusDownTheTree(treeNode: T, isChecked: boolean): void;
    setStatusUpTheTree(treeNode: T | undefined): void;
    isNodeSelected(treeNode: T): boolean;
    isNodeIndeterminate(treeNode: T): boolean;
    setLeafNodeStatusMap(value: Record<string, boolean>): void;
    private rebuildParentNodeStatus;
    getParent(treeNode: T): T | undefined;
    /**
     * Gets the node status of treeNode.  If treeNode is a parent, it will
     * also set the status in this.parentNodeStatus[treeNode.id]
     *
     * NOTE: The first time this method was written, there were performance issues so be careful
     * when modifying this.
     */
    calculateNodeStatus(treeNode: T): boolean | 'indeterminate';
}
