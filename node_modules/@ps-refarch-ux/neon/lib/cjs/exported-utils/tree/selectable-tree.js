"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SelectableTree = void 0;
var _exportedUtils = require("../../exported-utils");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _createForOfIteratorHelper(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (!it) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = it.call(o); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var SelectableTree = /*#__PURE__*/function () {
  function SelectableTree(tree, disabledItems, leafNodeStatus) {
    _classCallCheck(this, SelectableTree);
    //Holds the values for the check
    _defineProperty(this, "parentNodeStatus", {});
    //Holds the true/false values for the leaves
    //For this file, a leaf is defined as an node with no children.
    _defineProperty(this, "leafNodeStatus", {});
    _defineProperty(this, "treeNodeParents", {});
    _defineProperty(this, "disabledItems", {});
    if (disabledItems) {
      this.disabledItems = disabledItems;
    }
    if (leafNodeStatus != null) {
      this.leafNodeStatus = leafNodeStatus;
    }
    this.setTreeNodes(tree);
  }
  _createClass(SelectableTree, [{
    key: "setTreeNodes",
    value: function setTreeNodes(treeNodes) {
      var _this = this;
      this.treeNodes = treeNodes;
      this.treeNodeParents = {};

      //Populate the parent map.
      //Also check for duplicates.
      var idsMap = {};
      (0, _exportedUtils.traverseTree)(this.treeNodes, function (treeNode, parentNode) {
        if (idsMap[treeNode.id]) {
          console.error("It appears there is a duplicate id: ".concat(treeNode.id, " in your tree."));
        }
        idsMap[treeNode.id] = true;
        if (parentNode != null) {
          _this.treeNodeParents[treeNode.id] = parentNode;
        }
      });
    }
  }, {
    key: "setDisabledItems",
    value: function setDisabledItems(value) {
      this.disabledItems = value;
      this.rebuildParentNodeStatus();
    }

    /**
     * Sets and individual status of the tree.
     * @param treeNode
     * @param isChecked
     */
  }, {
    key: "setStatus",
    value: function setStatus(treeNode, isChecked) {
      //set status down the tree
      this.setStatusDownTheTree(treeNode, isChecked);
      //set status up the tree
      this.setStatusUpTheTree(treeNode);
    }

    //Sets the status of the tree Node, and all of it's descendants to the value isChecked.
  }, {
    key: "setStatusDownTheTree",
    value: function setStatusDownTheTree(treeNode, isChecked) {
      if (this.disabledItems[treeNode.id]) {
        //Disabled nodes are treated like they do not exist.
        return;
      }
      if (treeNode.children != null) {
        this.parentNodeStatus[treeNode.id] = isChecked;
        var _iterator = _createForOfIteratorHelper(treeNode.children),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var childNode = _step.value;
            this.setStatusDownTheTree(childNode, isChecked);
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      } else {
        this.leafNodeStatus[treeNode.id] = isChecked;
      }
    }

    //Sets the value of all of the treeNode and all of the
    //treeNode's ancestors to the proper status based on
    //the status of each ancestor's direct children.
  }, {
    key: "setStatusUpTheTree",
    value: function setStatusUpTheTree(treeNode) {
      if (treeNode == null || this.disabledItems[treeNode.id]) {
        return;
      }
      if (treeNode.children == null) {
        //If a leaf node is passed in, set the status of the parent.
        this.setStatusUpTheTree(this.getParent(treeNode));
        return;
      }
      var checked = 0;
      var unchecked = 0;
      var isIndeterminate = false;
      var _iterator2 = _createForOfIteratorHelper(treeNode.children),
        _step2;
      try {
        loopOnChildren: for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var childNode = _step2.value;
          if (this.disabledItems[childNode.id]) {
            continue;
          }
          if (childNode.children != null) {
            var status = this.parentNodeStatus[childNode.id] || false;
            if (status === true) {
              checked++;
            } else if (status === false) {
              unchecked++;
            } else {
              //status must be indeterminate
              isIndeterminate = true;
              break loopOnChildren; //We are done looping.
            }
          } else {
            if (this.leafNodeStatus[childNode.id]) {
              checked++;
            } else {
              unchecked++;
            }
          }
          if (checked > 0 && unchecked > 0) {
            //status must be indeterminate
            isIndeterminate = true;
            break loopOnChildren; //We are done looping.
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      if (isIndeterminate) {
        this.parentNodeStatus[treeNode.id] = 'indeterminate';
      } else if (checked > 0) {
        this.parentNodeStatus[treeNode.id] = true;
      } else if (unchecked > 0) {
        this.parentNodeStatus[treeNode.id] = false;
      }
      var parentNode = this.treeNodeParents[treeNode.id];
      if (parentNode != null) {
        this.setStatusUpTheTree(parentNode);
      }
    }
  }, {
    key: "isNodeSelected",
    value: function isNodeSelected(treeNode) {
      if (treeNode.children != null) {
        return this.parentNodeStatus[treeNode.id] === true;
      } else {
        return this.leafNodeStatus[treeNode.id];
      }
    }
  }, {
    key: "isNodeIndeterminate",
    value: function isNodeIndeterminate(treeNode) {
      if (treeNode.children != null) {
        return this.parentNodeStatus[treeNode.id] === 'indeterminate';
      } else {
        return false; //leaf nodes can only be true or false
      }
    }
  }, {
    key: "setLeafNodeStatusMap",
    value: function setLeafNodeStatusMap(value) {
      //Set the new leaf statuses
      this.leafNodeStatus = value;
      this.rebuildParentNodeStatus();
    }
  }, {
    key: "rebuildParentNodeStatus",
    value: function rebuildParentNodeStatus() {
      var _iterator3 = _createForOfIteratorHelper(this.treeNodes),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var childNode = _step3.value;
          this.calculateNodeStatus(childNode);
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
    }
  }, {
    key: "getParent",
    value: function getParent(treeNode) {
      return this.treeNodeParents[treeNode.id];
    }

    /**
     * Gets the node status of treeNode.  If treeNode is a parent, it will
     * also set the status in this.parentNodeStatus[treeNode.id]
     *
     * NOTE: The first time this method was written, there were performance issues so be careful
     * when modifying this.
     */
  }, {
    key: "calculateNodeStatus",
    value: function calculateNodeStatus(treeNode) {
      if (treeNode.children == null) {
        return this.leafNodeStatus[treeNode.id] || false;
      } else {
        var nodes = 0;
        var checkedNodes = 0;
        var uncheckedNodes = 0;

        //Since this method not only gets the state, it also sets it,
        //We have to loop over all of the nodes and not short circuit
        //even if we know the state must be indeterminate.
        var _iterator4 = _createForOfIteratorHelper(treeNode.children),
          _step4;
        try {
          for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
            var childNode = _step4.value;
            var childStatus = this.calculateNodeStatus(childNode);
            if (this.disabledItems[childNode.id]) {
              continue;
            }
            nodes++;
            if (childStatus === true) {
              checkedNodes++;
            } else if (childStatus === false) {
              uncheckedNodes++;
            }
          }
        } catch (err) {
          _iterator4.e(err);
        } finally {
          _iterator4.f();
        }
        if (nodes === uncheckedNodes) {
          //This is also the correct case for when nodes === 0.
          this.parentNodeStatus[treeNode.id] = false;
        } else if (nodes === checkedNodes) {
          this.parentNodeStatus[treeNode.id] = true;
        } else {
          this.parentNodeStatus[treeNode.id] = 'indeterminate';
        }
        return this.parentNodeStatus[treeNode.id];
      }
    }
  }]);
  return SelectableTree;
}();
exports.SelectableTree = SelectableTree;