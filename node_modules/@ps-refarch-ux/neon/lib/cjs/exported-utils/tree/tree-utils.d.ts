import { type TreeNode } from '../../interfaces/tree-node';
export declare function getNodeFromTree<T extends TreeNode>(tree: Array<T>, id: string): T | undefined;
export declare function addNodeToTree<T extends TreeNode>(tree: Array<T>, parentId: string, node: T): void;
export declare function getTreeNodeParent<T extends TreeNode>(tree: Array<T>, id: string, count?: number): T | undefined;
export declare function removeNodeFromTree<T extends TreeNode>(tree: Array<T>, id: string): T | undefined;
export declare function traverseTree<T extends TreeNode>(tree: Array<T>, callback: (treeNode: T, parentNode: T | undefined) => void | boolean): void;
/**
 * Need to traverse a tree that doesn't have a property called `children`?
 * This is the function for you. Simply pass in a callback function that will return the array of
 * children.
 * @param tree An array of objects that may or may not have children.
 * @param callback The callback that gets run one each of the tree nodes
 * @param getChildren The way the algorithm access the children
 */
export declare function traverseTreeGeneric<T>(tree: Array<T>, callback: (treeNode: T, parentNode: T | undefined) => void | boolean, getChildren: (treeNode: T) => Array<T> | undefined): void;
export declare function toTreeNodeHashMap<T extends TreeNode>(tree: Array<T>): Record<string, T>;
/**
 * Creates a map with key: nodeId, value: the parent node for that nodeId.
 * @param tree
 * @returns
 */
export declare function toTreeParentNodeHashMap<T extends TreeNode>(tree: Array<T>): Record<string, T>;
/**
 * Gets a list of the ancestor node ids for the target node id.
 * @param id The id of the target node
 * @param tree The tree to search through
 * @returns An Array of the ancestor nodes for the target node id.
 */
export declare function getAncestorsOf<T extends TreeNode>(id: string, tree: Array<T>): Array<string>;
