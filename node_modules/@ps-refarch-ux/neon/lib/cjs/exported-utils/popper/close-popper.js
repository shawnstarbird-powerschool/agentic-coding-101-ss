"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.closePopperById = closePopperById;
/**
 * Closes a popper element by its ID.
 * @param neonComponentId - The ID of the neon component.
 * @param docRoot - The document or shadow-root that the popper is being rendered in.
 * @example closePopperById('__mfe__user-settings-popover')
 */
function closePopperById(neonComponentId, docRoot) {
  if (docRoot == null) {
    docRoot = document;
  }
  var neonComponent = docRoot.getElementById(neonComponentId);
  if (neonComponent != null) {
    if (!/^neon-/i.test(neonComponent.tagName)) {
      console.log(neonComponent.tagName.toLowerCase() + ' is not a neon component.  It cannot be closed by ID.');
    }
    if (typeof neonComponent.closePopover === 'function') {
      neonComponent.closePopover();
    }
    if (typeof neonComponent.removePopper === 'function') {
      neonComponent.removePopper();
    }
    if (typeof neonComponent.removeTooltip === 'function') {
      neonComponent.removeTooltip();
    }
    if (typeof neonComponent.closePopperCallback === 'function') {
      neonComponent.closePopperCallback();
    }
  }
}