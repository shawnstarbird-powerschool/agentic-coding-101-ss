/**
 * Gets the neon theme object that can be passed into FusionCharts.
 * This can be accessed by using the theme name 'neon' from FusionCharts
 */
export declare function getNeonTheme(): any;
/**
 * Run this function to apply the FusionCharts license key so that the Fusion Charts logo does not show up.
 * @param fusionChartsObject The FusionCharts object that you import from 'fusioncharts'
 */
export declare function setLicenseKey(fusionChartsObject: any): void;
/**
 * Gets an array of 9 design tokens related to the "9 Color Palette" for data visualizations.
 * (The names of the tokens, not the hex values.)
 * @returns The array of design token names.
 */
export declare function get9ColorPaletteTokens(): Array<string>;
/**
 * Gets an array of 9 design token values related to the "9 Color Palette" for data visualizations.
 * (The hex values for the design tokens.)
 * @returns The array of hex values.
 */
export declare function get9ColorPalette(): Array<string>;
/**
 * Gets an array of 6 design tokens related to the "6 Color Cool Palette" for data visualizations.
 * (The names of the tokens, not the hex values.)
 * @returns The array of design token names.
 */
export declare function get6ColorCoolPaletteTokens(): Array<string>;
/**
 * Gets an array of 6 design token values related to the "6 Color Cool Palette" for data visualizations.
 * (The hex values for the design tokens.)
 * @returns The array of hex values.
 */
export declare function get6ColorCoolPalette(): Array<string>;
/**
 * Gets an array of 6 design tokens related to the "6 Color Warm Palette" for data visualizations.
 * (The names of the tokens, not the hex values.)
 * @returns The array of design token names.
 */
export declare function get6ColorWarmPaletteTokens(): Array<string>;
/**
 * Gets an array of 6 design token values related to the "6 Color Warm Palette" for data visualizations.
 * (The hex values for the design tokens.)
 * @returns The array of hex values.
 */
export declare function get6ColorWarmPalette(): Array<string>;
/**
 * Gets an array of 9 design tokens related to the "Line Color Palette" for data visualizations.
 * (The names of the tokens, not the hex values.)
 * @returns The array of design token names.
 */
export declare function getLineColorPaletteTokens(): Array<string>;
/**
 * Gets an array of 9 design token values related to the "Line Color Palette" for data visualizations.
 * (The hex values for the design tokens.)
 * @returns The array of hex values.
 */
export declare function getLineColorPalette(): Array<string>;
/**
 * When FusionCharts cycles through the set color palette for background colors on bars, the font color
 * needs to be set based on what color is given.  This function allows you to pass in your dataset (which is an array)
 * and it will apply the correct font color which will have proper contrast from the the color pallette being
 * used on that particular bar.
 * @param dataset An Array of Fusion Charts data items.
 * @param tokens The Array of design token keys (not values) to cycle through.
 */
export declare function applyFontColorsToDataSet(dataset: Array<any>, tokens: Array<string>): void;
/**
 * Given a background design token, this function will return either the hex value for black or white.
 * The value will have the appropriate contrast.
 * @param backgroundDesignToken One of our data-vis design token.
 * @returns The hex value for $color-white or $color-black
 *
 * @example getFontColorBasedOnBackgroundDesignToken('$color-data-vis-magenta-300') // returns the hex value for black.
 * @example getFontColorBasedOnBackgroundDesignToken('$color-data-vis-magenta-600') // returns the hex value for white.
 */
export declare function getFontColorBasedOnBackgroundDesignToken(backgroundDesignToken: string): string;
export declare function getBorderForToken(backgroundDesignToken: string): string;
/**
 * When FusionCharts cycles through the set color palette for background colors on bars, the border color
 * needs to be set based on what color is given.  This function allows you to pass in your dataset (which is an array)
 * and it will apply the correct font color which will have proper contrast from the the color pallette being
 * used on that particular bar.
 * @param dataset An Array of Fusion Charts data items.
 * @param tokens The Array of design token keys (not values) to cycle through.
 */
export declare function applyBorderColorsToDataSet(dataset: Array<any>, tokens: Array<string>): void;
