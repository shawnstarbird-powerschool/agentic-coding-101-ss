"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.loadNeonTheme = loadNeonTheme;
var _reactUtils = require("../react-adapters/utils/react-utils");
function loadNeonTheme(theme) {
  var existingThemeStyleTag = document.getElementById('neon-theme-style-link');
  if (existingThemeStyleTag == null) {
    if (theme !== 'default') {
      var themeStyleTag = document.createElement('link');
      themeStyleTag.setAttribute('rel', 'stylesheet');
      themeStyleTag.setAttribute('href', (0, _reactUtils.getUnversionedNeonServer)() + '/themes/' + theme + '.css');
      themeStyleTag.id = 'neon-theme-style-link';
      document.head.appendChild(themeStyleTag);
    }
  } else {
    if (theme !== 'default') {
      existingThemeStyleTag.setAttribute('href', (0, _reactUtils.getUnversionedNeonServer)() + '/themes/' + theme + '.css');
    } else {
      existingThemeStyleTag.remove();
    }
  }
}