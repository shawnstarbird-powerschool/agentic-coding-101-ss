"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.paginateData = paginateData;
function paginateData(data, currentPage, pageLength) {
  var startIndex = (currentPage - 1) * pageLength + 1;
  var endIndex = Math.min(startIndex + pageLength - 1, data.length);
  return data.slice(startIndex - 1, endIndex);
}