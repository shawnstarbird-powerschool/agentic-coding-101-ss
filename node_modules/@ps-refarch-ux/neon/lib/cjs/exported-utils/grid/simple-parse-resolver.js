"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SimpleParseResolver = void 0;
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var SUPPORTED_STRINGS_REGEX = /^[A-Za-z0-9_]+(\(\))?(\.[A-Za-z0-9_]+(\(\))?)*$/;
var PLAIN_KEY_REGEX = /^[A-Za-z0-9]+$/;
var FUNCTION_CALL_REGEX = /^([A-Za-z0-9]+)\( *\)$/;

/**
 * ParseResolver was created to mimic what Angular 1 gave us in the $parse service.
 */
var SimpleParseResolver = /*#__PURE__*/function () {
  /**
   * @param parseText: an object lookup key that can contain functions or hash lookups.
   */
  function SimpleParseResolver(parseText) {
    var _this = this;
    _classCallCheck(this, SimpleParseResolver);
    _defineProperty(this, "tokens", []);
    if (!SUPPORTED_STRINGS_REGEX.test(parseText)) {
      throw 'parseText not supported: ' + parseText;
    }
    parseText.split('.').forEach(function (textToken) {
      if (PLAIN_KEY_REGEX.test(textToken)) {
        _this.tokens.push({
          hash: textToken,
          isFunction: false
        });
      } else if (FUNCTION_CALL_REGEX.test(textToken)) {
        var pieces = FUNCTION_CALL_REGEX.exec(textToken);
        if (pieces != null) {
          _this.tokens.push({
            hash: pieces[1],
            isFunction: true
          });
        }
      }
    });
  }

  /**
   * resolves the parseText against the target object. Any references other
   * than the main reference are resolved against the context.
   * @param target - The object to resolve the parseText against.
   * @param context - The object to resolve any references inside the parseText against.
   * @returns {any}
   */
  _createClass(SimpleParseResolver, [{
    key: "resolve",
    value: function resolve(target) {
      var t = target;
      this.tokens.find(function (token) {
        if (t == null) {
          //Make the looping stop.
          return true;
        }
        if (token.isFunction) {
          var fn = t[token.hash];
          //t is the object that points fn,
          //so that is the context we must call the function in.
          t = fn.call(t);
        } else {
          t = t[token.hash];
        }
        return false;
      });
      return t;
    }
  }]);
  return SimpleParseResolver;
}();
exports.SimpleParseResolver = SimpleParseResolver;