"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Breadcrumbs = void 0;
var _stream = require("./stream");
var _logger = require("../logger");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var log = (0, _logger.getLogFunction)('breadcrumbs', 'breadcrumbs.ts');
var Breadcrumbs = /*#__PURE__*/function () {
  function Breadcrumbs() {
    _classCallCheck(this, Breadcrumbs);
    _defineProperty(this, "breadcrumbSets", []);
    _defineProperty(this, "defaultClickHandler", function (breadcrumb, e) {
      //no-op -> Let the link do it's thing.
    });
    _defineProperty(this, "changeStream", new _stream.Stream());
  }
  _createClass(Breadcrumbs, [{
    key: "turnOnBreadcrumbsFor",
    value:
    //Turn on the breadcrumbs for the MFE
    function turnOnBreadcrumbsFor(breadCrumbSetName) {
      log("breadcrumbController - turnOnBreadcrumbsFor('".concat(breadCrumbSetName, "')"));
      //First see if the breadcrumbs are in there already.
      var newBreadcrumbStack = this.breadcrumbSets.slice();
      var existingIndex = newBreadcrumbStack.findIndex(function (mfeBreadcrumbs) {
        return mfeBreadcrumbs.name === breadCrumbSetName;
      });
      if (existingIndex < 0) {
        newBreadcrumbStack.push({
          name: breadCrumbSetName,
          breadcrumbs: [] //Empty array here that will get set later.
        });
      }

      this.breadcrumbSets = newBreadcrumbStack;
    }

    //Sets the breadcrumbs for an MFE
  }, {
    key: "setBreadcrumbs",
    value: function setBreadcrumbs(setName, breadcrumbs) {
      var _this = this;
      log("breadcrumbController - setBreadcrumbs('".concat(setName, "')"), breadcrumbs);

      //First see if the breadcrumbs are in there already.
      var newBreadcrumbStack = this.breadcrumbSets.slice();
      var existingIndex = newBreadcrumbStack.findIndex(function (breadcrumbSet) {
        return breadcrumbSet.name === setName;
      });
      if (existingIndex >= 0) {
        var newBreadcrumbSet = {
          name: setName,
          breadcrumbs: breadcrumbs.map(function (breadcrumb) {
            if (typeof breadcrumb.onUserClick !== 'function') {
              var newBreadcrumb = Object.assign({}, breadcrumb, {
                onUserClick: function onUserClick(e) {
                  _this.defaultClickHandler(newBreadcrumb, e);
                }
              });
              return newBreadcrumb;
            }
            return breadcrumb;
          })
        };
        newBreadcrumbStack.splice(existingIndex, 1, newBreadcrumbSet);
        this.breadcrumbSets = newBreadcrumbStack;
        this.changeStream.fire(this.getBreadcrumbs());
      }
    }
  }, {
    key: "setDefaultBreadcrumbClickHandler",
    value: function setDefaultBreadcrumbClickHandler(handler) {
      log('breadcrumbController - setDefaultBreadcrumbClickHandler()');
      this.defaultClickHandler = handler;
    }

    //Turn off the breadcrumbs for the MFE
  }, {
    key: "turnOffBreadcrumbsFor",
    value: function turnOffBreadcrumbsFor(setName) {
      log("breadcrumbController - turnOffBreadcrumbsForMfe('".concat(setName, "')"));
      var newBreadcrumbStack = this.breadcrumbSets.slice();
      var existingIndex = newBreadcrumbStack.findIndex(function (mfeBreadcrumbs) {
        return mfeBreadcrumbs.name === setName;
      });
      if (existingIndex >= 0) {
        newBreadcrumbStack.splice(existingIndex, 1); //Remove the element
        this.breadcrumbSets = newBreadcrumbStack;
      }
      this.changeStream.fire(this.getBreadcrumbs());
    }
  }, {
    key: "getBreadcrumbs",
    value: function getBreadcrumbs() {
      log('breadcrumbController - getBreadcrumbs()');
      var breadcrumbs = this.breadcrumbSets.map(function (breadcrumbSet) {
        return breadcrumbSet.breadcrumbs;
      }).flat();
      log('breadcrumbController - returning', breadcrumbs);
      return breadcrumbs;
    }
  }]);
  return Breadcrumbs;
}();
exports.Breadcrumbs = Breadcrumbs;