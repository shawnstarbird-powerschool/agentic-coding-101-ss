import { type ReadOnlyStream } from '../../interfaces/read-only-stream';
declare class ReadOnlyStreamImpl<T> implements ReadOnlyStream<T> {
    protected element: HTMLElement;
    constructor(element?: HTMLElement);
    subscribe(callback: (data: T) => void): () => void;
}
export declare class Stream<T> extends ReadOnlyStreamImpl<T> {
    getReadOnlyStream(): ReadOnlyStream<T>;
    fire(data: T): void;
}
export {};
