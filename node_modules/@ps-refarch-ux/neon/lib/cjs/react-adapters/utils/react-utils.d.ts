import { type ComponentDependency } from './component-dependency';
import React from 'react';
import { type NeonCommonProps } from '../../interfaces/react-adapters/neon-common-props';
export declare function toAttribute(value: any): string | undefined;
export declare function toStyle(value?: Record<string, any>): string | undefined;
/**
 * If you want to locally run an MFE and want the react adapter to pick up local neon changes,
 * run localStorage.setItem('neon-is-use-local-host', 'true') in your browser's console.
 *
 * If Neon is running on a port besides 8080, run localStorage.setItem('neon-is-use-local-host', '9090')
 * and replace 9090 with the port number it's running on.
 */
export declare function getVersionedNeonServer(): string;
export declare function getUnversionedNeonServer(): string;
/**
 * This is our own custom React hook.
 * @param urls
 * @param callback This callback will NOT be called if the component using it has been unloaded.
 */
export declare function useScripts(urls: Array<string>, callback: () => void): void;
export declare function createComponentDependency(componentName: string): ComponentDependency;
/**
 * Wrapper for functional components that ensures that the web component is registered before rendering.
 * @param tag The neon web component tag to load.
 * @param Component The functional component wrapper
 * @param dependencies Array of dependencies to resolve
 */
export declare function withNeonWebComponent<P>(tag: string, Component: React.FunctionComponent<P>, dependencies: Array<ComponentDependency>): React.ForwardRefExoticComponent<React.PropsWithoutRef<P> & React.RefAttributes<HTMLElement>>;
export declare function buildPropsMap(commonProps: NeonCommonProps, propsMap: Record<string, any>, copyDataAttributes?: boolean): Record<string, any>;
/**
 * Custom hook for imperatively set props. That is, props that we have to explicitly change via the web component's API.
 * @param ref A ref to the web component.
 * @param props A mapping of property names to property values.
 */
export declare function useImperativeProps<T>(ref: React.MutableRefObject<T | undefined>, props: Record<string, unknown>): void;
/**
 * Custom hook for managing webcomponent callbacks.
 * @param ref A ref to the web component.
 * @param eventName The name of the event listener (as provided to addEventListener)
 * @param callback The callback to the event listeners
 */
export declare function useListener<T extends HTMLElement>(ref: React.MutableRefObject<T | undefined>, eventName: string, callback: EventListenerOrEventListenerObject): void;
/**
 * The plural of useListener
 * @param ref A ref to the web component
 * @param listeners A mapping of event listener names to callbacks
 */
export declare function useListeners<T extends HTMLElement>(ref: React.MutableRefObject<T | undefined>, listeners: Record<string, EventListenerOrEventListenerObject>): void;
