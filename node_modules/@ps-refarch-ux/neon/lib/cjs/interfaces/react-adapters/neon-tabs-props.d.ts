import { Tab } from "../tab";
export interface NeonTabsProps {
    id: string;
    currentTab?: string;
    tabs?: Array<Tab>;
    dataLegend?: string;
    tabSelectionChanged?: (selectedTab: string | undefined) => void;
    children?: any;
}
