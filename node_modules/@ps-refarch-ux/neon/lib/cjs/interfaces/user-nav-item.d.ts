import { NavItem } from './nav-item';
export interface UserNavItem {
    id: string;
    name: string;
    firstName: string;
    lastName: string;
    profilePicture?: {
        src: string;
        alt?: string;
    };
    children: Array<NavItem>;
}
