import './neon-breadcrumbs.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type Breadcrumb } from '../../../interfaces/breadcrumb';
import { type NeonItemPicker } from '../neon-item-picker/neon-item-picker';
import { type ConditionalElement } from '../shared/conditional-element';
import { type MenuOption } from '../../../interfaces';
interface BreadCrumbWithWidth extends Breadcrumb {
    width: number;
    minWidth: number;
}
export declare class NeonBreadcrumbs extends NeonWidget {
    readonly widgetTag = "neon-breadcrumbs";
    resizeObserver: ResizeObserver;
    minimumAfterEllipse: number;
    i18nUnsubscribe?: () => void;
    isIdRequired: boolean;
    private _breadcrumbs?;
    private hiddenBreadcrumbs;
    private breadcrumbDisplayType;
    private containerWidth;
    private tripleDotIndicatorWidth;
    private widthOfAllBreadcrumbs;
    private minimumWidthForPartial;
    private breadcrumbController?;
    private unsubscribeFromBreadcrumbController?;
    itemPicker?: NeonItemPicker;
    removePopper?: (event?: Event) => void;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    set breadcrumbs(value: Array<BreadCrumbWithWidth> | undefined);
    get breadcrumbs(): Array<BreadCrumbWithWidth> | undefined;
    setBreadcrumbs(value: Array<BreadCrumbWithWidth> | undefined): void;
    getBreadcrumbs(): Array<BreadCrumbWithWidth>;
    createBreadcrumbController(): void;
    subscribeToBreadcrumbController(): void;
    convertBreadcrumbToMenuOption(breadcrumb: Breadcrumb, id: string, index: number): MenuOption;
    isAvatarRenderable(breadcrumb: Breadcrumb | BreadCrumbWithWidth | undefined, index: number): boolean;
    isShowBreadcrumbAtIndex(index: number): boolean;
    private calculateBreadcrumbWidths;
    calculateBreadcrumbDisplayType(): void;
    private hasResizedBreadcrumbs;
    calculateBreadcrumbBreakpoints(): void;
    renderItemPicker(): NeonItemPicker;
    renderEllipsisExpandButton(): HTMLElement;
    renderMobileEllipsisExpandButton(): HTMLElement;
    renderBreadcrumb(breadcrumb: Breadcrumb, index: number): HTMLElement;
    renderSlashForward(): HTMLElement;
    render(): ConditionalElement;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
export {};
