import './neon-image.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type ConditionalElement } from '../shared/conditional-element';
export declare class NeonImage extends NeonWidget {
    widgetTag: string;
    mediaImageFailure: boolean;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    validateInputs(): void;
    renderConditionalImage(): ConditionalElement;
    renderConditionalFallbackImage(): ConditionalElement;
    render(): Array<ConditionalElement>;
}
