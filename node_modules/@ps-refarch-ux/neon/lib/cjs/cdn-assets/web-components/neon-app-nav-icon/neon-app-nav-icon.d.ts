import './neon-app-nav-icon.scss';
import { NeonWidget } from '../shared/neon-widget';
export declare class NeonAppNavIcon extends NeonWidget {
    widgetTag: string;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    isSafari(): boolean;
    renderDefaultIcon(): HTMLElement;
    render(): HTMLElement | Array<HTMLElement | Element> | Element;
}
