import { ConditionalElement } from './conditional-element';
import { DynamicAttribute, DynamicProperty, DynamicText } from './dynamic-element-data';
import { DynamicLoop } from './dynamic-loop';
export type SpecialAttributeValue = string | null | undefined | DynamicAttribute;
export type CreateElementExtension = (element: HTMLElement) => void;
export declare const focusableElementSelector = "a, button, input, textarea, select, details, [tabindex]";
export interface ElementOptions {
    text?: string | DynamicText | null | undefined;
    attrs?: Record<string, SpecialAttributeValue>;
    properties?: Record<string, any>;
    listeners?: Record<string, (event: Event) => void>;
    extensions?: CreateElementExtension | Array<CreateElementExtension>;
}
export declare function getPageDirection(): 'ltr' | 'rtl';
export declare function createElement(tag: string, options?: ElementOptions, children?: DynamicLoop): HTMLElement;
export declare function createElement(tag: string, options?: ElementOptions, ...children: Array<Node | ConditionalElement | undefined | null>): HTMLElement;
/**
 * Loops through the array of ConditionalElement objects.
 * If one should render and has a comment marker in place, it renders the element and swaps it in place of the comment.
 * If one should no longer be rendered, it creates a comment and swaps it in place of the element.
 * @param conditionalElements - An array of ConditionalElement objects to check and change.
 */
export declare function hotSwapConditionalElements(conditionalElements: Array<ConditionalElement>): void;
export declare function useElementIf(shouldRender: () => boolean, getElement: () => Node): ConditionalElement;
export declare function useElementIfElse(shouldRender: () => boolean, getIfElement: () => Node, getElseElement: () => Node): Array<ConditionalElement>;
export declare function createElementIf(shouldRender: () => boolean, tag: string, options?: ElementOptions, renderChildren?: () => (Node | ConditionalElement | undefined | null | Array<Node | ConditionalElement | undefined | null>)): ConditionalElement;
export declare function dynamicAttr(callback: () => string | null | undefined): DynamicAttribute;
export declare function dynamicProperty(callback: () => any): DynamicProperty;
export declare function dynamicText(callback: () => string | null | undefined): DynamicText;
export declare function dynamicTextWithHighlight(callback: () => string | null | undefined, highlightCallback?: () => string | undefined | null): DynamicText;
export declare function dynamicLoop(callback: () => Array<HTMLElement | ConditionalElement>): DynamicLoop;
/**
 * Simple function for setting an attribute where the value may be a string or null or undefined.
 * If it's a string, then the attribute is set. If it's null or undefined, then the attribute is removed.
 */
export declare function setAttribute(element: Element, attribute: string, value: string | null | undefined): void;
export declare function updateDynamicText(container: HTMLElement): void;
export declare function updateDynamicAttribute(container: HTMLElement, attributeName: string): void;
export declare function updateDynamicAttributeNoChildren(container: HTMLElement, attributeName: string): void;
export declare function updateDynamicAttributeOnChildren(container: HTMLElement, attributeName: string): void;
export declare function updateDynamicProperty(container: HTMLElement, propertyName: string): void;
export declare function updateDynamicPropertyNoChildren(element: HTMLElement, propertyName: string): void;
export declare function updateDynamicPropertyOnChildren(container: HTMLElement, propertyName: string): void;
export declare function updateDynamicLoops(container: HTMLElement): void;
/**
 * This goes through and checks to see if elements should be rendered and only renders elements
 * that have not yet been rendered. It does not rerender any elements that are already there.
 *
 * It will also remove any elements that should no longer be in the DOM.
 * @param container The container that contains elements that are conditional.
 */
export declare function updateConditionalElements(container: HTMLElement): void;
/**
 * Updates all of the dynamicAttributes, dynamicText, dynamicProperties, and conditionalElements on the
 * element and all of its descendants.
 * @param element The element to update.
 */
export declare function updateAllDynamicSettings(element: HTMLElement): void;
export declare function updateAllDynamicSettingsNoChildren(element: HTMLElement): void;
export declare function updateAllDynamicSettingsOnChildren(element: HTMLElement): void;
/**
   * element and selector are required to get this function to work properly.  Passing in undefined to either of them will
   * simply make the function return undefined.  We built it this way because sometimes this function will be called with
   * "optional"
   * Sub for $.closest()
   *
   * Note that if the element passed in matches the selector, the passed in element will be returned.
   * @param element
   * @param selector
   * @param inclusive: true means that the selector will also be checked against the passed in element. (default)
   *                   false means that the selector will only be applied to ancestors of element.
   * @returns {any}
   */
export declare function getClosest(element: Element | undefined | null, selector: string | undefined, inclusive?: boolean): HTMLElement | undefined;
/**
 * Returns true if parentElement === descendantElement or parentElement is an ancestor of descendantElement.
 * @param parentElement
 * @param descendantElement
 * @returns
 */
export declare function isDescendantOf(parentElement: HTMLElement | null | undefined, descendantElement: HTMLElement | null | undefined): boolean;
export declare function prepend(newNode: Element, referenceNode: Element): void;
export declare function append(newNode: Element, referenceNode: Element): void;
export declare function insertBefore(newNode: Element, referenceNode: Node): void;
export declare function insertAfter(newNode: Node, referenceNode: Node): void;
/**
 * Inserts the newNode directly above the referenceNode and then removes the referenceNode.
 * Note that the replacement only happens when both the new node and the reference node exist.
 * @param newNode
 * @param referenceNode
 * @returns
 */
export declare function replace(newNode: Element | undefined | null, referenceNode: Element | undefined | null): void;
/**
 * Removes all children from the parent
 */
export declare function clear(parent: Node | undefined | null): void;
export declare function toggleClass(element: Element, className: string, onOrOff?: boolean): void;
export declare function registerComponent(tag: string, componentClass: CustomElementConstructor): void;
export declare function elementIsDisabled(element: HTMLElement | null | undefined): boolean;
/**
 * WARNING: Using MutationObserver objects a lot could lead to performance issues.  This function was intended to be used
 * to only observe attributes for slots or children of our web components.
 * This is the singular version of attributeChangeCallbacks.
 */
export declare function attributeChangeCallback(attribute: string, callback: (modifiedElement: Element, newValue: string | null, oldValue: string | null) => void): (mutationRecords: Array<MutationRecord>, mutationObserver: MutationObserver) => void;
/**
 * WARNING: Using MutationObserver objects a lot could lead to performance issues.  This function was intended to be used
 * to only observe attributes for slots or children of our web components.
 *
 * Use this function in order to bypass all the busy work when using a MutationObserver.
 * Example new MutationObserver(attributeChangeCallbacks({id: (modifiedElement: Element, newValue: string | null, oldValue: string | null)=> {
 *    console.log('modifiedElement: ' + modifiedElement);
 *    console.log('new attribute value: ' + newValue);
 *    console.log('previous attribute value: ' + oldValue);
 * }}));
 * @param callbackMap key: attributeName, value: a callback function to call when the attribute on the element changes.
 * @returns A function to insert into the constructor of a new MutationObserver object.
 */
export declare function attributeChangeCallbacks(callbackMap: Record<string, (modifiedElement: Element, newValue: string | null, oldValue: string | null) => void>): (mutationRecords: Array<MutationRecord>, mutationObserver: MutationObserver) => void;
/**
 * Determines the appropriate class name based on the width of the given element
 * and a set of breakpoints.
 *
 * @param elementToMeasure - The HTML element whose width is being measured.
 * @param breakpoints - An object containing breakpoint values for different sizes.
 * @returns The class name representing the appropriate size for the element.
 */
export declare function getClassByBreakpoint(elementToMeasure: HTMLElement, breakpoints: Record<string, number>): string | undefined;
/**
 * @param element - We check if the mouse cursor is inside the boundaries of the given HTML element
 * @returns true if the element is inside the given elements boundaries and false otherwise
 * Mouse
 */
export declare function isCursorInsideElementBounds(element: HTMLElement): boolean;
/**
 * Runs a callback function on every node in the node tree.
 * @param rootNode The node to traverse
 * @param callback The callback to run. Return true if you would like it to stop traversing.
 * @returns true if all of the nodes were traversed.
 */
export declare function traverseNodeTree(rootNode: Node, callback: ((node: Node) => boolean | void)): boolean;
export declare function createElementsFomTranslate(messageKey: string, translationParams: Record<string, HTMLElement>): Array<HTMLElement>;
/**
 * Splits up the passed in string into tokens.  Similar to String.prototype.split(regex),
 * but the return array also holds tokens that match the pattern as well.  Each token
 * is run through the mutateFn. This is handy if you want to treat the tokens that match a different way
 * than ones that don't match.
 * @param str The string to split into tokens.
 * @param pattern The regex to split the string on. (Don't forget to include /g if you want multiple matches.)
 * @param mutateFn The function that can turn the string token into a DOM Node
 * @returns An array of the mutated tokens.
 */
export declare function tokenizeMapViaRegex<T>(str: string, pattern: RegExp, mutateFn: (token: string, passesRegex: boolean, index: number) => T): Array<T>;
export declare function tokenizeMapViaString<T>(stringToTokenize: string, pattern: string, mutateFn: (token: string, passesRegex: boolean, index: number) => T): Array<T>;
/**
 * Brian snagged this algorithm from Stack Overflow
 * https://stackoverflow.com/questions/73907746/simulating-a-tab-key-press-using-plain-javascript-supporting-shadow-dom
 */
export declare function getAllTabbableElementsInTabOrder(): Array<Element>;
export declare function getNextTabbableElement(element: Element): Element | undefined;
export declare function getPreviousTabbableElement(element: Element): Element | undefined;
/**
 * Saves the focus element id, runs the updateFunction, and then sets focus back on the element with the same id.
 * @param container: The container that the focus is expected to be in.
 * @param updateFunction The function that will mutate something inside of the container.
 */
export declare function executeAndRetainFocus(container: HTMLElement, updateFunction: () => void): void;
export declare function runWithBody(callback: (documentBody: HTMLBodyElement) => void): Promise<void>;
/**
 * Determines if a DOM node is inside a shadow root.
 * @param node: The node in question.
 * @returns Returns true if the node is in inside a shadow root.
 */
export declare function isInShadowRoot(node: Node): boolean;
/**
 * Gets the root document for whatever DOM node is passed in.
 * @returns The document if the element is not in the shadow-dom.
 *          The shadow-root if the element is in the shadow-dom.
 */
export declare function getRootDoc(webComponentElement: Node): Document | DocumentFragment;
export declare function classNames(map: Record<string, boolean>): string;
export declare function dynamicClass(map: Record<string, boolean>): DynamicAttribute;
export declare function getNumberFromAttribute(element: HTMLElement, attributeName: string): number | undefined;
/**
 * This gets the width of the element and rounds up the sub-pixels so the return
 * value will always be an integer.
 * @param element The element to measure
 * @param webComponent when styles are scoped to a web component we need to measure from width of the web component to calculate widths correctly. Not needed if the element measured is a neon component.
 * @returns The width in pixels (no sub-pixels).
 */
export declare function getElementWidthInt(element: HTMLElement, webComponent?: Element): number;
/**
 * Grabs the root font size and then sets the --neon-font-scaling-factor
 * to compensate if it is indeed set.
 * This function will only set the property once. After that, it will
 * see it's already been set and exit early.
 */
export declare function autoSetNeonFontScalingFactor(): void;
/**
 * Calculates what the --neon-font-scaling-factor property should be set on the
 * the root element of the document based on the font-size passed in.
 * @param currentFontSize Something like 20px or 80%
 * @returns a number as a string.  .8 means 80% scale, 1.2 means 120% scale.
 */
export declare function calculateNeonFontScaleFactor(currentFontSize: string): string | undefined;
/**
 * TODO: After we see this works for a couple of versions of neon, it would be
 * nice to move this to exported-utils so that other developers can use this function
 * in their MFEs.
 *
 * @param textToAnnounce
 * @returns
 */
export declare function announceText(textToAnnounce: string): Promise<void>;
