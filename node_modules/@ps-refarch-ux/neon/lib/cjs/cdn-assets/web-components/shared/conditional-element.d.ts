export declare class ConditionalElement {
    private shouldRender;
    private renderedElementOrPlaceholder?;
    private getElement;
    constructor(shouldRender: () => boolean, getElement: () => Node);
    /**
     * Gets Some sort of Node to insert into the DOM.
     * @returns shouldRender() returns false : A comment node.
     *          shouldRender() returns true : The element returned from getElement()
     */
    renderElementOrPlaceholder(): Node;
    /**
     * Returns the reference to the rendered node if the condition is true.
     * @returns The rendered Node
     */
    getRenderedNode(): Node | undefined;
    private renderElement;
    private renderPlaceholder;
    swapElementsIfNecessary(): void;
}
