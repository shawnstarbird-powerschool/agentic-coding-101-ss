import { type DateRange } from '../../../interfaces';
import { type CalendarDay } from '../../../interfaces/calendar-day';
/**
 * This function generates an array of arrays where each element in the array is a calendar day.
 * Each sub array represents one week.  Element 0 is always a sunday, and element 6 is always a Saturday.
 * In order to get all of the days of passed in month, some days from the previous month or next month must
 * be added as filler similar to what you would see on a calendar.  Calendar days not in the passed in month
 * will have the isFillerDate property set to true.
 * @param month 0 is January.  6 is July, 11 is December.
 * @param year 2022 means 2022.
 * @returns Array<Array<CalendarDay>>
 */
export declare function generateCalendarDays(month: number, year: number): Array<Array<CalendarDay>>;
export declare function isDateInDateRange(date: Date, dateRange: DateRange): boolean;
export declare function isSameYearMonthDay(date1: Date | undefined, date2: Date | undefined): boolean;
export declare function isValidDate(d: any): boolean;
export declare function removeTimeFromDate(d: Date): Date;
export declare function getDateFromAttribute(element: HTMLElement, attributeName: string): Date | undefined;
