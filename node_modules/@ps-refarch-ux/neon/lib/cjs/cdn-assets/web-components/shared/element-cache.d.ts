import { type CreateElementExtension } from './dom-manipulation';
export declare class ElementCache {
    cache: Record<string, HTMLElement>;
    /**
     * This extension allows you to easily cache the newly created element.
     * @param elementCache - A hash map of where to cache the element.
     * @param key - The name off the variable to cache.
     * @returns
     */
    assignTo(key: string): CreateElementExtension;
    set(key: string, element: HTMLElement | undefined | null): void;
    get(key: string): HTMLElement | undefined;
}
