import './neon-tooltip.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type PopperPlacement } from '../../../interfaces';
export declare class NeonTooltip extends NeonWidget {
    widgetTag: string;
    static get observedAttributes(): Array<string>;
    triggerElementMouseEnterListener: (event: any) => void;
    triggerElementMouseLeaveListener: (event: any) => void;
    removeTooltip: (event?: MouseEvent) => void;
    parentContainerMutationObserver: undefined | MutationObserver;
    isInitialized: boolean;
    isTooltipShowing: boolean;
    tooltipContainer: HTMLElement | undefined;
    refreshElementBeforeShowing?: (element: HTMLElement) => void;
    options: undefined | {
        popperClosedCallback?: () => void;
        placement?: PopperPlacement;
        offset?: number;
    };
    private _targetElement?;
    set targetElement(value: HTMLElement | undefined | null);
    get targetElement(): HTMLElement | undefined;
    redraw(): void;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    setTooltipPlacement(): void;
    setTooltipClosedCallback(): void;
    setTooltipOffset(): void;
    removeOldTargetEventListenersById(oldTargetId: string): void;
    removeOldTargetEventListeners(oldTarget: HTMLElement | null | undefined): void;
    initializeTooltip(targetElement: HTMLElement): void;
    /**
     * The target element may not be rendered at the same time as the tooltip.
     * If we try to get the target and it's there, we initialize the tooltip and
     * set the listeners up right away. If not then we create a mutation observer
     * that will check for when the target has been added to the container element.
     *
     * The mutation observer is removed after we find the trigger and initialize the tooltip
     */
    getTargetAndInitializeTooltip(targetId: string): void;
    getTooltipContainerClasses(): string;
    connectedCallback(): void;
    moveTooltipContentsToContainer(): void;
    disconnectedCallback(): void;
    render(): Element | undefined;
}
