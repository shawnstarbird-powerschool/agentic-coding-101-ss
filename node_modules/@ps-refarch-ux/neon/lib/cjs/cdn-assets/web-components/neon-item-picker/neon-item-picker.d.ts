import './neon-item-picker.scss';
import { type MenuOption, type PopperPlacement } from '../../../interfaces';
import { type ConditionalElement } from '../shared/conditional-element';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
export declare class NeonItemPicker extends NeonWidgetSlotted {
    widgetTag: string;
    _menuOptions?: Array<MenuOption | 'DIVIDER'>;
    filteredOptions: Array<MenuOption | 'DIVIDER'>;
    _filterString: string;
    isIdRequired: boolean;
    _value?: Record<string, boolean>;
    private isSelectAllChecked;
    private pageOfOptions;
    private currentPage;
    private pageLength;
    set value(value: Record<string, boolean> | undefined);
    get value(): Record<string, boolean> | undefined;
    validateInputs(): void;
    /**
     * The neon-menu component has an optional context input that needs to be passed
     * into the onClickCallback function on the MenuItem.  This property provides
     * a way for the neon-menu to get the reference into the onClickCallback function.
     */
    context?: any;
    static get observedAttributes(): Array<string>;
    set menuOptions(value: Array<MenuOption | 'DIVIDER'>);
    get menuOptions(): Array<MenuOption | 'DIVIDER'>;
    set filterString(value: string);
    get filterString(): string;
    setFilteredOptions(value: Array<MenuOption | 'DIVIDER'>): void;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    private isShowFilter;
    render(): ConditionalElement;
    getItemTooltipPopperPlacement(): PopperPlacement;
    filterMenuOptions(menuOptions: Array<MenuOption | 'DIVIDER'>, filterString: string | undefined): Array<MenuOption | 'DIVIDER'>;
    menuOptionPassesFilterString(menuOption: MenuOption, filterString: string): boolean;
    /**
     * Counts the number of non-'DIVIDER' MenuOption items in the given array, including nested children.
     *
     * This method recursively counts the total number of menu options in a nested structure.
     * If an option is not a 'DIVIDER' and contains child options, it counts those as well.
     *
     */
    private countOptions;
    renderConditionalSelectAllOption(): ConditionalElement;
    /**
     * Updates the state of the "Select All" checkbox based on the current value and menu options.
     *
     * This method checks each menu option to determine if all options are selected,
     * some options are selected, or none are selected. Based on this evaluation,
     * it sets the state of the "Select All" checkbox to true, false, or 'indeterminate'.
     */
    updateSelectAllCheckbox(): void;
    renderMenuOptions(menuOptions: Array<MenuOption | 'DIVIDER'>): Array<HTMLElement | ConditionalElement>;
    /**
     * This function removes the menu option from the DOM and any local arrays.
     * Even if the elements are further down the trees, they are still removed
     * from memory.
     * @param menuOption The MenuOption to remove.
     */
    removeItem(menuOption: MenuOption): void;
    /**
     * Sets the focus in the appropriate place depending on the use case.
     * If the filter input is present it will focus on that.
     * else if one of the sub items is selected, it will focus on that.
     * else it will focus on the first list item.
     */
    setFocus(): void;
}
