import './neon-footer.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type FooterInfo } from '../../../interfaces/footer-info';
export declare class NeonFooter extends NeonWidget {
    widgetTag: string;
    localFooterInfo: FooterInfo | undefined;
    year: number;
    i18nUnsubscribe?: () => void;
    constructor();
    set footerInfo(footerInfo: FooterInfo | undefined);
    get footerInfo(): FooterInfo | undefined;
    getFooterClasses(): string;
    renderAddressContainer(addressTitle: string | undefined, addressLines: Array<{
        addressLine: string;
        id: string;
    }> | undefined): HTMLElement | undefined;
    renderAddressLines(addressLines: Array<{
        addressLine: string;
        id: string;
    }>): Array<HTMLElement>;
    renderNavListItem(name: string, url: string, id: string, isNewTab?: boolean): HTMLElement;
    renderNavList(navData: Array<{
        name: string;
        url: string;
        id: string;
        isNewTab?: boolean;
    }> | undefined): HTMLElement | undefined;
    render(): HTMLElement | undefined;
}
