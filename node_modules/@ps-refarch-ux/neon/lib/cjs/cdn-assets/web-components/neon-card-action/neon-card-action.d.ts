import { NeonWidget } from '../shared/neon-widget';
import './neon-card-action.scss';
import { type SelectOption } from '../../../interfaces/select-option';
import { type AvatarUser } from '../../../interfaces/avatar-user';
export declare class NeonCardAction extends NeonWidget {
    widgetTag: string;
    private _selectOptions;
    private _users;
    private _value;
    static get observedAttributes(): Array<string>;
    set selectOptions(select: Array<SelectOption>);
    get selectOptions(): Array<SelectOption>;
    set users(userAvatars: Array<AvatarUser>);
    get users(): Array<AvatarUser>;
    set value(val: string);
    get value(): string;
    render(): HTMLElement;
    attributeChanged(attribute: string, oldValue: string, newValue: string): void;
}
