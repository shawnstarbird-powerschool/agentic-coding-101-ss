import './neon-tabs-vertical.scss';
import { NeonWidget } from '../shared/neon-widget';
import { TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
import { type MenuOption, type Tab } from '../../../interfaces';
import { type NeonItemPicker } from '../neon-item-picker/neon-item-picker';
export declare class NeonTabsVertical extends NeonWidget {
    widgetTag: string;
    i18nUnsubscribe?: () => void;
    tooltipOnEllipsisObserver: TooltipOnEllipsisObserver;
    private _tabs;
    private _currentTab;
    isIdRequired: boolean;
    mobileTabButton?: HTMLElement;
    listItemPopper?: NeonItemPicker;
    removePopper?: (event?: Event) => void;
    constructor();
    set tabs(tabs: Array<Tab>);
    get tabs(): Array<Tab>;
    set currentTab(tabId: string);
    get currentTab(): string;
    validateInputs(): void;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    convertTabToMenuOption(tab: Tab): MenuOption;
    handleTabCallback(tab: Tab): void;
    renderListItemPopper(): NeonItemPicker;
    renderDesktop(): HTMLElement;
    renderTabElement(tab: Tab): HTMLElement;
    renderMobile(): HTMLElement;
    render(): Array<HTMLElement>;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
