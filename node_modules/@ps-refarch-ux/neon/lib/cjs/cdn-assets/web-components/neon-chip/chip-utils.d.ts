import { type MenuOption, type Chip } from '../../../interfaces';
export declare function convertChipsToMenuOptions(chips: Array<Chip> | undefined, context: Element, removeChipInfo: {
    parentId?: string;
    removeText: string;
}): Array<MenuOption> | undefined;
export declare function isChipValuesEqual(chip1: Chip, chip2: Chip): boolean;
