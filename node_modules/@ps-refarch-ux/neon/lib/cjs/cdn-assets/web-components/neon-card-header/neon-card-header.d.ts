import './neon-card-header.scss';
import { type ConditionalElement } from '../shared/conditional-element';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
export declare class NeonCardHeader extends NeonWidgetSlotted {
    widgetTag: string;
    isIdRequired: boolean;
    imageError: boolean;
    tooltipOnEllipsisObserver: TooltipOnEllipsisObserver;
    static get observedAttributes(): Array<string>;
    constructor();
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    render(): Array<HTMLElement | ConditionalElement>;
    /**
     * This function encapsulates the three types of headings that can come up with.
     * data-heading-type="text" > <h2><span>heading text</span></h2>
     * data-heading-type="link" > <h2><a>heading text</a></h2>
     * data-heading-type="link" > <button><h2>heading text</h2></button>
     */
    renderConditionalHeadingElements(): Array<ConditionalElement>;
    getCardSubheadingIndentClass(): string;
    getSubheadingIndentSize(): string | undefined;
    getIconType(): undefined | 'icon' | 'icon-stack' | 'avatar';
    connectedCallback(): void;
    disconnectedCallback(): void;
}
