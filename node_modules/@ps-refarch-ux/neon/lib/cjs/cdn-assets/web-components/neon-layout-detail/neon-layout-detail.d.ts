import './neon-layout-detail.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { type FocusTrap } from '../shared/focus-trap';
import { type ConditionalElement } from '../shared/conditional-element';
/**
 * Interaction with the system banner
 *
 * When the system-banner (inside .neon-2_8_0-app-layout-system-banner)
 * is in the dom, we need to measure it and make the neon-layout-detail
 * smaller so the system banner will show behind it.
 *
 * If the page resizes and the system-banner (inside .neon-2_8_0-app-layout-system-banner)
 * is in the dom then the neon-layout-detail needs to update its size
 * to properly match the new size of neon-2_8_0-app-layout-system-banner.
 *
 * If the neon-layout-detail is up and the system banner
 * (inside .neon-2_8_0-app-layout-system-banner) gets added or
 * removed, the neon-layout-detail needs to respond properly.
 *
 */
export declare class NeonLayoutDetail extends NeonWidgetSlotted {
    static get observedAttributes(): Array<string>;
    focusTrap: FocusTrap;
    widgetTag: string;
    i18nUnsubscribe?: () => void;
    navigateBackCallback: (event: Event) => void;
    systemBannerResizeObserver: ResizeObserver;
    resizeObserver: ResizeObserver;
    mobileSecondaryPaneOpen: boolean;
    currentBreakpoint: string;
    secondaryPane?: NeonLayoutDetail;
    preCloseCallback?: (event?: Event) => void | boolean | undefined | Promise<boolean | undefined> | Promise<void>;
    myMutationobserver: MutationObserver;
    constructor();
    validateInputs(): void;
    navigateBack(event?: Event): void;
    onKeyDown(event: KeyboardEvent): void;
    addFocusTrap(): void;
    getMobileSecondaryPane(): Node;
    connectedCallback(): void;
    disconnectedCallback(): void;
    renderDetailHeader(): HTMLElement;
    renderDetailBody(): HTMLElement;
    renderDetailFooter(): HTMLElement | undefined;
    renderConditionalLayoutDetailButtonFooter(): ConditionalElement;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    renderMobileSecondaryPane(): NeonLayoutDetail;
    render(): Array<HTMLElement | ConditionalElement | undefined>;
    updateHeight(): void;
}
