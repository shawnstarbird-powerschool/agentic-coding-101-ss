export function neonToolkitHas(key) {
    /* eslint-disable no-prototype-builtins */
    return !!window.neonToolkits && window.neonToolkits.hasOwnProperty(key);
}
export function getNeonToolkit(key) {
    var _a;
    return (_a = window.neonToolkits) === null || _a === void 0 ? void 0 : _a[key];
}
export function addNeonToolkit(key, toolkit) {
    if (!window.neonToolkits) {
        window.neonToolkits = {};
    }
    if (neonToolkitHas(key)) {
        console.info('Attempted to add ' + key + ' to the Neon Toolkit, but it already exists. This was likely an error.');
    }
    else {
        window.neonToolkits[key] = toolkit;
    }
}
