// These flag utils are for literal country flags, not a flag boolean
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { getUnversionedNeonServer } from '../react-adapters/utils/react-utils';
/**
 * Returns the path to the flag image for the specified country.
 *
 * @param countryKey - The key of the country.
 * @returns The path to the flag image.
 * @example getFlagPath('united-states') // returns 'NEON_UNVERSIONED_CDN_URL/images/flags/united-states.svg'
 * @note getFlagPath does not check if the country key is valid. Providing an invalid country key will result in a path to a file that does not exist
 * @example getFlagPath('invalid-country') // returns 'NEON_UNVERSIONED_CDN_URL/images/flags/invalid-country.svg'
 */
export function getFlagPath(countryKey) {
    if (countryKey != null) {
        return `${getUnversionedNeonServer()}/images/flags/${countryKey}.svg`;
    }
}
/**
 * Retrieves the English name of a country based on its country key.
 * @param countryKey - The country key.
 * @returns The English name of the country.
 * @example getEnglishName('united-states') // returns 'United States'
 * @example getEnglishName('this-is-not-a-valid-key') // returns 'This Is Not A Valid Key'
 * @note getEnglishName does not check if the country key is valid, it is a basic transformation of the country key
 */
export function getEnglishName(countryKey) {
    if (countryKey != null && typeof countryKey === 'string') {
        const words = countryKey.split('-');
        const titleCaseWords = words.map((word) => { return word.charAt(0).toUpperCase() + word.slice(1); });
        return titleCaseWords.join(' ');
    }
}
/**
 * Retrieves a map of country flags with their English names and paths.
 * @param countryKeys An optional array of country keys. This acts as a filter for the returned map.
 * @returns A promise that resolves to a record containing the flag map.
 * @example getFlagMap(['united-states', 'canada']) // returns { 'united-states': { englishName: 'United States', path: 'NEON_UNVERSIONED_CDN_URL/images/flags/united-states.svg' }, 'canada': { englishName: 'Canada', path: 'NEON_UNVERSIONED_CDN_URL/images/flags/canada.svg' } }
 * @example getFlagMap() // returns a map of ALL flags for ~260 countries
 */
export function getFlagMap(countryKeys) {
    return __awaiter(this, void 0, void 0, function* () {
        const flagMap = {};
        if (countryKeys != null) {
            countryKeys.map((flag) => {
                const countryName = getEnglishName(flag);
                const flagPath = getFlagPath(flag);
                if (countryName && flagPath) {
                    flagMap[countryName] = { englishName: countryName, path: flagPath };
                }
            });
        }
        else {
            try {
                const response = yield fetch(`${getUnversionedNeonServer()}/neon-flags.generated.json`);
                if (!response.ok) {
                    console.error('Failed to fetch flag list:', response.statusText);
                }
                yield response.json().then((data) => {
                    data.map((countryKey) => {
                        const countryName = getEnglishName(countryKey);
                        const flagPath = getFlagPath(countryKey);
                        if (countryName && flagPath) {
                            flagMap[countryKey] = { englishName: countryName, path: flagPath };
                        }
                    });
                });
            }
            catch (error) {
                console.error('Error fetching flag list:', error);
            }
        }
        return Promise.resolve(flagMap);
    });
}
