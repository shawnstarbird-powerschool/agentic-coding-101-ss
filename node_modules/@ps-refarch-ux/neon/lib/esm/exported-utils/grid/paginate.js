export function paginateData(data, currentPage, pageLength) {
    const startIndex = (currentPage - 1) * pageLength + 1;
    const endIndex = Math.min((startIndex + pageLength - 1), data.length);
    return data.slice(startIndex - 1, endIndex);
}
