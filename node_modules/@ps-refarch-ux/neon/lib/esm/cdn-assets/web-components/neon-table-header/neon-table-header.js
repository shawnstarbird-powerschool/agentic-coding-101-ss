import './neon-table-header.scss';
import { NeonWidget } from '../shared/neon-widget';
import { dynamicText, createElement, dynamicAttr, updateDynamicText, createElementIf, updateDynamicAttributeOnChildren, updateConditionalElements, useElementIfElse, getClosest, prepend } from '../shared/dom-manipulation';
import { addTranslationListener, translate } from '@ps-refarch-ux/mfe-utils';
import { getNeonToolkit } from '../../../exported-utils/neon-toolkit';
import { stringHasCharacters } from '../shared/string-utils';
//This example shows off how to do a table accessibly
//https://www.w3.org/WAI/ARIA/apg/patterns/table/examples/sortable-table/
export class NeonTableHeader extends NeonWidget {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-table-header';
        this.isIdRequired = true;
        //The neon-tooltip removes its child nodes and stores them in memory.
        //Our dynamic system requires elements to be in the DOM to be updated so
        //instead of using that, we create a TextNode object and just keep it updated
        //with whatever is in the data-tooltip attribute.  This can be updated whether it's
        //in the DOM or not.
        this.tooltipTextNode = document.createTextNode('');
    }
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-text',
            'data-sort-controller-id',
            'data-header-name',
            'data-tooltip-text',
            'data-tooltip-icon',
            'aria-label',
            'data-button-aria-label'
        ];
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldValue, newValue) {
        super.attributeChanged(attrName, oldValue, newValue);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttributeOnChildren(this, 'id');
                updateDynamicAttributeOnChildren(this, 'data-target-id');
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-text':
                updateDynamicText(this);
                break;
            case 'data-button-aria-label':
                updateDynamicAttributeOnChildren(this, 'aria-label');
                break;
            case 'data-sort-controller-id':
                this.unsubscribeFromChangeStream(); //Unsubscribe from the old one.
                this.subscribeToChangeStream();
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-tooltip-text':
                this.tooltipTextNode.nodeValue = newValue;
                updateConditionalElements(this);
                break;
            case 'data-tooltip-icon':
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            default:
        }
    }
    subscribeToChangeStream() {
        const sortControllerId = this.getAttribute('data-sort-controller-id');
        if (sortControllerId != null) {
            this.sortController = getNeonToolkit('neon-2_20_1-table-header').get(sortControllerId);
            if (this.sortController != null) {
                this.changeStreamUnsubscribe = this.sortController.getChangeStream().subscribe(() => {
                    this.updateAriaSort();
                    updateDynamicAttributeOnChildren(this, 'data-icon');
                    updateConditionalElements(this);
                });
            }
        }
        else {
            this.sortController = undefined;
        }
    }
    unsubscribeFromChangeStream() {
        if (this.changeStreamUnsubscribe != null) {
            this.changeStreamUnsubscribe();
        }
    }
    updateAriaSort() {
        if (this.sortController != null && this.getAttribute('data-auto-aria-sort') !== 'false') {
            const ancestorTh = getClosest(this, 'th,td', false);
            if (ancestorTh != null) {
                if (this.sortController.getSortDirection() != null && this.sortController.getCurrentSort() === this.getControlName()) {
                    ancestorTh.setAttribute('aria-sort', this.sortController.getSortDirection());
                }
                else {
                    ancestorTh.removeAttribute('aria-sort');
                }
            }
        }
    }
    getTooltipTriggerId() {
        return 'neon-table-header-tooltip-trigger-' + this.getAttribute('id');
    }
    renderTooltipIcon() {
        return [
            createElementIf(() => {
                return stringHasCharacters(this.getAttribute('data-tooltip-text'));
            }, 'neon-2_20_1-icon', {
                attrs: {
                    id: dynamicAttr(() => { return this.getTooltipTriggerId(); }),
                    class: 'neon-table-header-tooltip-trigger',
                    'data-icon': dynamicAttr(() => {
                        return this.getAttribute('data-tooltip-icon') || 'circle-info';
                    })
                }
            }),
            createElementIf(() => {
                return stringHasCharacters(this.getAttribute('data-tooltip-text'));
            }, 'neon-2_20_1-tooltip', {
                attrs: {
                    'data-target-id': dynamicAttr(() => {
                        return this.getTooltipTriggerId();
                    })
                }
            }, () => { return this.tooltipTextNode; })
        ];
    }
    renderButton() {
        return createElement('button', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id'); }),
                class: 'neon-table-header-main-container',
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-button-aria-label'); })
            },
            listeners: {
                click: () => {
                    if (this.sortController != null) {
                        if (this.sortController.getCurrentSort() === this.getControlName()) {
                            //This control has already been changed, we need to
                            switch (this.sortController.getSortDirection()) {
                                default:
                                    this.sortController.setSortDirection('ascending');
                                    break;
                                case 'ascending':
                                    this.sortController.setSortDirection('descending');
                                    break;
                                case 'descending':
                                    this.sortController.setSortDirection(undefined);
                                    break;
                            }
                        }
                        else {
                            //This control was not already selected so we will set it and also set to select ascending.
                            this.sortController.setSortAndDirection(this.getControlName(), 'ascending');
                        }
                        //NOTE: Normally we would see calls to update the DOM based on conditional elements and dynamic attributes here.
                        //We aren't doing that here because it will be taken care of in the sortControllerStreamListener.
                    }
                }
            }
        }, createElement('span', {
            attrs: {
                class: 'neon-table-header-text'
            },
            text: dynamicText(() => { return this.getAttribute('data-text'); })
        }), createElement('neon-2_20_1-icon', {
            attrs: {
                'data-icon': dynamicAttr(() => {
                    var _a;
                    if (this.sortController != null && this.getControlName() === this.sortController.getCurrentSort()) {
                        switch ((_a = this.sortController) === null || _a === void 0 ? void 0 : _a.getSortDirection()) {
                            case 'ascending':
                                return 'arrow-upward';
                            case 'descending':
                                return 'arrow-downward';
                            default: //do nothing
                        }
                    }
                    return 'none';
                }),
                'data-size': 'x-small'
            }
        }));
    }
    renderTextOnly() {
        return createElement('span', {
            attrs: { class: 'neon-table-header-main-container neon-table-header-text' },
            text: dynamicText(() => { return this.getAttribute('data-text'); })
        });
    }
    render() {
        return [
            ...useElementIfElse(() => { return this.getAttribute('data-sort-controller-id') != null; }, () => { return this.renderButton(); }, () => { return this.renderTextOnly(); }),
            ...this.renderTooltipIcon()
        ];
    }
    getControlName() {
        return this.getAttribute('data-header-name') || this.getAttribute('id') || undefined;
    }
    addTableCaptionIfNotThere() {
        if (this.getAttribute('data-auto-table-caption') !== 'false') {
            const tableEl = getClosest(this, 'table', true);
            if (tableEl != null) {
                this.ancestorTable = tableEl;
                let captionEl = tableEl.querySelector('caption');
                if (captionEl == null) {
                    //No caption, we need to create one.
                    captionEl = createElement('caption', { attrs: { class: 'neon-2_20_1-table-caption' } });
                    prepend(captionEl, tableEl);
                }
                const existingDescription = captionEl.querySelector('.neon-2_20_1-table-header-sort-button-description');
                if (existingDescription == null) {
                    captionEl.appendChild(createElement('p', {
                        attrs: {
                            class: 'neon-2_20_1-table-header-sort-button-description',
                            'data-added-automatically-by-neon': ''
                        },
                        text: translate('neon|table-header|Buttons in the table headers will sort the table.')
                    }));
                }
            }
        }
    }
    removeTableCaptionIfApplicable() {
        var _a;
        if (this.getAttribute('data-auto-table-caption') !== 'false') {
            if (this.ancestorTable != null) {
                //Look to see if there are any table headers in the DOM
                //If there aren't, then we can remove the caption if applicable.
                if (this.ancestorTable.querySelector('neon-2_20_1-table-header') == null) {
                    (_a = this.ancestorTable.querySelector('.neon-2_20_1-table-header-sort-button-description[data-added-automatically-by-neon]')) === null || _a === void 0 ? void 0 : _a.remove();
                }
            }
        }
    }
    connectedCallback() {
        super.connectedCallback();
        this.addTableCaptionIfNotThere();
        this.subscribeToChangeStream();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        this.removeTableCaptionIfApplicable();
        this.unsubscribeFromChangeStream();
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
}
