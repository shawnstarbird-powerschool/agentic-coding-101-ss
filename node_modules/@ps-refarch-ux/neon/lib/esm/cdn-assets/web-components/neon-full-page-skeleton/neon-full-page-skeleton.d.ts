import './neon-full-page-skeleton.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type ConditionalElement } from '../shared/conditional-element';
export declare class NeonFullPageSkeleton extends NeonWidget {
    widgetTag: string;
    i18nUnsubscribe?: () => void;
    static get observedAttributes(): Array<string>;
    renderHeader(): HTMLElement;
    isHeaderEndRenderable(): boolean;
    /**
     * Renders the tabs for the post header section.
     *
     * @returns An HTMLElement representing the tabs for the post header.
     */
    private renderTabs;
    /**
    * Renders the pre-content section with optional additional classes for the start and end blocks.
    *
    * @param startAdditionalClasses Additional classes for the start block.
    * @param endAdditionalClasses Additional classes for the end block.
    * @returns An HTMLElement representing the pre-content section.
    */
    private renderPreContent;
    isPreContentStartRenderable(): boolean;
    isPreContentEndRenderable(): boolean;
    /**
     * Generates of HTMLElement representing fee elements with shimmering effects.
     * @param info The number of fee elements to generate.
     * @returns the HTMLElement representing fee elements with shimmering effects.
     */
    private renderFees;
    /**
    * Renders dynamic tiles based on the provided data array using a callback function.
    *
    * @param tiles An array of numbers representing the data for the tiles.
    * @param callback A callback function that generates an HTMLElement based on the data.
    * @returns An array of HTMLElements representing the dynamic tiles.
    */
    private renderDynamicTiles;
    /**
    * Renders the content section with dynamic tiles based on the provided data and callback function.
    *
    * @param tiles An array of numbers representing the data for the tiles.
    * @param callback A callback function that generates an HTMLElement based on the data.
    * @returns An HTMLElement representing the content section with dynamic tiles.
    */
    private renderContent;
    /**
     * Renders the footer of the component.
     * @returns the HTMLElement representing the footer with a shimmer effect.
     */
    private renderFooter;
    /**
     * Checks if the current index is the last or second last in a loop.
     *
     * @param index The current index in the loop.
     * @param loopLength The total length of the loop.
     * @returns An object containing information about whether the index is the last or second last in the loop.
     */
    private isLastAndSecondLast;
    renderMobileTimeSlot(opacity: number): HTMLElement;
    renderDesktopTimeSlot(opacity: number): HTMLElement;
    /**
     * Renders the calendar content with skeleton elements.
     *
     * @returns the HTMLElement representing the calendar content with skeleton elements.
     */
    private renderCalendarContent;
    renderAttendanceTitles(opacity: number): HTMLElement;
    renderTableTitles(opacity: number): HTMLElement;
    renderAssignmentTitles(opacity: number): HTMLElement;
    renderAccordionTitles(opacity: number): HTMLElement;
    /**
     * Generates skeleton columns for a mobile table with a specific base class name and number of columns.
     * @param baseClassName The base class name for the columns.
     * @param columns The number of columns to generate.
     * @returns An array of HTMLElements representing the skeleton columns for the mobile table.
     */
    private renderMobileTableColumns;
    /**
     * Generates skeleton rows for a mobile table with a specific number of columns and rows.
     * @returns An array of HTMLElements representing the skeleton rows for the mobile table.
     */
    private renderMobileTableRows;
    renderTableRow(): HTMLElement;
    /**
     * Generates skeleton rows for a table with dynamic styling based on row index and conditions.
     * @returns An array of HTMLElements representing the skeleton rows for the table.
     */
    private renderDesktopTableRows;
    /**
     * Generates skeleton rows and columns view for a calendar table based on the number of rows and columns to generate.
     * @param generateRows The number of rows to generate.
     * @param generateColumns The number of columns to generate.
     * @returns An array of HTMLElements representing the skeleton rows and columns view for the calendar table.
     */
    private renderTableRowsColumnsView;
    /**
     * Renders columns for the desktop view of the calendar table row.
     *
     * @param rowIndex - The index of the row.
     * @param generateColumns - The number of columns to generate.
     * @returns An HTMLElement representing the rendered columns.
     */
    private renderDesktopCalendarColumns;
    private renderMobileCalendarColumns;
    /**
     * Generates skeleton headers for the mobile view of a calendar table.
     * @returns An array of HTMLElements representing the skeleton headers for the mobile view.
     */
    private renderMobileCalendarHeaders;
    /**
     * Generates skeleton headers for the desktop view of a calendar table.
     * @returns An array of HTMLElements representing the skeleton headers for the desktop view.
     */
    private renderDesktopCalendarHeaders;
    private renderCalendarPostHeader;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    /**
     * Renders the appropriate variant of the skeleton based on the 'data-type' attribute.
     * If the 'data-type' attribute is present, it determines the variant based on its value.
     * If the attribute is not set, it defaults to rendering the 'calendar' variant.
     *
     * @returns An array of HTMLElements representing the rendered skeleton variant.
     */
    render(): Array<HTMLElement | ConditionalElement>;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
