import './neon-full-page-skeleton.scss';
import { NeonWidget } from '../shared/neon-widget';
import { createElement, createElementIf, updateDynamicText } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
export class NeonFullPageSkeleton extends NeonWidget {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-full-page-skeleton';
    }
    static get observedAttributes() {
        return ['data-type'];
    }
    renderHeader() {
        return createElement('div', {
            attrs: {
                class: 'neon-full-page-skeleton-header',
            }
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-block-start',
            },
        }), createElementIf(() => { return this.isHeaderEndRenderable(); }, 'div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-block-end',
            },
        }));
    }
    isHeaderEndRenderable() {
        switch (this.getAttribute('data-type')) {
            case 'accordion':
            case 'assignments':
            case 'grades':
                return true;
            default:
                return false;
        }
    }
    /**
     * Renders the tabs for the post header section.
     *
     * @returns An HTMLElement representing the tabs for the post header.
     */
    renderTabs() {
        const tabs = [];
        for (let tab = 0; tab < 6; tab++) {
            tabs.push(createElement('div', {
                attrs: {
                    class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-tab',
                },
            }));
        }
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-tabs' },
        }, createElement('div', {
            attrs: {
                class: 'neon-skeleton-tab-group',
            },
        }, ...tabs), createElement('hr', { attrs: { class: 'neon-2_20_1-skeleton-line neon-skeleton-tab-line' } }));
    }
    /**
    * Renders the pre-content section with optional additional classes for the start and end blocks.
    *
    * @param startAdditionalClasses Additional classes for the start block.
    * @param endAdditionalClasses Additional classes for the end block.
    * @returns An HTMLElement representing the pre-content section.
    */
    renderPreContent() {
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-pre-content' },
        }, createElementIf(() => { return this.isPreContentStartRenderable(); }, 'div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-block-start',
            },
        }), createElementIf(() => { return this.isPreContentEndRenderable(); }, 'div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-block-end',
            },
        }));
    }
    isPreContentStartRenderable() {
        switch (this.getAttribute('data-type')) {
            case 'accordion':
            case 'grades':
            case 'attendance':
            case 'attendance-body':
            case 'assignments':
                return true;
            default:
                return false;
        }
    }
    isPreContentEndRenderable() {
        switch (this.getAttribute('data-type')) {
            case 'accordion':
            case 'assignments':
            case 'grades':
            case 'attendance':
            case 'attendance-body':
            case 'table':
                return true;
            default:
                return false;
        }
    }
    /**
     * Generates of HTMLElement representing fee elements with shimmering effects.
     * @param info The number of fee elements to generate.
     * @returns the HTMLElement representing fee elements with shimmering effects.
     */
    renderFees(info) {
        const fees = [];
        for (let i = 0; i < info; i++) {
            fees.push(createElement('div', {
                attrs: {
                    class: 'neon-skeleton-fee',
                },
            }, createElement('div', {
                attrs: {
                    class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-fee-price',
                },
            }), createElement('div', {
                attrs: {
                    class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-fee-description',
                },
            })));
        }
        return createElement('div', { attrs: { class: 'neon-skeleton-fee-structure' } }, ...fees);
    }
    /**
    * Renders dynamic tiles based on the provided data array using a callback function.
    *
    * @param tiles An array of numbers representing the data for the tiles.
    * @param callback A callback function that generates an HTMLElement based on the data.
    * @returns An array of HTMLElements representing the dynamic tiles.
    */
    renderDynamicTiles(tiles, callback) {
        return tiles.map((n) => {
            return callback(n);
        });
    }
    /**
    * Renders the content section with dynamic tiles based on the provided data and callback function.
    *
    * @param tiles An array of numbers representing the data for the tiles.
    * @param callback A callback function that generates an HTMLElement based on the data.
    * @returns An HTMLElement representing the content section with dynamic tiles.
    */
    renderContent(tiles, callback) {
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-content' },
        }, ...this.renderDynamicTiles(tiles, callback));
    }
    /**
     * Renders the footer of the component.
     * @returns the HTMLElement representing the footer with a shimmer effect.
     */
    renderFooter() {
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-footer' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-footer',
            },
        }));
    }
    /**
     * Checks if the current index is the last or second last in a loop.
     *
     * @param index The current index in the loop.
     * @param loopLength The total length of the loop.
     * @returns An object containing information about whether the index is the last or second last in the loop.
     */
    isLastAndSecondLast(index, loopLength) {
        const isLastLoop = index >= loopLength - 1;
        const lastSecondLoop = index >= loopLength - 2 && !isLastLoop;
        return { last: isLastLoop, lastSecond: lastSecondLoop };
    }
    renderMobileTimeSlot(opacity) {
        return createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row-time-content calendar-mobile-row-timeslot',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle calendar-mobile-row-timeslot-view',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    renderDesktopTimeSlot(opacity) {
        return createElement('div', {
            attrs: {
                class: 'calendar-desktop-row-timeslot',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle calendar-desktop-row-timeslot-view',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    /**
     * Renders the calendar content with skeleton elements.
     *
     * @returns the HTMLElement representing the calendar content with skeleton elements.
     */
    renderCalendarContent() {
        const skeletonContent = [];
        const desktopViewRowRecords = 8;
        const desktopViewColumnRecords = 7;
        skeletonContent.push(...this.renderDesktopCalendarHeaders());
        skeletonContent.push(...this.renderMobileCalendarHeaders());
        skeletonContent.push(createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row calendar-desktop-table-row-view',
            },
        }, createElement('div', {
            attrs: { class: 'calendar-desktop-table-row-timeline-view' },
        }, ...(() => {
            const timeSlotSkeleton = [];
            timeSlotSkeleton.push(...[1, 1, 1, 1, 1, 1, 0.75, 0.5].map((n) => {
                return this.renderDesktopTimeSlot(n);
            }));
            return timeSlotSkeleton;
        })()), createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-container neon-skeleton-calendar-scheduler',
            },
        }, ...this.renderTableRowsColumnsView(desktopViewRowRecords, desktopViewColumnRecords))));
        const mobileViewRowRecords = 7;
        const mobileViewColumnRecord = 1;
        skeletonContent.push(createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row calendar-mobile-table-row-view',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row-timeline calendar-mobile-table-row-timeline-view neon-skeleton-calendar-table-row-mobile-view-timeline',
            },
        }, ...(() => {
            const timeSlotSkeleton = [];
            timeSlotSkeleton.push(...[1, 1, 1, 1, 1, 0.75, 0.5].map((n) => {
                return this.renderMobileTimeSlot(n);
            }));
            return timeSlotSkeleton;
        })()), createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-container neon-skeleton-calendar-scheduler',
            },
        }, ...this.renderTableRowsColumnsView(mobileViewRowRecords, mobileViewColumnRecord))));
        return createElement('div', {
            attrs: {
                class: 'neon-full-page-skeleton-content neon-skeleton-calendar-content',
            },
        }, ...skeletonContent);
    }
    renderAttendanceTitles(opacity) {
        return createElement('div', {
            attrs: { class: 'neon-skeleton-attendance' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-attendance-heading',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-attendance-subheading',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    renderTableTitles(opacity) {
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-content-tiles' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-tile-heading',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-tile-subheading',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-tile-end',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    renderAssignmentTitles(opacity) {
        return createElement('div', {
            attrs: { class: 'neon-full-page-skeleton-content-tiles' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-circle neon-circle-assessment',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: { class: 'neon-heading-assessment' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-heading-assessment-primary',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-heading-assessment-secondary',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-heading-assessment-tertiary',
                style: `opacity: ${opacity}`,
            },
        })), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-assessment-end',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    renderAccordionTitles(opacity) {
        return createElement('div', {
            attrs: { class: 'neon-skeleton-accordion' },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-accordion-heading',
                style: `opacity: ${opacity}`,
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-block-accordion-subheading',
                style: `opacity: ${opacity}`,
            },
        }));
    }
    /**
     * Generates skeleton columns for a mobile table with a specific base class name and number of columns.
     * @param baseClassName The base class name for the columns.
     * @param columns The number of columns to generate.
     * @returns An array of HTMLElements representing the skeleton columns for the mobile table.
     */
    renderMobileTableColumns(baseClassName, columns) {
        const tableCellSkeleton = [];
        const cellColumnNames = ['first', 'second', 'third', 'fourth', 'fifth'];
        for (let col = 0; col < columns; col++) {
            const isLastColumn = this.isLastAndSecondLast(col, columns).last;
            const lastSecondColumn = this.isLastAndSecondLast(col, columns).lastSecond
                ? ' last-second-opacity'
                : '';
            const lastColumn = isLastColumn ? ' last-opacity' : '';
            tableCellSkeleton.push(createElement('div', {
                attrs: { class: `${baseClassName}` },
            }, createElement('div', {
                attrs: {
                    class: `neon-2_20_1-skeleton-rectangle ${baseClassName}-${cellColumnNames[col]}${lastColumn}${lastSecondColumn}`,
                },
            })));
        }
        return tableCellSkeleton;
    }
    /**
     * Generates skeleton rows for a mobile table with a specific number of columns and rows.
     * @returns An array of HTMLElements representing the skeleton rows for the mobile table.
     */
    renderMobileTableRows() {
        const columns = 5;
        const rows = 2;
        const tableRowsSkeleton = [];
        for (let row = 0; row < rows; row++) {
            tableRowsSkeleton.push(createElement('div', {
                attrs: { class: 'neon-skeleton-table-mobile-row' },
            }, createElement('div', {
                attrs: {
                    class: 'neon-skeleton-table-mobile-column neon-skeleton-table-mobile-column-first',
                },
            }, ...this.renderMobileTableColumns('neon-skeleton-table-mobile-cell-header', columns)), createElement('div', {
                attrs: {
                    class: 'neon-skeleton-table-mobile-column neon-skeleton-table-mobile-column-second',
                },
            }, ...this.renderMobileTableColumns('neon-skeleton-table-mobile-cell-names', columns))));
        }
        return tableRowsSkeleton;
    }
    renderTableRow() {
        const tableCellSkeleton = [];
        const cellColumnNames = ['first', 'second', 'third', 'fourth', 'fifth'];
        for (const col of cellColumnNames) {
            tableCellSkeleton.push(createElement('div', {
                attrs: { class: 'neon-skeleton-table-cell' },
            }, createElement('div', {
                attrs: {
                    class: `neon-2_20_1-skeleton-rectangle neon-skeleton-table-cell-${col}`,
                },
            })));
        }
        return createElement('div', {
            attrs: {
                class: 'neon-skeleton-table-row',
            },
        }, ...tableCellSkeleton);
    }
    /**
     * Generates skeleton rows for a table with dynamic styling based on row index and conditions.
     * @returns An array of HTMLElements representing the skeleton rows for the table.
     */
    renderDesktopTableRows() {
        const rows = 10;
        const tableRowsSkeleton = [];
        for (let row = 0; row < rows; row++) {
            tableRowsSkeleton.push(this.renderTableRow());
        }
        return createElement('div', {
            attrs: { class: 'neon-skeleton-desktop-table' },
        }, ...tableRowsSkeleton);
    }
    /**
     * Generates skeleton rows and columns view for a calendar table based on the number of rows and columns to generate.
     * @param generateRows The number of rows to generate.
     * @param generateColumns The number of columns to generate.
     * @returns An array of HTMLElements representing the skeleton rows and columns view for the calendar table.
     */
    renderTableRowsColumnsView(generateRows, generateColumns) {
        const skeletonTableRow = [];
        for (let row = 0; row < generateRows; row++) {
            skeletonTableRow.push(createElement('div', {
                attrs: { class: 'neon-skeleton-calendar-table-rows' },
            }, ...(() => {
                const skeletonTableColumns = [];
                skeletonTableColumns.push(createElement('div', { attrs: { class: 'neon-skeleton-calendar-table-row-weeks' } }, this.renderDesktopCalendarColumns(row, generateColumns), // hide or show toggled via css media query
                this.renderMobileCalendarColumns(row, generateColumns) // hide or show toggled via css media query
                ));
                return skeletonTableColumns;
            })()));
        }
        return skeletonTableRow;
    }
    /**
     * Renders columns for the desktop view of the calendar table row.
     *
     * @param rowIndex - The index of the row.
     * @param generateColumns - The number of columns to generate.
     * @returns An HTMLElement representing the rendered columns.
     */
    renderDesktopCalendarColumns(rowIndex, columns) {
        return createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row-columns table-row-column-desktop-view',
            },
        }, ...(() => {
            const skeletonTableColumnsDivision = [];
            for (let col = 0; col < columns; col++) {
                if (rowIndex % 2 === 0) {
                    skeletonTableColumnsDivision.push(createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column neon-skeleton-calendar-table-column-even',
                        },
                    }, createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-first-half',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-divider',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-second-half',
                        },
                    })));
                }
                else {
                    skeletonTableColumnsDivision.push(createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column neon-skeleton-calendar-table-column-odd',
                        },
                    }, createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-first-half',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-divider',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-second-half',
                        },
                    })));
                }
            }
            return skeletonTableColumnsDivision;
        })());
    }
    renderMobileCalendarColumns(rowIndex, generateColumns) {
        return createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-row-columns table-row-column-mobile-view',
            },
        }, ...(() => {
            const skeletonTableColumnsDivision = [];
            for (let i = 0; i < generateColumns; i++) {
                if (rowIndex % 2 === 0) {
                    skeletonTableColumnsDivision.push(createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column neon-skeleton-calendar-table-column-even',
                        },
                    }, createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-first-half',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-divider',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-second-half',
                        },
                    })));
                }
                else {
                    skeletonTableColumnsDivision.push(createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column neon-skeleton-calendar-table-column-odd',
                        },
                    }, createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-first-half',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-divider',
                        },
                    }), createElement('div', {
                        attrs: {
                            class: 'neon-skeleton-calendar-table-column-second-half',
                        },
                    })));
                }
            }
            return skeletonTableColumnsDivision;
        })());
    }
    /**
     * Generates skeleton headers for the mobile view of a calendar table.
     * @returns An array of HTMLElements representing the skeleton headers for the mobile view.
     */
    renderMobileCalendarHeaders() {
        const skeletonTableHeader = [];
        skeletonTableHeader.push(createElement('div', {
            attrs: { class: 'neon-skeleton-calendar-table-head-mobile-row' },
        }, createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-mobile-row-column row-column-arrow',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle mobile-view-arrow-icon',
            },
        })), createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-mobile-row-column row-column-timeline',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle mobile-view-head-hour',
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle mobile-view-head-period',
            },
        })), createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-mobile-row-column row-column-arrow',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle mobile-view-arrow-icon',
            },
        }))));
        return skeletonTableHeader;
    }
    /**
     * Generates skeleton headers for the desktop view of a calendar table.
     * @returns An array of HTMLElements representing the skeleton headers for the desktop view.
     */
    renderDesktopCalendarHeaders() {
        const skeletonTableHeader = [];
        skeletonTableHeader.push(createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-row-desktop-view',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-row-columns row-columns-1',
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-table-head-row-columns row-columns-7',
            },
        }, ...(() => {
            const skeletonTableColumns = [];
            skeletonTableColumns.push(createElement('div', {
                attrs: {
                    class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-table-head-arrow-icon left-arrow-icon',
                },
            }));
            for (let i = 0; i < 7; i++) {
                skeletonTableColumns.push(createElement('div', {
                    attrs: {
                        class: 'neon-skeleton-calendar-table-head-row-column',
                    },
                }, createElement('div', {
                    attrs: {
                        class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-table-head-hour',
                    },
                }), createElement('div', {
                    attrs: {
                        class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-table-head-period',
                    },
                })));
            }
            skeletonTableColumns.push(createElement('div', {
                attrs: {
                    class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-table-head-arrow-icon right-arrow-icon',
                },
            }));
            return skeletonTableColumns;
        })())));
        return skeletonTableHeader;
    }
    renderCalendarPostHeader() {
        const skeletonPostHeader = [];
        skeletonPostHeader.push(createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-post-header-desktop-view',
            },
        }, 
        // Create a div for desktop view header
        createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedules',
            },
        }), 
        // Create a shimmering div for other schedules
        createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedules',
            },
        }), 
        // Create a shimmering div for today's schedule in desktop view
        createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedule-today-desktop-view',
            },
        })));
        skeletonPostHeader.push(createElement('div', {
            attrs: {
                class: 'neon-skeleton-calendar-post-header-mobile-view',
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedules neon-skeleton-schedules-mobile-view',
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedules',
            },
        }), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-skeleton-rectangle neon-skeleton-schedule-today-mobile-view',
            },
        })));
        return createElement('div', {
            attrs: { class: 'neon-skeleton-calendar-post-header' },
        }, ...skeletonPostHeader);
    }
    //Life Cycle Hooks
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'data-type':
                this.redraw();
                break;
        }
    }
    /**
     * Renders the appropriate variant of the skeleton based on the 'data-type' attribute.
     * If the 'data-type' attribute is present, it determines the variant based on its value.
     * If the attribute is not set, it defaults to rendering the 'calendar' variant.
     *
     * @returns An array of HTMLElements representing the rendered skeleton variant.
     */
    render() {
        const skeleton = [];
        switch (this.getAttribute('data-type')) {
            case 'grades':
                skeleton.push(this.renderHeader(), this.renderTabs(), this.renderPreContent(), this.renderContent([1, 1, 1, 0.75, 0.5], this.renderTableTitles), this.renderFooter());
                break;
            case 'assignments':
                skeleton.push(this.renderHeader(), this.renderTabs(), this.renderPreContent(), this.renderContent([1, 1, 1, 0.75, 0.5], this.renderAssignmentTitles), this.renderFooter());
                break;
            case 'attendance':
                skeleton.push(this.renderHeader(), this.renderTabs(), this.renderPreContent(), this.renderFees(2), this.renderContent([1, 1, 1, 0.75, 0.5], this.renderAttendanceTitles), this.renderFooter());
                break;
            case 'attendance-body':
                skeleton.push(this.renderPreContent(), this.renderFees(2), this.renderContent([1, 1, 1, 0.75, 0.5], this.renderAttendanceTitles), this.renderFooter());
                break;
            case 'accordion':
                skeleton.push(this.renderHeader(), this.renderTabs(), this.renderPreContent(), this.renderContent([1, 1, 1, 0.75, 0.5], this.renderAccordionTitles));
                break;
            case 'table':
                skeleton.push(this.renderHeader(), this.renderTabs(), this.renderPreContent(), this.renderFees(1), createElement('div', {
                    attrs: { class: 'neon-full-page-skeleton-content' },
                }, this.renderDesktopTableRows(), ...this.renderMobileTableRows()));
                break;
            case 'calendar':
                skeleton.push(this.renderHeader(), this.renderCalendarPostHeader(), this.renderCalendarContent());
                break;
            default:
                skeleton.push(this.renderContent([1, 1, 1, 0.75, 0.5], this.renderTableTitles));
                break;
        }
        return skeleton;
    }
    connectedCallback() {
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
}
