import { NeonWidget } from '../shared/neon-widget';
import { dynamicText, createElement, updateDynamicText, updateDynamicAttribute, dynamicAttr, dynamicProperty, updateConditionalElements, updateDynamicProperty, createElementIf, useElementIf, dynamicLoop, updateDynamicLoops, registerComponent, updateDynamicAttributeOnChildren } from '../shared/dom-manipulation';
import { addTranslationListener, translate } from '@ps-refarch-ux/mfe-utils';
import { TooltipOnEllipsisObserver, tooltipOnEllipsis } from '../shared/extensions/tooltip-on-ellipsis';
import { showPopper } from '../shared/poppers';
import { getIdSafeString } from '../shared/string-utils';
import { Stream } from '../../../exported-utils/models/stream';
export class NeonAppNavSwitcher extends NeonWidget {
    constructor() {
        super();
        this.widgetTag = 'neon-app-nav-switcher';
        this.changeStream = new Stream();
        this.isIdRequired = true;
        this.useItemPickerDrawer = false;
        // these are the files as exported from figma
        // they are used as a fallback if no image is provided
        this.backgroundImageOptions = {
            'district': 'https://assets.powerschool.com/neon/unversioned/images/neon-app-nav-switcher/Mask group.svg',
            'school': 'https://assets.powerschool.com/neon/unversioned/images/neon-app-nav-switcher/school-light2 1.svg',
            'classroom': 'https://assets.powerschool.com/neon/unversioned/images/neon-app-nav-switcher/School3.1 1.svg'
        };
        this.appNav = neonToolkits['neon-2_20_1-app-nav'].get('app-nav');
        this.appNav.changeStream.subscribe((event) => {
            if (event === 'nav-switcher-current-option-changed') {
                this.currentSwitcherOption = this.appNav.currentSwitcherOption;
            }
            if (event === 'nav-switcher-options-changed') {
                this.switcherOptions = this.appNav.switcherOptions || [];
            }
        });
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
    }
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-app-nav-collapsed'
        ];
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateDynamicText(this);
                break;
            case 'data-app-nav-collapsed':
                updateDynamicText(this);
                updateDynamicAttribute(this, 'data-avatar-size');
                if (this.removePopper != null) {
                    this.removePopper();
                }
                break;
            default:
        }
    }
    set currentSwitcherOption(value) {
        var _a, _b;
        const imageChanged = ((_a = this._currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.imgSrc) !== (value === null || value === void 0 ? void 0 : value.imgSrc);
        const placeholderImageChanged = ((_b = this._currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.placeholderImage) !== (value === null || value === void 0 ? void 0 : value.placeholderImage);
        this._currentSwitcherOption = value;
        if (imageChanged || placeholderImageChanged) {
            updateDynamicAttribute(this, 'src');
        }
        updateDynamicText(this);
        updateDynamicAttribute(this, 'data-profile-picture-src');
        updateDynamicAttribute(this, 'data-first-name');
        updateDynamicAttribute(this, 'data-last-name');
        updateDynamicAttributeOnChildren(this, 'class');
        updateConditionalElements(this);
        updateDynamicLoops(this);
        this.dispatchEvent(new CustomEvent('neonAppNavSwitcherCurrentSwitcherOptionChange', {
            bubbles: true, detail: {
                currentSwitcherOption: this._currentSwitcherOption
            }
        }));
    }
    get currentSwitcherOption() {
        return this._currentSwitcherOption;
    }
    set switcherOptions(value) {
        if (value == null || value.length === 0) {
            this._switcherOptions = value;
            updateConditionalElements(this); // the mobile secondary menu removes the switcher options to remove the caret icon and button functionality
            updateDynamicAttributeOnChildren(this, 'class');
            return;
        }
        // if we have nav items, convert them to menu options
        this._switcherOptions = value;
        this.itemPicker = this.renderItemPicker();
        updateDynamicAttributeOnChildren(this, 'class');
        updateDynamicProperty(this, 'menuOptions');
        updateConditionalElements(this);
    }
    get switcherOptions() {
        return this._switcherOptions || [];
    }
    getImageSrc() {
        var _a, _b, _c, _d;
        const globalImgSrc = (_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.imgSrc;
        const localImageSrc = (_b = this.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.imgSrc;
        const imageSrc = globalImgSrc || localImageSrc;
        const globalPlaceholderImage = (_c = this.appNav.currentSwitcherOption) === null || _c === void 0 ? void 0 : _c.placeholderImage;
        const localPlaceholderImage = (_d = this.currentSwitcherOption) === null || _d === void 0 ? void 0 : _d.placeholderImage;
        const placeholderImage = globalPlaceholderImage || localPlaceholderImage;
        if (imageSrc != null) {
            return imageSrc;
        }
        if (placeholderImage != null) {
            return this.backgroundImageOptions[placeholderImage];
        }
        return this.getRandomFallbackImageSrc();
    }
    getRandomFallbackImageSrc() {
        const keys = Object.keys(this.backgroundImageOptions);
        return this.backgroundImageOptions[keys[Math.floor(Math.random() * keys.length)]];
    }
    // getSwitcherTitle should pull from the active menu option
    getSwitcherTitle(event) {
        var _a, _b;
        if (this.getAttribute('data-app-nav-collapsed') === 'true') {
            return this.getCurrentEntityInitials();
        }
        if (((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.heading) != null) {
            return this.appNav.currentSwitcherOption.heading;
        }
        if (((_b = this.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.heading) != null) {
            return this.currentSwitcherOption.heading;
        }
        return this.getCurrentEntityFirstName() + ' ' + this.getCurrentEntityLastName();
    }
    getCurrentEntityFirstName() {
        var _a;
        if (((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.firstName) != null) {
            return this.appNav.currentSwitcherOption.firstName;
        }
        if (this._currentSwitcherOption != null) {
            return this._currentSwitcherOption.firstName;
        }
    }
    getCurrentEntityLastName() {
        var _a;
        if (((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.lastName) != null) {
            return this.appNav.currentSwitcherOption.lastName;
        }
        if (this._currentSwitcherOption != null) {
            return this._currentSwitcherOption.lastName;
        }
    }
    getCurrentEntityInitials() {
        var _a, _b;
        const firstName = this.getCurrentEntityFirstName();
        const lastName = this.getCurrentEntityLastName();
        if (((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.initials) != null) {
            return this.appNav.currentSwitcherOption.initials;
        }
        if (((_b = this._currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.initials) != null) {
            return this._currentSwitcherOption.initials;
        }
        if (firstName == null || lastName == null) {
            return '';
        }
        return firstName.charAt(0) + lastName.charAt(0);
    }
    getSwitcherSubtitle() {
        if (this.appNav.currentSwitcherOption != null && this.appNav.currentSwitcherOption.subheading != null) {
            return this.appNav.currentSwitcherOption.subheading;
        }
        if (this._currentSwitcherOption != null && this._currentSwitcherOption.subheading != null) {
            return this._currentSwitcherOption.subheading;
        }
        return this.getAttribute('data-subtitle') || undefined;
    }
    getMenuTriggerClasses() {
        const classes = ['neon-app-nav-switcher-menu-trigger'];
        if (this.isMenuContentValid() === false) {
            classes.push('neon-app-nav-switcher-menu-trigger-disabled');
        }
        return classes.join(' ');
    }
    convertAppNavSwitcherItemToMenuOption(navItem) {
        if (navItem === 'DIVIDER') {
            return 'DIVIDER';
        }
        const menuOption = {
            id: navItem.id,
            text: navItem.heading,
            subtext: navItem.menuSubheading || navItem.subheading,
            avatarFirstName: navItem.firstName,
            avatarLastName: navItem.lastName,
            avatarProfilePictureSrc: navItem.profilePictureSrc,
            avatarBackgroundOption: 'theme',
            additionalInfo: navItem.metadata,
            isSelected: () => { var _a, _b; return (((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.id) === navItem.id || ((_b = this.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.id) === navItem.id); },
            onClickCallback: (menuOption, context, event) => {
                this.appNav.setCurrentSwitcherOption(navItem);
                if (this.removePopper != null) {
                    this.removePopper();
                }
                event.target.dispatchEvent(new Event('neonDrawerClose', { bubbles: true }));
            }
        };
        return menuOption;
    }
    isMenuContentValid() {
        var _a;
        if (this.switcherOptions == null || this.switcherOptions.length === 0) {
            return false;
        }
        if (this.switcherOptions.length === 1 && (((_a = this.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.id) === this.switcherOptions[0].id)) {
            return false;
        }
        return true;
    }
    isAvatarRenderable() {
        var _a, _b, _c;
        return ((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.profilePictureSrc) != null || (((_b = this.appNav.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.firstName) != null || ((_c = this.appNav.currentSwitcherOption) === null || _c === void 0 ? void 0 : _c.lastName) != null);
    }
    renderBackgroundImage() {
        return createElement('img', {
            attrs: {
                class: 'neon-app-nav-switcher-image',
                role: 'presentation',
                alt: '',
                // A null attribute tells screen readers and other assistive technologies that you’re intentionally including an image that does not need alt text. You’re sending a signal that the AT can safely ignore the image — the user doesn’t need to know that the image exists in order to understand your content.
                src: dynamicAttr(() => { return this.getImageSrc(); }),
            },
            listeners: {
                error: (event) => {
                    const target = event.target;
                    target.src = this.getRandomFallbackImageSrc();
                }
            }
        });
    }
    renderMenuTrigger() {
        return createElement('button', {
            attrs: {
                class: dynamicAttr(() => { return this.getMenuTriggerClasses(); }),
                id: dynamicAttr(() => { return this.getAttribute('id') + '-menu-trigger'; }),
            },
            listeners: {
                click: (event) => {
                    // the app-nav-switcher in the mobile app-nav uses this onClickCallback to open a secondaryMenu instead of rendering a popover
                    if (typeof this.onClickCallback === 'function') {
                        this.onClickCallback(event);
                        return;
                    }
                    if (this.removePopper != null) {
                        this.removePopper();
                        return;
                    }
                    if (this.switcherOptions.length > 250) {
                        this.useItemPickerDrawer = true;
                        updateConditionalElements(this);
                        return;
                    }
                    const relativeElement = this.querySelector('button');
                    if (this.itemPicker != null && this.isMenuContentValid() === true) {
                        this.removePopper = showPopper(relativeElement, this.itemPicker, {
                            placement: 'right-start',
                            offset: 0,
                            trapFocus: true,
                            popperPostClosedCallback: () => {
                                delete this.removePopper;
                            },
                            clickTarget: relativeElement,
                            popperShownCallback: () => {
                                if (this.itemPicker != null) {
                                    this.itemPicker.setFocus();
                                }
                            }
                        });
                    }
                }
            }
        }, this.renderAvatar(), createElement('div', { attrs: {
                class: 'neon-app-nav-switcher-heading-container'
            } }, createElement('div', { attrs: {
                class: 'neon-app-nav-switcher-hading-and-subtext'
            } }, createElement('h2', {
            text: dynamicText(() => { return this.getSwitcherTitle(); }),
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-title'; }),
                class: 'neon-app-nav-switcher-entity-title',
            },
            extensions: tooltipOnEllipsis
        }), createElement('p', {
            text: dynamicText(() => { return this.getSwitcherSubtitle(); }),
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-subtitle'; }),
                class: 'neon-app-nav-switcher-entity-subtitle',
            },
            extensions: tooltipOnEllipsis
        })), //end neon-app-nav-switcher-hading-and-subtext
        createElementIf(() => { return this.switcherOptions != null && this.switcherOptions.length > 1; }, 'neon-2_20_1-icon', {
            attrs: {
                'class': 'neon-app-nav-switcher-menu-trigger-icon',
                'data-icon': 'chevron-right-sm|rtl-flip'
            }
        })), //end neon-app-nav-switcher-heading-container
        useElementIf(() => { var _a, _b, _c; return this.appNav.currentSwitcherOption != null && ((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.metadata) != null && ((_c = (_b = this.appNav.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.metadata) === null || _c === void 0 ? void 0 : _c.length) > 0; }, () => { return this.renderMetadata(); }));
    }
    renderAvatar() {
        return createElementIf(() => { return this.isAvatarRenderable(); }, 'neon-2_20_1-avatar', {
            attrs: {
                class: 'neon-app-nav-switcher-avatar',
                'data-background-option': 'theme',
                'data-avatar-size': dynamicAttr(() => { return this.getAttribute('data-app-nav-collapsed') === 'true' ? 'small' : 'xx-large'; }),
                'data-first-name': dynamicAttr(() => { var _a; return ((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.firstName) || undefined; }),
                'data-last-name': dynamicAttr(() => { var _a; return ((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.lastName) || undefined; }),
                'data-profile-picture-src': dynamicAttr(() => { var _a; return ((_a = this.appNav.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.profilePictureSrc) || undefined; }),
                'data-use-parent-ui-state': dynamicAttr(() => { return this.isMenuContentValid() ? 'true' : 'false'; }),
            }
        });
    }
    renderItemPicker() {
        return createElement('neon-2_20_1-item-picker', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-menu'; }),
                class: 'neon-app-nav-switcher-menu',
                'data-header-text': dynamicAttr(() => { return translate('neon|app-nav-switcher|menu-header|Change current selection'); }),
                'data-show-header-text': 'false',
                'data-item-tooltip-placement': 'right-start',
                'data-control-type': dynamicAttr(() => { return this.switcherOptions.length > 250 ? 'drawer' : 'popper'; }),
                'data-show-filter': 'auto',
                'data-constrain-width': dynamicAttr(() => { return this.switcherOptions.length > 250 ? 'false' : 'true'; }),
                'data-popper-max-height': 'large'
            },
            properties: {
                menuOptions: dynamicProperty(() => {
                    return this.switcherOptions.map((item) => {
                        return this.convertAppNavSwitcherItemToMenuOption(item);
                    });
                })
            }
        });
    }
    renderConditionalItemPickerDrawer() {
        return createElement('neon-2_20_1-modal-drawer', {
            attrs: {
                'data-animate': 'true',
                'data-injection-spacing': 'false',
            },
            listeners: {
                neonDrawerHasClosed: () => {
                    this.useItemPickerDrawer = false;
                    updateConditionalElements(this);
                }
            },
            properties: {
                slots: {
                    'drawer-header-title': createElement('div', {
                        attrs: {
                            'data-slot': 'drawer-header-title'
                        },
                        text: dynamicText(() => {
                            return translate('neon|app-nav-switcher|menu-header|Change current selection');
                        })
                    }),
                    'drawer-body': createElement('div', {
                        attrs: {
                            'data-slot': 'drawer-body'
                        }
                    }, this.renderItemPicker())
                }
            }
        });
    }
    // renderMetadataItem should render a dt/dd pair
    // the dt and dd need to have unique ids
    // without setting the id explicitly they were receiving the same id from the extension
    // current assumption is theres something funky going on with the tooltipOnEllipsis extension working inside dynamic loops
    renderMetadataItem(key, value) {
        return createElement('div', {
            attrs: {
                class: 'neon-app-nav-switcher-entity-info-item'
            }
        }, createElement('dt', {
            text: dynamicText(() => { return key; }),
            attrs: {
                class: 'neon-app-nav-switcher-entity-info-item-key',
                id: dynamicAttr(() => { return getIdSafeString(this.id + '-entity-info-item-key-' + key); }),
            },
            extensions: tooltipOnEllipsis
        }), createElement('dd', {
            text: dynamicText(() => { return value; }),
            attrs: {
                class: 'neon-app-nav-switcher-entity-info-item-value',
                id: dynamicAttr(() => { return getIdSafeString(this.id + '-entity-info-item-value-' + value); }),
            },
            extensions: tooltipOnEllipsis
        }));
    }
    renderMetadataItems() {
        var _a, _b;
        //We take the metadata from the local currentSwitcherOption over what is in the appNav if it's present.
        const itemMetadata = ((_a = this.currentSwitcherOption) === null || _a === void 0 ? void 0 : _a.metadata) || ((_b = this.appNav.currentSwitcherOption) === null || _b === void 0 ? void 0 : _b.metadata) || [];
        if (itemMetadata.length > 2) {
            console.error('neon-app-nav-switcher: metadata should not have more than 2 items', this.currentSwitcherOption || this.appNav.currentSwitcherOption);
        }
        return itemMetadata.slice(0, 2).map((metadataItem) => {
            return this.renderMetadataItem(metadataItem.key, metadataItem.value);
        });
    }
    renderMetadata() {
        return createElement('dl', {
            attrs: {
                class: 'neon-app-nav-switcher-entity-info'
            }
        }, dynamicLoop(() => {
            return this.renderMetadataItems();
        }));
    }
    render() {
        return [
            this.renderBackgroundImage(),
            this.renderMenuTrigger(),
            useElementIf(() => { return this.switcherOptions.length > 250 && this.useItemPickerDrawer; }, () => { return this.renderConditionalItemPickerDrawer(); })
        ];
    }
    connectedCallback() {
        this.tooltipOnEllipsisObserver.connect();
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        this.tooltipOnEllipsisObserver.disconnect();
    }
}
export function registerNeonAppNavSwitcher() {
    registerComponent('neon-2_20_1-app-nav-switcher', NeonAppNavSwitcher);
}
