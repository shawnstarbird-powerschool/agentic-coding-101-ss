import './neon-floating-action-button.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { createElement, updateDynamicText, updateDynamicAttribute, dynamicAttr, updateConditionalElements, createElementIf, useElementIf, getClosest, updateDynamicAttributeNoChildren, } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { bindCallback, getCallback } from '../shared/neon-widget';
import { addToEscapeKeyStack, isTopOfEscapeKeyStack, removeFromEscapeKeyStack } from '../shared/escape-key-stack';
import { createFocusTrap } from '../shared/focus-trap';
import { stringHasCharacters } from '../shared/string-utils';
export class NeonFloatingActionButton extends NeonWidgetSlotted {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-floating-action-button';
        //The neon-tooltip removes its child nodes and stores them in memory.
        //Our dynamic system requires elements to be in the DOM to be updated so
        //instead of using that, we create a TextNode object and just keep it updated
        //with whatever is in the data-tooltip attribute.  This can be updated whether it's
        //in the DOM or not.
        this.tooltipTextNode = document.createTextNode('');
        this.isIdRequired = true;
        this._contentsVisible = false;
        this.hasHoveredWhileHasMessages = false;
    }
    validateInputs() {
        super.validateInputs();
        if (!stringHasCharacters(this.getAttribute('data-content-aria-label'))) {
            console.error('data-content-aria-label is required to label the content for accessibility.');
        }
        if (!stringHasCharacters(this.getAttribute('data-img-alt')) && !this.slotExists('floating-action-button-button-content')) {
            console.error('data-img-alt is required for accessibility');
        }
    }
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-has-messages',
            'data-contents-visible',
            'data-tooltip-text',
            'data-img-src',
            'data-img-alt',
            'data-aria-label',
            'data-content-aria-label',
            'data-use-close-icon',
            'disabled'
        ];
    }
    get contentsVisible() {
        return this._contentsVisible;
    }
    set contentsVisible(value) {
        var _a;
        this._contentsVisible = value;
        if (this.getAttribute('disabled') === 'true') {
            return;
        }
        updateConditionalElements(this);
        if (value === true) {
            addToEscapeKeyStack(this);
            if (this.focusTrap != null) {
                this.focusTrap.activate();
            }
            else {
                this.addFocusTrap();
            }
        }
        else {
            removeFromEscapeKeyStack(this);
            (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
            updateDynamicAttributeNoChildren(this.floatingActionButton, 'class');
        }
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        var _a;
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateDynamicAttribute(this, 'data-target-id');
                break;
            case 'data-has-messages':
                this.hasHoveredWhileHasMessages = false;
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-tooltip-text':
                this.tooltipTextNode.nodeValue = newVal;
                updateConditionalElements(this);
                break;
            case 'data-img-src':
                updateConditionalElements(this);
                updateDynamicAttribute(this, 'src');
                break;
            case 'data-img-alt':
                updateDynamicAttribute(this, 'alt');
                break;
            case 'data-aria-label':
                updateDynamicAttribute(this, 'aria-label');
                break;
            case 'data-content-aria-label':
                updateDynamicAttribute(this, 'aria-label');
                break;
            case 'data-contents-visible':
                this.contentsVisible = newVal === 'true';
                updateDynamicAttribute(this, 'class');
                break;
            case 'disabled':
                updateDynamicAttribute(this, 'disabled');
                updateDynamicAttribute(this, 'class');
                if (oldVal === 'true' && newVal !== 'true' && this.contentsVisible) {
                    if (this.focusTrap != null) {
                        this.focusTrap.activate();
                    }
                    else {
                        this.addFocusTrap();
                    }
                }
                if (newVal === 'true') {
                    (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
                }
                updateConditionalElements(this);
                break;
            default:
        }
    }
    getButtonClasses() {
        const buttonClasses = ['neon-floating-action-button'];
        if (this.getAttribute('data-has-messages') === 'true' && this.getAttribute('disabled') !== 'true' && this.hasHoveredWhileHasMessages === false) {
            buttonClasses.push('neon-floating-action-button-new-message-alert');
        }
        if (this.contentsVisible && this.getAttribute('disabled') !== 'true') {
            buttonClasses.push('neon-floating-action-button-has-chat-open');
        }
        return buttonClasses.join(' ');
    }
    getButtonId() {
        return this.getAttribute('id') + '-button';
    }
    getImageSrc() {
        let src = this.getAttribute('data-img-src');
        if (src == null || src === '') {
            src = 'https://assets.powerschool.com/neon/unversioned/images/neon-floating-action-button/power-buddy-ai-logo.svg';
        }
        return src;
    }
    renderSlottedContent() {
        return createElement('div', {
            attrs: {
                class: 'neon-floating-action-button-content-container',
                role: 'dialog',
                'aria-modal': 'true',
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-content-aria-label'); })
            }
        }, this.getSlot('floating-action-button-content'));
    }
    render() {
        this.floatingActionButton = createElement('button', {
            attrs: {
                id: dynamicAttr(() => { return this.getButtonId(); }),
                'class': dynamicAttr(() => { return this.getButtonClasses(); }),
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-aria-label'); }),
                'disabled': dynamicAttr(() => { return this.getAttribute('disabled'); })
            },
            listeners: {
                click: (e) => {
                    this.contentsVisible = !this.contentsVisible;
                    updateConditionalElements(this);
                    updateDynamicAttribute(this, 'class');
                    this.dispatchEvent(new CustomEvent('neonFloatingActionButtonClick', { detail: { 'contentsVisible': this.contentsVisible } }));
                },
                mouseenter: (e) => {
                    this.hasHoveredWhileHasMessages = true;
                    updateDynamicAttribute(this, 'class');
                },
                focus: (e) => {
                    this.hasHoveredWhileHasMessages = true;
                    updateDynamicAttribute(this, 'class');
                }
            }
        }, createElementIf(() => { return (this.contentsVisible === false || this.getAttribute('disabled') === 'true') && this.slotExists('floating-action-button-button-content') !== true; }, 'img', {
            attrs: {
                class: 'neon-floating-action-button-picture',
                src: dynamicAttr(() => { return this.getImageSrc(); }),
                'alt': dynamicAttr(() => { return this.getAttribute('data-img-alt'); })
            }
        }), useElementIf(() => { return this.slotExists('floating-action-button-button-content') && (this.contentsVisible === false || this.getAttribute('data-use-close-icon') === 'false'); }, () => {
            return createElement('div', {
                attrs: {
                    class: 'neon-floating-action-button-button-content',
                    // 'data-neon-floating-action-button-close': '',
                    // 'tabindex': '0'
                }
            }, this.getSlot('floating-action-button-button-content'));
        }), createElementIf(() => { return (this.contentsVisible && this.getAttribute('disabled') !== 'true' && this.getAttribute('data-use-close-icon') !== 'false'); }, 'neon-2_20_1-icon', {
            attrs: {
                'class': 'neon-floating-action-button-close-icon',
                'data-icon': 'close',
                'data-color': 'white',
                'data-size': 'medium'
            }
        }));
        return [
            this.floatingActionButton,
            createElementIf(() => { return this.getAttribute('data-tooltip-text') != null && !this.contentsVisible && this.getAttribute('disabled') !== 'true'; }, 'neon-2_20_1-tooltip', {
                attrs: {
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-tooltip'; }),
                    'data-target-id': dynamicAttr(() => { return this.getButtonId(); }),
                    'data-tooltip-type': 'floating-action-button',
                    'data-tooltip-offset': '8',
                    'data-placement': 'top-end'
                }
            }, () => { return this.tooltipTextNode; }),
            useElementIf(() => { return this.contentsVisible && this.getAttribute('disabled') !== 'true'; }, () => { return this.renderSlottedContent(); })
        ];
    }
    handleButtonClick(event) {
        if (getClosest(event.target, '[data-neon-floating-action-button-close]')) {
            this.contentsVisible = false;
            updateDynamicAttribute(this, 'class');
        }
    }
    addFocusTrap() {
        //We have to use a setTimeout here because sometimes React takes a little while
        //to get all of the clickable elements inside the slots rendered.
        //This insures that when the focus trap activates that the focusable elements are available.
        setTimeout(() => {
            try {
                this.focusTrap = createFocusTrap(this, { allowOutsideClick: true,
                    escapeDeactivates: (event) => {
                        if (isTopOfEscapeKeyStack(this)) {
                            removeFromEscapeKeyStack(this);
                            this.contentsVisible = false;
                        }
                        //Always return false to prevent the focus trap from automatically deactivating.
                        return false;
                    } });
                this.focusTrap.activate();
            }
            catch (error) {
                console.error('It is possible that the reason you seeing the exception below is because you do not have any elements within the floating-action-button-content slot that can have focus set.');
                console.error(error);
            }
        });
    }
    onKeyDown(event) {
        var _a;
        event === null || event === void 0 ? void 0 : event.stopPropagation();
        if (((_a = event.key) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === 'escape' && isTopOfEscapeKeyStack(this)) {
            this.contentsVisible = false;
        }
    }
    connectedCallback() {
        super.connectedCallback();
        this.addEventListener('click', bindCallback(this.handleButtonClick, this));
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
        window.addEventListener('keydown', bindCallback(this.onKeyDown, this));
    }
    disconnectedCallback() {
        var _a;
        this.removeEventListener('click', getCallback(this.handleButtonClick, this));
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
        removeFromEscapeKeyStack(this);
        window.removeEventListener('keydown', getCallback(this.onKeyDown, this));
    }
}
