import './neon-chip.scss';
import { createElement, createElementIf, dynamicAttr, dynamicText, updateDynamicAttribute, updateDynamicText, updateConditionalElements, dynamicProperty, updateDynamicProperty, useElementIf, useElementIfElse, updateAllDynamicSettingsOnChildren } from '../shared/dom-manipulation';
import { translate, addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
import { ElementCache } from '../shared/element-cache';
import { NeonWidget } from '../shared/neon-widget';
import { convertChipsToMenuOptions } from './chip-utils';
import { removeNodeFromTree, getNodeFromTree } from '../../../exported-utils';
export class NeonChip extends NeonWidget {
    static get observedAttributes() {
        return [
            'id',
            'disabled',
            'data-size',
            'data-text',
            'data-subtext',
            'data-show-avatar',
            'data-avatar-first-name',
            'data-avatar-last-name',
            'data-avatar-profile-picture-src',
            'data-avatar-background-option',
            'data-is-on-gray',
            'data-is-read-only',
            'data-tooltip',
            'data-tooltip-position'
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-chip';
        this.elementCache = new ElementCache();
        super.isIdRequired = true;
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
    }
    set options(options) {
        if (options === this.options) {
            return;
        }
        this.localChipChildren = options;
        updateConditionalElements(this);
        updateDynamicText(this);
    }
    get options() {
        return this.localChipChildren;
    }
    removeOption(chipID) {
        if (this.options) {
            removeNodeFromTree(this.options, chipID);
            if (this.options.length === 0) {
                this.dispatchEvent(new CustomEvent('neonChipRemove', { bubbles: true, detail: { chipId: this.id } }));
            }
            updateDynamicProperty(this, 'options');
            updateDynamicText(this);
            updateConditionalElements(this);
        }
    }
    createOptionCountString() {
        var _a;
        let childCount = (_a = this.localChipChildren) === null || _a === void 0 ? void 0 : _a.length;
        if (this.localChipChildren != null && childCount != null && childCount > 0) {
            // when there is a remove all option, we do not include that in the child count
            if (getNodeFromTree(this.localChipChildren, this.id) != null) {
                childCount--;
            }
            return ' (' + childCount + ')';
        }
    }
    attributeChanged(attrName, oldVal, newVal) {
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        super.attributeChanged(attrName, oldVal, newVal);
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateConditionalElements(this);
                break;
            case 'data-size':
                updateDynamicAttribute(this, 'class');
                updateDynamicAttribute(this, 'data-avatar-size');
                break;
            case 'data-text':
                updateDynamicText(this);
                updateDynamicAttribute(this, 'title');
                break;
            case 'data-subtext':
                updateDynamicText(this);
                updateConditionalElements(this);
                updateDynamicAttribute(this, 'title');
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-show-avatar':
                updateConditionalElements(this);
                break;
            case 'data-avatar-first-name':
                updateDynamicAttribute(this, 'data-first-name');
                break;
            case 'data-avatar-last-name':
                updateDynamicAttribute(this, 'data-last-name');
                break;
            case 'data-avatar-profile-picture-src':
                updateDynamicAttribute(this, 'data-profile-picture-src');
                break;
            case 'data-avatar-background-option':
                updateDynamicAttribute(this, 'data-background-option');
                break;
            case 'disabled':
                if (newVal === 'true') {
                    this.removeOption(this.id);
                }
                updateDynamicAttribute(this, 'disabled');
                updateDynamicProperty(this, 'options');
                updateConditionalElements(this);
                break;
            case 'data-is-read-only':
                if (newVal === 'true') {
                    this.removeOption(this.id);
                }
                updateDynamicAttribute(this, 'class');
                updateDynamicProperty(this, 'options');
                updateConditionalElements(this);
                break;
            case 'data-tooltip':
                updateConditionalElements(this);
                updateDynamicText(this);
                break;
            case 'data-tooltip-position':
                updateDynamicAttribute(this, 'data-placement');
        }
    }
    getChipClasses() {
        let chipClasses = '';
        if (this.getAttribute('data-show-avatar') === 'true') {
            chipClasses += ' neon-chip-has-avatar';
        }
        if (this.getAttribute('data-is-read-only') === 'true') {
            chipClasses += ' neon-chip-read-only';
        }
        return chipClasses;
    }
    connectedCallback() {
        //The observer needs to be turned on when render() gets called.
        //That's why it's above super.connectedCallback()
        this.tooltipOnEllipsisObserver.connect();
        if (!this.isInitialized) {
            this.addEventListener('neonChipRemove', (event) => {
                var _a;
                if (((_a = this.options) === null || _a === void 0 ? void 0 : _a.length) === 0) {
                    updateAllDynamicSettingsOnChildren(this);
                    return;
                }
                this.removeOption(event.detail.chipId);
            });
        }
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
            updateDynamicAttribute(this, 'aria-label');
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        this.tooltipOnEllipsisObserver.disconnect();
    }
    render() {
        return createElement('div', {
            attrs: {
                class: dynamicAttr(() => { return 'neon-chip' + this.getChipClasses(); }),
                disabled: dynamicAttr(() => { return this.getAttribute('disabled') === 'true' ? 'true' : undefined; })
            },
            extensions: this.elementCache.assignTo('chip'),
        }, createElementIf(() => { return this.getAttribute('data-show-avatar') === 'true'; }, 'neon-2_20_1-avatar', {
            attrs: {
                'class': 'neon-chip-avatar',
                'data-first-name': dynamicAttr(() => { return this.getAttribute('data-avatar-first-name'); }),
                'data-last-name': dynamicAttr(() => { return this.getAttribute('data-avatar-last-name'); }),
                'data-profile-picture-src': dynamicAttr(() => { return this.getAttribute('data-avatar-profile-picture-src'); }),
                'data-background-option': dynamicAttr(() => { return this.getAttribute('data-avatar-background-option'); }),
                'data-avatar-size': dynamicAttr(() => { return this.getAttribute('data-size') === 'medium' ? 'extra-small' : 'xx-small'; }),
                'data-avatar-size-mobile': 'small',
                disabled: dynamicAttr(() => { return this.getAttribute('disabled'); }),
            }
        }), createElement('div', {
            attrs: {
                id: dynamicAttr(() => { return this.getTooltipTriggerId(); }),
                class: dynamicAttr(() => { return 'neon-chip-tooltip'; })
            }
        }, ...useElementIfElse(() => {
            return this.getAttribute('data-tooltip') != null;
        }, () => {
            return this.renderChipText();
        }, () => {
            return this.renderChipText(tooltipOnEllipsis);
        }), createElementIf(() => { return this.getAttribute('data-subtext') != null; }, 'div', {
            attrs: { class: 'neon-chip-text-divider' }
        }), useElementIf(() => { return this.getAttribute('data-subtext') != null && this.getAttribute('data-tooltip') != null; }, () => {
            return this.renderChipSubtext();
        }), useElementIf(() => { return this.getAttribute('data-subtext') != null && this.getAttribute('data-tooltip') == null; }, () => {
            return this.renderChipSubtext(tooltipOnEllipsis);
        })), createElementIf(() => { return this.getAttribute('data-tooltip') != null; }, 'neon-2_20_1-tooltip', {
            attrs: {
                'data-target-id': dynamicAttr(() => { return this.getTooltipTriggerId(); }),
                'data-placement': dynamicAttr(() => { return this.getAttribute('data-tooltip-position'); })
            },
            properties: {
                refreshElementBeforeShowing: (element) => {
                    updateDynamicText(element);
                }
            }
        }, () => {
            return createElement('span', {
                text: dynamicText(() => { return this.getAttribute('data-tooltip'); })
            });
        }), createElementIf(() => { return typeof this.createOptionCountString() === 'string' ? true : false; }, // this.createOptionCountString() !== null always returned true?...
        'span', {
            text: dynamicText(() => { return this.createOptionCountString(); }),
            attrs: {
                class: 'neon-chip-count',
            },
        }), createElementIf(() => { return (this.options == null || this.options.length === 0) && this.getAttribute('data-is-read-only') !== 'true'; }, 'button', {
            attrs: {
                class: 'neon-chip-button',
                disabled: dynamicAttr(() => { return this.getAttribute('disabled') === 'true' ? 'true' : undefined; }),
                id: dynamicAttr(() => { return this.getAttribute('id') + '-remove-button'; }),
                'aria-label': dynamicAttr(() => { return translate('neon|chip|Remove ChipText', { 'ChipText': this.getAttribute('data-text') }); })
            },
            listeners: {
                click: () => {
                    this.dispatchEvent(new CustomEvent('neonChipRemove', { bubbles: true, detail: { chipId: this.id } }));
                }
            }
        }, () => {
            return createElement('neon-2_20_1-icon', {
                attrs: {
                    class: 'neon-chip-icon',
                    'data-icon': 'circle-close',
                    'data-size': 'x-small',
                    'data-size-mobile': 'medium'
                }
            });
        }), createElementIf(() => { return this.options != null && this.options.length > 0; }, 'neon-2_20_1-menu', {
            attrs: {
                class: 'neon-chip-button',
                id: dynamicAttr(() => { return this.getAttribute('id') + '-menu'; }),
                'data-header-text': dynamicAttr(() => {
                    return this.getAttribute('data-text');
                }),
                'data-show-header-text': 'false'
            },
            properties: {
                options: dynamicProperty(() => {
                    if (this.localChipChildren != null) {
                        return convertChipsToMenuOptions(this.localChipChildren, this, { parentId: this.id, removeText: translate('neon|chip|Remove All') });
                    }
                }),
            }
        }, () => {
            return createElement('div', {
                attrs: {
                    'data-slot': 'popper-trigger'
                }
            }, createElement('button', {
                attrs: {
                    class: 'neon-chip-button',
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-menu-trigger'; }),
                    disabled: dynamicAttr(() => { return this.getAttribute('disabled') === 'true' ? 'true' : undefined; }),
                    'aria-label': dynamicAttr(() => { return translate('neon|chip|View ChipText Options', { 'ChipText': this.getAttribute('data-text') }); })
                },
            }, createElement('neon-2_20_1-icon', {
                attrs: {
                    class: 'neon-chip-icon',
                    'data-icon': 'circle-caret',
                    'data-size': 'x-small',
                    'data-size-mobile': 'medium'
                }
            })));
        }));
    }
    renderChipText(extensions) {
        return createElement('span', {
            text: dynamicText(() => { return this.getAttribute('data-text'); }),
            attrs: {
                class: dynamicAttr(() => { return 'neon-chip-text' + (this.getAttribute('data-subtext') != null ? ' neon-chip-text-has-subtext' : ''); }),
            },
            extensions: extensions
        });
    }
    renderChipSubtext(extensions) {
        return createElement('span', {
            text: dynamicText(() => { return this.getAttribute('data-subtext'); }),
            attrs: {
                class: 'neon-chip-subtext',
            },
            extensions: extensions
        });
    }
    getTooltipTriggerId() {
        return this.getAttribute('id') + 'tooltip-trigger';
    }
}
