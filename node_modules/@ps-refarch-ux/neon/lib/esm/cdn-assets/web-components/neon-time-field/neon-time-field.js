import { AbstractInputField } from '../shared/abstract-input-field';
import './neon-time-field.scss';
import { createElement, createElementIf, dynamicAttr, dynamicProperty, dynamicText, getClosest, updateConditionalElements, updateDynamicAttribute, updateDynamicProperty, useElementIfElse } from '../shared/dom-manipulation';
import { deLocalizeTime, localizeTime, SimpleTime, translate } from '@ps-refarch-ux/mfe-utils';
import { getBasicTimeValidationRules, getTimeValidators } from '../shared/validation-engine/validate-engine';
import { showPopper } from '../shared/poppers';
export class NeonTimeField extends AbstractInputField {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-time-field';
    }
    static get observedAttributes() {
        return super.observedAttributes.concat([
            'data-placeholder-text',
            'autocomplete',
            'data-min-value',
            'data-max-value',
        ]);
    }
    generateValidationRules() {
        const validationRules = getBasicTimeValidationRules();
        validationRules.setProperty('isRequired', this.getAttribute('data-is-required'), Boolean);
        validationRules.setProperty('minValue', this.getAttribute('data-min-value'), SimpleTime);
        validationRules.setProperty('maxValue', this.getAttribute('data-max-value'), SimpleTime);
        this.validators = getTimeValidators(validationRules);
        this.addCustomValidateToValidators();
    }
    setValue(value, skip) {
        if (value == null) {
            value = '';
        }
        const inputEl = this.inputElement;
        if (inputEl != null && value !== inputEl.value) {
            inputEl.value = value;
        }
        if (skip !== true) {
            this.setModelValue(deLocalizeTime(value), true);
        }
    }
    validateModelValueType(modelValue) {
        if (modelValue instanceof Date || (modelValue instanceof Object &&
            typeof modelValue.to24HourTime === 'function' &&
            typeof modelValue.getHours === 'function' &&
            typeof modelValue.setHours === 'function' &&
            typeof modelValue.setMinutes === 'function' &&
            typeof modelValue.getMinutes === 'function' &&
            typeof modelValue.set === 'function' &&
            typeof modelValue.getTime === 'function')) {
            return modelValue;
        }
        return undefined;
    }
    setModelValue(modelValue, skip) {
        if (this.modelValue == null && modelValue == null) {
            return false;
        }
        const ele = this.inputElement;
        if (ele != null && modelValue !== ele.modelValue) {
            ele.modelValue = modelValue;
            if (skip !== true) {
                this.setValue(localizeTime(modelValue), true);
            }
            return true;
        }
        return false;
    }
    renderInputElement() {
        return createElement('input', {
            attrs: {
                id: dynamicAttr(() => { return this.getInputId(); }),
                type: 'text',
                'aria-label': dynamicAttr(() => { return this.getAriaLabelForInput(); }),
                autocomplete: dynamicAttr(() => { return this.getAttribute('autocomplete'); }),
                placeholder: dynamicAttr(() => { return this.getAttribute('data-placeholder-text'); })
            },
            properties: {
                disabled: dynamicProperty(() => { return this.getDisabledStatus(); })
            },
            listeners: {
                'input': (event) => { this.inputChange(); },
                'blur': (event) => { return this.focusLost(event); }
            }
        });
    }
    renderContent() {
        return [createElement('div', {
                attrs: {
                    class: dynamicAttr(() => { return 'neon-time-field-input-container neon-input-field-first' + this.getSizeClass(); })
                },
            }, ...useElementIfElse(() => { return this.getAttribute('data-is-read-only') === 'true'; }, () => { return this.readOnlyElement; }, () => { return this.inputElement; }), createElementIf(() => { return this.getAttribute('data-is-read-only') !== 'true'; }, 'button', {
                attrs: {
                    class: 'neon-date-field-picker-button',
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-button'; }),
                    'aria-label': translate('neon|date field|Select A Date')
                },
                properties: { disabled: dynamicProperty(() => { return this.getAttribute('disabled') === 'true'; }) },
                listeners: {
                    click: (event) => {
                        if (this.getAttribute('data-disable-picker-dialog') === 'true') {
                            this.dispatchEvent(new CustomEvent('neonTimeFieldButtonPressed', { bubbles: true, detail: { 'clickEvent': event, 'timePickerButton': event.target } }));
                            return;
                        }
                        console.log('The time picker button was pressed!');
                        this.showTimePicker(event);
                    }
                }
            }, () => {
                return createElement('neon-2_20_1-icon', {
                    attrs: {
                        'data-icon': 'schedule-outline',
                        'data-size': 'small',
                        'data-size-mobile': 'medium'
                    }
                });
            }))
        ];
    }
    showTimePicker(event) {
        if (typeof this.closePopover === 'function') {
            // Close the options
            this.closePopover();
            this.closePopover = undefined;
        }
        else {
            // Open the options
            const placement = (this.getAttribute('data-popper-placement') || 'bottom-start');
            this.closePopover = showPopper(this, this.renderTimePicker(), {
                clickTarget: getClosest(event === null || event === void 0 ? void 0 : event.target, 'button'),
                placement: placement,
                trapFocus: true,
                makePopperSameWidthAs: 'auto',
                popperPreClosedCallback: (clickEvent) => {
                    var _a, _b;
                    if (clickEvent == null) {
                        return;
                    }
                    const eventOriginButton = event === null || event === void 0 ? void 0 : event.detail.timePickerButton;
                    if (getClosest(clickEvent.target, '.neon-2_20_1-time-field-time-picker') == null &&
                        getClosest(clickEvent.target, '#' + this.getAttribute('id') + '-button') == null) {
                        // We know that the event did not come from inside of one of the popovers so the window listener
                        // will close all of the articles.
                        this.closePopover = undefined;
                        if (eventOriginButton != null) {
                            eventOriginButton.focus();
                        }
                        return false;
                    }
                    else if (((_a = clickEvent.key) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === 'escape' ||
                        ((_b = clickEvent.key) === null || _b === void 0 ? void 0 : _b.toLowerCase()) === 'tab') {
                        if (eventOriginButton != null) {
                            eventOriginButton.focus();
                        }
                        this.closePopover = undefined;
                    }
                    else {
                        // The event came from inside a popover or the main button
                        // so return true to prevent the window listener from closing the menu
                        return true;
                    }
                },
                popperPostClosedCallback: () => {
                    // delete this.popperElement;
                    delete this.closePopover;
                    //We wait until the popper is closed before we show the validation message.
                    this.validateWidget(true);
                }
            });
        }
    }
    renderTimePicker() {
        return createElement('article', {
            attrs: {
                class: 'neon-2_20_1-time-article',
                'aria-modal': 'true',
                'role': 'dialog',
            }
        }, createElement('h2', {
            attrs: { class: 'neon-sr-only' },
            text: dynamicText(() => {
                return translate('neon|time field|Select A Time');
            })
        }), createElement('neon-2_20_1-time-picker', {
            attrs: {
                'id': this.getAttribute('id') + '-time-picker',
                'class': 'neon-2_20_1-time-field-time-picker',
                'data-neon-prevent-closing': '',
                'data-show-time-display': 'false'
            },
            properties: {
                value: dynamicProperty(() => { return this.modelValue; })
            },
            listeners: {
                'neonValueChange': (event) => {
                    this.modelValue = event.target.value;
                    this.inputChange();
                },
                neonLastColumnClick: () => {
                    if (this.closePopover != null) {
                        this.closePopover();
                    }
                },
                keydown: (event) => {
                    if (event instanceof KeyboardEvent && this.closePopover != null) {
                        if (event.key.toLowerCase() === 'enter') {
                            this.closePopover();
                        }
                    }
                }
            }
        }));
    }
    attributeChanged(attribute, oldValue, newValue) {
        super.attributeChanged(attribute, oldValue, newValue);
        switch (attribute) {
            case 'autocomplete':
                updateDynamicAttribute(this, 'autocomplete');
                break;
            case 'data-placeholder-text':
                updateDynamicAttribute(this, 'placeholder');
                break;
            case 'data-is-read-only':
                updateDynamicProperty(this, 'disabled');
                updateConditionalElements(this);
                break;
        }
    }
}
