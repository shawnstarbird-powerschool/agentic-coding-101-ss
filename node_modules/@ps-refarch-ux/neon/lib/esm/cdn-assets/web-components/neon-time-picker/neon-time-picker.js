import './neon-time-picker.scss';
import { NeonWidget } from '../shared/neon-widget';
import { dynamicText, createElement, updateDynamicText, updateDynamicAttribute, dynamicAttr, updateDynamicPropertyOnChildren, dynamicProperty, createElementIf, useElementIf, updateConditionalElements } from '../shared/dom-manipulation';
import { SimpleTime, addTranslationListener, addLocalListener, formatTime, getCurrentLocale, translate } from '@ps-refarch-ux/mfe-utils';
export class NeonTimePicker extends NeonWidget {
    set value(value) {
        if (value == null) {
            return;
        }
        this._value = value;
        updateDynamicPropertyOnChildren(this, 'checked');
        updateDynamicText(this);
    }
    get value() {
        return this._value;
    }
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-show-time-display'
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-time-picker';
        this.isIdRequired = true;
        if (this.value == null) {
            this.value = roundTime(new Date());
        }
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateDynamicText(this);
                break;
            case 'data-show-time-display':
                updateConditionalElements(this);
                break;
            default:
        }
    }
    renderMinutesOptions() {
        const options = [0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55];
        return createElement('fieldset', {
            attrs: {
                class: dynamicAttr(() => {
                    return 'neon-time-picker-minute-options' + (is24HourTime() ? ' neon-time-picker-flush-right' : '');
                })
            }
        }, createElement('legend', {
            attrs: { class: 'neon-sr-only' },
            text: dynamicText(() => { return translate('neon|time-picker|Minutes'); })
        }), createElement('div', { attrs: { class: 'neon-time-picker-options-column' } }, ...options.map((minutesOption) => {
            return createElement('label', {
                attrs: {
                    draggable: 'false'
                },
                listeners: {
                    //Using mouseup instead of click: because the up/down keys trigger click events
                    //and we only want to intercept this event when the mouse clicks
                    mouseup: (event) => {
                        if (is24HourTime()) {
                            this.fireLastColumnClicked();
                        }
                    }
                }
            }, createElement('input', {
                attrs: {
                    id: dynamicAttr(() => {
                        return this.getAttribute('id') + '-minute-control';
                    }),
                    class: 'neon-sr-only',
                    name: dynamicAttr(() => {
                        return this.getAttribute('id') + 'minute-control-group';
                    }),
                    type: 'radio'
                },
                properties: {
                    checked: dynamicProperty(() => {
                        return this.value.getMinutes() === minutesOption;
                    })
                },
                listeners: {
                    change: () => {
                        this.value = new SimpleTime(this.value.getHours(), minutesOption);
                        this.fireValueChange();
                    }
                }
            }), createElement('span', { text: padZeroes(minutesOption) }));
        })));
    }
    //Renders a single option with a number of hours in it.
    //Note, when in AmPm time format, passing in 0 will be labeled as 12.
    renderSingleHourOption(hoursOption) {
        let hourLabel = String(hoursOption);
        if (is24HourTime() && hoursOption < 10) {
            hourLabel = '0' + hourLabel;
        }
        if (!is24HourTime() && hoursOption === 0) {
            hourLabel = '12';
        }
        return createElement('label', {
            attrs: {
                draggable: 'false'
            }
        }, createElement('input', {
            attrs: {
                id: dynamicAttr(() => {
                    return this.getAttribute('id') + '-minute-control';
                }),
                name: dynamicAttr(() => {
                    return this.getAttribute('id') + 'hour-control-group';
                }),
                type: 'radio',
                class: 'neon-sr-only'
            },
            properties: {
                checked: dynamicProperty(() => {
                    if (this.value == null) {
                        return false;
                    }
                    let currentHours = this.value.getHours();
                    if (is24HourTime()) {
                        return currentHours === hoursOption;
                    }
                    else {
                        //01:00 AM -> 1 hr
                        //01:00 PM -> 13 hr
                        //Both of those should highlight the 1
                        if (currentHours >= 12) {
                            currentHours -= 12;
                            //Note; we could have used %12 here, but I think this code is more readable.
                        }
                        return currentHours === hoursOption;
                    }
                })
            },
            listeners: {
                change: () => {
                    let hours = hoursOption;
                    // this.value.getHours() >= 12 means it's PM
                    if (!is24HourTime() && hours < 12 && this.value.getHours() >= 12) {
                        hours += 12;
                    }
                    this.value = new SimpleTime(hours, this.value.getMinutes());
                    this.fireValueChange();
                }
            }
        }), createElement('span', { text: hourLabel }));
    }
    fireValueChange() {
        this.dispatchEvent(new Event('neonValueChange'));
    }
    fireLastColumnClicked() {
        this.dispatchEvent(new Event('neonLastColumnClick'));
    }
    //Subroutine for the renderHoursOptions function to call.
    //Renders the column of AM PM hour options
    renderAmPmHourOptions() {
        const hourOptions = [];
        //Hours 1 - 11
        for (let i = 1; i <= 11; i++) {
            hourOptions.push(this.renderSingleHourOption(i));
        }
        //Adding Option 12 here with the value of 0 because 12 AM is 0 hours in 24Hr format.
        hourOptions.push(this.renderSingleHourOption(0));
        return hourOptions;
    }
    //Subroutine for the renderHoursOptions function to call.
    //There are 2 sets of number for 24 hour time.
    //Pass in 1 you get the first 12, pass in 2 and you get the second set.
    render24HourOptions(set) {
        const hourOptions = [];
        const start = set === 1 ? 0 : 12;
        for (let i = start; i <= (start + 11); i++) {
            hourOptions.push(this.renderSingleHourOption(i));
        }
        return hourOptions;
    }
    //Renders the correct hours options whether it be 24 hour time options or AMPM hou options.
    renderHoursOptions() {
        return createElement('fieldset', {
            attrs: { class: 'neon-time-picker-hour-options' }
        }, createElement('legend', {
            attrs: { class: 'neon-sr-only' },
            text: dynamicText(() => { return translate('neon|time-picker|Hours'); })
        }), createElementIf(() => { return !is24HourTime(); }, 'div', { attrs: { class: 'neon-time-picker-options-column neon-time-picker-flush-left' } }, () => {
            return this.renderAmPmHourOptions();
        }), createElementIf(() => { return is24HourTime(); }, 'div', { attrs: { class: 'neon-time-picker-options-column neon-time-picker-flush-left' } }, () => {
            return this.render24HourOptions(1);
        }), createElementIf(() => { return is24HourTime(); }, 'div', { attrs: { class: 'neon-time-picker-options-column' } }, () => {
            return this.render24HourOptions(2);
        }));
    }
    renderAmPmOptions() {
        return createElement('fieldset', {
            attrs: { class: 'neon-time-picker-ampm-options' }
        }, createElement('legend', {
            attrs: { class: 'neon-sr-only' },
            text: dynamicText(() => { return getCurrentLocale().DATETIME_FORMATS.AMPMS.join(` ${translate('neon|time-picker|or')} `); })
        }), createElement('div', { attrs: { class: 'neon-time-picker-options-column  neon-time-picker-flush-right' } }, createElement('label', {
            attrs: {
                draggable: 'false'
            },
            listeners: {
                //Using mouseup instead of click: because the up/down keys trigger click events
                //and we only want to intercept this event when the mouse clicks
                mouseup: (event) => {
                    this.fireLastColumnClicked();
                }
            }
        }, createElement('input', {
            attrs: {
                id: dynamicAttr(() => {
                    return this.getAttribute('id') + '-control-am';
                }),
                name: dynamicAttr(() => {
                    return this.getAttribute('id') + 'ampm-control-group';
                }),
                type: 'radio',
                class: 'neon-sr-only'
            },
            properties: {
                checked: dynamicProperty(() => {
                    return this.value != null && this.value.getHours() < 12;
                })
            },
            listeners: {
                change: () => {
                    if (this.value.getHours() < 12) {
                        return;
                    }
                    this.value = new SimpleTime(this.value.getHours() - 12, this.value.getMinutes());
                    //AM is always in the last column
                    this.fireValueChange();
                }
            }
        }), createElement('span', { text: dynamicText(() => { return translate('mfe-utils|am-pm|AM'); }) })), createElement('label', {
            attrs: {
                draggable: 'false'
            },
            listeners: {
                //Using mouseup instead of click: because the up/down keys trigger click events
                //and we only want to intercept this event when the mouse clicks
                mouseup: (event) => {
                    this.fireLastColumnClicked();
                }
            }
        }, createElement('input', {
            attrs: {
                id: dynamicAttr(() => {
                    return this.getAttribute('id') + '-control-pm';
                }),
                name: dynamicAttr(() => {
                    return this.getAttribute('id') + 'ampm-control-group';
                }),
                type: 'radio',
                class: 'neon-sr-only'
            },
            properties: {
                checked: dynamicProperty(() => {
                    return this.value != null && this.value.getHours() >= 12;
                })
            },
            listeners: {
                change: () => {
                    if (this.value.getHours() >= 12) {
                        return;
                    }
                    this.value = new SimpleTime(this.value.getHours() - 12, this.value.getMinutes());
                    //PM is always in the last column
                    this.fireValueChange();
                }
            }
        }), createElement('span', { text: dynamicText(() => { return translate('mfe-utils|am-pm|PM'); }) }))));
    }
    render() {
        return [
            createElementIf(() => { return this.getAttribute('data-show-time-display') !== 'false'; }, 'output', {
                attrs: { class: 'neon-time-picker-selected-time' },
                text: dynamicText(() => {
                    if (this.value != null) {
                        return formatTime(this.value, 'TIME');
                    }
                    else {
                        return '';
                    }
                })
            }),
            createElement('div', {
                attrs: { class: 'neon-time-picker-time-options' }
            }, this.renderHoursOptions(), this.renderMinutesOptions(), useElementIf(() => { return !is24HourTime(); }, () => { return this.renderAmPmOptions(); }))
        ];
    }
    connectedCallback() {
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
        this.localeUnsubscribe = addLocalListener(() => {
            updateConditionalElements(this);
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        if (this.localeUnsubscribe != null) {
            this.localeUnsubscribe();
            this.localeUnsubscribe = undefined;
        }
    }
}
function is24HourTime() {
    const locale = getCurrentLocale();
    return locale.DATETIME_FORMATS.shortTime === 'HH:mm';
}
function padZeroes(value) {
    const stringValue = String(value);
    if (stringValue.length === 1) {
        return '0' + stringValue;
    }
    else {
        return stringValue;
    }
}
function roundTime(value) {
    switch (value.getMinutes() % 5) {
        default:
        case 0:
            return new SimpleTime(value.getHours(), value.getMinutes());
        case 1:
            return new SimpleTime(value.getHours(), value.getMinutes() - 1);
        case 2:
            return new SimpleTime(value.getHours(), value.getMinutes() - 2);
        case 3:
            return new SimpleTime(value.getHours(), value.getMinutes() + 2);
        case 4:
            return new SimpleTime(value.getHours(), value.getMinutes() + 1);
    }
}
