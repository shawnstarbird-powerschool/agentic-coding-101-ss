import './neon-card-media.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { dynamicText, createElement, createElementIf, updateDynamicText, updateDynamicAttribute, dynamicAttr, updateDynamicAttributeOnChildren, updateConditionalElements, dynamicProperty, updateDynamicProperty, useElementIf, getClosest, getRootDoc, } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
export class NeonCardMedia extends NeonWidgetSlotted {
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-heading',
            'data-header-icon',
            'data-header-icon-color',
            'data-header-icon-color-weight',
            'data-avatar-first-name',
            'data-avatar-last-name',
            'data-avatar-profile-picture-src',
            'data-avatar-background-option',
            'data-heading-url',
            'data-heading-url-open-in-new-tab',
            'data-subheading',
            'data-subheading-icon',
            'data-orientation',
            'data-media-aspect-ratio',
            'data-media-fit',
            'data-media-size',
            'data-media-img-src',
            'data-media-img-alt',
            'data-media-illustration',
            'data-media-illustration-color',
            'data-media-illustration-background',
            'data-media-illustration-show-people',
            'data-media-icon',
            'data-media-icon-color',
            'data-media-icon-color-weight',
            'data-footer-border',
            'data-media-border',
            'disabled',
            'data-is-loading',
            'data-is-white-background',
            'data-heading-level',
            'value',
            'name',
            'data-control-type',
            'data-header-size'
        ];
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttributeOnChildren(this, 'id');
                updateDynamicAttribute(this.input, 'for');
                updateDynamicAttribute(this.input, 'id');
                updateDynamicText(this);
                break;
            case 'data-heading':
                updateDynamicText(this);
                updateDynamicAttributeOnChildren(this, 'aria-labeledby');
                break;
            case 'data-header-icon':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-header-icon-color':
                updateDynamicAttributeOnChildren(this, 'data-color');
                break;
            case 'data-header-icon-color-weight':
                updateDynamicAttributeOnChildren(this, 'data-color-weight');
                break;
            case 'data-avatar-first-name':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-first-name');
                break;
            case 'data-avatar-last-name':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-last-name');
                break;
            case 'data-avatar-profile-picture-src':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-profile-picture-src');
                break;
            case 'data-avatar-background-option':
                updateDynamicAttributeOnChildren(this, 'data-background-option');
                break;
            case 'data-heading-url':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'href');
                break;
            case 'data-heading-url-open-in-new-tab':
                updateDynamicAttributeOnChildren(this, 'target');
                break;
            case 'data-subheading':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'aria-describedby');
                updateDynamicText(this);
                break;
            case 'data-subheading-icon':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-orientation':
                updateDynamicAttributeOnChildren(this, 'class');
                updateConditionalElements(this);
                break;
            case 'data-media-border':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-media-aspect-ratio':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-media-fit':
                updateDynamicAttributeOnChildren(this, 'class');
                updateDynamicAttributeOnChildren(this, 'data-object-fit');
                break;
            case 'data-media-img-src':
                updateDynamicAttributeOnChildren(this, 'data-src');
                updateConditionalElements(this);
                break;
            case 'data-media-img-alt':
                updateDynamicAttributeOnChildren(this, 'data-alt');
                break;
            case 'data-media-illustration':
                updateDynamicAttributeOnChildren(this, 'style');
                updateDynamicAttributeOnChildren(this, 'data-illustration');
                updateConditionalElements(this);
                break;
            case 'data-media-illustration-color':
                updateDynamicAttributeOnChildren(this, 'data-color');
                // updateDynamicAttributeOnChildren(this, 'style');
                break;
            case 'data-media-illustration-background':
                updateDynamicAttributeOnChildren(this, 'data-background');
                break;
            case 'data-media-illustration-show-people':
                updateDynamicAttributeOnChildren(this, 'data-show-people');
                updateConditionalElements(this);
                break;
            case 'data-media-icon':
                updateDynamicAttributeOnChildren(this, 'data-icon');
                updateDynamicAttributeOnChildren(this, 'style');
                updateConditionalElements(this);
                break;
            case 'data-media-icon-color':
                updateDynamicAttributeOnChildren(this, 'data-color');
                updateDynamicAttributeOnChildren(this, 'style');
                break;
            case 'data-media-icon-color-weight':
                updateDynamicAttributeOnChildren(this, 'data-color-weight');
                break;
            case 'data-media-size':
                updateDynamicAttributeOnChildren(this, 'class');
                updateConditionalElements(this);
                break;
            case 'data-footer-border':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'disabled':
                updateDynamicAttributeOnChildren(this, 'disabled');
                updateDynamicProperty(this.input, 'disabled');
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-is-loading':
                updateDynamicAttributeOnChildren(this, 'class');
                updateConditionalElements(this);
                break;
            case 'data-is-white-background':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-heading-level':
                updateConditionalElements(this);
                break;
            case 'data-header-size':
                updateDynamicAttributeOnChildren(this, 'class');
                updateDynamicAttributeOnChildren(this, 'data-size');
                updateDynamicAttributeOnChildren(this, 'data-avatar-size');
                break;
            case 'data-control-type':
                updateDynamicAttribute(this.input, 'for');
                updateDynamicAttribute(this.input, 'id');
                updateDynamicAttributeOnChildren(this, 'data-icon');
                updateDynamicAttribute(this.input, 'type');
                updateConditionalElements(this);
                break;
            case 'name':
                updateDynamicAttribute(this.input, 'name');
                break;
            case 'value':
                this.value = newVal === 'indeterminate' ? 'indeterminate' : (newVal === 'true');
                break;
            default:
        }
    }
    validateInputs() {
        super.validateInputs();
        if (this.getAttribute('data-media-img-src') != null && this.getAttribute('data-media-img-alt') == null) {
            console.error('The data-media-img-alt attribute is required when data-media-img-src is set');
        }
        if (this.getAttribute('data-control-type') != null && this.getAttribute('id') == null) {
            console.error('The id attribute is required when data-control-type is set');
        }
    }
    set value(value) {
        const isIndeterminate = value === 'indeterminate';
        if (isIndeterminate && this.getAttribute('data-check-type') === 'toggle') {
            this.input.indeterminate = false;
            this.input.checked = false;
            console.error('The indeterminate state for data-check-type="toggle" is not supported');
        }
        this.input.indeterminate = isIndeterminate;
        this.input.checked = value === true;
        updateDynamicAttributeOnChildren(this, 'data-icon');
        updateDynamicAttributeOnChildren(this, 'class');
        updateDynamicProperty(this.input, 'checked');
        updateConditionalElements(this);
    }
    get value() {
        return this.input.getValue();
    }
    constructor() {
        super();
        this.widgetTag = 'neon-card-media';
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
        this.input = createElement('input', {
            attrs: {
                type: dynamicAttr(() => { return this.getAttribute('data-control-type'); }),
                class: 'neon-sr-only neon-card-media-input',
                id: dynamicAttr(() => { return 'checkbox-' + this.getAttribute('id'); }),
                name: dynamicAttr(() => { return this.getAttribute('name'); }),
                // disabled: dynamicAttr(() => { return this.getAttribute('disabled'); }),
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-heading'); }),
            },
            properties: {
                //getValue is a function not a dynamic property
                getValue: function () {
                    return this.indeterminate ? 'indeterminate' : this.checked;
                },
                checked: dynamicProperty(() => { return this.getAttribute('value') === 'true'; }),
                indeterminate: this.getAttribute('value') === 'indeterminate',
                disabled: dynamicProperty(() => { return this.getAttribute('disabled') === 'true'; })
            },
            listeners: {
                change: (event) => {
                    updateDynamicAttributeOnChildren(this, 'data-icon');
                    updateDynamicAttributeOnChildren(this, 'class');
                    if (this.getAttribute('data-control-type') === 'radio' && this.getAttribute('data-auto-radio-updates') === 'true' && this.value === true) {
                        //We can't use document.querySelectorAll here because this needs to work within the shadow-dom context.
                        for (const cardMediaElement of Array.from(getRootDoc(this).querySelectorAll(`neon-2_20_1-card-media[name="${this.getAttribute('name')}"]`))) {
                            if (cardMediaElement instanceof NeonCardMedia && cardMediaElement !== this) {
                                cardMediaElement.value = false;
                            }
                        }
                    }
                    this.dispatchEvent(new CustomEvent('neonValueChange', {
                        bubbles: true,
                        detail: {
                            value: this.value
                        }
                    }));
                },
                blur: (event) => {
                    //blur events don't bubble, so we need to make the neon-2_20_1-radio-single blast it out to whoever wants to listen to it.
                    this.dispatchEvent(new Event('blur'));
                }
            }
        });
    }
    getMediaType() {
        if (this.slotExists('card-media-object')) {
            return 'slot';
        }
        else if (this.getAttribute('data-media-img-src') != null) {
            return 'image';
        }
        else if (this.getAttribute('data-media-illustration') != null) {
            return 'illustration';
        }
        else if (this.getAttribute('data-media-icon') != null) {
            return 'icon';
        }
        else {
            return 'none';
        }
    }
    getAspectRatioClass() {
        return `neon-aspect-ratio-${(this.getAttribute('data-media-aspect-ratio') || '3:2').replace(':', '-')}`;
    }
    getMediaCardClasses() {
        // this class is also being used in _layout-master.scss for handling transparency when components are children of a themed gradient background
        // if you change this class please search the whole codebase for its usage and update those occurrences as well
        const classes = ['neon-card-media'];
        if (this.getAttribute('data-orientation') === 'horizontal') {
            classes.push('neon-card-media-horizontal');
        }
        if (this.getAttribute('data-is-white-background') === 'true') {
            classes.push('neon-card-media-on-white');
        }
        if (this.value === true) {
            classes.push('neon-card-media-selected');
        }
        if (this.getAttribute('disabled') === 'true') {
            classes.push('neon-card-media-disabled');
        }
        if (this.getAttribute('data-is-loading') === 'true') {
            classes.push('neon-card-media-skeleton');
        }
        return classes.join(' ');
    }
    getCardMediaContainerClasses() {
        const classes = ['neon-card-media-container'];
        if (this.getAttribute('data-media-size') !== null) {
            classes.push(`neon-card-media-container-${this.getAttribute('data-media-size')}`);
        }
        else {
            classes.push('neon-card-media-container-medium');
        }
        if (this.getMediaType() === 'icon') {
            classes.push('neon-card-media-container-icon');
        }
        if (this.getAttribute('data-media-aspect-ratio') !== null) {
            classes.push(this.getAspectRatioClass());
        }
        return classes.join(' ');
    }
    // https://www.w3.org/TR/css-color-5/#relative-colors
    // https://caniuse.com/css-relative-colors
    getCardMediaContainerInlineStyles() {
        const backgroundColor = this.getAttribute('data-media-icon-color') || 'gray';
        if (this.getMediaType() === 'icon') {
            return `background-color: rgb(from var(--neon-2_20_1-color-${backgroundColor}-500) r g b / 10%)`;
        }
    }
    getMediaBlockClasses() {
        const classes = ['neon-card-media-object'];
        const mediaType = this.getMediaType();
        if (mediaType !== 'none') {
            classes.push(`neon-card-media-object-${mediaType}`);
            classes.push(this.getAspectRatioClass());
        }
        if (this.getAttribute('data-media-fit') === 'contain') {
            classes.push('neon-card-media-object-fit-contain');
        }
        else {
            classes.push('neon-card-media-object-fit-cover');
        }
        if (this.getAttribute('data-media-border') === 'true') {
            classes.push('neon-card-media-object-border');
        }
        if (this.getAttribute('data-is-loading') === 'true') {
            classes.push('neon-card-media-object-skeleton neon-2_20_1-skeleton-rectangle neon-2_20_1-skeleton-shimmer');
        }
        return classes.join(' ');
    }
    getHeadingElementTag() {
        switch (this.getAttribute('data-heading-level')) {
            case 'h2':
                return 'h2';
            case 'h3':
                return 'h3';
            case 'h4':
                return 'h4';
            case 'h5':
                return 'h5';
            case 'h6':
                return 'h6';
            default:
                return 'h2';
        }
    }
    getHeadingClasses() {
        const classes = ['neon-card-media-heading'];
        switch (this.getAttribute('data-header-size')) {
            case 'medium':
                classes.push('neon-card-media-heading-medium');
                break;
            case 'large':
                classes.push('neon-card-media-heading-large');
                break;
            default:
                classes.push('neon-card-media-heading-default');
        }
        return classes.join(' ');
    }
    getHeaderIconSize() {
        switch (this.getAttribute('data-header-size')) {
            case 'medium':
                return 'large';
            case 'large':
                return 'x-large';
            default:
                return 'medium';
        }
    }
    getHeaderAvatarSize() {
        switch (this.getAttribute('data-header-size')) {
            case 'medium':
                return 'small';
            case 'large':
                return 'medium';
            default:
                return 'extra-small';
        }
    }
    // there should be a shared util for this at some point
    getInputVisualIcon() {
        switch (this.getAttribute('data-control-type')) {
            case 'checkbox':
                if (this.value === true) {
                    return 'checkbox-selected';
                }
                else {
                    return 'checkbox-unselected';
                }
            case 'radio':
                if (this.value === true) {
                    return 'radio-selected';
                }
                else {
                    return 'radio-unselected';
                }
            default:
                return '';
        }
    }
    isAvatarRenderable() {
        return this.getAttribute('data-avatar-profile-picture-src') != null || (this.getAttribute('data-avatar-first-name') != null || this.getAttribute('data-avatar-last-name') != null);
    }
    // it looks like a checkbox or radio button, but its not!
    renderConditionalInputVisual() {
        return createElementIf(() => { return this.getAttribute('data-control-type') != null; }, 'div', {
            attrs: {
                class: dynamicAttr(() => {
                    const classes = ['neon-card-media-input-visual'];
                    switch (this.getAttribute('data-control-type')) {
                        case 'checkbox':
                            classes.push('neon-card-media-input-visual-checkbox');
                            break;
                        case 'radio':
                            classes.push('neon-card-media-input-visual-radio');
                            break;
                    }
                    return classes.join(' ');
                }),
            }
        }, () => {
            return createElement('neon-2_20_1-icon', {
                attrs: {
                    class: 'neon-card-media-input-visual-icon',
                    'data-icon': dynamicAttr(() => { return this.getInputVisualIcon(); })
                }
            });
        });
    }
    renderConditionalMediaImage() {
        return createElementIf(() => { return this.getMediaType() === 'image'; }, 'neon-2_20_1-image', {
            attrs: {
                class: dynamicAttr(() => { return this.getMediaBlockClasses(); }),
                'data-src': dynamicAttr(() => { return this.getAttribute('data-media-img-src'); }),
                'data-alt': dynamicAttr(() => { return this.getAttribute('data-media-img-alt'); }),
                'data-object-fit': dynamicAttr(() => { return this.getAttribute('data-media-fit'); }),
            },
        });
    }
    // when new components are created we should start exporting render functions that take in an object of attributes so can stop repeating these pattern
    renderConditionalMediaIllustration() {
        return createElementIf(() => { return this.getMediaType() === 'illustration'; }, 'neon-2_20_1-illustration', {
            attrs: {
                class: dynamicAttr(() => { return this.getMediaBlockClasses(); }),
                'data-illustration': dynamicAttr(() => { return this.getAttribute('data-media-illustration'); }),
                'data-color': dynamicAttr(() => { return this.getAttribute('data-media-illustration-color'); }),
                'data-background': dynamicAttr(() => { return this.getAttribute('data-media-illustration-background'); }),
                'data-show-people': dynamicAttr(() => { return this.getAttribute('data-media-illustration-show-people') !== 'false' ? 'true' : 'false'; })
            }
        });
    }
    // this icon is really big
    // it grows to the size of its container with is dictated by the aspect-ratio and the width of the media card
    renderConditionalMediaIcon() {
        return createElementIf(() => { return this.getMediaType() === 'icon'; }, 'neon-2_20_1-icon', {
            attrs: {
                class: dynamicAttr(() => { return this.getMediaBlockClasses(); }),
                'data-icon': dynamicAttr(() => { return this.getAttribute('data-media-icon'); }),
                'data-color': dynamicAttr(() => { return this.getAttribute('data-media-icon-color'); }),
                'data-color-weight': dynamicAttr(() => { return this.getAttribute('data-media-icon-color-weight'); })
            }
        });
    }
    renderHeader() {
        return createElement('header', {
            attrs: {
                class: 'neon-card-media-header',
            }
        }, createElementIf(() => { return this.isAvatarRenderable(); }, 'neon-2_20_1-avatar', {
            attrs: {
                class: 'neon-card-media-avatar',
                'data-avatar-size': dynamicAttr(() => { return this.getHeaderAvatarSize(); }),
                'data-first-name': dynamicAttr(() => { return this.getAttribute('data-avatar-first-name'); }),
                'data-last-name': dynamicAttr(() => { return this.getAttribute('data-avatar-last-name'); }),
                'data-profile-picture-src': dynamicAttr(() => { return this.getAttribute('data-avatar-profile-picture-src'); }),
                'data-background-option': dynamicAttr(() => { return this.getAttribute('data-avatar-background-option'); }),
            }
        }), createElementIf(() => { return this.getAttribute('data-header-icon') != null && this.isAvatarRenderable() === false; }, 'neon-2_20_1-icon', {
            attrs: {
                class: 'neon-card-media-heading-icon',
                'data-icon': dynamicAttr(() => { return this.getAttribute('data-header-icon'); }),
                'data-color': dynamicAttr(() => { return this.getAttribute('data-header-icon-color'); }),
                'data-color-weight': dynamicAttr(() => { return this.getAttribute('data-header-icon-color-weight'); }),
                'data-size': dynamicAttr(() => { return this.getHeaderIconSize(); }),
            }
        }), createElementIf(() => { return this.getAttribute('data-heading-url') == null; }, 
        // this.getHeadingElementTag() only gets 'called' once so a developer does not have a way to dynamically change heading levels
        // they could bounce an attribute off of another attribute to trigger a re-render, but that is not ideal
        // Brian and Scott talked about this and have a plan to address it in the future
        this.getHeadingElementTag(), {
            'text': dynamicText(() => { return this.getAttribute('data-heading'); }),
            attrs: {
                class: dynamicAttr(() => { return this.getHeadingClasses(); }),
            },
            extensions: tooltipOnEllipsis
        }), createElementIf(() => { return this.getAttribute('data-heading-url') != null; }, 'a', {
            'text': dynamicText(() => { return this.getAttribute('data-heading'); }),
            attrs: {
                class: dynamicAttr(() => { return this.getHeadingClasses(); }),
                href: dynamicAttr(() => { return this.getAttribute('data-heading-url') != null ? this.getAttribute('data-heading-url') : undefined; }),
                target: dynamicAttr(() => { return this.getAttribute('data-heading-url-open-in-new-tab') === 'true' ? '_blank' : undefined; })
            },
            listeners: {
                click: (event) => {
                    if (typeof this.headingUrlOnClickCallback === 'function') {
                        event.preventDefault();
                        event.stopImmediatePropagation();
                        this.headingUrlOnClickCallback(event);
                    }
                }
            },
            extensions: tooltipOnEllipsis
        }), 
        // card-media-actions is like card-actions of neon-card-standard
        createElementIf(() => { return this.slotExists('card-media-actions'); }, 'div', {
            attrs: {
                class: 'neon-card-media-header-actions'
            }
        }, () => { return this.getSlot('card-media-actions'); }));
    }
    renderSubheading() {
        return createElementIf(() => { return this.getAttribute('data-subheading') != null; }, 'div', {
            attrs: {
                class: 'neon-card-media-subheading-container'
            },
        }, () => {
            return [
                createElementIf(() => { return this.getAttribute('data-subheading-icon') != null; }, 'neon-2_20_1-icon', {
                    attrs: {
                        class: 'neon-card-media-subheading-icon',
                        'data-size': 'small',
                        'data-icon': dynamicAttr(() => { return this.getAttribute('data-subheading-icon'); }),
                        'data-color': dynamicAttr(() => { return this.getAttribute('data-subheading-icon-color'); }),
                        'data-color-weight': dynamicAttr(() => { return this.getAttribute('data-subheading-icon-color-weight'); })
                    }
                }),
                createElement('p', {
                    attrs: {
                        class: 'neon-card-media-subheading'
                    },
                    text: dynamicText(() => { return this.getAttribute('data-subheading'); }),
                    extensions: tooltipOnEllipsis
                })
            ];
        });
    }
    renderBody() {
        return createElementIf(() => { return this.slotExists('card-media-body'); }, 'div', {
            attrs: {
                class: 'neon-card-media-body'
            }
        }, () => { return this.getSlot('card-media-body'); });
    }
    renderFooter() {
        return createElementIf(() => { return this.slotExists('card-media-footer'); }, 'div', {
            attrs: {
                class: dynamicAttr(() => { return 'neon-card-media-footer' + (this.getAttribute('data-footer-border') === 'true' ? ' neon-card-media-footer-border' : ''); })
            }
        }, () => { return this.getSlot('card-media-footer'); });
    }
    // renders the main primary content of the card-media.
    // The render function will wrap it in a label tag, or not, depending on the control type.
    renderPrimaryContent() {
        return [
            createElement('div', {
                attrs: {
                    class: 'neon-card-media-primary-content'
                }
            }, createElement('div', {
                attrs: {
                    class: dynamicAttr(() => { return this.getCardMediaContainerClasses(); }),
                    style: dynamicAttr(() => { return this.getCardMediaContainerInlineStyles(); })
                }
            }, this.renderConditionalInputVisual(), this.renderConditionalMediaImage(), this.renderConditionalMediaIllustration(), this.renderConditionalMediaIcon(), this.slotExists('card-media-object') ? this.getSlot('card-media-object') : undefined), createElement('div', {
                attrs: {
                    class: 'neon-card-media-content'
                }
            }, this.renderHeader(), this.renderSubheading(), this.renderBody())),
            this.renderFooter()
        ];
    }
    renderLoadingSkeleton() {
        return createElement('div', {
            attrs: {
                class: dynamicAttr(() => { return this.getMediaCardClasses(); })
            }
        }, createElement('div', {
            attrs: {
                class: 'neon-card-media-primary-content'
            }
        }, createElement('div', {
            attrs: {
                class: dynamicAttr(() => { return this.getCardMediaContainerClasses(); }),
            }
        }, createElement('div', {
            attrs: {
                class: dynamicAttr(() => { return this.getMediaBlockClasses(); }),
            }
        })), createElement('div', {
            attrs: {
                class: 'neon-card-media-content neon-card-media-content-skeleton'
            }
        }, createElement('div', {
            attrs: {
                class: 'neon-card-media-loading-skeleton-heading  neon-2_20_1-skeleton-rectangle neon-2_20_1-skeleton-shimmer'
            }
        }), createElement('div', {
            attrs: {
                class: 'neon-card-media-loading-skeleton-subheading  neon-2_20_1-skeleton-rectangle neon-2_20_1-skeleton-shimmer'
            }
        }), createElement('div', {
            attrs: {
                class: 'neon-card-media-loading-skeleton-body neon-2_20_1-skeleton-rectangle neon-2_20_1-skeleton-shimmer'
            }
        }))), createElement('div', {
            attrs: {
                class: 'neon-card-media-footer'
            }
        }, createElement('div', {
            attrs: {
                class: 'neon-card-media-loading-skeleton-footer  neon-2_20_1-skeleton-rectangle neon-2_20_1-skeleton-shimmer'
            }
        })));
    }
    render() {
        return [
            useElementIf(() => { return this.getAttribute('data-is-loading') === 'true'; }, () => { return this.renderLoadingSkeleton(); }),
            createElementIf(() => { return this.getAttribute('data-control-type') != null && this.getAttribute('data-is-loading') !== 'true'; }, 'label', {
                attrs: {
                    for: dynamicAttr(() => {
                        if (this.getAttribute('id') != null) {
                            return 'checkbox-' + this.getAttribute('id');
                        }
                    }),
                    class: dynamicAttr(() => { return this.getMediaCardClasses(); }),
                },
                listeners: {
                    click: (event) => {
                        if (getClosest(event.target, '[data-neon-card-media-exclude-click-handler],[data-slot="card-media-actions"],[data-slot="card-media-footer"]')) {
                            if (getClosest(event.target, 'a') == null) {
                                //This prevents the checkbox from changing and letting
                                //the click continue up the DOM.
                                event.preventDefault();
                            }
                        }
                    }
                }
            }, () => {
                return [this.input, ...this.renderPrimaryContent()];
            }),
            createElementIf(() => { return this.getAttribute('data-control-type') === null && this.getAttribute('data-is-loading') !== 'true'; }, 'div', {
                attrs: {
                    class: dynamicAttr(() => { return this.getMediaCardClasses(); }),
                }
            }, () => {
                return this.renderPrimaryContent();
            })
        ];
    }
    connectedCallback() {
        //The observer needs to be turned on when render() gets called.
        //That's why it's above super.connectedCallback()
        this.tooltipOnEllipsisObserver.connect();
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        this.tooltipOnEllipsisObserver.disconnect();
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
}
