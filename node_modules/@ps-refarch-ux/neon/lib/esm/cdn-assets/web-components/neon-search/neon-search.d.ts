import './neon-search.scss';
import { NeonWidget } from '../shared/neon-widget';
import { type MenuOption } from '../../../interfaces';
import { type Search } from './search';
import { type ShowPopperOptions } from '../shared/poppers';
import { type ConditionalElement } from '../shared/conditional-element';
import { type NeonItemPicker } from '../neon-item-picker/neon-item-picker';
import { type FocusTrap } from '../shared/focus-trap';
export declare class NeonSearch extends NeonWidget {
    widgetTag: string;
    i18nUnsubscribe?: () => void;
    globalSearch: Search | undefined;
    globalSearchUnsubscribe?: () => void;
    closePopperCallback?: (event?: Event, optionOverrides?: ShowPopperOptions) => void;
    private _value;
    private _searchResults;
    isIdRequired: boolean;
    dispatchEventDebounced: (event: Event) => void;
    focusTrap: FocusTrap;
    listItemPopper?: NeonItemPicker;
    clearButton?: HTMLElement;
    constructor();
    static get observedAttributes(): Array<string>;
    get value(): string;
    set value(value: string);
    get searchResults(): Array<MenuOption | 'DIVIDER'> | undefined;
    set searchResults(searchResults: Array<MenuOption | 'DIVIDER'> | undefined);
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    setDispatchEventDebounce(debounceTimeInMs: number): void;
    hookupNewSearch(): void;
    renderListItemPopper(): NeonItemPicker;
    renderClearButton(): HTMLElement;
    render(): Array<HTMLElement | ConditionalElement>;
    getSizeClass(): string;
    isPopperOpen(): boolean;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
