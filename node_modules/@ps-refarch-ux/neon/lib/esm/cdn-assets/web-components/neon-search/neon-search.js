import './neon-search.scss';
import { NeonWidget } from '../shared/neon-widget';
import { dynamicText, createElement, updateDynamicText, updateDynamicAttribute, dynamicAttr, updateDynamicPropertyOnChildren, dynamicProperty, updateDynamicProperty, getNextTabbableElement, updateConditionalElements, updateDynamicAttributeOnChildren, useElementIf } from '../shared/dom-manipulation';
import { addTranslationListener, translate } from '@ps-refarch-ux/mfe-utils';
import { showPopper } from '../shared/poppers';
import { debounce } from '../../../exported-utils/throttle';
import { createFocusTrap } from '../shared/focus-trap';
export class NeonSearch extends NeonWidget {
    constructor() {
        super();
        this.widgetTag = 'neon-search';
        this._value = '';
        this.isIdRequired = true;
        this.setDispatchEventDebounce(500);
    }
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-placeholder-text',
            'data-label-text',
            'data-empty-results-helper-text',
            'data-debounce-time',
            'data-size',
            'data-is-clearable'
        ];
    }
    get value() {
        return this._value;
    }
    set value(value) {
        this._value = value || '';
        updateDynamicPropertyOnChildren(this, 'value');
        updateConditionalElements(this);
        if (this.listItemPopper != null) {
            updateDynamicProperty(this.listItemPopper, 'filterString');
        }
    }
    get searchResults() {
        return this._searchResults;
    }
    set searchResults(searchResults) {
        if (searchResults !== this._searchResults) {
            this._searchResults = searchResults;
            if (this.closePopperCallback == null) {
                //Case 1: No popper is visible.
                if (searchResults != null) {
                    this.closePopperCallback = showPopper(this, this.renderListItemPopper(), {
                        placement: this.getAttribute('data-popper-placement') || 'bottom-start',
                        makePopperSameWidthAs: 'relative-element',
                        offset: 4,
                        popperPreClosedCallback: () => {
                            var _a;
                            (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
                        },
                        popperPostClosedCallback: () => {
                            var _a;
                            delete this.closePopperCallback;
                            (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
                            const inputEl = this.querySelector('input');
                            if (inputEl != null) {
                                inputEl.focus();
                            }
                        }
                    });
                }
                //Else, do nothing.
            }
            else {
                //Case 2: A popper is already visible.
                if (searchResults == null) {
                    //if the search results were set to null then that means that we don't want to show the popper anymore.
                    this.closePopperCallback();
                }
                else {
                    //refresh the contents of the list item.
                    if (this.listItemPopper != null) {
                        updateDynamicProperty(this.listItemPopper, 'filterString');
                        updateDynamicProperty(this.listItemPopper, 'menuOptions');
                    }
                }
            }
        }
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateDynamicText(this);
                this.hookupNewSearch();
                break;
            case 'data-placeholder-text':
                updateDynamicAttribute(this, 'placeholder');
                break;
            case 'data-label-text':
                updateDynamicText(this);
                break;
            case 'data-empty-results-helper-text':
                updateDynamicAttribute(this, 'data-empty-results-helper-text');
                break;
            case 'data-size':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-debounce-time':
                if (!isNaN(newVal) && newVal != null) {
                    const debounceTimeInMs = Number(this.getAttribute('data-debounce-time'));
                    this.setDispatchEventDebounce(debounceTimeInMs);
                }
                break;
            case 'data-is-clearable':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            default:
        }
    }
    setDispatchEventDebounce(debounceTimeInMs) {
        this.dispatchEventDebounced = debounce((event) => {
            this.dispatchEvent(event);
        }, debounceTimeInMs);
    }
    hookupNewSearch() {
        var _a, _b;
        //Remove any previous listeners
        if (this.globalSearchUnsubscribe != null) {
            this.globalSearchUnsubscribe();
            delete this.globalSearchUnsubscribe;
        }
        const id = this.getAttribute('id');
        if (id != null) {
            this.globalSearch = window.neonToolkits['neon-2_20_1-search'].get(id);
            const searchResultsUnsubscribe = (_a = this.globalSearch) === null || _a === void 0 ? void 0 : _a.getSearchResultsStream().subscribe((searchResults) => {
                //Automatically opens the popper when set.
                this.searchResults = searchResults;
            });
            const searchTextUnsubscribe = (_b = this.globalSearch) === null || _b === void 0 ? void 0 : _b.getCharacterStream().subscribe((value) => {
                if (this.value !== value) {
                    this.value = value;
                }
            });
            this.globalSearchUnsubscribe = () => {
                if (searchResultsUnsubscribe != null && searchTextUnsubscribe != null) {
                    searchResultsUnsubscribe();
                    searchTextUnsubscribe();
                }
            };
        }
    }
    renderListItemPopper() {
        this.listItemPopper = createElement('neon-2_20_1-item-picker', {
            attrs: {
                id: this.getAttribute('id') + 'item-picker',
                'data-header-text': translate('neon|search|Search Results'),
                'data-constrain-width': 'false',
                'data-empty-results-helper-text': dynamicAttr(() => { return this.getAttribute('data-empty-results-helper-text'); }),
                'data-control-type': 'popper'
            },
            properties: {
                filterString: dynamicProperty(() => {
                    return this.value;
                }),
                menuOptions: dynamicProperty(() => { return this.searchResults; })
            },
            listeners: {
                keydown: (event) => {
                    var _a, _b, _c, _d, _e;
                    const keyEvent = event;
                    const clickableElementsInPopper = Array.from(((_a = this.listItemPopper) === null || _a === void 0 ? void 0 : _a.querySelectorAll('a,button')) || []);
                    const firstElementWasFocused = clickableElementsInPopper.length > 0 && clickableElementsInPopper[0].matches(':focus');
                    if ((/tab/i).test(keyEvent.key)) {
                        if (clickableElementsInPopper.length === 0) {
                            return;
                        }
                        //How do I tell which item is in focus?
                        // if shift was down and the first item on the list is focussed, then focus on the search input.
                        if (keyEvent.shiftKey === true) {
                            if (firstElementWasFocused) {
                                event.preventDefault();
                                (_b = this.focusTrap) === null || _b === void 0 ? void 0 : _b.deactivate();
                                if (this.clearButton instanceof HTMLElement) {
                                    this.clearButton.focus();
                                }
                                else {
                                    (_c = this.querySelector('input')) === null || _c === void 0 ? void 0 : _c.focus();
                                }
                            }
                        }
                        else {
                            // if it's the last item in the list, then close the popper and go to the next element.
                            if ((_d = clickableElementsInPopper.at(-1)) === null || _d === void 0 ? void 0 : _d.matches(':focus')) {
                                const inputEl = this.querySelector('input');
                                if (inputEl != null) {
                                    const nextTabbableElement = getNextTabbableElement(inputEl);
                                    if (nextTabbableElement instanceof HTMLElement) {
                                        //if there are no tabbable elements between the input and the listItemPopper,
                                        //then the nextTabbableElement will be inside the listItemPopper.
                                        //In that case, we want the tab key to just tab out of the browser.
                                        //So we only focus on that next element if it's NOT in the listItemPopper.
                                        if (!((_e = this.listItemPopper) === null || _e === void 0 ? void 0 : _e.contains(nextTabbableElement))) {
                                            event.preventDefault();
                                            setTimeout(() => {
                                                nextTabbableElement.focus();
                                            });
                                        }
                                    }
                                }
                                // setTimeout(() => {
                                //This is in a setTimout because when it isn't, the focus isn't allowed to properly leave the page if
                                //It's supposed to.
                                if (this.closePopperCallback != null) {
                                    this.closePopperCallback(undefined, { focusOnClose: false });
                                }
                                // }, 2000);
                            }
                        }
                    }
                }
            }
        });
        return this.listItemPopper;
    }
    renderClearButton() {
        this.clearButton = createElement('button', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + 'clear-button'; }),
                class: 'neon-search-clear-button',
                'aria-label': translate('neon|search|Clear Search'),
                'data-size': 'medium'
            },
            listeners: {
                click: () => {
                    var _a;
                    this.value = '';
                    (_a = this.globalSearch) === null || _a === void 0 ? void 0 : _a.setSearchText('');
                    this.dispatchEvent(new CustomEvent('neonSearchValueChange'));
                },
                keydown: (event) => {
                    const keyEvent = event;
                    if (this.isPopperOpen() && keyEvent.shiftKey !== true) {
                        if ((/tab/i).test(keyEvent.key)) {
                            //prevent the browser from changing focus from the tab key getting pressed.
                            event.preventDefault();
                            setTimeout(() => {
                                if (this.listItemPopper != null) {
                                    this.focusTrap = createFocusTrap(this.listItemPopper, {
                                        // escapeDeactivates: false, // Pressing escape should not deactivate the focus trap.
                                        clickOutsideDeactivates: true,
                                        delayInitialFocus: true,
                                        onActivate: () => {
                                            var _a;
                                            const firstClickable = (_a = this.listItemPopper) === null || _a === void 0 ? void 0 : _a.querySelector('button,a');
                                            if (firstClickable != null) {
                                                firstClickable.focus();
                                            }
                                            else {
                                                // this is the case when the clear button is focused while the listItemPopper is open displaying the empty results.
                                                if (this.clearButton instanceof HTMLElement) {
                                                    const nextTabbableElement = getNextTabbableElement(this.clearButton);
                                                    if (nextTabbableElement instanceof HTMLElement) {
                                                        nextTabbableElement.focus();
                                                    }
                                                }
                                            }
                                        }
                                    });
                                    this.focusTrap.activate();
                                    this.focusTrap.pause(); // we pause it because we don't actually want the focus trap to trap focus for the search. this is needed to "escape" the focus trap of its parents (drawer, dialog, layout-detail)
                                }
                            });
                        }
                    }
                }
            }
        }, createElement('neon-2_20_1-icon', {
            attrs: {
                'data-icon': 'close',
                'data-size': 'small'
            }
        }));
        return this.clearButton;
    }
    render() {
        return [
            createElement('label', {
                attrs: { class: dynamicAttr(() => { return this.getSizeClass(); }) }
            }, createElement('span', {
                attrs: { class: 'neon-sr-only' },
                text: dynamicText(() => {
                    return this.getAttribute('data-label-text') || translate('neon|search|Search');
                })
            }), createElement('neon-2_20_1-icon', {
                attrs: {
                    'data-icon': 'search|rtl-flip',
                    'data-size': 'medium',
                    'data-size-mobile': 'large',
                    class: 'neon-search-main-icon'
                }
            }), createElement('input', {
                attrs: {
                    id: dynamicAttr(() => {
                        return this.getAttribute('id') + 'search-input';
                    }),
                    class: 'neon-search-main-input',
                    type: 'text',
                    placeholder: dynamicAttr(() => {
                        return this.getAttribute('data-placeholder-text');
                    })
                },
                listeners: {
                    input: (event) => {
                        var _a;
                        const searchText = event.target.value;
                        this.value = searchText;
                        (_a = this.globalSearch) === null || _a === void 0 ? void 0 : _a.setSearchText(searchText);
                        this.dispatchEvent(new CustomEvent('neonSearchValueChange'));
                        this.dispatchEventDebounced(new CustomEvent('neonSearchUserStoppedTyping'));
                    },
                    keydown: (event) => {
                        const keyEvent = event;
                        if (this.isPopperOpen() && keyEvent.shiftKey !== true) {
                            if ((/tab/i).test(keyEvent.key)) {
                                //prevent the browser from changing focus from the tab key getting pressed.
                                event.preventDefault();
                                setTimeout(() => {
                                    var _a;
                                    let firstClickable;
                                    if (this.clearButton instanceof HTMLElement) {
                                        firstClickable = this.clearButton;
                                    }
                                    else {
                                        firstClickable = (_a = this.listItemPopper) === null || _a === void 0 ? void 0 : _a.querySelector('button,a');
                                    }
                                    if (firstClickable != null) {
                                        firstClickable.focus();
                                    }
                                });
                            }
                        }
                    }
                },
                properties: {
                    value: dynamicProperty(() => {
                        return this.value;
                    })
                }
            }), useElementIf(() => { return this.value != null && this.value !== '' && this.getAttribute('data-is-clearable') !== 'false'; }, () => { return this.renderClearButton(); }))
        ];
    }
    getSizeClass() {
        const size = this.getAttribute('data-size');
        switch (size) {
            case 'small':
                return ' input-size-small';
            case 'medium':
                return ' input-size-medium';
            case 'large':
                return ' input-size-large';
            case 'x-large':
                return ' input-size-x-large';
            case 'auto':
                return ' input-size-auto';
            default:
                return '';
        }
    }
    isPopperOpen() {
        return this.closePopperCallback != null;
    }
    connectedCallback() {
        super.connectedCallback();
        this.hookupNewSearch();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        var _a;
        if (this.closePopperCallback != null) {
            this.closePopperCallback(undefined, { focusOnClose: false });
        }
        (_a = this.focusTrap) === null || _a === void 0 ? void 0 : _a.deactivate();
        if (this.globalSearchUnsubscribe != null) {
            this.globalSearchUnsubscribe();
            delete this.globalSearchUnsubscribe;
        }
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
}
