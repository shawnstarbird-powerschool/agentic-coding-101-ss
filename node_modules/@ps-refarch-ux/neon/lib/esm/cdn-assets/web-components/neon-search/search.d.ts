import { type MenuOption } from '../../../interfaces';
import { type ReadOnlyStream } from '../../../interfaces/read-only-stream';
import { type Search as SearchInterface } from '../../../interfaces/search';
export declare class Search implements SearchInterface {
    private userCharacterStream;
    private searchResultsStream;
    setSearchResults(results: Array<MenuOption | 'DIVIDER'> | undefined): void;
    getCharacterStream(): ReadOnlyStream<string>;
    getDebouncedCharacterStream(timeInMs?: number): ReadOnlyStream<string>;
    getSearchResultsStream(): ReadOnlyStream<Array<MenuOption | 'DIVIDER'> | undefined>;
    setSearchText(value: string): void;
}
