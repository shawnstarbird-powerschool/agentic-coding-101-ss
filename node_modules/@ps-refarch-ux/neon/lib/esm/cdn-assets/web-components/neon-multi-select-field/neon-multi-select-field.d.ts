import { AbstractInputField } from '../shared/abstract-input-field';
import './neon-multi-select-field.scss';
import type { ConditionalElement } from '../shared/conditional-element';
import type { MultiSelectOptionWithDividers, MultiSelectOptionWithDividersGroup } from '../../../interfaces';
import { TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
import { type NeonItemPicker } from '../neon-item-picker/neon-item-picker';
export declare class NeonMultiSelectField extends AbstractInputField {
    widgetTag: string;
    isUsesLegend: boolean;
    tooltipOnEllipsisObserver: TooltipOnEllipsisObserver;
    static get observedAttributes(): Array<string>;
    i18nUnsubscribe?: () => void;
    runValidationOnceOptionsSet: boolean;
    dropdownButton?: HTMLElement;
    private _value;
    private _modelValue;
    private tempModelValue;
    popoverElement?: NeonItemPicker;
    closePopover?: (clickEvent?: Event) => void;
    private _options;
    private isDrawerOrDialogSHowing;
    private drawerOrDialogElement?;
    set options(value: Array<MultiSelectOptionWithDividers | MultiSelectOptionWithDividersGroup | 'DIVIDER'> | undefined);
    get options(): Array<MultiSelectOptionWithDividers | MultiSelectOptionWithDividersGroup | 'DIVIDER'> | undefined;
    validateModelValueType(modelValue: any): any;
    setModelValue(modelValue: any, skip?: boolean): boolean;
    setValue(value: any, skip?: boolean): void;
    set value(value: string);
    get value(): string;
    get modelValue(): Array<string> | undefined;
    set modelValue(value: any);
    attributeChanged(attribute: string, oldValue: string, newValue: string): void;
    getOptionsMap(): Record<string, MultiSelectOptionWithDividers>;
    constructor();
    /**
     * Handles the removal of a chip from the multi-select field.
     * @param event The event object containing details about the chip removal.
     */
    neonChipRemoved(event: Event): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    generateValidationRules(): void;
    /**
     * @returns true when a modal should be shown, false when a popper should be shown.
     */
    isModalThresholdMet(): boolean;
    showMultiSelectOptionsModal(): void;
    closeDrawerOrDialog(): void;
    renderModalButtons(): HTMLElement;
    renderDialog(): HTMLElement;
    renderDrawer(): HTMLElement;
    showMultiSelectOptionsPopper(): void;
    getMainButtonId(): string;
    getAriaLabelForInput(): string | undefined | null;
    render(): Array<HTMLElement | ConditionalElement>;
    renderDrawerOrDialog(): HTMLElement;
    renderInputElement(): HTMLElement;
    renderItemPicker(controlType: 'popper' | 'drawer' | 'dialog'): NeonItemPicker | undefined;
    isIntermediateSelection(): boolean;
    /**
      * Renders the intermediate selection Buttons.
      * @returns {ConditionalElement} The rendered intermediate selection button.
      */
    renderConditionalIntermediateSelectionButtons(): ConditionalElement;
    /**
     * Takes this.tempModelValue and commits it to this.modelValue and updates everything in the web-component to render correctly.
     */
    commitTempModelValue(): void;
    /**
     * Renders the selected options (chips) in the multi-select field.
     * This function generates the HTML structure representing the selected options (chips) based on the current state of the component.
     * @returns {ConditionalElement} The HTML structure representing the selected options (chips) within a ConditionalElement.
     */
    renderConditionalChipSetOptions(): ConditionalElement;
    /**
     * Redraws the selected options (chips) in the multi-select field.
     * This function updates the display of selected options based on the current state of the component.
     */
    redrawChipSetOptions(): void;
    private isMultiSelectOptionGroup;
    private convertToMultiSelectMenuOption;
    private convertOptionsToMultiSelectMenuOptions;
    private convertToMultiSelectMenuOptionGroup;
    private convertModelValueToValueMap;
    private createChipsFromOptions;
}
