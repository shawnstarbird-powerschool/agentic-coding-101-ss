import { NeonWidgetSlotted, type RequiredSlotInfo } from '../shared/neon-widget-slotted';
import './neon-avatar-button.scss';
/** Overall, this component code is simple. It's a button that wraps around
 *  the neon avatar component to give it click functionality.
 *  The only thing we should note is that if the Avatar inputs or code change,
 *  then we need to make sure this Avatar Button component does not break as well
 */
export declare class NeonAvatarButton extends NeonWidgetSlotted {
    static get observedAttributes(): Array<string>;
    static get requiredSlots(): Array<RequiredSlotInfo>;
    widgetTag: string;
    constructor();
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    renderAvatarButton(): HTMLElement;
    validateInputs(): void;
    render(): HTMLElement;
}
