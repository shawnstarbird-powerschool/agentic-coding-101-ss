import './neon-card-selectable.scss';
import { createElement, updateDynamicText, createElementIf, updateConditionalElements, dynamicAttr, dynamicText, useElementIf, dynamicProperty, updateDynamicPropertyOnChildren, updateDynamicAttributeOnChildren, useElementIfElse, getClosest } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver, } from '../shared/extensions/tooltip-on-ellipsis';
import { stringHasCharacters } from '../shared/string-utils';
export class NeonCardSelectable extends NeonWidgetSlotted {
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            // Container
            'id',
            'value',
            'name',
            'disabled',
            'data-disabled-tooltip-text',
            // Heading
            'data-heading',
            'data-heading-url',
            'data-heading-url-open-in-new-tab',
            //input checkbox or radio
            'data-control-type',
            'data-header-tag-icon',
            'data-header-tag-text',
            'data-header-tag-type',
            'data-header-tag-subtext',
            //icons
            'data-header-icon',
            'data-header-icon-color',
            'data-header-icon-color-weight',
            //content
            'data-subheading',
            'data-injection-spacing'
        ];
    }
    get value() {
        return this._value;
    }
    set value(value) {
        this._value = value;
        if (value === 'indeterminate' && this.getAttribute('data-control-type') === 'radio') {
            console.error('The indeterminate state for data-control-type="radio" is not supported.');
        }
        updateDynamicPropertyOnChildren(this, 'value');
        updateDynamicPropertyOnChildren(this, 'indeterminate');
        updateDynamicAttributeOnChildren(this, 'class');
        updateDynamicAttributeOnChildren(this, 'data-icon');
    }
    constructor() {
        super();
        this.widgetTag = 'neon-card-selectable';
        this.isIdRequired = true;
        this._value = false;
        //The neon-tooltip removes its child nodes and stores them in memory.
        //Our dynamic system requires elements to be in the DOM to be updated so
        //instead of using that, we create a TextNode object and just keep it updated
        //with whatever is in the data-tooltip attribute.  This can be updated whether it's
        //in the DOM or not.
        this.tooltipTextNode = document.createTextNode('');
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttributeOnChildren(this, 'id');
                updateDynamicAttributeOnChildren(this, 'data-target-id');
                break;
            case 'value':
                if (newVal === 'indeterminate') {
                    this.value = 'indeterminate';
                }
                else {
                    this.value = newVal === 'true';
                }
                break;
            case 'data-header-icon':
                updateDynamicAttributeOnChildren(this, 'data-icon');
                updateConditionalElements(this);
                break;
            case 'data-header-tag-icon':
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-header-tag-text':
                updateDynamicAttributeOnChildren(this, 'data-text');
                updateConditionalElements(this);
                break;
            case 'data-header-tag-subtext':
                updateDynamicAttributeOnChildren(this, 'data-subtext');
                break;
            case 'data-header-tag-type':
                updateDynamicAttributeOnChildren(this, 'data-type');
                break;
            case 'data-header-icon-color':
                updateDynamicAttributeOnChildren(this, 'data-color');
                break;
            case 'data-header-icon-color-weight':
                updateDynamicAttributeOnChildren(this, 'data-color-weight');
                break;
            case 'data-heading':
                updateDynamicText(this);
                break;
            case 'data-control-type':
                updateDynamicAttributeOnChildren(this, 'name');
                updateDynamicAttributeOnChildren(this, 'data-icon');
                break;
            case 'data-heading-url':
                updateDynamicAttributeOnChildren(this, 'href');
                updateConditionalElements(this);
                break;
            case 'data-heading-url-open-in-new-tab':
                updateDynamicAttributeOnChildren(this, 'target');
                break;
            case 'data-subheading':
                updateDynamicText(this);
                updateConditionalElements(this);
                break;
            case 'disabled':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'disabled');
                updateDynamicPropertyOnChildren(this, 'disabled');
                updateDynamicAttributeOnChildren(this, 'data-color');
                updateDynamicAttributeOnChildren(this, 'data-color-weight');
                break;
            case 'data-disabled-tooltip-text':
                updateConditionalElements(this);
                this.tooltipTextNode.nodeValue = newVal;
                break;
            case 'name':
                updateDynamicPropertyOnChildren(this, 'data-label-text');
                updateDynamicAttributeOnChildren(this, 'name');
                break;
            case 'data-injection-spacing':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            default:
        }
    }
    //Normally we would put this right inline but it's in a useElementIfElse which
    //works nicer if it's calling functions that encapsulate everything
    renderHeaderIcon() {
        return createElement('neon-2_20_1-icon', {
            attrs: {
                class: 'neon-card-selectable-header-icon',
                'data-icon': dynamicAttr(() => {
                    return this.getAttribute('data-header-icon');
                }),
                'data-size': 'medium',
                'data-color': dynamicAttr(() => {
                    if (this.getAttribute('disabled') !== 'true') {
                        return this.getAttribute('data-header-icon-color');
                    }
                    else {
                        return 'gray';
                    }
                }),
                'data-color-weight': dynamicAttr(() => {
                    if (this.getAttribute('disabled') !== 'true') {
                        return this.getAttribute('data-header-icon-color-weight');
                    }
                    else {
                        return '700';
                    }
                }),
            }
        });
    }
    renderCheckboxOrRadioIcon() {
        return createElement('neon-2_20_1-icon', {
            attrs: {
                class: 'neon-card-selectable-control-icon',
                'data-size': 'x-large',
                'data-icon': dynamicAttr(() => {
                    switch (this.getAttribute('data-control-type')) {
                        default:
                        case 'checkbox':
                            if (this.value === 'indeterminate') {
                                return 'checkbox-indeterminate';
                            }
                            else if (this.value === true) {
                                return 'checkbox-selected';
                            }
                            else {
                                return 'checkbox-unselected';
                            }
                        case 'radio':
                            if (this.value === true) {
                                return 'radio-selected';
                            }
                            else {
                                return 'radio-unselected';
                            }
                    }
                })
            }
        });
    }
    renderTag() {
        return createElement('neon-2_20_1-tag', {
            attrs: {
                class: 'neon-card-selectable-header-tag',
                'data-type': dynamicAttr(() => {
                    return this.getAttribute('data-header-tag-type');
                }),
                'data-icon': dynamicAttr(() => {
                    return this.getAttribute('data-header-tag-icon');
                }),
                'data-text': dynamicAttr(() => {
                    return this.getAttribute('data-header-tag-text');
                }),
                'data-subtext': dynamicAttr(() => {
                    return this.getAttribute('data-header-tag-subtext');
                })
            },
        });
    }
    renderHeader() {
        return createElement('header', {
            attrs: {
                class: 'neon-card-selectable-header',
            },
        }, ...useElementIfElse(() => { return this.getAttribute('data-header-icon') != null; }, () => { return this.renderHeaderIcon(); }, () => { return this.renderCheckboxOrRadioIcon(); }), createElement('div', {
            attrs: {
                class: 'neon-card-selectable-heading-and-tag',
            },
        }, createElement('h2', { attrs: { class: 'neon-card-selectable-heading' } }, createElementIf(() => {
            return this.getAttribute('data-heading-url') == null || this.getAttribute('disabled') === 'true';
        }, 'span', {
            attrs: {
                class: 'neon-card-selectable-heading-text',
            },
            text: dynamicText(() => {
                return this.getAttribute('data-heading');
            }),
            extensions: tooltipOnEllipsis,
        }), createElementIf(() => {
            return this.getAttribute('data-heading-url') != null && this.getAttribute('disabled') !== 'true';
        }, 'a', {
            attrs: {
                class: 'neon-card-selectable-heading-link',
                tabindex: dynamicAttr(() => { return this.getAttribute('disabled') === 'true' ? '-1' : undefined; }),
                href: dynamicAttr(() => {
                    return this.getAttribute('data-heading-url');
                }),
                target: dynamicAttr(() => {
                    if (this.getAttribute('data-heading-url-open-in-new-tab') === 'true') {
                        return '_blank';
                    }
                }),
            },
            properties: {
                disabled: dynamicProperty(() => { return this.getAttribute('disabled') === 'true'; })
            },
            listeners: {
                click: (event) => {
                    if (typeof this.headingUrlOnClickCallback === 'function') {
                        event.preventDefault();
                        event.stopImmediatePropagation();
                        this.headingUrlOnClickCallback(event);
                    }
                }
            },
            text: dynamicText(() => {
                return this.getAttribute('data-heading');
            }),
            extensions: tooltipOnEllipsis,
        })), useElementIf(() => { return stringHasCharacters(this.getAttribute('data-header-tag-text')); }, () => { return this.renderTag(); })), useElementIf(() => { return this.getAttribute('data-header-icon') != null; }, () => { return this.renderCheckboxOrRadioIcon(); }), createElementIf(() => { return this.slotExists('card-selectable-actions'); }, 'div', {
            attrs: {
                class: 'neon-card-selectable-header-actions',
            },
        }, () => {
            return this.getSlot('card-selectable-actions');
        }));
    }
    renderBody() {
        return createElementIf(() => {
            return this.getAttribute('data-subheading') != null || this.slotExists('card-selectable-body');
        }, 'div', {
            attrs: {
                class: 'neon-card-selectable-content',
            },
        }, () => {
            return [
                createElementIf(() => { return this.getAttribute('data-subheading') != null; }, 'p', {
                    attrs: {
                        class: 'neon-card-selectable-subtext',
                    },
                    text: dynamicText(() => {
                        return this.getAttribute('data-subheading');
                    }),
                }),
                createElementIf(() => { return this.slotExists('card-selectable-body'); }, 'div', {
                    attrs: {
                        class: dynamicAttr(() => {
                            return 'neon-card-selectable-content-slot' + (this.getAttribute('data-injection-spacing') !== 'false' ? ' neon-2_20_1-card-selectable-body' : '');
                        }),
                    },
                }, () => {
                    return this.getSlot('card-selectable-body');
                }),
            ];
        });
    }
    render() {
        return createElement('label', {
            attrs: {
                id: dynamicAttr(() => { return `card-selectable-${this.id}`; }),
                class: dynamicAttr(() => {
                    //Note: If you change this class name make sure to check _responsive-grid.scss because it gets styled there too.
                    let classList = 'neon-card-selectable';
                    if (this.value) {
                        classList += ' neon-card-selectable-is-selected';
                    }
                    return classList;
                })
            },
            listeners: {
                click: (event) => {
                    if (getClosest(event.target, '[data-neon-card-selectable-exclude-click-handler],[data-slot="card-selectable-actions"],[data-slot="card-selectable-footer"]')) {
                        //If the element is inside an anchor tag, we can't prevent default so we added that check here.
                        if (getClosest(event.target, 'a') == null) {
                            //This prevents the checkbox from changing and letting
                            //the click continue up the DOM.
                            event.preventDefault();
                        }
                    }
                }
            }
        }, createElement('input', {
            attrs: {
                id: dynamicAttr(() => { return 'card-selectable-input-' + this.getAttribute('id'); }),
                class: 'neon-card-selectable-input neon-sr-only',
                type: dynamicAttr(() => {
                    return this.getAttribute('data-control-type') === 'radio' ? 'radio' : 'checkbox';
                }),
                name: dynamicAttr(() => {
                    return this.getAttribute('name');
                })
            },
            properties: {
                checked: dynamicProperty(() => {
                    return this.value === true;
                }),
                indeterminate: dynamicProperty(() => {
                    return this.value === 'indeterminate';
                }),
                disabled: dynamicProperty(() => { return this.getAttribute('disabled') === 'true'; })
            },
            listeners: {
                change: (event) => {
                    console.log('Neon Card Selectable Change Event');
                    const isChecked = event.target.checked;
                    //TODO: This pattern is NOT something we prefer to do but we are in a time crunch with this ticket.
                    //The preferred way would have been to base everything in css based on the :checked property of the input.
                    //Maybe someday we will refactor it. -> Nope, We actually changed away from using the css selectors in the checkbox and radio single components.
                    if (this.getAttribute('data-control-type') === 'radio' && this.getAttribute('data-auto-radio-updates') === 'true' && isChecked) {
                        for (const cardSelectableElement of Array.from(document.querySelectorAll(`neon-2_20_1-card-selectable[name="${this.getAttribute('name')}"]`))) {
                            if (cardSelectableElement instanceof NeonCardSelectable && cardSelectableElement !== this) {
                                cardSelectableElement.value = false;
                            }
                        }
                    }
                    //We set the value here without using the property setter because
                    //We don't want it to update the DOM anywhere else when this happens.
                    this.value = isChecked;
                    this.dispatchEvent(new CustomEvent('neonValueChange', {
                        bubbles: true,
                        detail: {
                            value: isChecked
                        }
                    }));
                },
                blur: () => {
                    //Blur events don't bubble so we make card standard fire one when the input is blurred.
                    this.dispatchEvent(new Event('blur'));
                }
            }
        }), this.renderHeader(), this.renderBody(), createElementIf(() => { return this.slotExists('card-selectable-footer'); }, 'div', {
            attrs: {
                class: 'neon-card-selectable-footer'
            },
        }, () => { return this.getSlot('card-selectable-footer'); }), createElementIf(() => {
            return this.getAttribute('disabled') === 'true' && this.getAttribute('data-disabled-tooltip-text') != null;
        }, 'neon-2_20_1-tooltip', {
            attrs: {
                'data-target-id': dynamicAttr(() => { return `card-selectable-${this.id}`; }),
            },
        }, () => {
            return this.tooltipTextNode;
        }));
    }
    connectedCallback() {
        super.connectedCallback();
        this.tooltipOnEllipsisObserver.connect();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        this.tooltipOnEllipsisObserver.disconnect();
    }
}
