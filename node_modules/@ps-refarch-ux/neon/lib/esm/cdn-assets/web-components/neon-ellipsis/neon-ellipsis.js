import './neon-ellipsis.scss';
import { NeonWidget } from '../shared/neon-widget';
import { updateDynamicAttribute } from '../shared/dom-manipulation';
import { createResizeObserverFor, insertOrUpdateTooltip, removeTooltip, isEllipsisShowing, } from '../shared/extensions/tooltip-on-ellipsis';
import { debounce } from '../../../exported-utils/throttle';
export class NeonEllipsis extends NeonWidget {
    static get observedAttributes() {
        return [
            'id',
            'data-line-clamp',
            'data-popper-placement',
            'data-tooltip-text',
            'data-show-tooltip',
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-ellipsis';
        this.isIdRequired = true;
        this.tooltipTextNode = document.createTextNode('');
        this.neonTooltipOnEllipseObserver = createResizeObserverFor(this);
        this.ellipsesTextObserver = new MutationObserver(debounce((mutationsList) => {
            for (const mutation of mutationsList) {
                if (mutation.type === 'characterData' && mutation.target) {
                    // Character data of the target element has changed
                    this.customInsertOrUpdateTooltip();
                    break;
                }
            }
        }, 500));
    }
    attributeChanged(attrName, oldVal, newVal) {
        var _a, _b;
        // There is no action for attribute changes.
        super.attributeChanged(attrName, oldVal, newVal);
        switch (attrName) {
            case 'data-popper-placement':
                updateDynamicAttribute(this, 'data-popper-placement');
                (_a = this.nextElementSibling) === null || _a === void 0 ? void 0 : _a.setAttribute('data-placement', newVal);
                break;
            case 'data-show-tooltip':
                if ((this.getAttribute('data-show-tooltip') === 'false')) {
                    removeTooltip(this);
                    this.neonTooltipOnEllipseObserver.disconnect();
                }
                else {
                    if (isEllipsisShowing(this) && ((_b = this.nextElementSibling) === null || _b === void 0 ? void 0 : _b.tagName.toLowerCase()) !== 'neon-2_20_1-tooltip') {
                        insertOrUpdateTooltip(this);
                    }
                    // Regard less of the ellipsis status, we need to turn ON the observable...
                    this.neonTooltipOnEllipseObserver.observe(this);
                }
                break;
            case 'data-tooltip-text':
                this.customInsertOrUpdateTooltip();
                break;
            case 'data-line-clamp':
                if (newVal != null && !isNaN(newVal)) {
                    this.style.setProperty('--neon-2_20_1-ellipsis-line-clamp', String(Math.floor(Number(newVal))));
                }
                else {
                    this.style.removeProperty('--neon-2_20_1-ellipsis-line-clamp');
                }
                break;
        }
    }
    //This function is specifically named so it will be picked up by
    //tooltip-on-ellipsis
    neonTooltipOnEllipsisText() {
        return this.getAttribute('data-tooltip-text');
    }
    render() {
        return null;
    }
    redraw() {
        // The `redraw()` method is a virtual method, which is a overridden of NeonWidget redraw, to avoid empty render call.
    }
    customInsertOrUpdateTooltip() {
        var _a, _b;
        if (!isEllipsisShowing(this)) {
            //If the ellipsis isn't showing, there should be no tooltip up and running.
            //If it's there, remove it.
            if (((_a = this.nextElementSibling) === null || _a === void 0 ? void 0 : _a.tagName.toLowerCase()) === 'neon-2_20_1-tooltip') {
                (_b = this.nextElementSibling) === null || _b === void 0 ? void 0 : _b.remove();
            }
        }
        else if (this.getAttribute('data-show-tooltip') !== 'false') {
            //The ellipsis is showing so we need to add or update the tooltip so it will be there.
            insertOrUpdateTooltip(this);
        }
    }
    connectedCallback() {
        this.neonTooltipOnEllipseObserver.observe(this);
        this.ellipsesTextObserver.observe(this, {
            characterData: true,
            subtree: true
        });
        super.connectedCallback();
    }
    disconnectedCallback() {
        this.neonTooltipOnEllipseObserver.disconnect();
        this.ellipsesTextObserver.disconnect();
    }
}
