import './neon-ai-chat-launcher.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { createElement, updateDynamicText, dynamicProperty, dynamicAttr, updateConditionalElements, updateDynamicAttributeOnChildren, updateDynamicPropertyOnChildren, updateDynamicAttribute, useElementIf, dynamicText, createElementIf, } from '../shared/dom-manipulation';
import { translate, addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { bindCallback, getCallback } from '../shared/neon-widget';
const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
export class NeonAiChatLauncher extends NeonWidgetSlotted {
    get value() {
        return this._value;
    }
    set value(value) {
        this._value = value || '';
        updateDynamicAttributeOnChildren(this, 'value');
        updateDynamicPropertyOnChildren(this, 'value');
        if (this.speechRecognition != null) {
            updateDynamicAttribute(this.micButton, 'disabled');
        }
        updateDynamicAttribute(this.submitButton, 'disabled');
    }
    static get observedAttributes() {
        return [
            'id',
            'data-max-length',
            'disabled',
            'data-disable-speech-to-text',
            'data-display-speech-to-text',
            'data-is-loading',
            'data-is-loading-text',
            'data-error-text',
            'data-placeholder',
            'data-aria-label',
            'data-input-aria-label',
            'data-mic-aria-label',
            'data-submit-aria-label',
            'data-type'
        ];
    }
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        switch (attrName) {
            case 'id':
                updateDynamicAttributeOnChildren(this, 'id');
                updateDynamicText(this);
                break;
            case 'data-max-length':
                updateDynamicAttributeOnChildren(this, 'maxlength');
                break;
            case 'data-disable-speech-to-text':
                updateDynamicAttributeOnChildren(this, 'disabled');
                break;
            case 'data-display-speech-to-text':
                updateConditionalElements(this);
                break;
            case 'data-error-text':
                updateConditionalElements(this);
                updateDynamicText(this);
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-is-loading':
                updateConditionalElements(this);
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            case 'data-is-loading-text':
                updateDynamicText(this);
                break;
            case 'data-placeholder':
                updateDynamicAttributeOnChildren(this, 'placeholder');
                break;
            case 'disabled':
                updateDynamicAttributeOnChildren(this, 'disabled');
                break;
            case 'data-aria-label':
            case 'data-input-aria-label':
            case 'data-mic-aria-label':
            case 'data-submit-aria-label':
                updateDynamicAttributeOnChildren(this, 'aria-label');
                break;
            case 'data-type':
                updateDynamicAttributeOnChildren(this, 'class');
                break;
            default:
        }
    }
    constructor() {
        super();
        this.widgetTag = 'neon-ai-chat-launcher';
        this.isIdRequired = true;
        // Firefox does not support the SpeechRecognition API
        // SpeechRecognition: any = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
        this.speechRecognition = SpeechRecognition != null ? new SpeechRecognition() : undefined;
        this.microphoneIsListening = false;
        this._value = '';
        if (this.speechRecognition == null) {
            return;
        }
        this.speechRecognition.interimResults = true;
        this.speechRecognition.continuous = true;
        this.speechRecognition.onstart = () => {
            this.microphoneIsListening = true;
            updateConditionalElements(this);
            this.micIsListeningButton.focus();
        };
        this.speechRecognition.onresult = (event) => {
            const currentTranscript = Array.from(event.results)
                .map((result) => {
                return (result != null && result.length > 0 ? result[0].transcript : undefined);
            }).filter((result) => {
                return result != null;
            })
                .join('');
            this.textInput.scrollLeft = this.textInput.scrollWidth;
            if (this.value !== currentTranscript) {
                this.value = currentTranscript;
            }
        };
        this.speechRecognition.onend = () => {
            this.microphoneIsListening = false;
            updateConditionalElements(this);
            if (this.value === '') {
                this.micButton.focus();
            }
            else {
                this.submitButton.focus();
            }
        };
        this.speechRecognition.onerror = (event) => {
            console.error('Speech recognition error:', event.error);
            this.speechRecognition.stop();
        };
    }
    isSpeechToTextDisabled() {
        return this.value !== '' || this.getAttribute('data-disable-speech-to-text') === 'true';
    }
    isSpeechRecognitionAvailable() {
        return this.getAttribute('data-display-speech-to-text') !== 'false' && this.speechRecognition != null;
    }
    listen() {
        this.microphoneIsListening = true;
        updateConditionalElements(this);
        this.speechRecognition.start();
    }
    getAiChatLauncherClasses() {
        const classes = ['neon-ai-chat-launcher'];
        if (this.getAttribute('data-error-text') != null || this.getAttribute('data-is-loading') === 'true') {
            classes.push('neon-ai-chat-launcher-error-state');
        }
        if (this.getAttribute('data-is-loading') === 'true') {
            classes.push('neon-ai-chat-launcher-loading-state');
        }
        if (this.getAttribute('data-type') === 'white') {
            classes.push('neon-ai-chat-launcher-white');
        }
        return classes.join(' ');
    }
    renderLogo() {
        return createElement('img', {
            attrs: {
                src: 'https://assets.powerschool.com/neon/unversioned/images/neon-ai-chat-launcher/powerbuddy-logo.svg',
                role: 'presentation',
                alt: '',
                class: 'neon-ai-chat-launcher-logo',
                width: '56',
                height: '56'
            }
        });
    }
    renderInputContainer() {
        return createElement('form', {
            attrs: {
                class: 'neon-ai-chat-launcher-input-container',
            }
        }, this.renderInput(), this.renderButtonContainer());
    }
    renderInput() {
        this.textInput = createElement('input', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-text-input'; }),
                class: 'neon-ai-chat-launcher-input',
                placeholder: dynamicAttr(() => { return this.getAttribute('data-placeholder') || translate('neon|ai-chat-launcher|Ask PowerBuddy'); }),
                maxlength: dynamicAttr(() => { return this.getAttribute('data-max-length') || '10000'; }),
                value: dynamicAttr(() => { return this.value; }),
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-input-aria-label') || translate('neon|ai-chat-launcher|Ask PowerBuddy'); }),
            },
            properties: {
                value: dynamicProperty(() => { return this.value; })
            },
            listeners: {
                input: (event) => {
                    this.value = event.target.value;
                }
            }
        });
        return this.textInput;
    }
    renderButtonContainer() {
        return createElement('div', {
            attrs: {
                class: 'neon-ai-chat-launcher-button-container'
            }
        }, 
        // firefox does not support the speech recognition api
        // while neon does not officially support firefox
        // the powerbuddy team accounts for this, so we should too
        useElementIf(() => { return this.microphoneIsListening === false && this.isSpeechRecognitionAvailable(); }, () => { return this.renderMicButton(); }), useElementIf(() => { return this.microphoneIsListening && this.isSpeechRecognitionAvailable(); }, () => { return this.renderMicListeningButton(); }), this.renderSubmitButton());
    }
    renderMicListeningButton() {
        this.micIsListeningButton = createElement('button', {
            attrs: {
                class: 'neon-ai-chat-launcher-mic-button-listening',
                id: dynamicAttr(() => { return this.getAttribute('id') + '-mic-listening-button'; }),
            },
            listeners: {
                click: () => {
                    this.speechRecognition.stop();
                }
            }
        }, createElement('neon-2_20_1-loading-indicator', {
            attrs: {
                class: 'neon-ai-chat-launcher-mic-stack'
            }
        }), createElement('neon-2_20_1-icon', {
            attrs: {
                'data-icon': 'close',
                'data-size': 'x-small',
            }
        }));
        return this.micIsListeningButton;
    }
    renderMicButton() {
        this.micButton = createElement('button', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-mic-button'; }),
                class: dynamicAttr(() => { return 'neon-ai-chat-launcher-mic-button' + (this.microphoneIsListening ? ' listening' : ''); }),
                disabled: dynamicAttr(() => { return this.isSpeechToTextDisabled() ? '' : undefined; }),
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-mic-aria-label') || translate('neon|ai-chat-launcher|Activate microphone'); }),
            },
            listeners: {
                click: () => {
                    this.listen();
                }
            }
        }, createElement('neon-2_20_1-icon', {
            attrs: {
                'data-icon': 'microphone',
                class: 'neon-ai-chat-launcher-mic-icon'
            }
        }));
        return this.micButton;
    }
    renderSubmitButton() {
        this.submitButton = createElement('button', {
            attrs: {
                id: dynamicAttr(() => { return this.getAttribute('id') + '-submit-button'; }),
                class: 'neon-ai-chat-launcher-submit-button',
                disabled: dynamicAttr(() => { return this.value === '' ? '' : undefined; }),
                type: 'submit',
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-submit-aria-label') || translate('neon|ai-chat-launcher|Submit question'); }),
            },
            listeners: {
                click: () => {
                    this.speechRecognition.stop();
                    this.dispatchEvent(new CustomEvent('neonValueChange', { detail: { value: this.value } }));
                    this.value = '';
                    updateDynamicAttributeOnChildren(this, 'value');
                }
            }
        }, createElement('neon-2_20_1-icon', {
            attrs: {
                'data-icon': 'send|rtl-flip',
                class: 'neon-ai-chat-launcher-submit-icon'
            }
        }));
        return this.submitButton;
    }
    renderInputAndLogo() {
        return createElement('div', {
            attrs: { class: 'neon-ai-chat-launcher-content' },
        }, this.renderLogo(), useElementIf(() => { return this.getAttribute('data-error-text') == null && this.getAttribute('data-is-loading') !== 'true'; }, () => { return this.renderInputContainer(); }));
    }
    renderConditionalError() {
        return createElementIf(() => { return this.getAttribute('data-error-text') != null && this.getAttribute('data-is-loading') !== 'true'; }, 'div', {
            attrs: {
                class: 'neon-ai-chat-launcher-content-error neon-ai-chat-launcher-content-loading-error-container'
            },
        }, () => {
            return [
                createElement('p', {
                    attrs: { class: 'neon-ai-chat-launcher-error-text' },
                    text: dynamicText(() => { return this.getAttribute('data-error-text'); })
                }),
                this.getSlot('ai-chat-launcher-error')
            ];
        });
    }
    renderConditionalLoadingState() {
        return createElementIf(() => { return this.getAttribute('data-is-loading') === 'true'; }, 'div', {
            attrs: { class: 'neon-ai-chat-launcher-content-loading neon-ai-chat-launcher-content-loading-error-container' },
        }, () => {
            return [
                createElement('p', {
                    attrs: { class: 'neon-ai-chat-launcher-loading-text' },
                    text: dynamicText(() => { return this.getAttribute('data-is-loading-text') || translate('neon|ai-chat-launcher|Loading'); })
                }),
                createElement('div', {
                    attrs: {
                        class: 'neon-ai-chat-launcher-loading-ellipsis'
                    },
                }, createElement('div', { attrs: { class: 'neon-ai-chat-launcher-loading-ellipsis-dot' } }), createElement('div', { attrs: { class: 'neon-ai-chat-launcher-loading-ellipsis-dot neon-ai-chat-launcher-loading-ellipsis-dot-2' } }), createElement('div', { attrs: { class: 'neon-ai-chat-launcher-loading-ellipsis-dot neon-ai-chat-launcher-loading-ellipsis-dot-3' } }))
            ];
        });
    }
    render() {
        return createElement('div', {
            attrs: {
                class: dynamicAttr(() => { return this.getAiChatLauncherClasses(); }),
                role: 'searchbox',
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-aria-label') || translate('neon|ai-chat-launcher|PowerBuddy prompt launcher'); }),
            },
        }, this.renderInputAndLogo(), this.getSlot('ai-chat-launcher-suggestions'), this.renderConditionalLoadingState(), this.renderConditionalError());
    }
    onKeyDown(event) {
        var _a;
        event === null || event === void 0 ? void 0 : event.stopPropagation();
        if (((_a = event.key) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === 'escape' && this.microphoneIsListening === true) {
            this.speechRecognition.stop();
        }
    }
    connectedCallback() {
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
        window.addEventListener('keydown', bindCallback(this.onKeyDown, this));
    }
    disconnectedCallback() {
        window.removeEventListener('keydown', getCallback(this.onKeyDown, this));
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        if (this.speechRecognition != null) {
            this.speechRecognition.stop();
        }
    }
}
