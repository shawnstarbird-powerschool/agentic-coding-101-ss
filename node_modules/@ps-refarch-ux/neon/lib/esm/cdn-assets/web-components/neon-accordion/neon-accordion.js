import './neon-accordion.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { createElement, dynamicText, dynamicAttr, updateDynamicText, updateDynamicAttribute, updateConditionalElements, createElementIf, updateDynamicAttributeOnChildren } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
import { debounce } from '../../../exported-utils/throttle';
export class NeonAccordion extends NeonWidgetSlotted {
    static get observedAttributes() {
        return [
            'id',
            'data-heading',
            'data-subheading',
            'data-icon',
            'data-injection-spacing',
            'data-size',
            'data-border-type',
            'data-expanded',
            'data-content-preview',
            'data-icon-stack-icon',
            'data-icon-stack-color',
            'data-icon-stack-size',
            'data-icon-stack-shape',
            'data-icon-stack-type',
            'data-avatar-first-name',
            'data-avatar-last-name',
            'data-avatar-size',
            'data-avatar-profile-picture-src',
            'data-avatar-background-option'
        ];
    }
    constructor() {
        super();
        this.isIdRequired = true;
        this.widgetTag = 'neon-accordion';
        this.accordionItems = [];
        this.isExpanded = false;
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
        this.resizeObserver = new ResizeObserver(debounce(() => {
            this.updateKeyboardNavigationAccessibility();
        }, 500));
    }
    render() {
        return createElement('article', {}, createElement('header', { attrs: { class: 'neon-accordion-header' } }, createElement('button', {
            attrs: {
                id: dynamicAttr(() => {
                    return `${this.getAttribute('id')}-toggle`;
                }),
                class: dynamicAttr(() => { return `neon-accordion-toggle-button${this.isExpanded ? ' neon-accordion-expanded' : ''}`; }),
                'aria-expanded': dynamicAttr(() => { return this.isExpanded ? 'true' : 'false'; }),
                'aria-controls': dynamicAttr(() => { return `${this.getAttribute('id')}-accordion-content`; }),
                'type': 'button' //type provided to assistive technology
            },
            listeners: {
                click: (e) => {
                    this.setExpanded(!this.isExpanded);
                    this.updateKeyboardNavigationAccessibility();
                    this.dispatchEvent(new CustomEvent('neonAccordionExpandedStateChange', { detail: { isExpanded: this.isExpanded } }));
                }
            }
        }, createElementIf(() => { return this.getAttribute('data-icon') != null; }, 'neon-2_20_1-icon', {
            attrs: {
                class: 'neon-accordion-input-icon',
                'data-icon': dynamicAttr(() => {
                    return this.getAttribute('data-icon');
                }),
                'data-size': dynamicAttr(() => {
                    return this.getAttribute('data-size') === 'small' ? 'small' : 'medium';
                })
            }
        }), createElementIf(() => { return this.getAttribute('data-icon-stack-icon') != null; }, 'neon-2_20_1-icon-stack', {
            attrs: {
                class: 'neon-accordion-input-icon-stack',
                'data-icon': dynamicAttr(() => {
                    return this.getAttribute('data-icon-stack-icon');
                }),
                'data-color': dynamicAttr(() => {
                    return this.getAttribute('data-icon-stack-color');
                }),
                'data-size': dynamicAttr(() => {
                    return this.getAttribute('data-icon-stack-size');
                }),
                'data-shape': dynamicAttr(() => {
                    return this.getAttribute('data-icon-stack-shape');
                }),
                'data-type': dynamicAttr(() => {
                    return this.getAttribute('data-icon-stack-type');
                })
            }
        }), createElementIf(() => { return this.isAvatarRenderable(); }, 'neon-2_20_1-avatar', {
            attrs: {
                class: 'neon-accordion-input-avatar',
                'data-avatar-size': dynamicAttr(() => {
                    return this.getAttribute('data-avatar-size');
                }),
                'data-first-name': dynamicAttr(() => {
                    return this.getAttribute('data-avatar-first-name');
                }),
                'data-last-name': dynamicAttr(() => {
                    return this.getAttribute('data-avatar-last-name');
                }),
                'data-profile-picture-src': dynamicAttr(() => {
                    return this.getAttribute('data-avatar-profile-picture-src');
                }),
                'data-background-option': dynamicAttr(() => {
                    return this.getAttribute('data-avatar-background-option');
                })
            }
        }), createElement('div', { attrs: { class: 'neon-accordion-title-container' } }, createElement('h2', {
            attrs: { class: 'neon-accordion-heading' },
            text: dynamicText(() => { return this.getAttribute('data-heading'); }),
            extensions: tooltipOnEllipsis
        }), createElementIf(() => { return this.getAttribute('data-subheading') != null; }, 'p', {
            attrs: { class: 'neon-accordion-subheading' },
            text: dynamicText(() => { return this.getAttribute('data-subheading'); }),
            extensions: tooltipOnEllipsis
        }), createElementIf(() => { return !this.isExpanded && this.getAttribute('data-content-preview') === 'true'; }, 'div', {
            attrs: {
                id: dynamicAttr(() => { return `${this.getAttribute('id')}-accordion-content`; }),
                class: 'neon-accordion-content-preview'
            }
        }, () => { return this.getNonSlottedNodes(); })), createElement('neon-2_20_1-icon', {
            attrs: {
                class: 'neon-accordion-toggle-icon',
                'data-icon': dynamicAttr(() => {
                    return this.isExpanded ? 'chevron-up' : 'chevron-down';
                }),
                'data-size': dynamicAttr(() => {
                    return this.getAttribute('data-size') === 'small' ? 'small' : 'medium';
                })
            }
        })) //end button
        ), //end header
        createElementIf(() => {
            return this.isExpanded;
        }, 'div', {
            attrs: {
                id: dynamicAttr(() => { return `${this.getAttribute('id')}-accordion-content`; }),
                class: dynamicAttr(() => {
                    return `neon-accordion-content${this.getAttribute('data-injection-spacing') !== 'false' ? ' neon-2_20_1-accordion-content' : ''}`;
                })
            }
        }, () => { return this.getNonSlottedNodes(); }));
    }
    connectedCallback() {
        //The observer needs to be turned on when render() gets called.
        //That's why it's above super.connectedCallback()
        this.tooltipOnEllipsisObserver.connect();
        super.connectedCallback();
        this.resizeObserver.observe(this);
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        this.resizeObserver.disconnect();
        this.tooltipOnEllipsisObserver.disconnect();
    }
    setExpanded(value) {
        this.isExpanded = value;
        updateDynamicAttribute(this, 'class');
        updateDynamicAttribute(this, 'data-icon');
        updateConditionalElements(this);
    }
    isAvatarRenderable() {
        return this.getAttribute('data-avatar-first-name') != null || this.getAttribute('data-avatar-last-name') != null || this.getAttribute('data-avatar-profile-picture-src') != null;
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                break;
            case 'data-icon':
                updateDynamicAttribute(this, 'data-icon');
                updateConditionalElements(this);
                break;
            case 'data-subheading':
                updateDynamicText(this);
                updateConditionalElements(this);
                break;
            case 'data-heading':
                updateDynamicText(this);
                break;
            case 'data-injection-spacing':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-size':
                updateDynamicAttribute(this, 'data-size');
                break;
            case 'data-expanded':
                this.setExpanded(newVal === 'true');
                break;
            case 'data-content-preview':
                updateConditionalElements(this);
                this.updateKeyboardNavigationAccessibility();
                break;
            case 'data-icon-stack-icon':
                updateConditionalElements(this);
                updateDynamicAttribute(this, 'data-icon');
                break;
            case 'data-icon-stack-color':
                updateDynamicAttributeOnChildren(this, 'data-color');
                break;
            case 'data-icon-stack-size':
                updateDynamicAttributeOnChildren(this, 'data-size');
                break;
            case 'data-icon-stack-shape':
                updateDynamicAttributeOnChildren(this, 'data-shape');
                break;
            case 'data-icon-stack-type':
                updateDynamicAttributeOnChildren(this, 'data-type');
                break;
            case 'data-avatar-size':
                updateDynamicAttributeOnChildren(this, 'data-avatar-size');
                break;
            case 'data-avatar-first-name':
                updateDynamicAttributeOnChildren(this, 'data-first-name');
                updateConditionalElements(this);
                break;
            case 'data-avatar-last-name':
                updateDynamicAttributeOnChildren(this, 'data-last-name');
                updateConditionalElements(this);
                break;
            case 'data-avatar-profile-picture-src':
                updateDynamicAttributeOnChildren(this, 'data-profile-picture-src');
                updateConditionalElements(this);
                break;
            case 'data-avatar-background-option':
                updateDynamicAttributeOnChildren(this, 'data-background-option');
                updateConditionalElements(this);
                break;
            default:
        }
    }
    updateKeyboardNavigationAccessibility() {
        // Element selectors for focusable elements
        const focusSelector = 'a[href], button, input, select, textarea, iframe, object, embed, [contenteditable], area[href], [tabindex]';
        // Get containers
        const accordionPreviewContainer = this.querySelector('.neon-accordion-content-preview');
        const accordionContentContainer = this.querySelector('.neon-accordion-content');
        /**
         * Updates the tabindex of elements to -1 within a container based on their visibility.
         * If they are not visible, we set the tabindex to -1 so the user cannot tab to it.
         *
         * @param container - The container element whose child elements' tabindex will be updated.
         * @param restoreOnly - If true, only restores the original tabindex values without checking visibility.
        */
        const handleTabIndex = (container, restoreOnly) => {
            const elements = container.querySelectorAll(focusSelector);
            const containerRect = container.getBoundingClientRect();
            for (const element of elements) {
                let isVisible = true;
                if (!restoreOnly) {
                    const childRect = element.getBoundingClientRect();
                    isVisible = childRect.right <= containerRect.right && childRect.bottom <= containerRect.bottom;
                }
                if (restoreOnly || isVisible) {
                    // Restore the original tabindex if it was modified, otherwise remove the attribute
                    const existingNeonTabIndex = element.getAttribute('neon-accordion-existing-tabindex');
                    if (existingNeonTabIndex != null) {
                        element.setAttribute('tabindex', existingNeonTabIndex);
                        element.removeAttribute('neon-accordion-existing-tabindex');
                    }
                    else if (element.getAttribute('tabindex') === '-1') {
                        element.removeAttribute('tabindex');
                    }
                }
                else {
                    // Save the existing tabindex if the element is not visible
                    const existingTabIndex = element.getAttribute('tabindex');
                    if (existingTabIndex != null && existingTabIndex !== '-1') {
                        element.setAttribute('neon-accordion-existing-tabindex', existingTabIndex);
                    }
                    element.setAttribute('tabindex', '-1');
                }
            }
        };
        // Handle elements in the previewContainer if present
        if (accordionPreviewContainer) {
            accordionPreviewContainer.setAttribute('aria-hidden', 'true');
            handleTabIndex(accordionPreviewContainer, false);
        }
        // Handle elements in the mainContainer if previewContainer is not found or handle main separately
        if (accordionContentContainer && (!accordionPreviewContainer || accordionPreviewContainer !== accordionContentContainer)) {
            handleTabIndex(accordionContentContainer, true);
        }
    }
}
