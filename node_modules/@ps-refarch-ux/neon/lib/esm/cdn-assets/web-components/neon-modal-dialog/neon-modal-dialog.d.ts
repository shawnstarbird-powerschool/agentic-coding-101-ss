import type { ConditionalElement } from '../shared/conditional-element';
import { type RequiredSlotInfo, NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { type FocusTrap } from '../shared/focus-trap';
export declare class NeonModalDialog extends NeonWidgetSlotted {
    focusTrap: FocusTrap;
    i18nUnsubscribe?: () => void;
    widgetTag: string;
    preCloseCallback?: (event?: Event) => void | boolean | undefined | Promise<boolean | undefined> | Promise<void>;
    static get requiredSlots(): Array<RequiredSlotInfo>;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    constructor();
    validateInputs(): void;
    closeDialog(event?: Event): Promise<boolean>;
    /**
     * Adds the focus trap for the modal dialog.  If no clickable elements have been added when connected to the DOM,
     * this method will retry 4 times to give React a chance to render some clickable elements in the slots.
     * @param tryNumber Do not pass this in except when the function is calling its self recursively.
     */
    addFocusTrap(tryNumber?: number): void;
    onKeyDown(event: KeyboardEvent): void;
    getOverlayClasses(): string;
    connectedCallback(): void;
    disconnectedCallback(): void;
    renderDialogElement(): HTMLElement;
    render(): Array<HTMLElement | ConditionalElement>;
}
export declare function registerNeonModalDialog(): void;
