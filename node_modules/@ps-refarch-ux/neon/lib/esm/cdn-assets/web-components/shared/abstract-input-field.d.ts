import { type ValidationFunction, type ValidationResult, type Validator } from '../../../interfaces/validation-engine';
import { type ConditionalElement } from './conditional-element';
import { LabeledWidget } from './labeled-widget';
export type InputSelectorResult = HTMLElement & {
    value: string | undefined | null;
    modelValue: any;
};
export declare abstract class AbstractInputField extends LabeledWidget {
    static get observedAttributes(): Array<string>;
    static get inputProperties(): Array<string>;
    isIdRequired: boolean;
    validators: Array<Validator>;
    private customValidate?;
    inputElement: InputSelectorResult;
    errorElement: HTMLElement;
    readOnlyElement: HTMLElement;
    successElement: HTMLElement;
    isValid: boolean;
    errorPresentWhenBlurOccurred: boolean;
    blurInterceptor?: (element: HTMLElement) => void;
    invalidRule: string | undefined;
    get customValidationFunction(): ValidationFunction | undefined;
    set customValidationFunction(value: ValidationFunction | undefined);
    get isReadOnly(): boolean;
    get value(): any;
    set value(value: string);
    get modelValue(): any;
    set modelValue(value: any);
    /**
     * The purpose of this function is to ensure that the value being passed in from the outside
     * is a type that is supported for the specific widget.
     *
     * For instance, the neon-date-field would ensure that the value being passed in is a Date object or undefined.
     * If it is neither of those things, then it should just return defined.
     *
     * @param value The value being set from outside the boundaries of the widget.
     */
    abstract validateModelValueType(value: any): any;
    abstract setValue(value: any, skip?: boolean): void;
    abstract setModelValue(modelValue: any, skip?: boolean): boolean;
    abstract renderInputElement(): HTMLElement;
    constructor();
    connectedCallback(): void;
    getReadOnlyText(): string | null | undefined;
    setDataSuccessMessage(): void;
    inputSelector(): InputSelectorResult;
    abstract generateValidationRules(): void;
    setReadonlyText(): void;
    getBadgeText(): string;
    isShowBadge(): boolean;
    render(): Array<HTMLElement | ConditionalElement> | HTMLElement;
    renderContent(): Array<HTMLElement | ConditionalElement>;
    inputChange(): void;
    clearInputField(): void;
    resetValidation(): void;
    /**
     * When the user enters data into this field and the focus is lost, the widget calls this function on blur and fires
     * a focus event. We check if there is an error message, if there is an error message,
     * then we start allowing the error messages to be shown
     */
    focusLost(event: any): void;
    /**
     *
     * @param showValidationResult
     * @param fromBlur
     * @param showValidationResultOnRequired Defaults to true. You can set this to false to prevent
     * the badge from getting highlighted if the field fails isRequired.  We needed this specifically when
     * changing steps with the stepper component.
     * @returns {boolean} - True means the widget is valid. False means the widget is invalid.
     */
    validateWidget(showValidationResult?: boolean, fromBlur?: boolean, showValidationResultOnRequired?: boolean): boolean;
    protected runValidationEngine(): ValidationResult;
    getSizeClass(): string;
    setError(value: string | undefined): void;
    redrawInput(): void;
    /**
     * Checks to be sure that any required attributes are present, otherwise an error gets logged.
     */
    validateInputs(): void;
    attributeChanged(attribute: string, oldValue: string, newValue: string): void;
    protected getDisabledStatus(): boolean;
    protected addCustomValidateToValidators(): void;
}
