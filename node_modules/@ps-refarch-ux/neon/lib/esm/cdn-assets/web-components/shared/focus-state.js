import { runWithBody } from './dom-manipulation';
export function maintainFocusState() {
    runWithBody((documentBody) => {
        if (documentBody.hasAttribute('neon-focus-state')) {
            return;
        }
        // we listen for specific keys because each can be used to navigate the page without a mouse
        // tabs focus on the next focusable element
        // space and enter activate the currently focused element
        // arrows can be used to navigate the page and make selections in radio groups, tabs, etc.
        // escape can be used to close modals, drawers, poppers
        document.addEventListener('keydown', (event) => {
            var _a;
            // if the user is interacting with an input, we don't want to interfere with that
            if (event.key === ' ' && event.target instanceof HTMLInputElement) {
                return;
            }
            if ((event === null || event === void 0 ? void 0 : event.key) === 'Tab' || (event === null || event === void 0 ? void 0 : event.key) === ' ' || (event === null || event === void 0 ? void 0 : event.key) === 'Enter' || ((_a = event === null || event === void 0 ? void 0 : event.key) === null || _a === void 0 ? void 0 : _a.startsWith('Arrow')) || (event === null || event === void 0 ? void 0 : event.key) === 'Escape') {
                documentBody.classList.remove('neon-mouse-navigation');
            }
        }, true);
        document.addEventListener('mousedown', () => {
            documentBody.classList.add('neon-mouse-navigation');
        }, true);
        documentBody.setAttribute('neon-focus-state', '');
    });
}
