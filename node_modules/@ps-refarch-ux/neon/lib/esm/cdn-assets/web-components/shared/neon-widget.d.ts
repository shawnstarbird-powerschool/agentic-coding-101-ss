import { ConditionalElement } from './conditional-element';
/**
 * All widgets should extend this.  We should be very careful about adding anything to this since it is inserted into all widgets.
 * Do not add static functions to this, they should go into files full of static files such as dom-manipulation.ts
 */
export declare abstract class NeonWidget extends HTMLElement {
    abstract widgetTag: string;
    conditionalChildElements: Array<ConditionalElement>;
    isIdRequired: boolean;
    isInitialized: boolean;
    attributesChangedBeforeInit: Set<string>;
    boundFunctionList?: Map<(event: Event) => void, (event: Event) => void>;
    validateInputs(): void;
    /**
     * Re-renders the widget and sets it into the DOM. This wipes out any existing elements within the widget.
     */
    redraw(): void;
    attributeChanged(attribute: string, oldValue: string, newValue: string): void;
    private attributeChangedCallback;
    /**
     * Returns the HTMLElement(s) representation of this widget.
     */
    abstract render(): Element | Array<Element | ConditionalElement | undefined | null> | ConditionalElement | undefined | null;
    connectedCallback(): void;
}
export declare function bindCallback<T extends Event>(classFunction: (event: T) => void, neonWidget: NeonWidget): (event: T) => void;
export declare function getCallback<T extends Event>(classFunction: (event: T) => void, neonWidget: NeonWidget): (event: T) => void;
