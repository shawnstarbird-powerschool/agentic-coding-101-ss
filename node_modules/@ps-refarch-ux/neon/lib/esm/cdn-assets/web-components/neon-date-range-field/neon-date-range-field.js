import './neon-date-range-field.scss';
import { NeonWidget } from '../shared/neon-widget';
import { createElement, updateDynamicAttribute, dynamicAttr, dynamicProperty, updateDynamicProperty, getClosest, dynamicText, updateDynamicAttributeOnChildren, updateDynamicPropertyOnChildren, updateDynamicAttributeNoChildren, registerComponent } from '../shared/dom-manipulation';
import { translate, addTranslationListener, getMmDdYyyyDateString, getYearMonthDayStringsFromLocalizedDateString } from '@ps-refarch-ux/mfe-utils';
import { getDateFromAttribute, isSameYearMonthDay, isValidDate } from '../shared/calendar-utils';
import { showPopper } from '../shared/poppers';
import { ElementCache } from '../shared/element-cache';
import { combineValidationFunctions } from '../shared/validation-engine/validate-engine';
export class NeonDateRangeField extends NeonWidget {
    //Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-start-date-label-text',
            'data-end-date-label-text',
            'data-month',
            'data-year',
            'data-is-required',
            'data-hide-required-badge',
            'data-show-optional-badge',
            'data-show-label',
            'data-is-read-only',
            'data-label-text-space',
            'data-helper-text-space',
            'disabled',
            'data-min-value',
            'data-max-value',
            'data-start-date-aria-label',
            'data-end-date-aria-label',
            'data-start-date-autocomplete',
            'data-end-date-autocomplete',
            'data-start-date-placeholder-text',
            'data-end-date-placeholder-text',
            'data-popper-placement',
            'data-weekdays-only'
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-date-range-field';
        this.elementCache = new ElementCache();
        this.isIdRequired = true;
        this.currentDate = new Date;
        this.renderDatePicker();
        setTimeout(() => {
            const inputPopperPlacement = this.getAttribute('data-popper-placement');
            if (inputPopperPlacement != null) {
                const placementOptions = ['top-start', 'top-end', 'bottom-start', 'bottom-end'];
                if (!placementOptions.includes(inputPopperPlacement)) {
                    console.error(`The supported options for popover placement in Neon Date Range are ${placementOptions.join(', ')}`);
                }
            }
        }, 1000);
    }
    get modelValue() {
        return this.dateRange;
    }
    set modelValue(value) {
        const isDateRangeEqual = this.isDateRangeEqual(this.dateRange, value);
        this.dateRange = value;
        updateDynamicAttribute(this, 'class');
        updateDynamicAttribute(this, 'data-min-value');
        updateDynamicAttribute(this, 'data-max-value');
        updateDynamicProperty(this, 'modelValue');
        if (!isDateRangeEqual) {
            this.dispatchEvent(new CustomEvent('modelValueChange', { bubbles: true, detail: { 'dateRange': this.modelValue } }));
        }
    }
    set customValidationFunction(value) {
        this.customValidate = value;
        updateDynamicPropertyOnChildren(this, 'customValidationFunction');
    }
    get customValidationFunction() {
        return this.customValidate;
    }
    set startDateCustomValidationFunction(value) {
        this.startDateCustomValidate = value;
        updateDynamicProperty(this, 'customValidationFunction');
    }
    get startDateCustomValidationFunction() {
        return this.startDateCustomValidate;
    }
    set endDateCustomValidationFunction(value) {
        this.endDateCustomValidate = value;
        updateDynamicProperty(this, 'customValidationFunction');
    }
    get endDateCustomValidationFunction() {
        return this.endDateCustomValidate;
    }
    // When the popper renders, it will get the month and year from the date field that triggered it
    // otherwise fallback to the stack that was used prior to 2.9.0
    getDatePickerMonth(month) {
        var _a, _b;
        if (month != null) {
            return String(month);
        }
        return this.getAttribute('data-month') || ((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.start) != null ? String((_b = this.dateRange) === null || _b === void 0 ? void 0 : _b.start.getMonth()) : String(this.currentDate.getMonth());
    }
    getDatePickerYear(year) {
        var _a, _b;
        if (year != null) {
            return String(year);
        }
        return this.getAttribute('data-year') || ((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.start) != null ? String((_b = this.dateRange) === null || _b === void 0 ? void 0 : _b.start.getFullYear()) : String(this.currentDate.getFullYear());
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                break;
            case 'data-start-date-label-text':
                updateDynamicAttribute(this, 'data-label-text');
                break;
            case 'data-end-date-label-text':
                updateDynamicAttribute(this, 'data-label-text');
                break;
            case 'data-month':
                updateDynamicAttribute(this, 'data-month');
                break;
            case 'data-year':
                updateDynamicAttribute(this, 'data-year');
                break;
            case 'data-is-required':
                updateDynamicAttribute(this, 'data-is-required');
                break;
            case 'data-hide-required-badge':
                updateDynamicAttribute(this, 'data-hide-required-badge');
                break;
            case 'data-show-optional-badge':
                updateDynamicAttribute(this, 'data-show-optional-badge');
                break;
            case 'data-show-label':
                updateDynamicAttribute(this, 'data-show-label');
                break;
            case 'data-is-read-only':
                updateDynamicAttribute(this, 'data-is-read-only');
                updateDynamicAttribute(this, 'disabled');
                break;
            case 'data-label-text-space':
                updateDynamicAttribute(this, 'data-label-text-space');
                break;
            case 'data-helper-text-space':
                updateDynamicAttribute(this, 'data-helper-text-space');
                break;
            case 'disabled':
                updateDynamicAttribute(this, 'disabled');
                break;
            case 'data-min-value':
                updateDynamicAttribute(this, 'data-min-value');
                break;
            case 'data-max-value':
                updateDynamicAttribute(this, 'data-max-value');
                break;
            case 'data-start-date-aria-label':
                updateDynamicAttribute(this, 'data-aria-label');
                break;
            case 'data-end-date-aria-label':
                updateDynamicAttribute(this, 'data-aria-label');
                break;
            case 'data-start-date-autocomplete':
                updateDynamicAttribute(this, 'autocomplete');
                break;
            case 'data-end-date-autocomplete':
                updateDynamicAttribute(this, 'autocomplete');
                break;
            case 'data-start-date-placeholder-text':
                updateDynamicAttribute(this, 'data-placeholder-text');
                break;
            case 'data-end-date-placeholder-text':
                updateDynamicAttribute(this, 'data-placeholder-text');
                break;
            case 'data-weekdays-only':
                updateDynamicAttributeOnChildren(this, 'data-weekdays-only');
                break;
            default:
        }
    }
    renderDatePicker(dateFieldOrigin) {
        var _a, _b;
        const month = (_a = dateFieldOrigin === null || dateFieldOrigin === void 0 ? void 0 : dateFieldOrigin.modelValue) === null || _a === void 0 ? void 0 : _a.getMonth();
        const year = (_b = dateFieldOrigin === null || dateFieldOrigin === void 0 ? void 0 : dateFieldOrigin.modelValue) === null || _b === void 0 ? void 0 : _b.getFullYear();
        return createElement('article', {
            attrs: {
                class: 'neon-2_20_1-date-range-article',
                'aria-modal': 'true',
                'role': 'dialog',
                'data-neon-prevent-ancestors-closing': ''
            }
        }, createElement('h2', {
            attrs: { class: 'neon-sr-only' },
            text: dynamicText(() => {
                return translate('neon|date-range|Select A Date Range');
            })
        }), createElement('neon-2_20_1-date-picker', {
            attrs: {
                'class': 'neon-2_20_1-date-range-field-date-picker',
                'data-mode': 'range',
                'data-month': dynamicAttr(() => { return this.getDatePickerMonth(month); }),
                'data-year': dynamicAttr(() => { return this.getDatePickerYear(year); }),
                'data-min-value': dynamicAttr(() => { return (isValidDate(getDateFromAttribute(this, 'data-min-value'))) ? this.getAttribute('data-min-value') : undefined; }),
                'data-max-value': dynamicAttr(() => { return (isValidDate(getDateFromAttribute(this, 'data-max-value'))) ? this.getAttribute('data-max-value') : undefined; }),
                'data-weekdays-only': dynamicAttr(() => { return this.getAttribute('data-weekdays-only'); })
            },
            properties: {
                rangeModeValue: dynamicProperty(() => { return this.modelValue; }),
                customValidationFunction: dynamicProperty(() => {
                    return this.customValidationFunction;
                })
            },
            listeners: {
                'neonDatePickerDateRangeChanged': (event) => {
                    this.modelValue = event.target.rangeModeValue;
                    updateDynamicProperty(this, 'modelValue');
                    this.elementCache.get('date-range-start-field').validateWidget(true);
                    this.elementCache.get('date-range-end-field').validateWidget(true);
                }
            },
            extensions: this.elementCache.assignTo('date-picker')
        }));
    }
    showDatePicker(dateFieldElement, datePickerButton) {
        if (this.closePopover != null) {
            // Close the options
            this.closePopover();
            // this.popoverElement = undefined;
            this.closePopover = undefined;
        }
        else {
            // Open the options
            const placement = (this.getAttribute('data-popper-placement') || 'bottom-start');
            this.closePopover = showPopper(this, this.renderDatePicker(dateFieldElement), {
                placement: placement,
                trapFocus: true,
                clickTarget: datePickerButton,
                popperPreClosedCallback: (clickEvent) => {
                    var _a, _b;
                    if (clickEvent == null) {
                        return;
                    }
                    if (getClosest(clickEvent.target, '.neon-2_20_1-date-range-field-date-picker') == null &&
                        getClosest(clickEvent.target, '#' + this.id + '-start-date-button') == null &&
                        getClosest(clickEvent.target, '#' + this.id + '-end-date-button') == null) {
                        // We know that the event did not come from inside of one of the popovers or the main button so the window listener
                        // will close all of the articles.
                        this.closePopover = undefined;
                        if (dateFieldElement != null) {
                            dateFieldElement.focus();
                        }
                        return false;
                    }
                    else if (((_a = clickEvent.key) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === 'escape' ||
                        ((_b = clickEvent.key) === null || _b === void 0 ? void 0 : _b.toLowerCase()) === 'tab') {
                        if (dateFieldElement != null) {
                            dateFieldElement.focus();
                        }
                        this.closePopover = undefined;
                    }
                    else {
                        // The event came from inside a popover or the main button
                        // so return true to prevent the window listener from closing the menu
                        return true;
                    }
                },
                popperPostClosedCallback: () => {
                    delete this.closePopover;
                },
            });
        }
    }
    render() {
        return [
            createElement('div', {
                attrs: {
                    class: 'neon-date-range-field'
                }
            }, createElement('neon-2_20_1-date-field', {
                attrs: {
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-start-date'; }),
                    class: 'neon-date-range-field-date-field',
                    'data-label-text': dynamicAttr(() => { return this.getAttribute('data-start-date-label-text') || translate('neon|date-range|Start Date'); }),
                    'data-size': 'auto',
                    'data-disable-picker-dialog': 'true',
                    'data-is-required': dynamicAttr(() => { return this.getAttribute('data-is-required'); }),
                    'data-hide-required-badge': dynamicAttr(() => { return this.getAttribute('data-hide-required-badge'); }),
                    'data-show-optional-badge': dynamicAttr(() => { return this.getAttribute('data-show-optional-badge'); }),
                    'data-show-label': dynamicAttr(() => { return this.getAttribute('data-show-label'); }),
                    'data-is-read-only': dynamicAttr(() => { return this.getAttribute('data-is-read-only'); }),
                    'data-label-text-space': dynamicAttr(() => { return this.getAttribute('data-label-text-space'); }),
                    'data-helper-text-space': dynamicAttr(() => { return this.getAttribute('data-helper-text-space'); }),
                    'disabled': dynamicAttr(() => { return this.getAttribute('disabled'); }),
                    'data-aria-label': dynamicAttr(() => { return this.getAttribute('data-start-date-aria-label'); }),
                    'autocomplete': dynamicAttr(() => { return this.getAttribute('data-start-date-autocomplete'); }),
                    'data-placeholder-text': dynamicAttr(() => { return this.getAttribute('data-start-date-placeholder-text'); }),
                    'data-min-value': dynamicAttr(() => { return (isValidDate(getDateFromAttribute(this, 'data-min-value'))) ? this.getAttribute('data-min-value') : undefined; }),
                    'data-max-value': dynamicAttr(() => {
                        var _a, _b;
                        return isValidDate((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.end) ? getMmDdYyyyDateString((_b = this.dateRange) === null || _b === void 0 ? void 0 : _b.end) : undefined;
                    }),
                    'data-weekdays-only': dynamicAttr(() => { return this.getAttribute('data-weekdays-only'); })
                },
                extensions: this.elementCache.assignTo('date-range-start-field'),
                properties: {
                    modelValue: dynamicProperty(() => { var _a; return isValidDate((_a = this.modelValue) === null || _a === void 0 ? void 0 : _a.start) ? this.modelValue.start : undefined; }),
                    customValidationFunction: dynamicProperty(() => {
                        return combineValidationFunctions(this.startDateCustomValidationFunction, this.customValidationFunction);
                    })
                },
                listeners: {
                    neonDateFieldButtonPressed: (event) => {
                        const neonDatePicker = this.elementCache.get('date-picker');
                        if (neonDatePicker != null) {
                            updateDynamicAttributeNoChildren(neonDatePicker, 'data-month');
                            updateDynamicAttributeNoChildren(neonDatePicker, 'data-year');
                        }
                        this.showDatePicker(this.elementCache.get('date-range-start-field'), event.detail.datePickerButton);
                    },
                    modelValueChange: (event) => {
                        var _a;
                        event.stopPropagation();
                        this.modelValue = {
                            start: event.target.modelValue,
                            end: ((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.end) || (this.isDateStringHasFullYear(event.target.value) ? event.target.modelValue : undefined)
                        };
                    }
                }
            }), createElement('neon-2_20_1-date-field', {
                attrs: {
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-end-date'; }),
                    class: 'neon-date-range-field-date-field',
                    'data-label-text': dynamicAttr(() => { return this.getAttribute('data-end-date-label-text') || translate('neon|date-range|End Date'); }),
                    'data-size': 'auto',
                    'data-disable-picker-dialog': 'true',
                    'data-is-required': dynamicAttr(() => { return this.getAttribute('data-is-required'); }),
                    'data-hide-required-badge': dynamicAttr(() => { return this.getAttribute('data-hide-required-badge'); }),
                    'data-show-optional-badge': dynamicAttr(() => { return this.getAttribute('data-show-optional-badge'); }),
                    'data-show-label': dynamicAttr(() => { return this.getAttribute('data-show-label'); }),
                    'data-is-read-only': dynamicAttr(() => { return this.getAttribute('data-is-read-only'); }),
                    'data-label-text-space': dynamicAttr(() => { return this.getAttribute('data-label-text-space'); }),
                    'data-helper-text-space': dynamicAttr(() => { return this.getAttribute('data-helper-text-space'); }),
                    'disabled': dynamicAttr(() => { return this.getAttribute('disabled'); }),
                    'data-aria-label': dynamicAttr(() => { return this.getAttribute('data-end-date-aria-label'); }),
                    'autocomplete': dynamicAttr(() => { return this.getAttribute('data-end-date-autocomplete'); }),
                    'data-placeholder-text': dynamicAttr(() => { return this.getAttribute('data-end-date-placeholder-text'); }),
                    'data-min-value': dynamicAttr(() => {
                        var _a, _b;
                        return isValidDate((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.start) ? getMmDdYyyyDateString((_b = this.dateRange) === null || _b === void 0 ? void 0 : _b.start) : undefined;
                    }),
                    'data-max-value': dynamicAttr(() => { return (isValidDate(getDateFromAttribute(this, 'data-max-value'))) ? this.getAttribute('data-max-value') : undefined; }),
                    'data-weekdays-only': dynamicAttr(() => { return this.getAttribute('data-weekdays-only'); }),
                },
                extensions: this.elementCache.assignTo('date-range-end-field'),
                properties: {
                    modelValue: dynamicProperty(() => { var _a; return isValidDate((_a = this.modelValue) === null || _a === void 0 ? void 0 : _a.end) ? this.modelValue.end : undefined; }),
                    customValidationFunction: dynamicProperty(() => {
                        return combineValidationFunctions(this.endDateCustomValidationFunction, this.customValidationFunction);
                    })
                },
                listeners: {
                    neonDateFieldButtonPressed: (event) => {
                        const neonDatePicker = this.elementCache.get('date-picker');
                        if (neonDatePicker != null) {
                            updateDynamicAttributeNoChildren(neonDatePicker, 'data-month');
                            updateDynamicAttributeNoChildren(neonDatePicker, 'data-year');
                        }
                        this.showDatePicker(this.elementCache.get('date-range-end-field'), event.detail.datePickerButton);
                    },
                    modelValueChange: (event) => {
                        var _a;
                        event.stopPropagation();
                        this.modelValue = {
                            start: ((_a = this.dateRange) === null || _a === void 0 ? void 0 : _a.start) || (this.isDateStringHasFullYear(event.target.value) ? event.target.modelValue : undefined),
                            end: event.target.modelValue,
                        };
                    }
                }
            }))
        ];
    }
    isDateStringHasFullYear(dateString) {
        const result = getYearMonthDayStringsFromLocalizedDateString(dateString);
        return result != null && result.year.length === 4;
    }
    isDateRangeEqual(value1, value2) {
        if (value1 != null && value2 != null) {
            //The isSameYearMonthay will return false if the inputs are both undefined but we want it to be true so we add the null check in before calling isSameYearMonthDay
            const startsAreEqual = (value1.start == null && value2.start == null) || isSameYearMonthDay(value1.start, value2.start);
            const endsAreEqual = (value1.end == null && value2.end == null) || isSameYearMonthDay(value1.end, value2.end);
            return startsAreEqual && endsAreEqual;
        }
        //They are the same if they are both undefined
        //They are different if one but not both are undefined
        return value1 == null && value2 == null;
    }
    connectedCallback() {
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicAttribute(this, 'data-label-text');
        });
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
}
export function registerNeonDateRangeField() {
    registerComponent('neon-2_20_1-date-range-field', NeonDateRangeField);
}
