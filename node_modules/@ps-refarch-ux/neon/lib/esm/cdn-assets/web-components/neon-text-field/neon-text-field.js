import './neon-text-field.scss';
import { createElement, createElementIf, dynamicAttr, dynamicProperty, dynamicText, registerComponent, updateConditionalElements, updateDynamicAttribute, updateDynamicProperty, updateDynamicText, useElementIfElse } from '../shared/dom-manipulation';
import { AbstractInputField } from '../shared/abstract-input-field';
import { getBasicTextValidationRules, getTextValidators } from '../shared/validation-engine/validate-engine';
import { stringHasCharacters } from '../shared/string-utils';
import { translate } from '@ps-refarch-ux/mfe-utils';
import { announceCharCountChange, renderCharCounter } from '../shared/char-counter';
import { debounce } from '../../../exported-utils/throttle';
export class NeonTextField extends AbstractInputField {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-text-field';
        //When this variable is true, it will show the password.
        this.isShowPassword = false;
        this.isActionButtonFocused = false;
        this.showHidePwdTooltip = createElement('span', {
            text: dynamicText(() => {
                return this.isShowPassword ? translate('neon|text field|Hide Password') : translate('neon|text field|Show Password');
            })
        });
    }
    static get observedAttributes() {
        return super.observedAttributes.concat([
            'autocomplete',
            'data-regex',
            'data-regex-modifiers',
            'data-regex-error-text',
            'data-is-regex-inverse',
            'data-is-read-only',
            'data-placeholder-text',
            'data-min-length',
            'data-max-length',
            'data-exclude-chars',
            'data-is-email',
            'data-is-password',
            'data-icon',
            'data-is-clearable',
            'data-show-count'
        ]);
    }
    generateValidationRules() {
        const validationRules = getBasicTextValidationRules();
        validationRules.setProperty('isRequired', this.getAttribute('data-is-required'), Boolean);
        validationRules.setProperty('minLength', this.getAttribute('data-min-length'), Number);
        validationRules.setProperty('maxLength', this.getAttribute('data-max-length'), Number);
        validationRules.setProperty('excludeChars', this.getAttribute('data-exclude-chars'), String);
        validationRules.setProperty('isEmail', this.getAttribute('data-is-email'), Boolean);
        const regexString = this.getAttribute('data-regex');
        const regexErrorText = this.getAttribute('data-regex-error-text');
        // 'data-regex-modifiers',
        if (stringHasCharacters(regexString) &&
            stringHasCharacters(regexErrorText)) {
            try {
                validationRules.regex = {
                    pattern: new RegExp(regexString, this.getAttribute('data-regex-modifiers') || undefined),
                    errorMessage: regexErrorText,
                    inverse: this.getAttribute('data-is-regex-inverse') === 'true'
                };
            }
            catch (err) {
                //When an invalid number gets put into a number property, the property in validationRules is not set with bad data.
                //And no error is logged. We will do the same thing here.
            }
        }
        this.validators = getTextValidators(validationRules);
        this.addCustomValidateToValidators();
    }
    setValue(value, skip) {
        if (value == null) {
            value = '';
        }
        const inputEl = this.inputElement;
        if (inputEl != null && value !== inputEl.value) {
            inputEl.value = value;
        }
        if (skip !== true) {
            this.setModelValue(value, true);
        }
    }
    validateModelValueType(modelValue) {
        if (typeof modelValue === 'string') {
            return modelValue;
        }
        return undefined;
    }
    setModelValue(modelValue, skip) {
        if ((this.modelValue == null || this.modelValue === '') && (modelValue == null || modelValue === '')) {
            return false;
        }
        const ele = this.inputElement;
        if (modelValue !== ele.modelValue) {
            ele.modelValue = modelValue;
            if (skip !== true) {
                this.setValue(modelValue, true);
            }
            return true;
        }
        return false;
    }
    getTextFieldClasses() {
        var _a;
        let classString = '';
        classString += this.getSizeClass();
        if (this.getAttribute('data-icon') != null) {
            classString += ' neon-text-field-has-icon';
        }
        //Normally we try to make comparisons with this.modelValue but we need to compare to this.value.length instead for a couple of reasons.
        //1) We want to know if there is anything inside the input and this.value directly accesses that which will
        // be the same in the neon-text-field and neon-date field if we ever need clear buttons there.
        //2) updateDynamicAttribute is called within validateWidget(), which is called BEFORE the modelValue is set.
        if (this.getAttribute('data-is-password') || (this.getAttribute('data-is-clearable') && ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length))) {
            classString += ' neon-text-field-has-action';
        }
        if (this.isActionButtonFocused) {
            classString += ' neon-text-field-has-action-focused';
        }
        return classString;
    }
    renderInputElement() {
        const inputChangedDebounced = debounce(() => {
            var _a;
            announceCharCountChange(this.getAttribute('data-show-count'), (_a = this.value) === null || _a === void 0 ? void 0 : _a.length, this.getAttribute('data-max-length'));
        }, 750);
        return createElement('input', {
            attrs: {
                id: dynamicAttr(() => { return this.getInputId(); }),
                // class: 'neon-input-field-first',
                type: dynamicAttr(() => { return (this.getAttribute('data-is-password') === 'true' && !this.isShowPassword) ? 'password' : 'text'; }),
                'aria-label': dynamicAttr(() => { return this.getAriaLabelForInput(); }),
                autocomplete: dynamicAttr(() => { return this.getAttribute('autocomplete'); }),
                placeholder: dynamicAttr(() => { return this.getAttribute('data-placeholder-text'); })
            },
            properties: {
                disabled: dynamicProperty(() => { return this.getDisabledStatus(); })
            },
            listeners: {
                'input': (event) => {
                    this.inputChange();
                    inputChangedDebounced();
                },
                'blur': (event) => { return this.focusLost(event); }
            }
        });
    }
    renderContent() {
        return [
            createElement('div', {
                attrs: { class: dynamicAttr(() => { return 'neon-text-field-input-container neon-input-field-first' + this.getTextFieldClasses(); }) }
            }, createElementIf(() => { return this.getAttribute('data-icon') !== null; }, 'div', {
                attrs: {
                    class: dynamicAttr(() => { return 'neon-text-field-icon' + (this.getDisabledStatus() ? ' neon-text-field-icon-disabled' : '') + (this.getAttribute('data-is-read-only') ? ' neon-text-field-icon-readonly' : ''); })
                }
            }, () => {
                return createElement('neon-2_20_1-icon', {
                    attrs: {
                        'data-size': 'medium',
                        'data-size-mobile': dynamicAttr(() => {
                            return this.getAttribute('data-is-read-only') ? 'x-small' : 'large';
                        }),
                        'data-icon': dynamicAttr(() => { return this.getAttribute('data-icon'); })
                    }
                });
            }), ...useElementIfElse(() => { return this.getAttribute('data-is-read-only') === 'true'; }, () => { return this.readOnlyElement; }, () => { return this.inputElement; }), createElementIf(() => {
                var _a;
                //Normally we try to make comparisons with this.modelValue but we need to compare to this.value.length instead for a couple of reasons.
                //1) We want to know if there is anything inside the input and this.value directly accesses that which will
                // be the same in the neon-text-field and neon-date field if we ever need clear buttons there.
                //2) updateConditionalElements is called within validateWidget(), which is called BEFORE the modelValue is set.
                return (this.getAttribute('data-is-password') !== 'true' && this.getAttribute('data-is-clearable') === 'true' && ((_a = this.value) === null || _a === void 0 ? void 0 : _a.length)) && this.getAttribute('data-is-read-only') !== 'true';
            }, 'button', {
                attrs: {
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-clear-button'; }),
                    'aria-label': dynamicAttr(() => {
                        return translate('neon|text field|Clear');
                    })
                },
                properties: {
                    disabled: dynamicProperty(() => { return this.getDisabledStatus(); })
                },
                listeners: {
                    click: () => {
                        this.clearInputField();
                    },
                    focus: () => {
                        this.isActionButtonFocused = true;
                        updateDynamicAttribute(this, 'class');
                    },
                    blur: () => {
                        this.isActionButtonFocused = false;
                        updateDynamicAttribute(this, 'class');
                    }
                }
            }, () => {
                return createElement('neon-2_20_1-icon', {
                    attrs: {
                        'data-size': 'small',
                        'data-size-mobile': 'medium',
                        'data-icon': 'close'
                    }
                });
            }), createElementIf(() => { return this.getAttribute('data-is-password') === 'true'; }, 'button', {
                attrs: {
                    id: dynamicAttr(() => { return this.getAttribute('id') + '-show-pwd-btn'; }),
                    'aria-label': dynamicAttr(() => {
                        return this.isShowPassword ? translate('neon|text field|Hide Password') : translate('neon|text field|Show Password');
                    })
                },
                properties: { disabled: dynamicProperty(() => { return this.getAttribute('disabled') === 'true'; }) },
                listeners: {
                    click: () => {
                        this.togglePasswordVisibility();
                    },
                    focus: () => {
                        this.isActionButtonFocused = true;
                        updateDynamicAttribute(this, 'class');
                    },
                    blur: () => {
                        this.isActionButtonFocused = false;
                        updateDynamicAttribute(this, 'class');
                    }
                }
            }, () => {
                return createElement('neon-2_20_1-icon', {
                    attrs: {
                        'data-size': 'small',
                        'data-size-mobile': 'medium',
                        'data-icon': dynamicAttr(() => {
                            return this.isShowPassword ? 'visibility-off' : 'visibility';
                        })
                    }
                });
            }), createElementIf(() => { return this.getAttribute('data-is-password') === 'true'; }, 'neon-2_20_1-tooltip', { attrs: { 'data-target-id': dynamicAttr(() => { return this.getAttribute('id') + '-show-pwd-btn'; }) } }, () => { return this.showHidePwdTooltip; })),
            renderCharCounter(() => {
                var _a;
                return {
                    showCharCountAttribute: this.getAttribute('data-show-count'),
                    currentChars: (_a = this.value) === null || _a === void 0 ? void 0 : _a.length,
                    maxChars: this.getAttribute('data-max-length')
                };
            })
        ];
    }
    togglePasswordVisibility() {
        this.isShowPassword = !this.isShowPassword;
        updateDynamicAttribute(this, 'type');
        updateDynamicAttribute(this, 'aria-label');
        updateDynamicAttribute(this, 'data-icon');
        setTimeout(() => {
            //There is an animation of the tooltip fading out when you click the button.
            //We don't want to update the text until after the fadeout completes
            //so we are using a set timeout here.
            updateDynamicText(this.showHidePwdTooltip);
        }, 200);
    }
    attributeChanged(attribute, oldValue, newValue) {
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        super.attributeChanged(attribute, oldValue, newValue);
        switch (attribute) {
            case 'id':
                updateDynamicAttribute(this, 'data-target-id');
                break;
            case 'autocomplete':
                updateDynamicAttribute(this, 'autocomplete');
                break;
            case 'data-placeholder-text':
                updateDynamicAttribute(this, 'placeholder');
                break;
            case 'data-is-password':
                updateDynamicAttribute(this, 'type');
                updateDynamicAttribute(this, 'class');
                updateConditionalElements(this);
                break;
            case 'disabled':
                updateDynamicAttribute(this, 'class');
                updateDynamicProperty(this, 'disabled');
                break;
            case 'data-is-read-only':
                updateDynamicAttribute(this, 'class');
                updateDynamicProperty(this, 'disabled');
                updateDynamicAttribute(this, 'data-size-mobile');
                break;
            case 'data-icon':
                updateDynamicAttribute(this, 'data-icon');
                updateDynamicAttribute(this, 'class');
                updateConditionalElements(this);
                break;
            case 'data-is-clearable':
                updateConditionalElements(this);
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-max-length':
                updateConditionalElements(this);
                updateDynamicText(this);
                break;
            case 'data-show-count':
                updateConditionalElements(this);
                break;
        }
    }
}
export function registerNeonTextField() {
    registerComponent('neon-2_20_1-text-field', NeonTextField);
}
