import './neon-menu.scss';
import { type MenuOption, type PopperPlacement } from '../../../interfaces';
import { NeonPopover } from '../neon-popover/neon-popover';
import { type RequiredSlotInfo } from '../shared/neon-widget-slotted';
export declare class NeonMenu extends NeonPopover {
    widgetTag: string;
    static get observedAttributes(): Array<string>;
    static get inputProperties(): Array<string>;
    static get requiredSlots(): Array<RequiredSlotInfo>;
    nestedPopperDefaultPlacement: PopperPlacement;
    context: any;
    options: Array<MenuOption> | undefined;
    i18nUnsubscribe?: () => void;
    constructor();
    validateInputs(): void;
    isDisabledItem(item: MenuOption): boolean;
    renderPopperElement(): HTMLElement;
    renderPopperFromMenuItems(menuOptions: Array<MenuOption | 'DIVIDER'>): HTMLElement;
    isArrowShowing(): boolean;
}
