import './neon-stepper-navigation.scss';
import { NeonWidget, bindCallback, getCallback } from '../shared/neon-widget';
import { dynamicText, createElement, updateDynamicText, updateDynamicAttribute, dynamicAttr, dynamicProperty, createElementIf, updateConditionalElements, updateDynamicProperty } from '../shared/dom-manipulation';
import { addTranslationListener } from '@ps-refarch-ux/mfe-utils';
import { translate } from '@ps-refarch-ux/mfe-utils';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
const largeMobileWidth = 992;
export class NeonStepperNavigation extends NeonWidget {
    // Any attribute that should be watched for changes should be listed here.
    static get observedAttributes() {
        return [
            'id',
            'data-is-horizontal',
            'data-is-numbered',
            'data-is-sequential'
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-stepper-navigation';
        this.isIdRequired = true;
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
    }
    // When the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        var _a;
        super.attributeChanged(attrName, oldVal, newVal);
        // AttributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                updateDynamicText(this);
                if (newVal != null) {
                    this.unsubscribeFromChangeStream();
                    this.stepperNavigation = window.neonToolkits['neon-2_20_1-stepper-navigation'].get(newVal);
                    this.subscribeToChangeStream();
                }
                else {
                    delete this.stepperNavigation;
                }
                //Everything that the neon-stepper-navigation has in it is going to be based off of data inside of the stepperNavigation object
                //So if the whole object changes, we just need to redraw everything.
                this.redraw();
                break;
            case 'data-is-horizontal':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-is-numbered':
                updateConditionalElements(this);
                break;
            case 'data-is-sequential':
                {
                    const isSequential = (newVal === 'true');
                    (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.setIsSequential(isSequential);
                    updateDynamicProperty(this, 'disabled');
                    updateDynamicAttribute(this, 'class');
                }
                break;
            default:
                break;
        }
    }
    //NOTE: The outside developer needs to be able to call the functions on the StepperNavigation object
    //and the web-component needs to update accordingly.
    //You can test this in the console by calling: document.querySelector('[neon-stepper-navigation]').stepperNavigation which
    //gets you the StepperNavigation object. You can call the functions from the console.
    stepperNavigationChangeHandler(event) {
        switch (event.name) {
            case 'active-step-change':
                updateDynamicAttribute(this, 'class');
                updateDynamicAttribute(this, 'data-icon');
                updateDynamicProperty(this, 'disabled');
                updateDynamicText(this);
                updateConditionalElements(this);
                break;
            case 'steps-property-change':
                updateDynamicAttribute(this, 'class');
                updateDynamicAttribute(this, 'data-icon');
                updateDynamicProperty(this, 'disabled');
                updateDynamicText(this);
                updateConditionalElements(this);
                break;
            case 'new-steps':
                this.redraw();
                break;
        }
    }
    // This function sets and removes the appropriate classes / focus based on screen width changes
    // At the 992px breakpoint it is expected to only use the horizontal stepper
    windowResizeListener(event) {
        if (window.innerWidth <= largeMobileWidth && this.querySelector('.neon-stepper-navigation-steps-list-horizontal-mobile') == null) {
            // We are moving to a mobile width screen and that the default direction is vertical
            // We add the class to change the direction to horizontal
            const listElement = this.querySelector('.neon-stepper-navigation-steps-list-vertical-desktop');
            listElement === null || listElement === void 0 ? void 0 : listElement.classList.add('neon-stepper-navigation-steps-list-horizontal-mobile');
        }
        else if (window.innerWidth > largeMobileWidth && this.querySelector('.neon-stepper-navigation-steps-list-vertical-desktop.neon-stepper-navigation-steps-list-horizontal-mobile') != null) {
            // We are moving to a desktop width and the default direction is vertical
            // We need to remove the horizontal class to change the direction back to vertical
            const listElement = this.querySelector('.neon-stepper-navigation-steps-list-vertical-desktop');
            listElement === null || listElement === void 0 ? void 0 : listElement.classList.remove('neon-stepper-navigation-steps-list-horizontal-mobile');
        }
        updateConditionalElements(this);
        updateDynamicAttribute(this, 'class');
    }
    getStepIconName(step, index) {
        var _a, _b, _c;
        let iconName = '';
        switch ((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getStepState(step.id)) {
            case 'complete':
                iconName = 'status-check';
                break;
            case 'error':
                iconName = 'status-error';
                break;
            case 'in-progress':
                iconName = 'status-in-progress';
                break;
            default:
                if (step.id === ((_c = (_b = this.stepperNavigation) === null || _b === void 0 ? void 0 : _b.getActiveStep()) === null || _c === void 0 ? void 0 : _c.id)) {
                    iconName = 'circle';
                }
                else {
                    iconName = 'circle-outline';
                }
        }
        return iconName;
    }
    renderActiveStepTitle() {
        return createElement('div', {
            attrs: {
                class: 'neon-stepper-navigation-mobile-active-title-container'
            }
        }, createElement('div', {
            attrs: {
                class: 'neon-stepper-navigation-step-title-header'
            },
            text: dynamicText(() => {
                var _a, _b, _c;
                const activeStep = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep();
                if (activeStep == null) {
                    return '';
                }
                const activeStepIndex = (_b = this.stepperNavigation) === null || _b === void 0 ? void 0 : _b.getStepIndexById(activeStep.id);
                if (activeStepIndex == null) {
                    return '';
                }
                else {
                    const stepNumber = String(activeStepIndex + 1);
                    const numberOfSteps = String(((_c = this.stepperNavigation) === null || _c === void 0 ? void 0 : _c.getSteps().length) || 0);
                    return translate('neon|stepper-navigation|Step stepNumber of numberOfSteps', { 'stepNumber': stepNumber, 'numberOfSteps': numberOfSteps });
                }
            })
        }), createElement('div', {
            attrs: {
                class: 'neon-stepper-navigation-title'
            },
            text: dynamicText(() => { var _a, _b; return (_b = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) === null || _b === void 0 ? void 0 : _b.text; }),
            extensions: tooltipOnEllipsis
        }));
    }
    renderNextStepTitle() {
        return createElementIf(() => {
            var _a;
            return !((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getIsLastStepActive()) || false;
        }, 'div', {
            attrs: {
                class: 'neon-stepper-navigation-mobile-next-title-container'
            }
        }, () => {
            return [createElement('div', {
                    attrs: {
                        class: 'neon-stepper-navigation-next-text'
                    },
                    text: translate('neon|stepper-navigation|Next Step')
                }),
                createElement('div', {
                    attrs: {
                        class: 'neon-stepper-navigation-next-step-title'
                    },
                    text: dynamicText(() => {
                        var _a;
                        const nextStep = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getNextStep();
                        if (nextStep == null) {
                            return '';
                        }
                        return nextStep.text;
                    }),
                    extensions: tooltipOnEllipsis
                })];
        });
    }
    renderSteps() {
        if (this.stepperNavigation == null || this.stepperNavigation.getSteps().length === 0) {
            return [];
        }
        const steps = this.stepperNavigation.getSteps();
        const stepElements = steps.flatMap((step, index) => {
            return [createElement('li', {
                    attrs: {
                        class: dynamicAttr(() => {
                            var _a, _b;
                            const numberOfSteps = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getSteps().length;
                            if (numberOfSteps == null || numberOfSteps === 0) {
                                return;
                            }
                            if (step.id === ((_b = this.stepperNavigation) === null || _b === void 0 ? void 0 : _b.getSteps()[numberOfSteps - 1].id)) {
                                return 'neon-stepper-navigation-step neon-stepper-navigation-last-step';
                            }
                            else {
                                return 'neon-stepper-navigation-step';
                            }
                        })
                    }
                }, createElementIf(() => {
                    return window.innerWidth > largeMobileWidth;
                }, 'button', {
                    attrs: {
                        id: dynamicAttr(() => {
                            return this.getAttribute('id') + '-step-' + String(index + 1) + '-button';
                        }),
                        class: dynamicAttr(() => {
                            var _a, _b;
                            if (((_b = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) === null || _b === void 0 ? void 0 : _b.id) === step.id) {
                                return 'neon-stepper-navigation-step-button neon-stepper-navigation-step-active';
                            }
                            else {
                                return 'neon-stepper-navigation-step-button';
                            }
                        }),
                        'aria-label': dynamicAttr(() => {
                            var _a;
                            let ariaLabelText = translate('neon|stepper-navigation|Step') + ' ' + String(index + 1) + ', ' + step.text;
                            const stepState = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getStepState(step.id);
                            if (step.subtext != null) {
                                ariaLabelText += ', ' + step.subtext;
                            }
                            switch (stepState) {
                                case 'complete':
                                    ariaLabelText += ', ' + translate('neon|stepper-navigation|complete');
                                    break;
                                case 'error':
                                    ariaLabelText += ', ' + translate('neon|stepper-navigation|error');
                                    break;
                                case 'in-progress':
                                    ariaLabelText += ', ' + translate('neon|stepper-navigation|in progress');
                                    break;
                            }
                            return ariaLabelText;
                        }),
                        'aria-current': dynamicAttr(() => {
                            var _a, _b;
                            if (((_b = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) === null || _b === void 0 ? void 0 : _b.id) === step.id) {
                                return 'true';
                            }
                            else {
                                return undefined;
                            }
                        }),
                        'aria-disabled': dynamicAttr(() => {
                            var _a;
                            if (this.stepperNavigation != null && ((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getIsStepDisabled(step.id)) === true) {
                                return 'true';
                            }
                            else {
                                return 'false';
                            }
                        })
                    },
                    listeners: {
                        click: () => {
                            var _a, _b;
                            if (((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getIsStepDisabled(step.id)) === true) {
                                return;
                            }
                            (_b = this.stepperNavigation) === null || _b === void 0 ? void 0 : _b.setStepToActive(step.id);
                        }
                    }
                }, () => {
                    return createElement('div', {
                        attrs: {
                            class: dynamicAttr(() => {
                                var _a, _b, _c, _d;
                                let classList = 'neon-stepper-navigation-icon-and-title-grid-container';
                                if (((_b = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) === null || _b === void 0 ? void 0 : _b.id) === step.id) {
                                    classList += ' neon-stepper-navigation-step-active';
                                }
                                if ((_c = this.stepperNavigation) === null || _c === void 0 ? void 0 : _c.getIsStepDisabled(step.id)) {
                                    classList += ' disabled';
                                }
                                if (((_d = this.stepperNavigation) === null || _d === void 0 ? void 0 : _d.getIsSequential()) === false) {
                                    classList += ' unordered';
                                }
                                return classList;
                            })
                        }
                    }, ...this.renderIconAndTitles(step, index));
                }), createElementIf(() => {
                    return window.innerWidth <= largeMobileWidth;
                }, 'div', {
                    attrs: {
                        class: dynamicAttr(() => {
                            var _a, _b, _c, _d;
                            let classList = 'neon-stepper-navigation-icon-and-title-grid-container';
                            if (((_b = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) === null || _b === void 0 ? void 0 : _b.id) === step.id) {
                                classList += ' neon-stepper-navigation-step-active';
                            }
                            if ((_c = this.stepperNavigation) === null || _c === void 0 ? void 0 : _c.getIsStepDisabled(step.id)) {
                                classList += ' disabled';
                            }
                            if (((_d = this.stepperNavigation) === null || _d === void 0 ? void 0 : _d.getIsSequential()) === false) {
                                classList += ' unordered';
                            }
                            return classList;
                        })
                    }
                }, () => {
                    if (steps.length >= 7 && window.innerWidth <= largeMobileWidth) {
                        return undefined;
                    }
                    else {
                        return this.renderIconAndTitles(step, index);
                    }
                })),
                createElementIf(() => {
                    var _a, _b;
                    const numberOfSteps = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getSteps().length;
                    if (numberOfSteps == null || numberOfSteps === 0) {
                        return false;
                    }
                    if (this.getAttribute('data-is-horizontal') !== 'true' && window.innerWidth > largeMobileWidth) {
                        return false;
                    }
                    return step.id !== ((_b = this.stepperNavigation) === null || _b === void 0 ? void 0 : _b.getSteps()[numberOfSteps - 1].id);
                }, 'li', {
                    attrs: {
                        class: 'neon-stepper-navigation-divider-line'
                    }
                })];
        });
        return stepElements;
    }
    renderIconAndTitles(step, index) {
        return [
            createElement('div', {
                attrs: {
                    class: 'neon-stepper-navigation-step-icon-container'
                }
            }, createElement('neon-2_20_1-icon', {
                attrs: {
                    class: 'neon-stepper-navigation-step-icon',
                    'data-icon': dynamicAttr(() => { return this.getStepIconName(step, index); })
                },
                properties: {
                    disabled: dynamicProperty(() => {
                        var _a;
                        return (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getIsStepDisabled(step.id);
                    })
                }
            }), createElementIf(() => {
                var _a;
                return this.getAttribute('data-is-numbered') === 'true' && ((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getStepState(step.id)) == null;
            }, 'span', {
                attrs: {
                    class: 'neon-stepper-navigation-step-number'
                },
                text: dynamicText(() => {
                    if (this.getAttribute('data-is-numbered') === 'true') {
                        return String(index + 1);
                    }
                    else {
                        return undefined;
                    }
                })
            })),
            createElementIf(() => {
                return window.innerWidth > largeMobileWidth;
            }, 'div', {
                attrs: {
                    class: 'neon-stepper-navigation-titles-container'
                }
            }, () => {
                return [
                    createElement('span', {
                        attrs: {
                            class: 'neon-stepper-navigation-title',
                            'data-prevent-layout-shift': dynamicAttr(() => {
                                return step.text;
                            })
                        },
                        text: dynamicText(() => { return step.text; }),
                        extensions: tooltipOnEllipsis
                    }),
                    createElement('span', {
                        attrs: {
                            class: 'neon-stepper-navigation-subtitle',
                            'data-prevent-layout-shift': dynamicAttr(() => {
                                return step.subtext;
                            })
                        },
                        text: dynamicText(() => { return step.subtext; }),
                        extensions: tooltipOnEllipsis
                    })
                ];
            })
        ];
    }
    renderMobileDisplayContainer() {
        return createElementIf(() => {
            var _a;
            return (window.innerWidth <= largeMobileWidth && ((_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getActiveStep()) != null);
        }, 'div', {
            attrs: {
                class: 'neon-stepper-navigation-steps-text-container'
            }
        }, () => {
            return [
                this.renderActiveStepTitle(),
                this.renderNextStepTitle()
            ];
        });
    }
    render() {
        var _a;
        const numberOfSteps = (_a = this.stepperNavigation) === null || _a === void 0 ? void 0 : _a.getSteps().length;
        if (numberOfSteps == null || numberOfSteps === 0) {
            return undefined;
        }
        return createElement('nav', {}, createElement('ol', {
            attrs: {
                class: dynamicAttr(() => {
                    let classList = '';
                    if (this.getAttribute('data-is-horizontal') === 'true') {
                        if (window.innerWidth > largeMobileWidth && this.stepperNavigation != null && this.stepperNavigation.getSteps().length > 6) {
                            console.error('Horizontal mode in desktop is not supported for more than 6 steps');
                            this.setAttribute('data-is-horizontal', 'false');
                        }
                        else {
                            classList += 'neon-stepper-navigation-steps-list neon-stepper-navigation-steps-list-horizontal-desktop neon-stepper-navigation-steps-list-horizontal-mobile';
                        }
                    }
                    else {
                        classList += 'neon-stepper-navigation-steps-list neon-stepper-navigation-steps-list-vertical-desktop';
                        if (window.innerWidth <= largeMobileWidth) {
                            classList += ' neon-stepper-navigation-steps-list-horizontal-mobile';
                        }
                    }
                    if (numberOfSteps > 6) {
                        classList += ' neon-stepper-navigation-steps-list-horizontal-no-icons';
                    }
                    return classList;
                })
            }
        }, ...this.renderSteps()), this.renderMobileDisplayContainer());
    }
    connectedCallback() {
        super.connectedCallback(); //Calls redraw
        this.tooltipOnEllipsisObserver.connect();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
            updateDynamicAttribute(this, 'aria-label');
        });
        this.subscribeToChangeStream();
        window.addEventListener('resize', bindCallback(this.windowResizeListener, this));
    }
    disconnectedCallback() {
        this.tooltipOnEllipsisObserver.disconnect();
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        this.unsubscribeFromChangeStream();
        window.removeEventListener('resize', getCallback(this.windowResizeListener, this));
    }
    subscribeToChangeStream() {
        if (this.stepperNavigation == null) {
            return;
        }
        this.unsubscribeFromNavStreamCallback = this.stepperNavigation.getChangeStream().subscribe((event) => {
            this.stepperNavigationChangeHandler(event);
        });
    }
    unsubscribeFromChangeStream() {
        if (this.unsubscribeFromNavStreamCallback != null) {
            this.unsubscribeFromNavStreamCallback();
            delete this.unsubscribeFromNavStreamCallback;
        }
    }
}
