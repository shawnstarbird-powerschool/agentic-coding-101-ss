import { type StepperNavigation as StepperNavigationInterface } from '../../../interfaces/stepper-navigation';
import { type Step } from '../../../interfaces/step';
import { type ReadOnlyStream } from '../../../interfaces/read-only-stream';
import { type StepperNavigationChangeEvent } from '../../../interfaces/stepper-navigation-change-event';
export declare class StepperNavigation implements StepperNavigationInterface {
    private id;
    private changeStream;
    private activeStep?;
    private steps;
    private isSequential;
    private disabledSteps;
    private stepsStates;
    getId(): string;
    getActiveStep(): Step | undefined;
    /**
     *
     * @returns If no step is active, returns the first step.
     * If the last step is active, undefined is returned
     * If any other step is active, then the following step is returned
     */
    getNextStep(): Step | undefined;
    /**
     *
     * @returns If no step is active, returns undefined.
     * If the first step is active, undefined is returned
     * If any other step is active, then the previous step is returned
     */
    getPreviousStep(): Step | undefined;
    getStepIndexById(stepId: string): number | undefined;
    getStepState(stepId: string): 'complete' | 'in-progress' | 'error' | undefined;
    getNonCompleteSteps(stepIds?: Array<string>): Array<string>;
    getSteps(): Array<Step>;
    getChangeStream(): ReadOnlyStream<StepperNavigationChangeEvent>;
    getIsStepDisabled(stepId: string): boolean;
    getIsSequential(): boolean;
    getIsFirstStepActive(): boolean;
    getIsLastStepActive(): boolean;
    nextStep(): void;
    previousStep(): void;
    setStepToActive(step: number | string): Promise<void>;
    setStepToComplete(step: number | string): void;
    setStepToInProgress(step: number | string): void;
    setStepToError(step: number | string): void;
    /**
     * This removes any state from the specified step. The "default" state
     * This also removes the disabled state from steps
     * @param step the step id or step number
     */
    setStepToDefault(step: number | string): void;
    setStepToDisabled(step: number | string): void;
    setSteps(steps: Array<Step>): void;
    setIsSequential(isSequential: boolean): void;
    setStepState(step: number | string, state: 'complete' | 'in-progress' | 'error' | undefined): void;
    private getStepFromStepIdOrNumber;
    private setPreviousSequentialStepsCompleted;
    private setStepsAfterActiveStepToDisabled;
}
