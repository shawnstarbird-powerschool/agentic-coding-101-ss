import { createElement, dynamicAttr, dynamicText, createElementIf, updateConditionalElements, updateDynamicText, dynamicProperty, updateDynamicAttribute, updateAllDynamicSettings, useElementIf, updateDynamicAttributeNoChildren, updateDynamicPropertyNoChildren } from '../shared/dom-manipulation';
import { AbstractInputField } from '../shared/abstract-input-field';
import './neon-select-field.scss';
import { getRequiredOnlyValidators } from '../shared/validation-engine/validate-engine';
import { showPopper } from '../shared/poppers';
import { addTranslationListener, translate } from '@ps-refarch-ux/mfe-utils';
import { traverseTreeGeneric } from '../../../exported-utils/tree/tree-utils';
import { getIdSafeString } from '../shared/string-utils';
import { tooltipOnEllipsis, TooltipOnEllipsisObserver } from '../shared/extensions/tooltip-on-ellipsis';
export class NeonSelectField extends AbstractInputField {
    static get inputProperties() {
        return super.inputProperties.concat(['options']);
    }
    static get observedAttributes() {
        return super.observedAttributes.concat([
            'data-placeholder-text',
            'data-show-filter',
            'data-popper-placement',
            'data-popper-max-height',
            'data-is-loading',
            'data-undefined-option',
            'data-undefined-option-text',
            'data-undefined-option-subtext'
        ]);
    }
    constructor() {
        super();
        this.widgetTag = 'neon-select-field';
        this.isIdRequired = true;
        this.isUsesLabel = false;
        this.imageFailedToLoad = false;
        this.tooltipOnEllipsisObserver = new TooltipOnEllipsisObserver(this);
        setTimeout(() => {
            const inputPopperPlacement = this.getAttribute('data-popper-placement');
            if (inputPopperPlacement != null) {
                const placementOptions = ['top-start', 'top-end', 'bottom-start', 'bottom-end', 'left-start', 'left', 'left-end', 'right-start', 'right', 'right-end'];
                if (!placementOptions.includes(inputPopperPlacement)) {
                    console.error(`The supported options for popover placement in Neon Select are ${placementOptions.join(', ')}`);
                }
            }
        }, 1000);
    }
    set options(selectOption) {
        if (selectOption === this.currentOptions) {
            return;
        }
        this.currentOptions = selectOption;
        const optionsForPopperMenuElement = this.convertOptionsToSelectMenuOptions(this.options || []);
        this.menuOptionsDropdown = this.renderPopperMenuElement(optionsForPopperMenuElement);
        this.redraw();
        this.setModelValue(this.modelValue);
        this.redrawInput();
    }
    get options() {
        return this.currentOptions;
    }
    set modelValue(value) {
        value = this.validateModelValueType(value);
        const modelValueChanged = this.setModelValue(value);
        if (modelValueChanged) {
            //only show the validation message IF an error message is already showing.
            const showValidationResult = !this.errorElement.classList.contains('neon-sr-only');
            this.validateWidget(showValidationResult);
        }
        this.setReadonlyText();
    }
    get modelValue() {
        return this._modelValue;
    }
    set value(value) {
        this.setValue(value);
        this.setReadonlyText();
    }
    get value() {
        return this._value;
    }
    redrawInput() {
        super.redrawInput();
        if (this.getAttribute('data-is-read-only') !== 'true') {
            //Even though this is slow, the alternative is to completely
            //redraw the element which won't be any faster.
            //We don't update each dynamic setting individually here because
            //there are so many of them and it's likely to be very difficult to maintain.
            updateAllDynamicSettings(this.inputElement);
        }
    }
    renderTag(tagClass) {
        return createElement('neon-2_20_1-tag', {
            attrs: {
                class: dynamicAttr(() => { return tagClass; }),
                'data-text': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagText;
                }),
                'data-subtext': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagSubtext;
                }),
                'data-type': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagType;
                }),
            },
        });
    }
    renderInputElement() {
        return createElement('button', {
            attrs: {
                id: dynamicAttr(() => {
                    return this.getInputId();
                }),
                class: dynamicAttr(() => {
                    return 'neon-select-field-button neon-input-field-first' + this.getSizeClass();
                }),
                'aria-label': dynamicAttr(() => {
                    return this.getAriaLabelForInput();
                }),
                'aria-disabled': dynamicAttr(() => {
                    return this.getAttribute('data-is-loading') === 'true' ? 'true' : undefined;
                })
            },
            properties: {
                disabled: dynamicProperty(() => {
                    return this.getDisabledStatus();
                }),
            },
            listeners: {
                click: (event) => {
                    if (this.getAttribute('data-is-loading') === 'true') {
                        return;
                    }
                    if (this.removePopper != null) {
                        this.removePopper();
                        return;
                    }
                    const relativeElement = this.querySelector('button');
                    const placement = (this.getAttribute('data-popper-placement') || 'bottom-start');
                    this.removePopper = showPopper(relativeElement, this.menuOptionsDropdown, {
                        placement: placement,
                        offset: 0,
                        trapFocus: true,
                        makePopperSameWidthAs: 'relative-element',
                        popperPostClosedCallback: () => {
                            delete this.removePopper;
                            this.menuOptionsDropdown.filterString = '';
                        },
                        clickTarget: relativeElement,
                        popperShownCallback: () => {
                            if (this.menuOptionsDropdown != null) {
                                this.menuOptionsDropdown.setFocus();
                            }
                        }
                    });
                },
                blur: (event) => {
                    this.focusLost(event);
                }
            },
        }, createElement('div', {
            attrs: {
                class: 'neon-select-field-selected-content',
            },
        }, createElementIf(() => { var _a; return ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.imageSrc) != null && this.imageFailedToLoad !== true; }, 'img', {
            attrs: {
                class: 'neon-select-field-selected-image',
                src: dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.imageSrc;
                }),
            },
            listeners: {
                error: () => {
                    this.imageFailedToLoad = true;
                    updateConditionalElements(this);
                    // this.redrawInput();
                }
            }
        }), useElementIf(() => { var _a, _b; return ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagPosition) !== 'right' && ((_b = this.selectedOption) === null || _b === void 0 ? void 0 : _b.tagText) != null; }, () => { return this.renderTag(); }), createElementIf(() => { var _a; return ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconStackIcon) != null; }, 'neon-2_20_1-icon-stack', {
            attrs: {
                'data-icon': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconStackIcon;
                }),
                'data-color': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconStackColor;
                }),
                'data-size': 'x-small',
                'data-shape': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconStackShape;
                }),
            },
        }), createElementIf(() => {
            var _a, _b;
            return (((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.icon) != null ||
                ((_b = this.selectedOption) === null || _b === void 0 ? void 0 : _b.iconName) != null);
        }, 'neon-2_20_1-icon', {
            attrs: {
                'data-icon': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.icon;
                }),
                'data-color': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconColor;
                }),
                'data-color-weight': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconColorWeight;
                }),
                'data-size': 'medium',
            },
        }), createElementIf(() => {
            var _a;
            return (this.selectedOption != null &&
                (this.selectedOption.showAvatar === true ||
                    this.selectedOption.avatarFirstName != null ||
                    this.selectedOption.avatarLastName != null ||
                    ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.avatarProfilePictureSrc) != null));
        }, 'neon-2_20_1-avatar', {
            attrs: {
                class: 'neon-select-field-selected-avatar-info',
                'data-avatar-size': 'extra-small',
                'data-first-name': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.avatarFirstName;
                }),
                'data-last-name': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.avatarLastName;
                }),
                'data-profile-picture-src': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.avatarProfilePictureSrc;
                }),
                'data-background-option': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.avatarBackgroundOption;
                }),
            },
        }), createElementIf(() => {
            return this.modelValue == null;
        }, 'span', {
            attrs: {
                class: 'neon-placeholder-text neon-select-field-placeholder-text'
            },
            text: dynamicText(() => {
                if (this.getAttribute('data-placeholder-text')) {
                    return this.getAttribute('data-placeholder-text');
                }
                else {
                    return translate('neon|select-field|Select value');
                }
            }),
            extensions: tooltipOnEllipsis
        }), createElementIf(() => {
            var _a, _b;
            return (((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.text) != null ||
                ((_b = this.selectedOption) === null || _b === void 0 ? void 0 : _b.subtext) != null);
        }, 'div', {
            attrs: {
                class: 'neon-select-field-selected-text-container',
            },
            extensions: tooltipOnEllipsis
        }, () => {
            return [
                // TODO: these need to be conditional elements, the current code adds extra spans to the DOM
                createElement('span', {
                    attrs: {
                        class: dynamicAttr(() => {
                            var _a;
                            if ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.text) {
                                return 'neon-select-field-selected-header-text';
                            }
                            return 'neon-select-field-placeholder-text';
                        }),
                    },
                    text: dynamicText(() => {
                        var _a;
                        return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.text;
                    }),
                    extensions: tooltipOnEllipsis
                }),
                createElement('span', {
                    attrs: {
                        class: dynamicAttr(() => {
                            var _a;
                            if ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.subtext) {
                                return 'neon-select-field-selected-subheader-text';
                            }
                        })
                    },
                    extensions: tooltipOnEllipsis,
                    text: dynamicText(() => {
                        var _a;
                        return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.subtext;
                    }),
                }),
            ];
        }), createElementIf(() => {
            var _a;
            return ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconRight) != null;
        }, 'neon-2_20_1-icon', {
            attrs: {
                'data-icon': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconRight;
                }),
                'data-color': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconRightColor;
                }),
                'data-color-weight': dynamicAttr(() => {
                    var _a;
                    return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.iconRightColorWeight;
                }),
                'data-size': 'small',
            },
        })), useElementIf(() => { var _a, _b; return ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagPosition) === 'right' && ((_b = this.selectedOption) === null || _b === void 0 ? void 0 : _b.tagText) != null; }, () => { return this.renderTag('neon-select-field-end-item'); }), createElementIf(() => { return this.getAttribute('data-is-loading') === 'true'; }, 'neon-2_20_1-loading-indicator', {
            attrs: {
                class: 'neon-select-field-loading-indicator neon-select-field-end-item',
                'data-size': 'small',
                'data-size-mobile': 'medium'
            }
        }), createElement('neon-2_20_1-icon', {
            attrs: {
                class: 'neon-select-field-dropdown-icon neon-select-field-end-item',
                'data-icon': 'caret-down',
            },
        }));
    }
    setValue(value, skip) {
        this._value = value || '';
        if (skip !== true) {
            this.setModelValue(value, true);
        }
    }
    validateModelValueType(modelValue) {
        if (typeof modelValue === 'string' && modelValue.length > 0) {
            return modelValue;
        }
        return undefined;
    }
    setModelValue(modelValue, skip) {
        if ((this.modelValue == null || this.modelValue === '') && (modelValue == null || modelValue === '')) {
            return false;
        }
        //If something tries to set the modelValue to empty string, we will treat it as if they set it to undefined.
        //This can happen if a SelectOption.value === ''.
        if (modelValue === '') {
            modelValue = undefined;
        }
        this._modelValue = modelValue;
        this.selectedOption = modelValue != null ? this.findSelectedOption(this.currentOptions || [], modelValue) : undefined;
        this.imageFailedToLoad = false;
        this.updateSelectButtonStatus();
        if (skip !== true) {
            this.setValue(modelValue, true);
        }
        return true;
    }
    generateValidationRules() {
        this.validators = getRequiredOnlyValidators(this.getAttribute('data-is-required') === 'true');
        this.addCustomValidateToValidators();
    }
    getReadOnlyText() {
        var _a;
        return (_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.text;
    }
    getResetOptionText() {
        let resetOptionText = this.getAttribute('data-undefined-option-text') || translate('neon|select-field|None selected');
        // this is using en-dash characters for whitespace because spaces are not wide enough, em-dash looks too wide.
        resetOptionText = '— ' + resetOptionText + ' —';
        return resetOptionText;
    }
    findSelectedOption(selectOption, modelValue) {
        // Loop through each option
        for (const option of selectOption) {
            if (option === 'DIVIDER') {
                continue; //DIVIDER is not a SelectOption. Just continue through the loop looking for the select option.
            }
            // If the option is a match, return it
            if (option.value === modelValue) {
                return option;
            }
            // If the option is a group and has nested values (inside value property), search within those values
            if (Array.isArray(option.value)) {
                const nestedMatch = this.findSelectedOption(option.value, modelValue);
                if (nestedMatch) {
                    return nestedMatch;
                }
            }
            // If the option has children (nested SelectOptions), search within those
            if (option.children != null) {
                const nestedMatch = this.findSelectedOption(option.children, modelValue);
                if (nestedMatch) {
                    return nestedMatch;
                }
            }
        }
    }
    updateSelectButtonStatus() {
        const status = [
            'aria-label',
            'data-icon',
            'data-color-weight',
            'data-color',
            'src',
            // 'data-icon-color',
            // 'data-icon-color-weight',
            // 'data-icon-right',
            // 'data-icon-right-color',
            // 'data-icon-right-color-weight',
            'data-avatar-size',
            'data-background-option',
            'data-first-name',
            'data-last-name',
            'data-profile-picture-src',
            'data-text',
            'aria-disabled',
        ];
        updateConditionalElements(this);
        updateDynamicText(this);
        for (const state of status) {
            updateDynamicAttribute(this, state);
        }
    }
    isSelectOptionGroup(option) {
        return option.value instanceof Array;
    }
    isShowUndefinedOption() {
        return (this.getAttribute('data-undefined-option') !== 'false' && this.getAttribute('data-undefined-option-text') != null) || this.getAttribute('data-undefined-option') === 'true';
    }
    getResetOption() {
        return {
            id: this.getAttribute('id') + '-reset-option',
            text: this.getResetOptionText(),
            subtext: this.getAttribute('data-undefined-option-subtext') || undefined,
            value: '',
            controlType: 'reset',
            onClickCallback: this.handleMenuOptionClick.bind(this),
            isSelected: () => {
                return this.modelValue == null || this.modelValue === '';
            }
        };
    }
    convertOptionsToSelectMenuOptions(options) {
        if (options == null) {
            return [];
        }
        const selectMenuOptions = [];
        for (const [index, option] of options.entries()) {
            if (option === 'DIVIDER') {
                selectMenuOptions.push('DIVIDER');
            }
            else if (this.isSelectOptionGroup(option)) {
                //It's a select option group
                selectMenuOptions.push(...this.createSelectMenuOptions(option, index.toString()));
            }
            else {
                //It's just a normal select option
                selectMenuOptions.push(this.convertToSelectMenuOption(option, index.toString()));
            }
        }
        return selectMenuOptions;
        //We can't use this.options.map because it requires a 1-to-1 conversion and the selectOptionGroup is going to be returning more than 1 options.
    }
    createSelectMenuOptions(option, index) {
        return [
            { id: index, text: option.text, controlType: 'heading' },
            ...this.convertOptionsToSelectMenuOptions(option.value).map((selectMenuOption) => {
                if (selectMenuOption === 'DIVIDER') {
                    return 'DIVIDER';
                }
                selectMenuOption.isNested = true;
                return selectMenuOption;
            })
        ];
    }
    renderPopperMenuElement(menuOptionsList) {
        return createElement('neon-2_20_1-item-picker', {
            attrs: {
                id: dynamicAttr(() => { return `${this.getAttribute('id')}-popper`; }),
                'data-item-tooltip-placement': (this.getAttribute('data-button-tooltip-placement') || 'top'),
                'data-constrain-width': 'false',
                'data-show-header-text': 'false',
                'data-popper-max-height': dynamicAttr(() => { return this.getAttribute('data-popper-max-height') || undefined; }),
                'data-use-arrow': 'false',
                'data-show-filter': dynamicAttr(() => { return this.getAttribute('data-show-filter'); }),
                'data-control-type': 'popper',
                'data-neon-prevent-ancestors-closing': ''
            },
            properties: {
                'menuOptions': dynamicProperty(() => {
                    let menuOptions = menuOptionsList.length > 0 ? menuOptionsList : null;
                    if (menuOptions != null && this.isShowUndefinedOption()) {
                        menuOptions = [this.getResetOption(), ...menuOptions];
                    }
                    return menuOptions;
                })
            }
        });
    }
    convertToSelectMenuOption(option, id) {
        return {
            id: id + '__' + getIdSafeString(option.value) || '',
            ariaLabel: option.ariaLabel,
            text: option.text,
            textRight: option.textRight,
            value: option.value,
            body: option.body,
            subtext: option.subtext,
            controlType: Array.isArray(option.value) ? 'none' : 'button',
            onClickCallback: this.handleMenuOptionClick.bind(this),
            tooltipText: option.tooltipText,
            isDisabled: () => {
                return option.disabled || false;
            },
            disabledTooltipText: option.disabledTooltipText,
            isNested: option.isNested || false,
            isSelected: (menuOption) => {
                let isSelected = false;
                if (this.modelValue == null) {
                    //If there is no modelValue, then nothing is selected.
                    return false;
                }
                traverseTreeGeneric([menuOption], (mo) => {
                    if (this.modelValue === mo.value) {
                        isSelected = true;
                        return true;
                    }
                    return false;
                }, (mo) => { return mo.menuOptionChildren; });
                return isSelected;
            },
            icon: option.icon,
            //@deprecated: Provides backwards compatibility for the MenuItems.  Use icon instead.
            iconName: option.icon,
            iconColor: option.iconColor,
            iconColorWeight: option.iconColorWeight || '500',
            iconRight: option.iconRight,
            iconRightColor: option.iconRightColor,
            iconRightColorWeight: option.iconRightColorWeight,
            imageSrc: option.imageSrc,
            iconStackColor: option.iconStackColor,
            iconStackSize: option.iconStackSize,
            iconStackShape: option.iconStackShape,
            iconStackIcon: option.iconStackIcon,
            showAvatar: option.showAvatar,
            avatarFirstName: option.avatarFirstName,
            avatarLastName: option.avatarLastName,
            avatarProfilePictureSrc: option.avatarProfilePictureSrc,
            avatarBackgroundOption: option.avatarBackgroundOption,
            tagText: option.tagText,
            tagSubtext: option.tagSubtext,
            tagType: option.tagType,
            tagPosition: option.tagPosition,
            illustrationSimple: option.illustrationSimple,
            illustrationSimpleSize: option.illustrationSimpleSize,
            menuOptionChildren: (option.children) ? this.convertOptionsToSelectMenuOptions(option.children) : undefined
        };
    }
    getAriaLabelForInput() {
        var _a, _b, _c, _d, _e, _f, _g, _h;
        let textReader = '';
        textReader += this.getAttribute('data-is-loading') ? translate('neon|select-field|Content is loading') : '';
        textReader += ((_a = this.selectedOption) === null || _a === void 0 ? void 0 : _a.tagText) ? (_b = this.selectedOption) === null || _b === void 0 ? void 0 : _b.tagText : '';
        textReader += ((_c = this.selectedOption) === null || _c === void 0 ? void 0 : _c.subtext) ? (_d = this.selectedOption) === null || _d === void 0 ? void 0 : _d.subtext : '';
        textReader += ((_e = this.selectedOption) === null || _e === void 0 ? void 0 : _e.text) ? (_f = this.selectedOption) === null || _f === void 0 ? void 0 : _f.text : '';
        textReader += ((_g = this.selectedOption) === null || _g === void 0 ? void 0 : _g.subtext) ? (_h = this.selectedOption) === null || _h === void 0 ? void 0 : _h.subtext : '';
        return (this.getAttribute('data-aria-label') && this.getAttribute('data-show-label') === 'false')
            ? this.getAttribute('data-label-text') + 'combo box'
            : (this.selectedOption && textReader) ? 'combo box selected ' + textReader : 'combo box';
    }
    attributeChanged(attribute, oldValue, newValue) {
        super.attributeChanged(attribute, oldValue, newValue);
        switch (attribute) {
            case 'data-placeholder-text':
                updateDynamicAttribute(this, 'class');
                updateDynamicText(this);
                break;
            case 'data-show-filter':
                if (this.menuOptionsDropdown != null) {
                    updateDynamicAttribute(this.menuOptionsDropdown, 'data-show-filter');
                }
                break;
            case 'data-is-loading':
                updateConditionalElements(this);
                updateDynamicAttribute(this, 'aria-disabled');
                break;
            case 'data-popper-max-height':
                if (this.menuOptionsDropdown != null) {
                    updateDynamicAttributeNoChildren(this.menuOptionsDropdown, 'data-popper-max-height');
                }
                break;
            case 'data-undefined-option':
            case 'data-undefined-option-text':
            case 'data-undefined-option-subtext':
                if (this.menuOptionsDropdown != null) {
                    updateDynamicPropertyNoChildren(this.menuOptionsDropdown, 'menuOptions');
                }
                break;
        }
    }
    connectedCallback() {
        //The observer needs to be turned on when render() gets called.
        //That's why it's above super.connectedCallback()
        this.tooltipOnEllipsisObserver.connect();
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
    }
    disconnectedCallback() {
        this.tooltipOnEllipsisObserver.disconnect();
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
    }
    handleMenuOptionClick(menuOption, context, event) {
        this.value = menuOption.value || '';
        this.focusLost(null);
        this.inputChange();
        updateDynamicText(this);
    }
}
