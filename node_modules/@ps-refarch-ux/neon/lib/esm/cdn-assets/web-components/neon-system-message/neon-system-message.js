import './neon-system-message.scss';
import { dynamicText, createElement, updateDynamicText, updateDynamicAttribute, createElementIf, dynamicAttr, updateConditionalElements, } from '../shared/dom-manipulation';
import { addTranslationListener, translate } from '@ps-refarch-ux/mfe-utils';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
import { addToEscapeKeyStack, isTopOfEscapeKeyStack, removeFromEscapeKeyStack } from '../shared/escape-key-stack';
import { bindCallback, getCallback } from '../shared/neon-widget';
export class NeonSystemMessage extends NeonWidgetSlotted {
    constructor() {
        super(...arguments);
        this.widgetTag = 'neon-system-message';
        this.isIdRequired = true;
    }
    static get observedAttributes() {
        return [
            'id',
            'data-position',
            'data-type',
            'data-is-dismissable',
            'data-title',
        ];
    }
    //when the attributes change this will run
    attributeChanged(attrName, oldVal, newVal) {
        super.attributeChanged(attrName, oldVal, newVal);
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                break;
            case 'data-type':
                updateDynamicAttribute(this, 'data-icon');
                break;
            case 'data-is-dismissable':
                updateConditionalElements(this);
                break;
            case 'data-title':
                updateConditionalElements(this);
                updateDynamicText(this);
                break;
            default:
        }
    }
    onKeyDown(event) {
        var _a;
        event === null || event === void 0 ? void 0 : event.stopPropagation();
        if (((_a = event.key) === null || _a === void 0 ? void 0 : _a.toLowerCase()) === 'escape' && isTopOfEscapeKeyStack(this)) {
            this.dismissSystemMessage();
        }
    }
    dismissSystemMessage() {
        this.dispatchEvent(new CustomEvent('neonSystemMessageClose', { bubbles: true, detail: { 'id': this.id } }));
        removeFromEscapeKeyStack(this);
    }
    render() {
        return [
            createElement('neon-2_20_1-icon', {
                attrs: {
                    class: 'neon-system-message-alert-icon',
                    'data-icon': dynamicAttr(() => {
                        const type = this.getAttribute('data-type');
                        switch (type) {
                            case 'success':
                                return 'status-check';
                            case 'error':
                                return 'status-error';
                            case 'warning':
                                return 'status-warning';
                            default:
                                return 'status-information';
                        }
                    }),
                    'data-size': 'medium',
                },
            }),
            createElement('div', { attrs: { class: 'neon-system-message-container' } }, createElementIf(() => {
                return this.getAttribute('data-title') != null;
            }, 'h2', {
                attrs: { class: 'neon-system-message-title' },
                text: dynamicText(() => {
                    return this.getAttribute('data-title');
                }),
            }), this.getSlot('message-content')),
            createElementIf(() => {
                return this.getAttribute('data-is-dismissable') === 'true';
            }, 'button', {
                attrs: {
                    id: dynamicAttr(() => {
                        return this.getAttribute('id') + '-system-message-button';
                    }),
                    class: 'neon-system-message-button',
                    'aria-label': translate('neon|system-message|Close Message'),
                },
                listeners: {
                    click: (e) => {
                        this.dismissSystemMessage();
                    },
                },
            }, () => {
                return createElement('neon-2_20_1-icon', {
                    attrs: {
                        class: 'neon-system-message-alert-close',
                        'data-icon': 'circle-close-outline|rtl-flip',
                        'data-size': 'medium',
                    },
                });
            }),
        ];
    }
    connectedCallback() {
        super.connectedCallback();
        this.i18nUnsubscribe = addTranslationListener(() => {
            updateDynamicText(this);
        });
        addToEscapeKeyStack(this);
        window.addEventListener('keydown', bindCallback(this.onKeyDown, this));
    }
    disconnectedCallback() {
        if (this.i18nUnsubscribe != null) {
            this.i18nUnsubscribe();
            this.i18nUnsubscribe = undefined;
        }
        removeFromEscapeKeyStack(this);
        window.removeEventListener('keydown', getCallback(this.onKeyDown, this));
    }
}
