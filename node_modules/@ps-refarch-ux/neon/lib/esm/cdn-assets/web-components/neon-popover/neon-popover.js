// Scott and Brian Put this together for the new Target APIs for popover and menu.
// <popover data-show-header-text="false">
// 	<div data-slot=popper-trigger>
// 		<neon-button>Custom Button</neon-button>
// 		<tooltip>
// 	</div>
// 	<h2 data-slot="popper-header">Hi Scott</h2>
// 	<div data-slot="popper-body">popover content</div>
// </popover>
// //We want to go with a slot on this one too because we have more flexibility in the future
// //If it uses a slot.
// <menu [options]="options" data-header-text="This is the menu header" data-show-header-text="false">
// 	<div data-slot=popper-trigger>
// 		<neon-button>Custom Button</neon-button>
// 		<tooltip>
// 	</div>
// </menu>
import './neon-popover.scss';
import { createElement, dynamicAttr, updateDynamicAttribute, getClosest, createElementIf, updateConditionalElements, updateDynamicAttributeNoChildren, updateDynamicAttributeOnChildren } from '../shared/dom-manipulation';
import { showPopper } from '../shared/poppers';
import { LabeledWidget } from '../shared/labeled-widget';
//This can still be overridden with the options property
const DEFAULT_POPPER_OFFSET = 12;
//If we ever decide we want to let things other than buttons
//show poppers, then this selector needs to be updated to include those
//additional elements as well.
export const POPOVER_CLICK_TARGET_SELECTOR = 'button';
export class NeonPopover extends LabeledWidget {
    static get observedAttributes() {
        const attributes = super.observedAttributes.concat([
            'id',
            'data-show-header-text',
            'data-injection-spacing',
            'data-constrain-popper',
            'data-popper-class',
            'data-close-on-popper-click',
            'data-restrict-popper-width',
            'data-aria-label'
        ]);
        return attributes;
    }
    static get requiredSlots() {
        return [
            { slot: 'popper-trigger', tag: 'div' },
            { slot: 'popper-header', tag: 'h2' },
            { slot: 'popper-body', tag: 'div' }
        ];
    }
    constructor() {
        super();
        this.widgetTag = 'neon-popover';
        this.isUsesLabel = false;
        this.isRequiresLabelText = false;
    }
    validateInputs() {
        var _a;
        super.validateInputs();
        const popperTriggerFromSlot = (_a = this.mainPopperButtonContainer) === null || _a === void 0 ? void 0 : _a.getRenderedNode();
        if (popperTriggerFromSlot.querySelectorAll('div > button, div > neon-2_20_1-button, div > neon-2_20_1-avatar-button, div > neon-2_20_1-list-item, div > neon-2_20_1-floating-action-button').length !== 1) {
            console.error('The popper-trigger slot must have exactly 1 button, neon-2_20_1-button, neon-2_20_1-list-item or neon-2_20_1-avatar-button as a direct child.');
        }
    }
    attributeChanged(attrName, oldVal, newVal) {
        // attributeChanged is called when attributes listed in "static get observedAttributes()" are changed.
        super.attributeChanged(attrName, oldVal, newVal);
        switch (attrName) {
            case 'id':
                updateDynamicAttribute(this, 'id');
                break;
            case 'data-show-header-text':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-injection-spacing':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-popper-class':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-constrain-popper':
                updateDynamicAttribute(this, 'class');
                break;
            case 'data-show-popper-arrow':
                if (this.popper != null) {
                    updateConditionalElements(this.popper);
                }
                break;
            case 'data-close-on-popper-click':
                if (this.popper != null) {
                    updateDynamicAttribute(this.popper, 'data-neon-prevent-closing');
                    updateDynamicAttributeNoChildren(this.popper, 'data-neon-prevent-closing');
                }
                updateDynamicAttribute(this, 'data-neon-prevent-closing');
                break;
            case 'data-aria-label': {
                updateDynamicAttributeOnChildren(this, 'aria-label');
                updateDynamicAttributeOnChildren(this, 'aria-labeledby');
                break;
            }
            default:
                break;
        }
    }
    getPopperElementArticleClasses() {
        let classes = 'neon-2_20_1-popper-content';
        const popperOverrideClass = this.getAttribute('data-popper-class');
        if (popperOverrideClass != null) {
            classes += ' ' + popperOverrideClass;
        }
        if (this.getAttribute('data-constrain-popper') === 'false') {
            classes += ' neon-popper-override-constraints';
        }
        if (this.getAttribute('data-show-header-text') === 'false') {
            classes += ' neon-sr-only-header';
        }
        return classes;
    }
    renderPopperElement() {
        return createElement('div', { attrs: {
                class: 'neon-2_20_1-popover-container'
            } }, createElement('article', {
            attrs: {
                id: this.id + '-neon-popper',
                class: dynamicAttr(() => {
                    return this.getPopperElementArticleClasses();
                }),
                'data-neon-prevent-closing': dynamicAttr(() => {
                    return this.getAttribute('data-close-on-popper-click') === 'false' ? '' : undefined;
                }),
                'aria-modal': 'true',
                'role': 'dialog',
                'aria-live': 'assertive',
                'tabindex': '0',
                'aria-label': dynamicAttr(() => { return this.getAttribute('data-aria-label'); }),
                'aria-labeledby': dynamicAttr(() => { return this.getAttribute('data-aria-label') == null ? this.id + '-neon-popper-header' : undefined; })
            },
            listeners: {
                click: (event) => {
                    // Always give them the reference so they can close the popper manually if they need to
                    event.neonPopperController = this;
                }
            }
        }, createElement('header', {
            attrs: {
                class: 'popper-header',
                id: this.id + '-neon-popper-header'
            }
        }, this.getSlot('popper-header')), createElement('div', {
            attrs: {
                class: 'neon-2_20_1-layout-scrollable'
            }
        }, createElement('div', {
            attrs: {
                class: dynamicAttr(() => {
                    return this.getAttribute('data-injection-spacing') !== 'false' ? ' neon-2_20_1-popover-popper-body' : '';
                })
            }
        }, this.getSlot('popper-body')))), createElementIf(() => { return this.isArrowShowing(); }, 'div', {
            attrs: { class: 'neon-popper-arrow' }
        }));
    }
    isArrowShowing() {
        return this.getAttribute('data-show-popper-arrow') !== 'false';
    }
    mainButtonClickListener(event) {
        // The popper is not showing, so let's render it and show it
        this.popper = this.renderPopperElement();
        const triggerElement = getClosest(event.target, POPOVER_CLICK_TARGET_SELECTOR);
        let placement = null;
        placement = this.getAttribute('data-popper-placement');
        if (triggerElement != null) {
            if (typeof this.removePopper === 'function') {
                this.removePopper();
            }
            else {
                this.removePopper = showPopper(triggerElement, this.popper, {
                    'placement': (placement ? placement : 'bottom-start'),
                    offset: DEFAULT_POPPER_OFFSET,
                    makePopperSameWidthAs: this.getAttribute('data-restrict-popper-width') === 'true' ? 'relative-element' : undefined,
                    clickTarget: triggerElement,
                    trapFocus: true,
                    popperPostClosedCallback: () => {
                        delete this.popper;
                        delete this.removePopper;
                    },
                });
            }
        }
    }
    renderMainButtonContainer() {
        return createElementIf(() => { return this.slotExists('popper-trigger'); }, 'div', {
            attrs: {
                id: dynamicAttr(() => {
                    return 'neon-popper-button-' + this.getAttribute('id');
                }),
                class: 'neon-popper-main-button neon-input-field-first'
            },
            listeners: {
                click: (event) => {
                    this.mainButtonClickListener(event);
                }
            }
        }, () => {
            this.mainPopperButtonContainer = this.getSlot('popper-trigger');
            if (!this.markClickTarget()) {
                setTimeout(() => {
                    if (!this.markClickTarget()) {
                        console.error('Something went wrong with the neon popover marking the click target.');
                    }
                }, 500);
            }
            return this.mainPopperButtonContainer;
        });
    }
    getClickTarget(element) {
        if (element == null) {
            return;
        }
        if (element.matches(POPOVER_CLICK_TARGET_SELECTOR)) {
            return element;
        }
        else {
            return element.querySelector(POPOVER_CLICK_TARGET_SELECTOR) || undefined;
        }
    }
    markClickTarget() {
        var _a;
        const clickTarget = this.getClickTarget((_a = this.mainPopperButtonContainer) === null || _a === void 0 ? void 0 : _a.getRenderedNode());
        if (clickTarget != null) {
            //When set, this prevents the popper the button is in from closing when it's clicked
            clickTarget.setAttribute('data-neon-prevent-closing', '');
            return true;
        }
        return false;
    }
    renderContent() {
        this.mainPopperButtonContainer = this.renderMainButtonContainer();
        return [
            this.mainPopperButtonContainer
        ];
    }
    getBadgeText() {
        //we aren't using a badge on this one so this is essentially a no-op.
        return '';
    }
    isShowBadge() {
        //Not using the badge, always return false.
        return false;
    }
}
