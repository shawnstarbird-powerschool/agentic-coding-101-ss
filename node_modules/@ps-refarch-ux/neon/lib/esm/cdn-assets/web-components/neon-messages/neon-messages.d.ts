import './neon-messages.scss';
import { NeonWidgetSlotted } from '../shared/neon-widget-slotted';
export declare class NeonMessages extends NeonWidgetSlotted {
    widgetTag: string;
    i18nUnsubscribe?: () => void;
    static get observedAttributes(): Array<string>;
    private generateUniqueSystemId;
    private generateUniqueToastId;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    render(): null;
    private addEventListeners;
    private removeEventListeners;
    private renderComplexSystemMessage;
    private getPriority;
    private insertMessage;
    private renderNeonSystemMessage;
    private autoDismissSystemMessageRequired;
    private neonSystemMessagehasOpened;
    private neonSystemMessageHasClosed;
    private neonToastMessageHasClosed;
    private renderComplexToastMessage;
    private renderNeonToastMessage;
    private neonToastMessageHasOpened;
    /**
   * Converts a given input into a string representation.
   * If the input is a string, it returns the string as is.
   * If the input is an array, it concatenates its elements into a single string.
   * Handles both strings and objects within the array.
   *
   * @param message - A string or an array containing strings and/or NeonSystemMessageDetail objects.
   * @returns The string representation of the input message.
   */
    private convertToString;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
