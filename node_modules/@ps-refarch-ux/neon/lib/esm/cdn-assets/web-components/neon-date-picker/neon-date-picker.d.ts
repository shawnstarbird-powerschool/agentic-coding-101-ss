import { type CalendarDay } from '../../../interfaces/calendar-day';
import { NeonWidget } from '../shared/neon-widget';
import './neon-date-picker.scss';
import { type DateRange } from '../../../interfaces';
import { type ValidationFunction, type Validator } from '../../../interfaces/validation-engine';
export declare class NeonDatePicker extends NeonWidget {
    widgetTag: string;
    selectedYear?: number;
    selectedMonth?: number;
    yearSelect: HTMLSelectElement;
    monthSelect: HTMLSelectElement;
    calendarDays?: Array<Array<CalendarDay>>;
    private _singleModeValue;
    private _rangeModeValue;
    private _multiModeValue;
    private externalValidators?;
    private internalValidators?;
    private customValidate?;
    today: Date;
    set validators(value: Array<Validator> | undefined);
    get validators(): Array<Validator> | undefined;
    set customValidationFunction(value: ValidationFunction | undefined);
    get customValidationFunction(): ValidationFunction | undefined;
    static get observedAttributes(): Array<string>;
    constructor();
    get singleModeValue(): Date | undefined;
    set singleModeValue(value: Date | undefined);
    get rangeModeValue(): DateRange | undefined;
    set rangeModeValue(value: DateRange | undefined);
    get multiModeValue(): Array<Date>;
    set multiModeValue(value: Array<Date>);
    getNumberOfMonthsInRange(): number | undefined;
    render(): HTMLElement | undefined;
    incrementMonth(): void;
    decrementMonth(): void;
    redraw(): void;
    redrawMonthsInSelect(): void;
    redrawYearsInSelect(): void;
    renderYearsInSelect(newCurrentYear: number): Array<HTMLElement>;
    /**
   * This method is used to select the year and month that is within the allowed range.
   * If the selected year or month is outside the allowed range, it updates them to the closest allowed value.
   */
    updateSelectedDate(): void;
    redrawTable(): void;
    /**
     * Generates CSS classes for a calendar day button based on its properties and selected date modes.
     * @param {CalendarDay} day - The calendar day for which to generate CSS classes.
     * @returns {string} - A string containing CSS classes for the calendar day button.
     */
    getButtonClasses(day: CalendarDay): string;
    getMinAllowedDate(): Date | undefined;
    getMaxAllowedDate(): Date | undefined;
    getCalendarCellClass(day: CalendarDay): string;
    isDayInRange(day: CalendarDay): boolean;
    isFirstDayInRange(day: CalendarDay): boolean;
    isLastDayInRange(day: CalendarDay): boolean;
    /**
     * Sets the date range based on a given date and optional event source.
     * @param {Date} date - The date to set in the date range.
     * @param {string} eventSource - An optional parameter indicating the source of the event triggering the date change.
     */
    setDateRange(date: Date, eventSource?: string): void;
    renderTable(): HTMLElement | undefined;
    /**
     * Validating each of the days is a relatively expensive operation so we break it out into its own
     * method that can store the results so they only need to be done once per change.
     */
    validateCalendarDays(): void;
    getMode(): 'single' | 'multi' | 'range';
    getCalendarButtonId(day: CalendarDay): string;
    generateValidationRules(): void;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
}
