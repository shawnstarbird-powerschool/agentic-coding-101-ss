import './neon-chip-set.scss';
import { NeonWidget } from '../shared/neon-widget';
import type { Chip } from '../../../interfaces/chip';
import type { ConditionalElement } from '../shared/conditional-element';
import { type NeonItemPicker } from '../neon-item-picker/neon-item-picker';
export declare class NeonChipSet extends NeonWidget {
    widgetTag: string;
    isIdRequired: boolean;
    allChips: Array<Chip>;
    visibleChips: Array<Chip>;
    overflowChips: Array<Chip>;
    removePopper?: (clickEvent?: Event) => void;
    resizeObserver: ResizeObserver;
    isDrawerOrDialogShowing: boolean;
    drawerOrDialogElement?: HTMLElement;
    itemPicker?: NeonItemPicker;
    clearButton: HTMLElement;
    i18nUnsubscribe?: () => void;
    constructor();
    handleChipRemoveEvent(event: Event): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    isOverflowMenuDisabled(): boolean;
    static get observedAttributes(): Array<string>;
    attributeChanged(attrName: string, oldVal: string, newVal: string): void;
    set chips(chips: Array<Chip>);
    get chips(): Array<Chip>;
    clearAllChips(): void;
    distributeChips(): void;
    getChipSetWebComponentWidth(): number;
    getChipSetWrapperWidth(): number;
    getAvailableWidth(): number;
    getChipWidth(chip: Chip): number;
    getClearAllButtonWidth(): number;
    getOverflowButtonWidth(overflowSize?: number): number;
    getOverflowMenuId(): string;
    moveChipToOverflow(chip: Chip): void;
    moveOverflowToChip(chip: Chip): void;
    isShowClearAll(): boolean;
    isClearAllThresholdMet(): boolean;
    removeChipFromDom(id: string): void;
    removeChip(chipId: string, isRemoveAllOperation?: boolean): void;
    renderChip(chip: Chip): HTMLElement;
    /**
    * Determines if a chip or chip set is in read-only mode based on various conditions.
    * @param chip The chip object to check for read-only state.
    * @returns True if the chip or chip set is in read-only mode, otherwise false.
    */
    private isChipOrChipSetReadOnly;
    /**
    * Retrieves the size of a chip or chip set, considering various conditions.
    * @param chip The chip object to determine its size.
    * @returns The size of the chip or chip set ('small', 'medium'), or undefined if not applicable.
    */
    private getChipOrChipSetSize;
    /**
    * Determines if a chip or chip set is on gray based on various conditions.
    * @param chip The chip object to check for being on gray.
    * @returns True if the chip or chip set is on gray, otherwise false.
    */
    private isChipOrChipSetOnGray;
    /**
    * Determines if a chip is disabled based on various conditions.
    * @param chip The chip object to check for disabled state.
    * @returns True if the chip is disabled, otherwise false.
    */
    private isChipOrChipSetDisabled;
    renderInitialChips(): Array<HTMLElement>;
    renderItemPicker(controlType: 'popper' | 'drawer' | 'dialog'): HTMLElement;
    showChipOverflowPopper(): void;
    renderOverflowChip(overflowSize?: number): HTMLElement;
    isModalThresholdMet(): boolean;
    showOverflowDrawerOrDialog(): void;
    closeDrawerOrDialog(): void;
    renderDialog(): HTMLElement;
    renderDrawer(): HTMLElement;
    renderDrawerOrDialog(): HTMLElement;
    render(): Array<ConditionalElement>;
}
