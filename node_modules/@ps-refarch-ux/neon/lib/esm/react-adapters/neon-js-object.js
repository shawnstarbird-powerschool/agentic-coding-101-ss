/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, 
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const jsObjectDependencies = [
    createComponentDependency('js-object'),
    ...iconDependencies
];
function JsObject(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        value: props.value
    });
    // * * * Set Listeners * * *
    // useListeners<NeonJsObjectElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-js-object', buildPropsMap(props, {
        ref: elementRef
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonJsObject = withNeonWebComponent('neon-2_20_1-js-object', JsObject, jsObjectDependencies);
