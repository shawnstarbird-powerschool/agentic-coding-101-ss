/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const progressCircleDependencies = [
    createComponentDependency('progress-circle'),
    ...iconDependencies
];
function ProgressCircle(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonProgressCircleElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonProgressCircleElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-progress-circle', buildPropsMap(props, {
        ref: elementRef,
        'data-size': props.dataSize,
        'data-high-contrast': toAttribute(props.dataHighContrast),
        'data-value': props.dataValue,
        'data-max-value': props.dataMaxValue,
        'data-percentage': props.dataPercentage,
        'data-text': props.dataText,
        'data-state': props.dataState
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonProgressCircle = withNeonWebComponent('neon-2_20_1-progress-circle', ProgressCircle, progressCircleDependencies);
