/* eslint-disable @typescript-eslint/no-namespace */
import { InstanceMap } from '../exported-utils/instance-map';
import { addNeonToolkit, getNeonToolkit, neonToolkitHas } from '../exported-utils/neon-toolkit';
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, useImperativeProps, withNeonWebComponent, } from './utils/react-utils';
import { tooltipDependencies } from './neon-tooltip';
import { Breadcrumbs } from '../exported-utils/models/breadcrumbs';
import { avatarDependencies } from './neon-avatar';
export const breadcrumbsDependencies = [
    createComponentDependency('breadcrumbs'),
    ...iconDependencies,
    ...tooltipDependencies,
    ...avatarDependencies
];
function BreadcrumbsElement(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        breadcrumbs: props.breadcrumbs,
    });
    // * * * Set Listeners * * *
    // useListeners<NeonBreadcrumbsElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-breadcrumbs', buildPropsMap(props, {
        ref: elementRef,
        'data-global-breadcrumbs': toAttribute(props.dataGlobalBreadcrumbs),
        'data-is-top-of-page': toAttribute(props.dataIsTopOfPage),
        'data-show-first-avatar-breadcrumb': toAttribute(props.dataShowFirstAvatarBreadcrumb)
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonBreadcrumbs = withNeonWebComponent('neon-2_20_1-breadcrumbs', BreadcrumbsElement, breadcrumbsDependencies);
export function getBreadcrumbs() {
    if (!neonToolkitHas('neon-breadcrumbs-unversioned')) {
        addNeonToolkit('neon-breadcrumbs-unversioned', new InstanceMap(() => {
            return new Breadcrumbs();
        }));
    }
    const breadcrumbsToolkit = getNeonToolkit('neon-breadcrumbs-unversioned');
    return Promise.resolve(breadcrumbsToolkit.get('default-breadcrumbs'));
}
