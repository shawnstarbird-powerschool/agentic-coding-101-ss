/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useListeners, withNeonWebComponent } from './utils/react-utils';
export const splashScreenDependencies = [
    createComponentDependency('splash-screen'),
    ...iconDependencies
];
function SplashScreen(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonSplashScreenElement>(elementRef, {
    //   splashScreenRemovedCallback: props.splashScreenRemovedCallback
    // });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonSplashScreenRemoved: function (event) {
            if (props.neonSplashScreenRemoved != null) {
                props.neonSplashScreenRemoved();
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-splash-screen', buildPropsMap(props, {
        ref: elementRef,
    }), props.children);
}
export const NeonSplashScreen = withNeonWebComponent('neon-2_20_1-splash-screen', SplashScreen, splashScreenDependencies);
