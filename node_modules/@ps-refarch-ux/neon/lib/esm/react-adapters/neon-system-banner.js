/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const systemBannerDependencies = [
    createComponentDependency('system-banner'),
    ...iconDependencies
];
function SystemBanner(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonSystemBannerElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonSystemBannerElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-system-banner', buildPropsMap(props, {
        ref: elementRef,
        'data-icon': props.dataIcon,
        'data-text': props.dataText
        //TODO: Add in your attribute mapping here
    }), props.children);
}
export const NeonSystemBanner = withNeonWebComponent('neon-2_20_1-system-banner', SystemBanner, systemBannerDependencies);
