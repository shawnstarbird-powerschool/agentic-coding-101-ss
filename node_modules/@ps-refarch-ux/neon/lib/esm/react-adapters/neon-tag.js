/* eslint-disable @typescript-eslint/no-namespace */
import React from 'react';
import { buildPropsMap, withNeonWebComponent } from './utils/react-utils';
import { iconDependencies } from './neon-icon';
import { createComponentDependency } from './utils/react-utils';
export const tagDependencies = [
    createComponentDependency('tag'),
    ...iconDependencies
];
const Tag = (props) => {
    return React.createElement('neon-2_20_1-tag', buildPropsMap(props, {
        'data-text': props.dataText,
        'data-subtext': props.dataSubText,
        'data-icon': props.dataIcon,
        'data-type': props.dataType,
        'data-position': props.dataPosition
    }));
};
export const NeonTag = withNeonWebComponent('neon-2_20_1-tag', Tag, tagDependencies);
