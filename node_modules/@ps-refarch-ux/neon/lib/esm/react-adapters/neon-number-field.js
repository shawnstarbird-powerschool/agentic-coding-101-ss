import React, { useRef } from 'react';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
import { abstractInputFieldDependencies } from './abstract-input-field';
import { buildPropsMap, createComponentDependency, toAttribute } from './utils/react-utils';
import { useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
export const numberFieldDependencies = [
    createComponentDependency('number-field'),
    ...abstractInputFieldDependencies,
];
const NumberField = (props) => {
    const componentRef = useRef();
    useImperativeProps(componentRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    useListeners(componentRef, {
        'modelValueChange': (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    return React.createElement('neon-2_20_1-number-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: componentRef,
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'disabled': toAttribute(props.disabled),
        'autocomplete': props.autocomplete,
        'data-placeholder-text': props.dataPlaceholderText,
        'data-is-integer': toAttribute(props.dataIsInteger),
        'data-min-length': props.dataMinLength,
        'data-max-length': props.dataMaxLength,
        'data-min-value': props.dataMinValue,
        'data-max-value': props.dataMaxValue,
        'data-min-value-exclusive': props.dataMinValueExclusive,
        'data-max-value-exclusive': props.dataMaxValueExclusive,
        'data-max-decimals': props.dataMaxDecimals,
    })));
};
export const NeonNumberField = withNeonWebComponent('neon-2_20_1-number-field', NumberField, numberFieldDependencies);
