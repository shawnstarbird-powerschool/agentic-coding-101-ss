/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { createComponentDependency, buildPropsMap, withNeonWebComponent } from './utils/react-utils';
import { avatarDependencies } from './neon-avatar';
export const avatarButtonDependencies = [
    createComponentDependency('avatar-button'),
    ...avatarDependencies
];
function AvatarButton(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonAccordionElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonAccordionElement>(elementRef, {
    //   //eventName: calback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-avatar-button', buildPropsMap(props, {
        ref: elementRef,
        'data-aria-label': props.dataAriaLabel,
        onClick: props.onClick
    }), props.children);
}
export const NeonAvatarButton = withNeonWebComponent('neon-2_20_1-avatar-button', AvatarButton, avatarButtonDependencies);
