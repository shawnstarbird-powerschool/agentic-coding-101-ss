/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
export const timePickerDependencies = [
    createComponentDependency('time-picker'),
    ...iconDependencies
];
function TimePicker(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        value: props.value
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonValueChange: function (event) {
            if (props.neonValueChange != null) {
                props.neonValueChange(event.target.value);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-time-picker', buildPropsMap(props, {
        ref: elementRef,
        'data-show-time-display': props.dataShowTimeDisplay
    }));
}
export const NeonTimePicker = withNeonWebComponent('neon-2_20_1-time-picker', TimePicker, timePickerDependencies);
