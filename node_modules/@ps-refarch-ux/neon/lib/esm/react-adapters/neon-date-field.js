import React, { useRef } from 'react';
import { withNeonWebComponent, useImperativeProps, useListeners, buildPropsMap, toAttribute } from './utils/react-utils';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
import { labeledWidgetDependencies } from './neon-labeled-widget';
import { iconDependencies } from './neon-icon';
import { buttonDependencies } from './neon-button';
import { datePickerDependencies } from './neon-date-picker';
import { createComponentDependency } from './utils/react-utils';
export const dateFieldDependencies = [
    createComponentDependency('date-field'),
    ...datePickerDependencies,
    ...iconDependencies,
    ...buttonDependencies,
    ...labeledWidgetDependencies
];
function DateField(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-date-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: elementRef,
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'disabled': toAttribute(props.disabled),
        'autocomplete': props.autocomplete,
        'data-placeholder-text': props.dataPlaceholderText,
        'data-max-value': props.dataMaxValue,
        'data-min-value': props.dataMinValue,
        'data-weekdays-only': props.dataWeekdaysOnly,
        'data-popper-placement': props.dataPopperPlacement
    })));
}
export const NeonDateField = withNeonWebComponent('neon-2_20_1-date-field', DateField, dateFieldDependencies);
