/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useListeners, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { iconStackDependencies } from './neon-icon-stack';
import { avatarDependencies } from './neon-avatar';
import { tagDependencies } from './neon-tag';
export const cardHeaderDependencies = [
    createComponentDependency('card-header'),
    ...iconStackDependencies,
    ...avatarDependencies,
    ...tagDependencies,
    ...iconDependencies
];
function CardHeader(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonCardHeaderElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonCardHeaderHeadingClick: (e) => {
            if (typeof props.neonCardHeaderHeadingClick === 'function') {
                props.neonCardHeaderHeadingClick(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-card-header', buildPropsMap(props, {
        ref: elementRef,
        'data-heading': props.dataHeading,
        'data-heading-type': props.dataHeadingType,
        'data-heading-url': props.dataHeadingUrl,
        'data-subheading': props.dataSubheading,
        'data-color': props.dataColor,
        'data-color-type': props.dataColorType,
        'data-background-image': props.dataBackgroundImage,
        'data-icon': props.dataIcon,
        'data-tag-text': props.dataTagText,
        'data-tag-subtext': props.dataTagSubtext,
        'data-tag-icon': props.dataTagIcon,
        'data-icon-size': props.dataIconSize,
        'data-icon-stack-shape': props.dataIconStackShape,
        'data-tag-type': props.dataTagType,
        'data-show-avatar': props.dataShowAvatar,
        'data-avatar-first-name': props.dataAvatarFirstName,
        'data-avatar-last-name': props.dataAvatarLastName,
        'data-avatar-profile-picture-src': props.dataAvatarProfilePictureSrc,
        'data-avatar-background-option': props.dataAvatarBackgroundOption,
    }), props.children);
}
export const NeonCardHeader = withNeonWebComponent('neon-2_20_1-card-header', CardHeader, cardHeaderDependencies);
