import React, { useRef } from 'react';
import { toAttribute, buildPropsMap, withNeonWebComponent, useImperativeProps, useListeners } from './utils/react-utils';
import { avatarDependencies } from './neon-avatar';
import { menuDependencies } from './neon-menu';
import { createComponentDependency } from './utils/react-utils';
export const avatarStackDependencies = [
    createComponentDependency('avatar-stack'),
    ...avatarDependencies,
    ...menuDependencies
];
function AvatarStack(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        users: props.users
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonAvatarStackOverflowButtonClick: (event) => {
            if (props.neonAvatarStackOverflowButtonClick != null) {
                props.neonAvatarStackOverflowButtonClick();
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-avatar-stack', buildPropsMap(props, {
        ref: elementRef,
        'data-avatar-size': toAttribute(props.avatarSize),
        'data-show-overflow-menu': toAttribute(props.showOverflowMenu),
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonAvatarStack = withNeonWebComponent('neon-2_20_1-avatar-stack', AvatarStack, avatarStackDependencies);
