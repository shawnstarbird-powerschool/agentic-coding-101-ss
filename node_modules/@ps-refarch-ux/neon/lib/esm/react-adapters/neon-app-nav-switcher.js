/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, 
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { menuDependencies } from './neon-menu';
import { avatarDependencies } from './neon-avatar';
import { paginationDependencies } from './neon-pagination';
export const appNavSwitcherDependencies = [
    createComponentDependency('app-nav-switcher'),
    ...iconDependencies,
    ...menuDependencies,
    ...avatarDependencies,
    ...paginationDependencies
];
function AppNavSwitcher(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        switcherOptions: props.switcherOptions,
        currentSwitcherOption: props.currentSwitcherOption
    });
    // * * * Set Listeners * * *
    // useListeners<NeonAppNavSwitcherElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-app-nav-switcher', buildPropsMap(props, {
        ref: elementRef,
        'data-app-nav-collapsed': props.dataAppNavCollapsed
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonAppNavSwitcher = withNeonWebComponent('neon-2_20_1-app-nav-switcher', AppNavSwitcher, appNavSwitcherDependencies);
