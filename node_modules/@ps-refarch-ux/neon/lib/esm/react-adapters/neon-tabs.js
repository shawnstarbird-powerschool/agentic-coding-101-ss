import React, { useRef } from 'react';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { badgeDependencies } from './neon-badge';
export const tabsDependencies = [
    createComponentDependency('tabs'),
    ...badgeDependencies
];
function Tabs(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        tabs: props.tabs,
        currentTab: props.currentTab,
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        tabSelectionChanged: (event) => {
            if (props.tabSelectionChanged != null) {
                props.tabSelectionChanged(event.detail);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-tabs', buildPropsMap(props, {
        ref: elementRef,
        'data-legend': props.dataLegend,
    }), props.children);
}
export const NeonTabs = withNeonWebComponent('neon-2_20_1-tabs', Tabs, tabsDependencies);
