/* eslint-disable @typescript-eslint/no-namespace */
import React from 'react';
import { buildPropsMap, createComponentDependency, withNeonWebComponent } from './utils/react-utils';
import { iconDependencies } from './neon-icon';
export const iconStackDependencies = [
    createComponentDependency('icon-stack'),
    ...iconDependencies
];
const IconStack = (props) => {
    //If you need to pass in child elements, pass props.children into the third argument of React.createElement()
    return React.createElement('neon-2_20_1-icon-stack', buildPropsMap(props, {
        'data-icon': props.dataIcon,
        'data-color': props.dataColor,
        'data-shape': props.dataShape,
        'data-size': props.dataSize,
        'data-type': props.dataType,
    }));
};
export const NeonIconStack = withNeonWebComponent('neon-2_20_1-icon-stack', IconStack, iconStackDependencies);
