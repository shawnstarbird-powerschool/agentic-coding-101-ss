/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { buildPropsMap, createComponentDependency, toAttribute, useImperativeProps, useListeners, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { chipSelectSingleDependencies } from './neon-chip-select-single';
import { labeledWidgetDependencies } from './neon-labeled-widget';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
export const chipMultiSelectFieldDependencies = [
    createComponentDependency('chip-multi-select-field'),
    ...chipSelectSingleDependencies,
    ...labeledWidgetDependencies
];
function ChipMultiSelectField(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        modelValue: props.modelValue,
        options: props.options,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor,
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-chip-multi-select-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: elementRef,
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'disabled': toAttribute(props.disabled),
        'data-max-selections': toAttribute(props.dataMaxSelections),
        'data-layout': props.dataLayout
    }))
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonChipMultiSelectField = withNeonWebComponent('neon-2_20_1-chip-select-field', ChipMultiSelectField, chipMultiSelectFieldDependencies);
