/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { chipDependencies } from './neon-chip';
import { menuDependencies } from './neon-menu';
import { buildPropsMap, createComponentDependency, toAttribute, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { buttonDependencies } from './neon-button';
import { modalDialogDependencies } from './neon-modal-dialog';
import { modalDrawerDependencies } from './neon-modal-drawer';
export const chipSetDependencies = [
    createComponentDependency('chip-set'),
    ...chipDependencies,
    ...menuDependencies,
    ...buttonDependencies,
    ...modalDialogDependencies,
    ...modalDrawerDependencies
];
function ChipSet(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        chips: props.chips
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonChipSetChipRemoved: (e) => {
            if (props.neonChipSetChipRemoved != null) {
                props.neonChipSetChipRemoved(e);
            }
        },
        neonChipSetChipAllChipsRemoved: (e) => {
            if (props.neonChipSetChipAllChipsRemoved != null) {
                props.neonChipSetChipAllChipsRemoved(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-chip-set', buildPropsMap(props, {
        ref: elementRef,
        'data-disable-overflow-menu': props.dataDisableOverflowMenu != null ? props.dataDisableOverflowMenu : props.dataIsDisableOverflowMenu,
        'data-enable-clear-all': props.dataIsEnableClearAll,
        'data-clear-button-text': props.dataClearButtonText,
        'data-is-on-gray': toAttribute(props.dataIsOnGray),
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'data-size': props.dataSize,
        'disabled': toAttribute(props.disabled),
        'data-modal-type': props.dataModalType,
        'data-modal-threshold': props.dataModalThreshold,
        'data-modal-title': props.dataModalTitle,
        'data-clear-all-threshold': props.dataClearAllThreshold,
        'data-label-text': props.dataLabelText,
        class: props.className
    }));
}
export const NeonChipSet = withNeonWebComponent('neon-2_20_1-chip-set', ChipSet, chipSetDependencies);
