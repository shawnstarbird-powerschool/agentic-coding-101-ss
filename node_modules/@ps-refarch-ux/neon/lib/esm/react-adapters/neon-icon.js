/* eslint-disable @typescript-eslint/no-namespace */
import React from 'react';
import { getUnversionedNeonServer, createComponentDependency, withNeonWebComponent, buildPropsMap } from './utils/react-utils';
export const iconDependencies = [
    createComponentDependency('icon'),
    {
        url: getUnversionedNeonServer() + '/load-sprite-sheet.js',
        isDependencyLoaded: () => {
            return document.getElementById('neon-sprite-sheet-container') != null;
        }
    }
];
function Icon(props) {
    return React.createElement('neon-2_20_1-icon', buildPropsMap(props, {
        'data-icon': props.dataIcon,
        'data-size': props.dataSize,
        'data-size-mobile': props.dataSizeMobile,
        'data-color': props.dataColor,
        'data-color-weight': props.dataColorWeight
    }));
}
export const NeonIcon = withNeonWebComponent('neon-2_20_1-icon', Icon, iconDependencies);
