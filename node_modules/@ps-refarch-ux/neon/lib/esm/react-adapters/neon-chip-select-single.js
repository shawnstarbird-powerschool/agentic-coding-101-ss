/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const chipSelectSingleDependencies = [
    createComponentDependency('chip-select-single'),
    ...iconDependencies
];
function ChipSelectSingle(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonChipSelectSingleElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonChipSelectSingleElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-chip-select-single', buildPropsMap(props, {
        ref: elementRef,
        //TODO: Add in your attribute mapping here
        'data-label-text': props.dataLabelText,
        'name': props.name,
        'value': props.value != null ? String(props.value) : undefined,
        'data-show-label': props.dataShowLabel,
        'disabled': props.disabled,
        'data-tooltip-text': props.dataTooltipText,
        'data-tooltip-placement': props.dataTooltipPlacement,
        'data-label-text-space': props.dataLabelTextSpace,
        'data-size': props.dataSize,
        'data-unchecked-icon': props.dataUncheckedIcon,
        'data-use-unchecked-icon': props.dataUseUncheckedIcon,
        'onInput': props.onInput
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonChipSelectSingle = withNeonWebComponent('neon-2_20_1-chip-select-single', ChipSelectSingle, chipSelectSingleDependencies);
