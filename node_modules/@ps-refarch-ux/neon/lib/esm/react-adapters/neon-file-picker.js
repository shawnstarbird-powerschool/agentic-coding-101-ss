import React, { useRef } from 'react';
import { toAttribute } from './utils/react-utils';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
import { abstractInputFieldDependencies } from './abstract-input-field';
import { buttonDependencies } from './neon-button';
import { withNeonWebComponent, useImperativeProps, useListeners, buildPropsMap, createComponentDependency } from './utils/react-utils';
export const filePickerDependencies = [
    createComponentDependency('file-picker'),
    ...abstractInputFieldDependencies,
    ...buttonDependencies
];
function FilePicker(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-file-picker', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: elementRef,
        'data-show-selected-files': toAttribute(props.dataShowSelectedFiles),
        'data-button-text': props.dataButtonText,
        'accept': props.accept,
        'multiple': toAttribute(props.multiple),
        'disabled': toAttribute(props.disabled),
        'data-drop-zone-text': props.dataDropZoneText,
        'data-show-file-drop-zone': toAttribute(props.dataShowFileDropZone),
        'data-show-image-thumbnails': toAttribute(props.dataShowImageThumbnails),
        'data-show-icons': toAttribute(props.dataShowIcons),
    })));
}
export const NeonFilePicker = withNeonWebComponent('neon-2_20_1-file-picker', FilePicker, filePickerDependencies);
