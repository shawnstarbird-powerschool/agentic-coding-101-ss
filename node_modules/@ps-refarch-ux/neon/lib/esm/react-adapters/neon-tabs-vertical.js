/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { badgeDependencies } from './neon-badge';
export const tabsVerticalDependencies = [
    createComponentDependency('tabs-vertical'),
    ...iconDependencies,
    ...badgeDependencies
];
function TabsVertical(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        tabs: props.tabs,
        currentTab: props.currentTab,
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonTabSelectionChanged: (event) => {
            if (props.neonTabSelectionChanged != null) {
                props.neonTabSelectionChanged(event.detail.currentTab);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-tabs-vertical', buildPropsMap(props, {
        ref: elementRef,
        'data-legend': props.dataLegend
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonTabsVertical = withNeonWebComponent('neon-2_20_1-tabs-vertical', TabsVertical, tabsVerticalDependencies);
