/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { avatarDependencies } from './neon-avatar';
import { menuDependencies } from './neon-menu';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent, toAttribute } from './utils/react-utils';
export const chipDependencies = [
    createComponentDependency('chip'),
    ...avatarDependencies,
    ...menuDependencies
];
function Chip(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        options: props.children,
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonChipRemove: (e) => {
            if (props.neonChipRemove != null) {
                props.neonChipRemove(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-chip', buildPropsMap(props, {
        ref: elementRef,
        'data-text': props.dataText,
        'data-subtext': props.dataSubtext,
        'disabled': toAttribute(props.disabled),
        'data-size': props.dataSize,
        'data-tooltip': props.dataTooltip,
        'data-tooltip-position': props.dataTooltipPosition,
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'data-is-on-gray': toAttribute(props.dataIsOnGray),
        'data-show-avatar': toAttribute(props.dataShowAvatar),
        'data-avatar-first-name': props.dataAvatarFirstName,
        'data-avatar-last-name': props.dataAvatarLastName,
        'data-avatar-profile-picture-src': props.dataAvatarProfilePictureSrc,
        'data-avatar-background-option': props.dataAvatarBackgroundOption
    }));
}
export const NeonChip = withNeonWebComponent('neon-2_20_1-chip', Chip, chipDependencies);
