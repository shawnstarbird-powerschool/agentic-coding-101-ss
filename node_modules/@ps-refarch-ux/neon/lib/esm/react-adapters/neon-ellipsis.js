/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent, } from './utils/react-utils';
export const ellipsisDependencies = [
    createComponentDependency('ellipsis'),
    ...iconDependencies,
];
function Ellipsis(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonEllipsisElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonEllipsisElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-ellipsis', buildPropsMap(props, {
        ref: elementRef,
        'data-line-clamp': props.dataLineClamp,
        'data-popper-placement': props.dataPopperPlacement,
        'data-show-tooltip': props.dataShowTooltip,
        'data-tooltip-text': props.dataTooltipText
    }), props.children // can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonEllipsis = withNeonWebComponent('neon-2_20_1-ellipsis', Ellipsis, ellipsisDependencies);
