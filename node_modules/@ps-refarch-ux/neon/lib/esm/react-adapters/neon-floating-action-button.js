/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
// useImperativeProps,
useListeners, withNeonWebComponent } from './utils/react-utils';
export const floatingActionButtonDependencies = [
    createComponentDependency('floating-action-button'),
    ...iconDependencies
];
function FloatingActionButton(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonFloatingActionButtonElement>(elementRef, {
    // });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonFloatingActionButtonClick: (e) => {
            if (props.neonFloatingActionButtonClick != null) {
                props.neonFloatingActionButtonClick(e.detail.contentsVisible);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-floating-action-button', buildPropsMap(props, {
        ref: elementRef,
        'data-has-messages': toAttribute(props.dataHasMessages),
        'data-use-close-icon': toAttribute(props.dataUseCloseIcon),
        'data-tooltip-text': props.dataTooltipText,
        'data-img-src': props.dataImgSrc,
        'data-img-alt': props.dataImgAlt,
        'data-contents-visible': toAttribute(props.dataContentsVisible),
        'data-aria-label': props.dataAriaLabel,
        'disabled': toAttribute(props.disabled),
        'data-content-aria-label': props.dataContentAriaLabel
    }), props.children);
}
export const NeonFloatingActionButton = withNeonWebComponent('neon-2_20_1-floating-action-button', FloatingActionButton, floatingActionButtonDependencies);
