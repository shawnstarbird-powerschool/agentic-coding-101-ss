/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, 
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const lastUpdatedDependencies = [
    createComponentDependency('last-updated'),
    ...iconDependencies
];
function LastUpdated(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        lastUpdated: props.lastUpdated
    });
    // * * * Set Listeners * * *
    // useListeners<NeonLastUpdatedElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-last-updated', buildPropsMap(props, {
        ref: elementRef,
        'data-text': props.dataText,
        'data-date': props.dataDate,
        'data-time': props.dataTime,
        'data-color': props.dataColor,
        // 'data-tz': props.dataTz,
        'data-time-zone-string': props.dataTimeZoneString
        //TODO: Add in your attribute mapping here
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonLastUpdated = withNeonWebComponent('neon-2_20_1-last-updated', LastUpdated, lastUpdatedDependencies);
