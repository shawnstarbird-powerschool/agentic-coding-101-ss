/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, useImperativeProps, useListeners, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { tooltipDependencies } from './neon-tooltip';
export const buttonGroupDependencies = [
    createComponentDependency('button-group'),
    ...iconDependencies,
    ...tooltipDependencies
];
function ButtonGroup(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        'buttons': props.buttons,
        'value': props.value
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonButtonGroupValueChange: (event) => {
            if (props.neonButtonGroupValueChange) {
                props.neonButtonGroupValueChange(event.target.value);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-button-group', buildPropsMap(props, {
        ref: elementRef,
        'data-layout': props.dateLayout,
        'data-label-text-space': toAttribute(props.dataLabelTextSpace),
        'data-helper-text-space': toAttribute(props.dataHelperTextSpace),
        'data-allow-deselect': toAttribute(props.dataAllowDeselect),
        'data-mobile-full-width': toAttribute(props.dataMobileFullWidth)
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonButtonGroup = withNeonWebComponent('neon-2_20_1-button-group', ButtonGroup, buttonGroupDependencies);
