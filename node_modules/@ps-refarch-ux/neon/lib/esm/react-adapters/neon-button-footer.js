/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, 
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { buttonDependencies } from './neon-button';
import { listItemPopperDependencies } from './neon-item-picker';
import { buttonSplitDependencies } from './neon-button-split';
export const buttonFooterDependencies = [
    createComponentDependency('button-footer'),
    ...iconDependencies,
    ...buttonDependencies,
    ...listItemPopperDependencies,
    ...buttonSplitDependencies,
];
function ButtonFooter(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        tertiaryButton: props.tertiaryButton,
        buttons: props.buttons,
        primaryButton: props.primaryButton
    });
    // * * * Set Listeners * * *
    // useListeners<NeonButtonFooterElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-button-footer', buildPropsMap(props, {
        ref: elementRef,
        // 'data-full-width': props.dataFullWidth,
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonButtonFooter = withNeonWebComponent('neon-2_20_1-button-footer', ButtonFooter, buttonFooterDependencies);
