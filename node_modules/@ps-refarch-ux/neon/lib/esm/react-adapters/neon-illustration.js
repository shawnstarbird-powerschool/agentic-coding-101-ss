/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const illustrationDependencies = [
    createComponentDependency('illustration'),
    ...iconDependencies
];
function Illustration(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonIllustrationElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonIllustrationElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-illustration', buildPropsMap(props, {
        ref: elementRef,
        'data-illustration': props.dataIllustration,
        'data-background': props.dataBackground,
        'data-color': props.dataColor,
        'data-show-people': toAttribute(props.dataShowPeople),
        'data-aspect-ratio': props.dataAspectRatio
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonIllustration = withNeonWebComponent('neon-2_20_1-illustration', Illustration, illustrationDependencies);
