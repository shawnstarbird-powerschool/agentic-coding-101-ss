/* eslint-disable @typescript-eslint/no-namespace */
import React from 'react';
import { avatarDependencies } from './neon-avatar';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const profileHeaderGreetingDependencies = [
    createComponentDependency('profile-header'),
    ...avatarDependencies
];
function ProfileHeaderGreeting(props) {
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-profile-header-greeting', buildPropsMap(props, {
        'data-heading': props.dataHeading,
        'data-subheading': props.dataSubheading,
        'data-body': props.dataBody,
        'data-first-name': props.dataFirstName,
        'data-last-name': props.dataLastName,
        'data-profile-picture-src': props.dataProfilePictureSrc
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonProfileHeaderGreeting = withNeonWebComponent('neon-2_20_1-profile-header-greeting', ProfileHeaderGreeting, profileHeaderGreetingDependencies);
