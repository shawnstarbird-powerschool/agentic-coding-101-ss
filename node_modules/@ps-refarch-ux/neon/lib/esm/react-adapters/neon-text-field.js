import React, { useRef } from 'react';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
import { abstractInputFieldDependencies } from './abstract-input-field';
import { iconDependencies } from './neon-icon';
import { tooltipDependencies } from './neon-tooltip';
import { createComponentDependency, buildPropsMap, toAttribute } from './utils/react-utils';
import { useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
export const textFieldDependencies = [
    createComponentDependency('text-field'),
    ...abstractInputFieldDependencies,
    ...iconDependencies,
    ...tooltipDependencies
];
function TextField(props) {
    const componentRef = useRef();
    useImperativeProps(componentRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    useListeners(componentRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    return React.createElement('neon-2_20_1-text-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: componentRef,
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'disabled': props.disabled,
        'autocomplete': props.autocomplete,
        'data-placeholder-text': props.dataPlaceholderText,
        'data-regex': props.dataRegex,
        'data-regex-modifiers': props.dataRegexModifiers,
        'data-regex-error-text': props.dataRegexErrorText,
        'data-is-regex-inverse': props.dataIsRegexInverse,
        'data-min-length': props.dataMinLength,
        'data-max-length': props.dataMaxLength,
        'data-is-email': toAttribute(props.dataIsEmail),
        'data-is-password': props.dataIsPassword,
        'data-exclude-chars': props.dataExcludeChars,
        'data-icon': props.dataIcon,
        'data-is-clearable': toAttribute(props.dataIsClearable),
        'data-show-count': toAttribute(props.dataShowCount)
    })));
}
export const NeonTextField = withNeonWebComponent('neon-2_20_1-text-field', TextField, textFieldDependencies);
