/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { buttonDependencies } from './neon-button';
export const toastDependencies = [
    createComponentDependency('toast'),
    ...buttonDependencies,
    ...iconDependencies
];
function Toast(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        dateTime: props.datetime
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonToastCloseButtonClick: (e) => {
            if (props.neonToastCloseButtonClick) {
                props.neonToastCloseButtonClick(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-toast', buildPropsMap(props, {
        ref: elementRef,
        'data-type': props.dataType,
        'data-heading': props.dataHeading,
        'data-subheading': props.dataSubheading
    }), props.children);
}
export const NeonToast = withNeonWebComponent('neon-2_20_1-toast', Toast, toastDependencies);
