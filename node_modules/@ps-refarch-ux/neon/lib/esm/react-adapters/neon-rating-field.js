/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { abstractInputFieldDependencies } from './abstract-input-field';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
export const ratingFieldDependencies = [
    createComponentDependency('rating-field'),
    ...abstractInputFieldDependencies,
    ...iconDependencies
];
function RatingField(props) {
    const componentRef = useRef();
    useImperativeProps(componentRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    useListeners(componentRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        }
    });
    return React.createElement('neon-2_20_1-rating-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: componentRef,
        'data-is-read-only': props.dataIsReadOnly,
        'disabled': props.disabled,
        'data-type': props.dataType,
        'data-success-message': props.dataSuccessMessage
    })));
}
export const NeonRatingField = withNeonWebComponent('neon-2_20_1-text-field', RatingField, ratingFieldDependencies);
