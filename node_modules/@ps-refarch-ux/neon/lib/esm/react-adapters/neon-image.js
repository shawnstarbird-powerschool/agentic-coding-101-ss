/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const imageDependencies = [
    createComponentDependency('image'),
    ...iconDependencies
];
function Image(props) {
    const elementRef = useRef();
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-image', buildPropsMap(props, {
        ref: elementRef,
        'data-src': toAttribute(props.dataSrc),
        'data-alt': toAttribute(props.dataAlt),
        'data-object-fit': toAttribute(props.dataObjectFit),
    }));
}
export const NeonImage = withNeonWebComponent('neon-2_20_1-image', Image, imageDependencies);
