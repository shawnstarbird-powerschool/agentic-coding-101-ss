/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const illustrationSimpleDependencies = [
    createComponentDependency('illustration-simple'),
    ...iconDependencies
];
function IllustrationSimple(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonIllustrationSimpleElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonIllustrationSimpleElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-illustration-simple', buildPropsMap(props, {
        ref: elementRef,
        'data-illustration': props.dataIllustration,
        'data-size': props.dataSize
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonIllustrationSimple = withNeonWebComponent('neon-2_20_1-illustration-simple', IllustrationSimple, illustrationSimpleDependencies);
