/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, withNeonWebComponent } from './utils/react-utils';
export const fullPageSkeletonDependencies = [
    createComponentDependency('full-page-skeleton'),
    ...iconDependencies
];
function FullPageSkeleton(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonFullPageSkeletonElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonFullPageSkeletonElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-full-page-skeleton', buildPropsMap(props, {
        ref: elementRef,
        'data-type': props.dataType
        //TODO: Add in your attribute mapping here
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonFullPageSkeleton = withNeonWebComponent('neon-2_20_1-full-page-skeleton', FullPageSkeleton, fullPageSkeletonDependencies);
