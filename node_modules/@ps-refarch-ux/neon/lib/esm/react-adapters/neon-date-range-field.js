/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent } from './utils/react-utils';
import { dateFieldDependencies } from './neon-date-field';
import { datePickerDependencies } from './neon-date-picker';
export const dateRangeFieldDependencies = [
    createComponentDependency('date-range-field'),
    ...iconDependencies,
    ...dateFieldDependencies,
    ...datePickerDependencies,
];
function DateRangeField(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        modelValue: props.modelValue,
        customValidationFunction: props.customValidationFunction,
        startDateCustomValidationFunction: props.startDateCustomValidationFunction,
        endDateCustomValidationFunction: props.endDateCustomValidationFunction
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        'modelValueChange': (event) => {
            props.modelValueChange(event.target.modelValue);
        },
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-date-range-field', buildPropsMap(props, {
        ref: elementRef,
        'data-start-date-label-text': props.dataStartDateLabelText,
        'data-end-date-label-text': props.dataEndDateLabelText,
        'data-year': props.dataYear,
        'data-month': props.dataMonth,
        'data-is-required': props.dataIsRequired,
        'data-hide-required-badge': props.dataHideRequiredBadge,
        'data-show-optional-badge': props.dataShowOptionalBadge,
        'data-show-label': props.dataShowLabel,
        'data-is-read-only': props.dataIsReadOnly,
        'data-label-text-space': props.dataLabelTextSpace,
        'data-helper-text-space': props.dataHelperTextSpace,
        'disabled': props.disabled,
        'data-min-value': props.dataMinValue,
        'data-max-value': props.dataMaxValue,
        'data-start-date-aria-label': props.dataStartDateAriaLabel,
        'data-end-date-aria-label': props.dataEndDateAriaLabel,
        'data-start-date-autocomplete': props.dataStartDateAutocomplete,
        'data-end-date-autocomplete': props.dataEndDateAutocomplete,
        'data-start-date-placeholder-text': props.dataStartDatePlaceholderText,
        'data-end-date-placeholder-text': props.dataEndDatePlaceholderText,
        'data-popper-placement': props.dataPopperPlacement,
        'data-weekdays-only': props.dataWeekdaysOnly
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonDateRangeField = withNeonWebComponent('neon-2_20_1-date-range-field', DateRangeField, dateRangeFieldDependencies);
