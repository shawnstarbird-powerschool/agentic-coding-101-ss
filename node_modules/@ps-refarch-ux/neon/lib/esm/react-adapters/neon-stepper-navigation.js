/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, withNeonWebComponent } from './utils/react-utils';
export const stepperNavigationDependencies = [
    createComponentDependency('stepper-navigation'),
    ...iconDependencies
];
function StepperNavigationReactElement(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonStepperNavigationElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonStepperNavigationElement>(elementRef, {
    // });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-stepper-navigation', buildPropsMap(props, {
        ref: elementRef,
        'data-is-horizontal': props.dataIsHorizontal,
        'data-is-numbered': props.dataIsNumbered,
        'data-is-sequential': props.dataIsSequential
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonStepperNavigation = withNeonWebComponent('neon-2_20_1-stepper-navigation', StepperNavigationReactElement, stepperNavigationDependencies);
export function getStepperNavigation(id) {
    return customElements.whenDefined('neon-2_20_1-stepper-navigation').then(() => {
        return window.neonToolkits['neon-2_20_1-stepper-navigation'].get(id);
    });
}
export function destroyStepperNavigation(id) {
    return customElements.whenDefined('neon-2_20_1-stepper-navigation').then(() => {
        return window.neonToolkits['neon-2_20_1-stepper-navigation'].delete(id);
    });
}
