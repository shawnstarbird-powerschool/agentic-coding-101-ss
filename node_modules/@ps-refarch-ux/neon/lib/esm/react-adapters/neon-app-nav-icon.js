/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const appNavIconDependencies = [
    createComponentDependency('app-nav-icon'),
    ...iconDependencies
];
function AppNavIcon(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonAppNavIconElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonAppNavIconElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-app-nav-icon', buildPropsMap(props, {
        ref: elementRef,
        'data-icon': props.dataIcon
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonAppNavIcon = withNeonWebComponent('neon-2_20_1-app-nav-icon', AppNavIcon, appNavIconDependencies);
