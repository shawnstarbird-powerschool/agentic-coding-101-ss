/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const progressIndicatorDependencies = [
    createComponentDependency('progress-indicator'),
    ...iconDependencies
];
function ProgressIndicator(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonProgressIndicatorElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonProgressIndicatorElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-progress-indicator', buildPropsMap(props, {
        ref: elementRef,
        value: props.value,
        'data-total': props.dataTotal,
        'data-label-text': props.dataLabelText,
        'data-layout': props.dataLayout,
        'data-color': props.dataColor,
        'data-bar-height': props.dataBarHeight,
        // 'data-show-label': props.dataShowLabel,
        'data-is-percent': props.dataIsPercent,
        'data-max-decimals': props.dataMaxDecimals,
        'data-text-size': props.dataTextSize
        //TODO: Add in your attribute mapping here
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonProgressIndicator = withNeonWebComponent('neon-2_20_1-progress-indicator', ProgressIndicator, progressIndicatorDependencies);
