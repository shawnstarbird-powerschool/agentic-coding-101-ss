import React from 'react';
import { type NeonTableHeaderProps } from '../interfaces/react-adapters/neon-table-header-props';
import { type ComponentDependency } from './utils/component-dependency';
import { type SortController } from '../cdn-assets/web-components/neon-table-header/sort-controller';
export declare const tableHeaderDependencies: Array<ComponentDependency>;
export declare const NeonTableHeader: React.ForwardRefExoticComponent<NeonTableHeaderProps & React.RefAttributes<HTMLElement>>;
/**
 * Gets an instance of a sort controller by using the passed in id.  If an instance has not been
 * created yet it will create one.
 * @param sortControlId The lookup id for the sort controller. This id is most often used to pass to the neon-table-header web-component.
 * @returns A SortController object
 */
export declare function getSortController(sortControlId: string): Promise<SortController>;
/**
 * Deletes the sort controller for the given id.  Do this as part of page cleanup after all of the neon-table-header web-components have been removed from the DOM.
 * @param sortControlId The lookup id for the sort controller. This id is most often used to pass to the neon-table-header web-component.
 */
export declare function deleteSortController(sortControlId: string): void;
