/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const masonryGridDependencies = [
    createComponentDependency('masonry-grid'),
    ...iconDependencies
];
function MasonryGrid(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonMasonryGridElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonMasonryGridElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-masonry-grid', buildPropsMap(props, {
        ref: elementRef,
        'data-columns': props.dataColumns,
        'data-preserve-order': toAttribute(props.dataPreserveOrder)
    }), props.children);
}
export const NeonMasonryGrid = withNeonWebComponent('neon-2_20_1-masonry-grid', MasonryGrid, masonryGridDependencies);
