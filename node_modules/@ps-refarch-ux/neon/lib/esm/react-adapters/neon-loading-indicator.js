/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, withNeonWebComponent } from './utils/react-utils';
export const loadingIndicatorDependencies = [
    createComponentDependency('loading-indicator'),
    ...iconDependencies
];
function LoadingIndicator(props) {
    const elementRef = useRef();
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-loading-indicator', buildPropsMap(props, {
        ref: elementRef,
        'data-size': props.dataSize,
        'data-size-mobile': props.dataSizeMobile,
        'data-inverse': props.dataInverse,
    }));
}
export const NeonLoadingIndicator = withNeonWebComponent('neon-2_20_1-loading-indicator', LoadingIndicator, loadingIndicatorDependencies);
