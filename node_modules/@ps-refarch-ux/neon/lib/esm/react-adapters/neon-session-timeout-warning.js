/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { modalDialogDependencies } from './neon-modal-dialog';
import { buttonDependencies } from './neon-button';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
useListeners, withNeonWebComponent } from './utils/react-utils';
export const sessionTimeoutWarningDependencies = [
    createComponentDependency('session-timeout-warning'),
    ...iconDependencies,
    ...modalDialogDependencies,
    ...buttonDependencies,
];
function SessionTimeoutWarning(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonSessionTimeoutWarningElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        neonSessionTimeoutWarningLogout: (event) => {
            if (props.neonSessionTimeoutWarningLogout != null) {
                props.neonSessionTimeoutWarningLogout();
            }
        },
        neonSessionTimeoutWarningLogoutWarningTimerStart: (event) => {
            if (props.neonSessionTimeoutWarningLogoutWarningTimerStart != null) {
                props.neonSessionTimeoutWarningLogoutWarningTimerStart();
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-session-timeout-warning', buildPropsMap(props, {
        ref: elementRef,
        'data-dialog-header-text': props.dataDialogHeaderText,
        'data-warning-header-text': props.dataWarningHeaderText,
        'data-warning-message-text': props.dataWarningMessageText,
        'data-sign-out-button-text': props.dataSignOutButtonText,
        'data-stay-signed-in-button-text': props.dataStaySignedInButtonText,
        'data-warning-time': props.dataWarningTime,
        'data-logout-time': props.dataLogoutTime
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonSessionTimeoutWarning = withNeonWebComponent('neon-2_20_1-session-timeout-warning', SessionTimeoutWarning, sessionTimeoutWarningDependencies);
