import React, { useRef } from 'react';
import { createComponentDependency } from './utils/react-utils';
import { useImperativeProps, withNeonWebComponent, buildPropsMap } from './utils/react-utils';
export const footerDependencies = [
    createComponentDependency('footer')
];
function Footer(props) {
    const componentRef = useRef();
    useImperativeProps(componentRef, {
        footerInfo: props.footerInfo
    });
    return React.createElement('neon-2_20_1-footer', buildPropsMap(props, {
        ref: componentRef
    }));
}
export const NeonFooter = withNeonWebComponent('neon-2_20_1-footer', Footer, footerDependencies);
