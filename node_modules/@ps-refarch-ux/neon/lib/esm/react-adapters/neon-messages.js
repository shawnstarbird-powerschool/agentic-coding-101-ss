import React, { useRef } from 'react';
import { buildPropsMap, createComponentDependency, toAttribute, withNeonWebComponent, } from './utils/react-utils';
import { systemMessageDependencies } from './neon-system-message';
import { toastDependencies } from './neon-toast';
export const messagesDependencies = [
    createComponentDependency('messages'),
    ...systemMessageDependencies,
    ...toastDependencies,
];
function Messages(props) {
    const elementRef = useRef();
    return React.createElement('neon-2_20_1-messages', buildPropsMap(props, {
        ref: elementRef,
        'data-type': toAttribute(props.dataType),
    }), props.children);
}
export const NeonMessages = withNeonWebComponent('neon-2_20_1-messages', Messages, messagesDependencies);
