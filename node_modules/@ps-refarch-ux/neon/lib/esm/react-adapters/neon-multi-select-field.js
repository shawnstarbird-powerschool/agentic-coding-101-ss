import React, { useRef } from 'react';
import { extendAbstractInputFieldAttrs } from './utils/abstract-input-field-utils';
import { loadingIndicatorDependencies } from './neon-loading-indicator';
import { abstractInputFieldDependencies } from './abstract-input-field';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent, toAttribute } from './utils/react-utils';
import { chipSetDependencies } from './neon-chip-set';
import { modalDialogDependencies } from './neon-modal-dialog';
import { buttonDependencies } from './neon-button';
import { listItemDependencies } from './neon-list-item';
import { paginationDependencies } from './neon-pagination';
export const multiSelectFieldDependencies = [
    createComponentDependency('multi-select-field'),
    ...abstractInputFieldDependencies,
    ...chipSetDependencies,
    ...loadingIndicatorDependencies,
    ...modalDialogDependencies,
    ...buttonDependencies,
    ...listItemDependencies,
    ...paginationDependencies
];
function MultiSelectField(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        modelValue: props.modelValue,
        options: props.options,
        customValidationFunction: props.customValidationFunction,
        blurInterceptor: props.blurInterceptor
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        modelValueChange: (event) => {
            props.modelValueChange(event.target.modelValue);
        },
        'blur': (e) => {
            if (props.onBlur != null) {
                props.onBlur(e);
            }
        },
        'neonPopperClose': (e) => {
            if (props.neonPopperClose != null) {
                props.neonPopperClose(e);
            }
        }
    });
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-multi-select-field', buildPropsMap(props, extendAbstractInputFieldAttrs(props, {
        ref: elementRef,
        'data-drop-down-text': props.dataDropDownText,
        'data-show-selected-items-list': toAttribute(props.dataShowSelectedItemsList),
        'data-is-read-only': toAttribute(props.dataIsReadOnly),
        'data-is-loading': toAttribute(props.dataIsLoading),
        'disabled': toAttribute(props.disabled),
        'data-use-intermediate-selection': toAttribute(props.dataUseIntermediateSelection),
        'data-show-filter': toAttribute(props.dataShowFilter),
        'data-popper-placement': props.dataPopperPlacement,
        'data-chip-set-is-disable-overflow-menu': toAttribute(props.dataChipSetIsDisableOverflowMenu),
        'data-show-select-all': toAttribute(props.dataShowSelectAll),
        'data-modal-type': props.dataModalType,
        'data-modal-threshold': props.dataModalThreshold,
        'data-chip-set-enable-clear-all': props.dataChipSetEnableClearAll,
        'data-chip-set-clear-all-threshold': props.dataChipSetClearAllThreshold,
        'data-popper-max-height': props.dataPopperMaxHeight,
        'data-select-all-text': props.dataSelectAllText,
        'data-select-all-subtext-message-key': props.dataSelectAllSubtextMessageKey
    }))
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonMultiSelectField = withNeonWebComponent('neon-2_20_1-multi-select-field', MultiSelectField, multiSelectFieldDependencies);
