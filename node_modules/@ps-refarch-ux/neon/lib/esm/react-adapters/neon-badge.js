/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, toAttribute, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
export const badgeDependencies = [
    createComponentDependency('badge'),
    ...iconDependencies
];
function Badge(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonBadgeElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonBadgeElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-badge', buildPropsMap(props, {
        ref: elementRef,
        'data-type': props.dataType,
        'data-count': toAttribute(props.dataCount),
        'data-overflow-count': toAttribute(props.dataOverflowCount),
        'data-show-zero': toAttribute(props.dataShowZero),
    })
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonBadge = withNeonWebComponent('neon-2_20_1-badge', Badge, badgeDependencies);
