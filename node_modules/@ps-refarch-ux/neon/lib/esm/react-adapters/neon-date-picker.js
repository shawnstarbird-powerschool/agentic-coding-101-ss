/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { buttonDependencies } from './neon-button';
import { buildPropsMap, createComponentDependency, useImperativeProps, useListeners, withNeonWebComponent, } from './utils/react-utils';
export const datePickerDependencies = [
    createComponentDependency('date-picker'),
    ...buttonDependencies
];
function DatePicker(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    useImperativeProps(elementRef, {
        singleModeValue: props.singleModeValue,
        rangeModeValue: props.rangeModeValue,
        multiModeValue: props.multiModeValue,
        customValidationFunction: props.customValidationFunction
    });
    // * * * Set Listeners * * *
    useListeners(elementRef, {
        'neonDatePickerSingleDateChanged': (event) => {
            var _a;
            if (props.neonDatePickerSingleDateChanged) {
                props.neonDatePickerSingleDateChanged((_a = event.target) === null || _a === void 0 ? void 0 : _a.singleModeValue);
            }
        },
        'neonDatePickerMultiDatesChanged': (event) => {
            if (props.neonDatePickerMultiDatesChanged) {
                props.neonDatePickerMultiDatesChanged(event.target.multiModeValue);
            }
        },
        'neonDatePickerDateRangeChanged': (event) => {
            if (props.neonDatePickerDateRangeChanged) {
                props.neonDatePickerDateRangeChanged(event.target.rangeModeValue);
            }
        }
    });
    return React.createElement('neon-2_20_1-date-picker', buildPropsMap(props, {
        ref: elementRef,
        'data-month': props.dataMonth,
        'data-year': props.dataYear,
        'data-mode': props.dataMode,
        'data-min-value': props.dataMinValue,
        'data-max-value': props.dataMaxValue,
        'data-weekdays-only': props.dataWeekdaysOnly
    }));
}
export const NeonDatePicker = withNeonWebComponent('neon-2_20_1-date-picker', DatePicker, datePickerDependencies);
