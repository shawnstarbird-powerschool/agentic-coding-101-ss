/* eslint-disable @typescript-eslint/no-namespace */
import React, { useRef } from 'react';
import { iconDependencies } from './neon-icon';
import { buildPropsMap, createComponentDependency, 
//useImperativeProps,
//useListeners,
withNeonWebComponent } from './utils/react-utils';
import { toAttribute } from './utils/react-utils';
import { illustrationDependencies } from './neon-illustration';
export const errorPageContentDependencies = [
    createComponentDependency('error-page-content'),
    ...illustrationDependencies,
    ...iconDependencies
];
function ErrorPageContent(props) {
    const elementRef = useRef();
    // * * * Set Props * * *
    // useImperativeProps<NeonErrorPageContentElement>(elementRef, {
    //   //propName: propValue
    // });
    // * * * Set Listeners * * *
    // useListeners<NeonErrorPageContentElement>(elementRef, {
    //   //eventName: callback function
    //});
    // * * * Set Attributes * * *
    return React.createElement('neon-2_20_1-error-page-content', buildPropsMap(props, {
        ref: elementRef,
        'data-error-code': props.dataErrorCode,
        'data-show-error-code': toAttribute(props.dataShowErrorCode),
        'data-heading': props.dataHeading,
        'data-subheading': props.dataSubheading,
        'data-body': props.dataBody,
        //TODO: Add in your attribute mapping here
    }), props.children
    //props.children can be passed in as the next argument into React.createElement() if the element is expected to have children.
    );
}
export const NeonErrorPageContent = withNeonWebComponent('neon-2_20_1-error-page-content', ErrorPageContent, errorPageContentDependencies);
