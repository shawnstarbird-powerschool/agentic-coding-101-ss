import { Stream } from './stream';
import { NavItem } from './nav-item';
import { NavUser } from './nav-user';
import { UserNavItem } from './user-nav-item';
import { AppNavSwitcherItem } from './app-nav-switcher-item';
export interface AppNav {
    navItems: Array<NavItem>;
    user: UserNavItem;
    currentSwitcherOption?: AppNavSwitcherItem;
    switcherOptions?: Array<AppNavSwitcherItem | "DIVIDER">;
    navItemDepth: Record<string, number>;
    navItemMaxDescendentDepth: Record<string, number>;
    navItemParentMap: Record<string, NavItem>;
    navItemActiveMap: Record<string, boolean>;
    changeStream: Stream<string>;
    setNavItems(navItems: Array<NavItem>): void;
    setUser(user: NavUser): void;
    setSwitcherOptions(switcherOptions: Array<AppNavSwitcherItem | "DIVIDER">): void;
    setCurrentSwitcherOption(currentSwitcherOption: AppNavSwitcherItem): void;
    populateNavItemDepth(): void;
    refreshNavItemActiveMap(): void;
    setItemDepthAndParent(navItem: NavItem, depth: number): {
        depth: number;
        isActive: boolean;
    };
    getDepth(navItem: NavItem): number;
    getParent(navItemId: string): NavItem | undefined;
    getMaxDescendentDepth(navItem: NavItem): number;
    isActive(navItem: NavItem): boolean;
    isLeavesApp(navItem: NavItem): boolean;
}
