export interface CheckboxOption {
    id: string;
    labelText: string;
    name: string;
    disabled?: boolean;
    helperText?: string;
    checkType?: 'check' | 'toggle';
    isIndented?: boolean;
}
