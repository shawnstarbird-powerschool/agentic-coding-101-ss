import { StepChange } from "./step-change";
import { StepperNavigation } from "./stepper-navigation";
export interface Step {
    id: string;
    text: string;
    subtext?: string;
    getStateBeforeLeavingStep: (currentStep: Step, nextStep: Step, stepperNavigation: StepperNavigation) => StepChange | Promise<StepChange>;
}
