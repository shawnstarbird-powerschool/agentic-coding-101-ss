import { NavItem } from './nav-item';
/**
 * This is the interface for the outside applications to set the information for the nav user.
 */
export interface NavUser {
    firstName: string;
    lastName: string;
    profilePicture?: {
        src: string;
        alt?: string;
    };
    userMenuOptions: Array<NavItem>;
}
