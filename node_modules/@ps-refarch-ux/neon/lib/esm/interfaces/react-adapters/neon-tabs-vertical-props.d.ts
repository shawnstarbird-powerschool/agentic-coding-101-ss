import { Tab } from '../tab';
import { NeonCommonProps } from './neon-common-props';
export interface NeonTabsVerticalProps extends NeonCommonProps {
    id: string;
    dataLegend: string;
    currentTab?: string;
    tabs?: Array<Tab>;
    neonTabSelectionChanged?: (currentTab: string | undefined) => void;
}
