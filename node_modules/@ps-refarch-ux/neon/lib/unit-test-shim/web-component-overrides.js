// Disable Focus Trap on all Neon register components.
customElements.whenDefined('neon-2_20_1-layout-detail').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-modal-dialog').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-modal-drawer').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});

// Disable Focus Trap on all Neon register components which uses show popper
customElements.whenDefined('neon-2_20_1-tooltip').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-select-field').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-popover').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-multi-select-field').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-list-item').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-date-range-field').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});
customElements.whenDefined('neon-2_20_1-date-field').then((customElement) => {
  customElement.prototype.addFocusTrap = function(){/* no-op */};
});

window.ResizeObserver = class ResizeObserver {
  constructor(debounceFunction) {
    //no-op
  }
  
  observe() {
    //no-op
  }
  
  disconnect() {
    //no-op
  }
  
  unobserve() {
    //no-op
  }
};

window.IntersectionObserver = class IntersectionObserver {
  disconnect() {
    //no-op
  }

  observe() {
    //no-op
  }

  takeRecords() {
    //no-op
  }

  unobserve() {
    //no-op
  }
}
