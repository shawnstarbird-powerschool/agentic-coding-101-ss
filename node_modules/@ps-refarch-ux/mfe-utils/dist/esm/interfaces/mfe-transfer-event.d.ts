/**
 * Internally, the mfe-communications functions use this object form to send, receive, and
 * store mfe events.
 */
export interface MfeTransferEvent {
    name: string;
    sourceMfe: string;
    jsonString?: string;
    isGuarded: boolean;
    unguardedContext?: any;
}
