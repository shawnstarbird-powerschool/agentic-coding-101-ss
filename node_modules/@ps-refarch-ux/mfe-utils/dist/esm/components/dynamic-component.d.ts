import React, { ReactElement } from 'react';
import { MfeInitFunction, DynamicComponentProps, MfeInitFunctionConfig } from '../interfaces';
/**
 * The DynamicComponent is a React component that renders the specified MFE inside its self.
 * The key props to pass in are:
 *   remote: string; //Syncs up with ModuleFederation 'name'
 *   module: string; //Syncs up with ModuleFederation 'exposes'
 *   url: string; //This will normally be remoteEntry.js and is primarily used as a fallback url
 *
 * You can see an example of how to export an mfe in the ModuleFederation configuration in this file:
 * https://gitlab.com/powerschoolgroup/ui_ux/mfe-architecture/mfe-starter-pack/-/blob/develop/webpack.config.js?ref_type=heads
 */
export declare class DynamicComponent extends React.Component<DynamicComponentProps> {
    state: {
        reactFunctionComponent?: any;
        initModule?: {
            psMfeInit: MfeInitFunction;
        };
        unloadMfe?: () => void;
        updateMfeConfig?: (newConfig: MfeInitFunctionConfig) => void;
        errorMessage?: string;
    };
    props: DynamicComponentProps;
    containerRef: any;
    constructor(props: DynamicComponentProps);
    createDynamicComponent(): any | undefined;
    componentDidUpdate(previousProps: DynamicComponentProps): void;
    unloadCurrentMfe(): void;
    componentWillUnmount(): void;
    render(): ReactElement;
}
