"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SimpleTime = void 0;
var _i18n = require("../utils/i18n");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
/**
 * This class purposefully has functions that match those from Date.
 * The reason is so someone wanting to pass a Date object into the pds-time-widget, or the
 * validation engine time rules they can do so and the code won't break.
 */
var SimpleTime = /*#__PURE__*/function () {
  function SimpleTime(hours, minutes, seconds) {
    _classCallCheck(this, SimpleTime);
    if (typeof hours === 'number') {
      // Positive or negative integer
      this.setHours(hours);
      this.setMinutes(minutes || 0);
      this.setSeconds(seconds || 0);
    } else if (typeof hours === 'string') {
      var parsedTime = (0, _i18n.parse24HourTime)(hours); // This means 24 hour time is given in the format of HH:mm OR HH:mm:ss
      if (parsedTime != null) {
        this.set(parsedTime);
      }
    } else {
      this.setHours(0);
      this.setMinutes(0);
      this.setSeconds(0);
    }
  }
  _createClass(SimpleTime, [{
    key: "to24HourTime",
    value: function to24HourTime() {
      var hours = this.getHours();
      var minutes = this.getMinutes();
      var seconds = this.getSeconds();
      if (hours < 10) {
        hours = '0' + hours;
      }
      if (minutes < 10) {
        minutes = '0' + minutes;
      }
      if (seconds < 10) {
        seconds = '0' + seconds;
      }
      return hours + ':' + minutes + ':' + seconds;
    }
  }, {
    key: "getHours",
    value: function getHours() {
      return this.hours;
    }
  }, {
    key: "getMinutes",
    value: function getMinutes() {
      return this.minutes;
    }
  }, {
    key: "getSeconds",
    value: function getSeconds() {
      return this.seconds;
    }
  }, {
    key: "setHours",
    value: function setHours(hours) {
      var hrs = hours % 24;
      if (hrs >= 0) {
        this.hours = hrs;
      } else {
        // hours are negative, so lets add 24 to the remainder given from modulus 24 operator
        this.hours = 24 + hrs;
      }
    }
  }, {
    key: "setMinutes",
    value: function setMinutes(minutes) {
      var overHours = Math.floor(minutes / 60);
      this.setHours(this.hours + overHours);
      var mins = minutes % 60;
      if (mins >= 0) {
        this.minutes = mins;
      } else {
        //60 - min, but min is already negative
        this.minutes = 60 + mins;
      }
    }
  }, {
    key: "setSeconds",
    value: function setSeconds(seconds) {
      var overMinutes = Math.floor(seconds / 60);
      this.setMinutes(this.minutes + overMinutes);
      var secs = seconds % 60;
      if (secs >= 0) {
        this.seconds = secs;
      } else {
        //60 - min, but min is already negative
        this.seconds = 60 + secs;
      }
    }
  }, {
    key: "getTime",
    value: function getTime() {
      return (this.hours * 60 * 60 + this.getMinutes() * 60 + this.getSeconds()) * 1000;
    }
  }, {
    key: "set",
    value: function set(time) {
      this.setHours(time.getHours());
      this.setMinutes(time.getMinutes());
      this.setSeconds(time.getSeconds());
    }
  }]);
  return SimpleTime;
}();
exports.SimpleTime = SimpleTime;