"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ExternalResolvePromise = void 0;
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
/**
 * The JavaScript API for creating a promise object is this:
 * new Promise((resolve, reject) => {
 *  do async things, then resolve or reject.
 * });
 *
 * Unfortunately, that means that you must know what the async call will be when you
 * create the promise.
 *
 * This class solves that by allowing something to create the promise and subscribe to it
 * without having direct access to the async call that will resolve it.
 *
 * file-1.ts
 * myPromises['promise-1'] = new ExternalResolvePromise();
 * myPromises['promise-1'].then(() => {
 *    //promise resolve logic.
 * });
 *
 * file-2.ts
 * if (myPromises['promise-1'] != null) {
 *    myPromises['promise-1'].externalResolve('payload');
 * }
 */
var ExternalResolvePromise = /*#__PURE__*/function () {
  function ExternalResolvePromise() {
    var _this = this;
    _classCallCheck(this, ExternalResolvePromise);
    _defineProperty(this, "_isResolved", false);
    _defineProperty(this, "_isRejected", false);
    this.promise = new Promise(function (resolve, reject) {
      _this.externalResolve = function () {
        _this._isResolved = true;
        resolve.apply(void 0, arguments);
      };
      _this.externalReject = function () {
        _this._isRejected = true;
        reject.apply(void 0, arguments);
      };
    });
  }
  _createClass(ExternalResolvePromise, [{
    key: "isResolved",
    get: function get() {
      return this._isResolved;
    }
  }, {
    key: "isRejected",
    get: function get() {
      return this._isRejected;
    }
  }]);
  return ExternalResolvePromise;
}();
exports.ExternalResolvePromise = ExternalResolvePromise;