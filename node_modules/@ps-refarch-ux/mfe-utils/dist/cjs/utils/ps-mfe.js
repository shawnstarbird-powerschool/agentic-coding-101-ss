"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.psMfe = psMfe;
/**
 * When you export a module from ModuleFederation, it should be run through this function.
 * The DynamicComponent can consume the contents of what is exported here.
 * @param options: Defining the options object is how you define the init function for bootstrapping your MFE.
 * @returns Right now this is just returning the options object but in the future we will provide validation steps.
 *
 * @example
 * import React from 'react';
 * import {psMfe, MfeInitFunctionConfig} from '@ps-refarch-ux/mfe-utils';
 * import {createRoot} from 'react-dom/client';
 *
 * export default psMfe({
 *   psMfeInit: function(config: MfeInitFunctionConfig): void {
 *     console.log('element: ', config.element);
 *     console.log('locale: ', config.locale);
 *     createRoot(config.element).render(<RootComponent/>);
 *   }
 * });
 */
function psMfe(options) {
  //We are using a function here so that in the future we can
  //run validations on the options if we need to.
  //It's also possible that there might be two different interfaces that this thing could return
  //and we want to be able to make sure they are correct through use of this function.

  return options;
}