export interface Time {
  to24HourTime(): string;
  getHours(): number;
  setHours(hours: number): void;
  setMinutes(minutes: number): void;
  getMinutes(): number;
  getSeconds(): number;
  getTime(): number;
  set(time: Time): void;
}