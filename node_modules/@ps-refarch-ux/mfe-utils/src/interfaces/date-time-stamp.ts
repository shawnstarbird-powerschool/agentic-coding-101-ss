export interface DateTimeStamp {
  year: number;
  monthIndex: number;
  day: number;
  hours: number;
  minutes: number;
  seconds: number;

  //Supported time zones can be listed by running the following in your browser console:
  //['utc'].concat(Intl.supportedValuesOf('timeZone'))
  timeZone: string;
}