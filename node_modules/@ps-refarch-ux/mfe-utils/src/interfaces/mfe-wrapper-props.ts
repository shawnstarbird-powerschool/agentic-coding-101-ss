import {ReactElement} from 'react';
import {type DynamicConfigController} from '../models';
import {MfeInitFunctionConfig} from './mfe-init-function-config';

export interface MfeWrapperProps {
  configController: DynamicConfigController,
  mfeComponent: (config: MfeInitFunctionConfig) => ReactElement
}