import type { Entity } from '../../../entity/index.js';
import type { Table } from '../../../table/index.js';
import { TableAction } from '../../../table/index.js';
import { $accessRole, $awsConfig, $metadata } from './constants.js';
import type { AccessRole } from './putAccessRole.js';
import type { AWSConfig } from './types.js';
interface EntityMetadata {
    entityIcon?: string;
    entityTitle?: string;
    entityDescription?: string;
}
interface Metadata<ENTITIES extends Entity[]> {
    awsAccountTitle?: string;
    awsAccountColor?: string;
    awsAccountDescription?: string;
    tableIcon?: string;
    tableTitle?: string;
    tableDescription?: string;
    entities?: Entity[] extends ENTITIES ? Record<string, EntityMetadata> : {
        [ENTITY in ENTITIES[number] as ENTITY['entityName']]?: EntityMetadata;
    };
}
export declare class Synchronizer<TABLE extends Table, ENTITIES extends Entity[]> extends TableAction<TABLE, ENTITIES> {
    static actionName: "synchronize";
    apiUrl: string;
    [$awsConfig]?: AWSConfig;
    [$accessRole]?: AccessRole;
    [$metadata]: Metadata<ENTITIES>;
    constructor(table: TABLE, entities?: ENTITIES, awsConfig?: AWSConfig, accessRole?: AccessRole, metadata?: Metadata<ENTITIES>);
    entities<NEXT_ENTITIES extends Entity[]>(...nextEntities: NEXT_ENTITIES): Synchronizer<TABLE, NEXT_ENTITIES>;
    awsConfig(awsConfig: AWSConfig): Synchronizer<TABLE, ENTITIES>;
    accessRole(accessRole: AccessRole): Synchronizer<TABLE, ENTITIES>;
    metadata(metadata: Metadata<ENTITIES>): Synchronizer<TABLE, ENTITIES>;
    sync({ apiKey, tableName: optionsTableName, deleteUnknownEntities, fetch: _fetch }: {
        apiKey: string;
        tableName?: string;
        deleteUnknownEntities?: boolean;
        fetch?: typeof fetch;
    }): Promise<void>;
}
export {};
