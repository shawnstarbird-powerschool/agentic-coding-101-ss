"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isTransformerWithDTO = exports.getDefaultsDTO = void 0;
const isFunction_js_1 = require("../../../../utils/validation/isFunction.js");
const isObject_js_1 = require("../../../../utils/validation/isObject.js");
const getDefaultsDTO = (schema) => {
    const defaultsDTO = {};
    for (const mode of ['keyDefault', 'putDefault', 'updateDefault']) {
        const modeDefault = schema.props[mode];
        if (modeDefault === undefined) {
            continue;
        }
        defaultsDTO[mode] = (0, isFunction_js_1.isFunction)(modeDefault)
            ? { defaulterId: 'custom' }
            : { defaulterId: 'value', value: modeDefault };
    }
    return defaultsDTO;
};
exports.getDefaultsDTO = getDefaultsDTO;
const isTransformerWithDTO = (transformer) => (0, isObject_js_1.isObject)(transformer) && 'transformerId' in transformer && 'toJSON' in transformer;
exports.isTransformerWithDTO = isTransformerWithDTO;
