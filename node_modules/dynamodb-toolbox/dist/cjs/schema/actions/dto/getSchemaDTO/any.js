"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAnySchemaDTO = void 0;
const utils_js_1 = require("./utils.js");
/**
 * @debt feature "handle defaults, links & validators DTOs"
 */
const getAnySchemaDTO = (schema) => {
    const defaultsDTO = (0, utils_js_1.getDefaultsDTO)(schema);
    const { required, hidden, key, savedAs, transform } = schema.props;
    return {
        type: 'any',
        ...(required !== undefined && required !== 'atLeastOnce' ? { required } : {}),
        ...(hidden !== undefined && hidden ? { hidden } : {}),
        ...(key !== undefined && key ? { key } : {}),
        ...(savedAs !== undefined ? { savedAs } : {}),
        ...(transform !== undefined
            ? {
                transform: ((0, utils_js_1.isTransformerWithDTO)(transform)
                    ? transform.toJSON()
                    : { transformerId: 'custom' })
            }
            : {}),
        ...defaultsDTO
    };
};
exports.getAnySchemaDTO = getAnySchemaDTO;
