import type { PrimitiveSchema, ResolvedPrimitiveSchema } from '../../schema/index.js';
export declare const isValidPrimitive: <SCHEMA extends PrimitiveSchema>(schema: SCHEMA, candidate: unknown) => candidate is ResolvedPrimitiveSchema;
