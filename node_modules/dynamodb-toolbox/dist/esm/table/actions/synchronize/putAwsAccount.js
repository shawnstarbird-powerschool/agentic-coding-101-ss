export const putAWSAccount = async (awsAccount, { apiUrl, fetch: _fetch = fetch, apiKey }) => {
    const response = await _fetch([apiUrl, 'aws-account'].join('/'), {
        method: 'PUT',
        headers: { Authorization: apiKey },
        body: JSON.stringify(awsAccount),
        signal: AbortSignal.timeout(30000)
    });
    if (!response.ok) {
        const { message, Message } = (await response.json());
        throw new Error(message !== null && message !== void 0 ? message : Message);
    }
};
