import { EntityAction } from '../../../entity/index.js';
export class WriteTransaction extends EntityAction {
}
export const isWriteTransactionImplementation = (input) => input instanceof WriteTransaction;
