import { isObject } from '../../../../utils/validation/isObject.js';
export const $IS_EXTENSION = Symbol('$IS_EXTENSION');
export const isExtension = (input) => isObject(input) &&
    $IS_EXTENSION in input &&
    input[$IS_EXTENSION] === true;
