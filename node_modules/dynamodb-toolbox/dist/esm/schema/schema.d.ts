import type { Schema } from '../schema/index.js';
export declare class SchemaAction<SCHEMA extends Schema = Schema> {
    schema: SCHEMA;
    static actionName: string;
    constructor(schema: SCHEMA);
}
