import { SchemaAction } from '../../../schema/index.js';
export class SubSchema extends SchemaAction {
    constructor({ schema, formattedPath, transformedPath }) {
        super(schema);
        this.formattedPath = formattedPath;
        this.transformedPath = transformedPath;
    }
}
