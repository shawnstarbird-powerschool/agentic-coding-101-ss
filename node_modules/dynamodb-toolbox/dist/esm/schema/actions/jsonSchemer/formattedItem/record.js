import { getFormattedValueJSONSchema } from './attribute.js';
export const getFormattedRecordJSONSchema = (schema) => ({
    type: 'object',
    propertyNames: getFormattedValueJSONSchema(schema.keys),
    additionalProperties: getFormattedValueJSONSchema(schema.elements)
});
