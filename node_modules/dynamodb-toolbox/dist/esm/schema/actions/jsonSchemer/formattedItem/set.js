import { getFormattedValueJSONSchema } from './attribute.js';
export const getFormattedSetJSONSchema = (schema) => ({
    type: 'array',
    items: getFormattedValueJSONSchema(schema.elements),
    uniqueItems: true
});
