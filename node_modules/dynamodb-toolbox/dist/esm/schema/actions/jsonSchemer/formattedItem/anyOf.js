import { getFormattedValueJSONSchema } from './attribute.js';
export const getFormattedAnyOfJSONSchema = (schema) => ({
    anyOf: schema.elements.map(element => getFormattedValueJSONSchema(element))
});
