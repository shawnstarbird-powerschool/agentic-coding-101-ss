import { any } from '../../../../schema/any/index.js';
/**
 * @debt feature "handle transforms, defaults, links & validators"
 */
export const fromAnySchemaDTO = ({ keyDefault, putDefault, updateDefault, keyLink, putLink, updateLink, transform, ...props }) => {
    keyDefault;
    putDefault;
    updateDefault;
    keyLink;
    putLink;
    updateLink;
    transform;
    return any(props);
};
