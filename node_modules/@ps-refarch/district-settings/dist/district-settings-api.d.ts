import { SecretsManagerClient } from '@aws-sdk/client-secrets-manager';
type HTTPMethod = 'get' | 'post';
/**
 * Configuration properties for making API calls to District Settings service.
 */
export interface CallProps {
    /**
     * Our namespace - dev, int, stgus, produs, etc.
     */
    namespace: string;
    /**
     * Shared service environment - int, stgus, produs, etc.
     */
    sharedServiceEnv: string;
    /**
     * Whether production or not.
     */
    isProduction: boolean;
    /**
     * Method to call. Defaults to METHOD_GET.
     */
    method?: HTTPMethod;
    /**
     * District settings path, e.g. "/district-settings/districts/12341234-1234-1234-123412341234/products"
     */
    path: string;
    /**
     * Data to send. Required if it's a POST, PUT, etc.
     */
    data?: any;
    /**
     * Optional logical client id.
     * @default If not passed, as with other auth0 creds, `common` is used
     */
    logicalClientId?: string;
    /**
     * Optional name for token cache table used by application. If not passed, it's computed from the
     * namespace. This was added to allow applications to have a token cache table with a fully
     * system-generated name, instead of forcing them to adhere to an enforced physical table name
     * pattern.
     */
    tokenCacheTableName?: string;
}
/** *********  Schemas of common API responses ************** */
/**
 * Represents a product enabled for a district in PowerSchool ecosystem.
 * Contains product details like name, URLs, and configuration settings.
 */
export interface DistrictProduct {
    productShortName: string;
    redirectUrl: string;
    uid?: string;
    productName?: string;
    productUid?: string;
    productFavicon?: string;
    productIsAppswitcherApp?: 0 | 1;
    isSsoEnabled?: 0 | 1;
    staffRedirectUrl?: string;
    teacherRedirectUrl?: string;
    studentRedirectUrl?: string;
    parentRedirectUrl?: string;
    displayOrder?: number;
    auth0ClientId?: string;
}
/**
 * Contains core information about a PowerSchool district.
 * Includes identifiers, location information, and authentication details.
 */
export interface DistrictInformation {
    uid: string;
    name: string;
    subdomain: string;
    city: string;
    state: string;
    country: string;
    salesforceId: string;
    organizationType: string;
    organizationTypeId: string;
    createdBy: string;
    updatedBy: string;
    auth0OrganizationId: string;
}
/**
 * Response type for the district products API endpoint.
 * Contains an array of products enabled for a specific district.
 */
export type GetDistrictSettingsDistrictsUidProductsResponse = DistrictProduct[];
export type GetDistrictSettingsDistrictsUidInformationResponse = DistrictInformation;
/**
 * Removes a trailing slash from a URL if present.
 *
 * @param url - The URL to process
 * @returns The URL without a trailing slash
 */
export declare function stripEndingSlash(url: string): string;
/**
 * Constructs the root URL for District Settings API based on environment.
 *
 * @param props - Environment configuration
 * @param props.isProduction - Whether to use production URLs
 * @param props.sharedServiceEnv - The shared service environment identifier
 * @returns The root URL for District Settings API
 */
export declare function getRootUrl(props: {
    isProduction: boolean;
    sharedServiceEnv: string;
}): string;
export interface AuthResults {
    access_token: string;
    expires_in: number;
    token_type: string;
}
/**
 * Retrieves an authentication token for District Settings API.
 *
 * Obtains a bearer token for authenticating with the District Settings API
 * using the configured M2M credentials for the specified environment.
 *
 * @param props - Token configuration properties
 * @returns Promise resolving to authentication results containing the access token
 */
export declare function getBearerToken(props: Pick<CallProps, 'logicalClientId' | 'namespace' | 'isProduction' | 'sharedServiceEnv' | 'tokenCacheTableName'>): Promise<AuthResults>;
/**
 * Makes an authenticated API call to the District Settings service.
 *
 * @template ResponseType - The expected response type
 * @param props - Call configuration including path, method, and authentication details
 * @returns Promise resolving to the response data of the specified type
 * @throws Error if the API call fails or returns a non-200 status code
 */
export declare function callDistrictSettings<ResponseType>(props: CallProps): Promise<ResponseType>;
/**
 * Get basic district information and enabled products for a district.
 * @param props properties
 * @returns
 */
/**
 * Retrieves comprehensive information about a district and its products.
 *
 * Makes two API calls to District Settings service to fetch:
 * 1. The list of enabled products for the district
 * 2. Basic information about the district itself
 *
 * @param props - Configuration properties for the API calls
 * @returns Promise resolving to district products and information, or undefined if the calls fail
 */
export declare function getDistrictInformation(props: {
    /**
     * Namespace (dev, int, stgus, produs)
     */
    namespace: string;
    /**
     * True if production
     */
    isProduction: boolean;
    /**
     * District UID to look up
     */
    districtUid: string;
    /**
     * Shared service environment (int, stgus, produs). Usually the same as namespace, except
     * for dev.
     */
    sharedServiceEnv: string;
    /**
     * Optional logical client id. Only if multiple unrelated MFEs in the same AWS account.
     */
    logicalClientId?: string;
    /**
     * Optional name for token cache table used by application. If not passed, it's computed from the
     * namespace. This was added to allow applications to have a token cache table with a fully
     * system-generated name, instead of forcing them to adhere to an enforced physical table name
     * pattern.
     */
    tokenCacheTableName?: string;
}): Promise<{
    districtProducts: DistrictProduct[];
    districtInformation: DistrictInformation;
} | undefined>;
export declare function reset(): void;
export declare function setDistrictSettingsAPIClients(clients: {
    smClient: SecretsManagerClient;
}): void;
export {};
