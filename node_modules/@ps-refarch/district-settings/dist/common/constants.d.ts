import { Runtime } from "aws-cdk-lib/aws-lambda";
/**
 * Default runtime for Lambda functions in the district-settings package.
 *
 * Currently set to Node.js 22.x which provides the latest stable Node.js features
 * while maintaining compatibility with AWS Lambda.
 */
export declare const DEFAULT_LAMBDA_VERSION: Runtime;
