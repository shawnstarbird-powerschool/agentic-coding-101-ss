import middy from '@middy/core';
import { Context, EventBridgeEvent } from 'aws-lambda';
import { TenantsEventDetail, TenantsEventType } from './district-event';
/**
 * Handler to just echo on a webhook call from District Settings service.
 * @param event
 * @param context
 */
/**
 * Sample event handler that logs district events to the console.
 *
 * This Lambda function demonstrates how to consume events from the district settings
 * event bus. It simply logs the received event to the console without further processing.
 * This can be used as a starting point for implementing custom event handlers.
 *
 * @param event - The EventBridge event containing district event details
 * @param context - Lambda execution context (unused)
 * @returns Promise that resolves when the event has been processed
 */
export declare const lambdaHandler: (event: EventBridgeEvent<TenantsEventType, TenantsEventDetail>, context?: Context) => Promise<void>;
export declare const handler: middy.MiddyfiedHandler<EventBridgeEvent<TenantsEventType, TenantsEventDetail>, void, Error, Context>;
