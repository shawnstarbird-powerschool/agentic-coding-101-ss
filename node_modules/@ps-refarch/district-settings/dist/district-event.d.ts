import { DistrictProduct } from './district-settings-api';
/**
 * Details of a district event sent through EventBridge.
 *
 * Contains information about the district, its products, and optionally client credentials.
 */
export interface TenantsEventDetail {
    districtUid: string;
    districtName: string;
    districtProducts?: DistrictProduct[];
    clientId?: string;
    clientSecret?: string;
}
/**
 * Event type identifier for when a district is added to the system.
 * Used as the DetailType in EventBridge events.
 */
export declare const ADD_DISTRICT_EVENT = "addDistrict";
/**
 * Event type identifier for when a district is removed from the system.
 * Used as the DetailType in EventBridge events.
 */
export declare const REMOVE_DISTRICT_EVENT = "removeDistrict";
/**
 * Event type identifier for when a district's information is updated.
 * Used as the DetailType in EventBridge events.
 */
export declare const UPDATE_DISTRICT_EVENT = "updateDistrict";
/**
 * Event type identifier for when a product is added to a district.
 * Used as the DetailType in EventBridge events.
 */
export declare const ADD_PRODUCT_EVENT = "addProduct";
/**
 * Event type identifier for when a product is removed from a district.
 * Used as the DetailType in EventBridge events.
 */
export declare const REMOVE_PRODUCT_EVENT = "removeProduct";
/**
 * Union type representing all possible district event types.
 * Used for type-safe handling of district events.
 */
export type TenantsEventType = typeof ADD_DISTRICT_EVENT | typeof REMOVE_DISTRICT_EVENT | typeof UPDATE_DISTRICT_EVENT | typeof ADD_PRODUCT_EVENT | typeof REMOVE_PRODUCT_EVENT;
