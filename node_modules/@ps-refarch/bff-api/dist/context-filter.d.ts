/**
 * Filter to apply. Right now it's just a simple string array, which specifies which
 * base elements to include in the result.
 */
export interface ContextFilterSpec {
    /**
     * List of base elements.
     */
    include: string[];
}
export declare function validateFilter(spec: ContextFilterSpec | undefined): void;
export type ContextData = {
    [key: string]: any;
};
/**
 * Simple context filter function.
 * @param data Source data to filter.
 * @param spec Filtering spec.
 */
export declare function filter(data: ContextData, spec: ContextFilterSpec | undefined): ContextData;
