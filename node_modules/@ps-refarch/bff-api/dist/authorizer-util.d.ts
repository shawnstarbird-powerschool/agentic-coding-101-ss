import { Logger } from '@aws-lambda-powertools/logger';
export declare const serviceName: string;
export declare const logger: Logger;
export declare class UnauthorizedError extends Error {
    constructor(message: string);
}
export declare class ConfigurationError extends Error {
    constructor(message: string);
}
/**
 * List of scopes. Each scope is a string.
 */
export type ScopeList = string[];
/**
 * Map of API patterns to scopes. The key is the API pattern, and the value is a list of scopes.
 *
 * Here's an example:
 *
 * {"/api1":["my.scope.1","my.scope.2"], "/api/*":["other.apis.scope"]}
 */
export interface ScopeMap {
    [apiPattern: string]: ScopeList;
}
/**
 * Parse the scope spec. The format of the spec is a JSON format value like:
 *
 * {"/api1":["my.scope.1","my.scope.2"], "/api/*":["other.apis.scope"]}
 *
 * @param apiScopes spec in format above
 * @returns
 */
export declare function parseScopeSpec(apiScopes: string): ScopeMap;
export declare function getScopeSpec(overrideScopes?: string): ScopeMap;
export declare function validateScopeSpecAgainstScope(props: {
    /**
     * Method and URI, like "GET /api/blah"
     */
    methodAndUri: string;
    /**
     * The scopes to validate against. These are the scopes defined as required in the API spec.
     */
    scopes?: ScopeMap;
    /**
     * All scopes for user. This is an array or space-separated string of scopes.
     */
    allScopes: string | string[] | undefined;
    /**
     * By default, it's an "OR" operation. If this is true, do an "AND" instead.
     */
    and?: boolean;
}): Promise<void>;
export declare function authorizerUtilReset(): void;
