import { MFEDomain } from '@ps-refarch/cdk-utils';
import { Construct } from 'constructs';
export interface ApiDocViewerProps {
    /**
     * This is the normal backend domain name setup for the project. A new one for the API docs
     * viewer will be created based on this one.
     */
    domain: MFEDomain;
    /**
     * Path to static API documentation. If not supplied, one included with bff-api will be used.
     */
    swaggerPath?: string;
}
export declare class ApiDocViewer extends Construct {
    domain: MFEDomain;
    /**
     * Public constructor.
     * @param scope parent construct
     * @param id local id
     * @param props specific properties
     */
    constructor(scope: Construct, id: string, props: ApiDocViewerProps);
}
