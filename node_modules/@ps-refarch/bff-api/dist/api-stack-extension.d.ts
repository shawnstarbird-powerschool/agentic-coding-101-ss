import { IResource, IRestApi, MethodOptions } from 'aws-cdk-lib/aws-apigateway';
import { Construct } from 'constructs';
import { AbstractApi, ApiPropsCommonBase, ApiTreeNode, Authorizers, SessionAuthorizerProps, UUAuthorizerEnvVars, UUAuthorizerProps } from '.';
import { OpenApiBuilder } from './openapi';
import { NodejsFunctionProps } from 'aws-cdk-lib/aws-lambda-nodejs';
import { MFEDomain } from '@ps-refarch/cdk-utils';
export interface ApiResource {
    path: string;
    resourceId: string;
}
export type ApiStackExPropsAny = ApiStackExPropsUnifiedUser | ApiStackExPropsUnifiedUserOld | ApiStackExPropsSession | ApiStackExPropsIAM | ApiStackExPropsCommon;
export type ApiStackExPropsUnifiedUser = ApiStackExPropsCommon & {
    /**
     * Configuration properties for the UU Lambda authorizer. Required if an authorizer
     * function is not pre-created.
     */
    uuAuthorizerProps: UUAuthorizerProps;
};
export type ApiStackExPropsUnifiedUserOld = ApiStackExPropsCommon & {
    /**
     * Environment variables for the UU Lambda authorizer. Required if an authorizer
     * function is not pre-created.
     *
     * @deprecated use uuAuthorizerProps instead
     */
    lambdaAuthorizerEnvVars: UUAuthorizerEnvVars;
};
export type ApiStackExPropsSession = ApiStackExPropsCommon & {
    /**
     * Authorizer properties for the session base lambda authorizer.  Required
     * if an authorizer function is not pre-created.
     */
    sessionAuthorizerProps: SessionAuthorizerProps;
};
export type ApiStackExPropsIAM = ApiStackExPropsCommon & {
    /**
     * List of AWS account IDs that are allowed to call this API. Required if we want to
     * use IAM authorizer. Note that you have to include your own account ID if you want
     * to call it.
     * @example accountAccounts: [this.account]
     */
    accessAccounts: string[];
};
export interface ApiStackExPropsCommon extends ApiPropsCommonBase {
    /**
     * The ID of the API Gateway RestApi.  This would be from the api property from the bffApi.
     * @see ApiStackExtension constructor for an example.
     */
    restApiId: string;
    /**
     * The name of the API Gateway RestApi. This would be from the api property from the bffApi.
     */
    restApiName?: string;
    /**
     * The resource ID of the root resource. This would be from the api property from the bffApi.
     * @see ApiStackExtension constructor for an example.
     */
    rootResourceId: string;
    /**
     * The namespace from the MFEDomain.
     */
    namespace: string;
    /**
     * Existing api resources indexed by path.
     *
     * This is captured by the on onAddResource callback in the bffApi or another bffStackExtensionApi.
     */
    existingApiResources?: Record<string, ApiResource>;
    /**
     * OpenApi from the BffApi.
     *
     * This is used to add to the openApi documentation.
     */
    openApi?: OpenApiBuilder;
    /**
     * The MFEDomain object, from @ps-refarch/cdk-utils.
     */
    domain?: MFEDomain;
    /**
     * If this is true, the API documenation will be public. If false, it will be private
     * behind the VPN. Normally this should be left as false, unless you have a specific
     * reason for exposing the API documentation to the public. And then, pre-prod documentation
     * should never be exposed; in fact, if it's not production and this is true, it will
     * throw an error.
     * @default false
     */
    allowPublicDocAccess?: boolean;
    /**
     * Additional IPs to add to the allowed IP set. This can be used to add additional IPs to allow access from
     * in addition to the default PowerSchool IPs. These can be either a plain IP address or an IP range
     * in CIDR notation (e.g. 1.2.3.4/30).
     */
    additionalIPs?: string[];
    /**
     * Enables building in the API documentation viewer. Only valid if we are building
     * an OpenApi doc.
     */
    buildApiDocViewer?: boolean;
}
export declare class ApiStackExtension extends AbstractApi {
    /**
     * The actual API we built. You might want to use it e.g. in a CfnOutput.
     */
    api: IRestApi;
    protected _props: ApiStackExPropsCommon;
    /**
     * Constructs an extension to the BFFApi that needs to exist in another stack.  Usually for resource reasons.
     * This is configured in a similar way to the BFFApi.
     * @param scope construct scope
     * @param id construct id
     * @param props construct props
     *
     * @example
     *
     * class RootStack extends Stack {
     *   constructor(scope: Construct) {
     *     super(scope, 'integ-restapi-import-RootStack');
     *
     *     const domain = new MFEDomain(...);
     *     const methods = [];
     *     const resources: Record<string, ApiResource> = {}
     *
     *     const bffApi = new BFFApi(this, 'bffApi', {
     *       domain,
     *       ..., // bff api config
     *       disableAutoBuildOpenApi: true, // delay the creation of the apidocs.
     *       onAddResource: (resourcePath, resource) => {
     *         resources[resourcePath] = resource;
     *       },
     *       onAddMethod: (methodName, methodPath, methodLink) => {
     *         methods.push(methodLink);
     *       }
     *     });
     *
     *     const stack1 = new Stack1(this, {
     *       restApiId: bffApi.api.restApiId,
     *       rootResourceId: bffApi.api.restApiRootResourceId,
     *       namespace: domain.namespace,
     *       openApi: bffApi.openApi,
     *       resources,
     *     });
     *     const stack2 = new Stack2(this, {
     *       restApiId: bffApi.api.restApiId,
     *       rootResourceId: bffApi.api.restApiRootResourceId,
     *       namespace: domain.namespace,
     *       openApi: bffApi.openApi,
     *       resources,
     *     });
     *     methods.push(...stack1.methods);
     *     methods.push(...stack2.methods);
     *     new DeployStack(this, {
     *       restApiId: restApi.restApiId,
     *       methods: methods,
     *     });
     *   }
     * }
     *
     * interface ResourceNestedStackProps extends NestedStackProps {
     *   readonly restApiId: string;
     *
     *   readonly rootResourceId: string;
     *
     *   readonly namespace: string;
     *
     *   readonly resources: Record<string, ApiResource>;
     *
     *   readonly openApi: OpenApiBuilder;
     * }
     *
     * class Stack1 extends NestedStack {
     *   public readonly methods: Method[] = [];
     *
     *   constructor(scope: Construct, props: ResourceNestedStackProps) {
     *     super(scope, 'integ-restapi-import-Stack1', props);
     *
     *     this.methods = [];
     *
     *     const api = new BFFStackExtensionApi(this, 'BffStackEx1', {
     *       restApiId: props.restApiId,
     *       openApi: props.openApi,
     *       rootResourceId: props.rootResourceId,
     *       namespace: props.namespace,
     *       existingApiResources: props.resources,
     *       ... // BFF Ex config,
     *       disableAutoBuildOpenApi: true, // delay the creation of the apidocs.
     *       onAddResource: (resourcePath, resource) => {
     *         props.resources[resourcePath] = resource;
     *       },
     *       onAddMethod: (methodName, methodPath, methodLink) => {
     *         this.methods.push(methodLink);
     *       }
     *     });
     *
     *   }
     * }
     *
     * class Stack2 extends NestedStack {
     *   public readonly methods: Method[] = [];
     *
     *   constructor(scope: Construct, props: ResourceNestedStackProps) {
     *     super(scope, 'integ-restapi-import-Stack2', props);
     *
     *     this.methods = [];
     *
     *     const api = new BFFStackExtensionApi(this, 'BffStackEx2', {
     *       restApiId: props.restApiId,
     *       openApi: props.openApi,
     *       rootResourceId: props.rootResourceId,
     *       namespace: props.namespace,
     *       existingApiResources: props.resources,
     *       ... // BFF Ex config,
     *       disableAutoBuildOpenApi: false, // create api documentation.
     *       onAddResource: (resourcePath, resource) => {
     *         props.resources[resourcePath] = resource;
     *       },
     *       onAddMethod: (methodName, methodPath, methodLink) => {
     *         this.methods.push(methodLink);
     *       }
     *     });
     *   }
     * }
     *
     * interface DeployStackProps extends NestedStackProps {
     *   readonly restApiId: string;
     *
     *   readonly methods?: Method[];
     * }
     *
     * class DeployStack extends NestedStack {
     *   constructor(scope: Construct, props: DeployStackProps) {
     *     super(scope, 'integ-restapi-import-DeployStack', props);
     *
     *     const deployment = new Deployment(this, 'Deployment', {
     *       api: RestApi.fromRestApiId(this, 'RestApi', props.restApiId),
     *     });
     *     if (props.methods) {
     *       for (const method of props.methods) {
     *         deployment.node.addDependency(method);
     *       }
     *     }
     *     new Stage(this, 'Stage', { deployment });
     *   }
     * }
     *
     * new RootStack(new App());
     */
    constructor(scope: Construct, id: string, props: ApiStackExPropsAny);
    protected getRoot(): IResource;
    protected getNamespace(): string;
    /**
     * Build a node from the API tree. Nodes can consist of methods and other nodes.
     * @param resource the API Gateway Resource to build on
     * @param uri the full URI of the current node
     * @param node the current node in the API tree
     * @param props properties to add into the NodejsFunctions created
     * @param methodOptions add method options and nested authorizer, if available
     * @param authorizers authorizers to add to the API Gateway
     */
    buildNode(resource: IResource, uri: string[], node: ApiTreeNode, props: NodejsFunctionProps, methodOptions: MethodOptions, authorizers: Authorizers): void;
    /**
     * After all the API methods added in, go ahead and build the API tree.
     * @param props properties to add into the NodejsFunctions created
     * When only aws integrations are desired, pass {}
     * @param methodOptions optional gateway methodOptions,
     * which will be applied to each method in this rest api.
     * If authorizer is already defined, add that in the nested prop.
     */
    build(props: NodejsFunctionProps, methodOptions?: MethodOptions): void;
    buildOpenApi(): void;
}
/**
 * Api construct similar to serverless stack's Api construct. This is designed to
 * make it easy for teams to build APIs that have built-in behaviors and follow best
 * practices according to PowerSchool's NFRs.
 */
export declare class BFFStackExtensionApi extends ApiStackExtension {
}
