import { JsonSchema, ModelOptions } from 'aws-cdk-lib/aws-apigateway';
export declare const updateApiRefs: (obj: unknown | Record<string, unknown>, restApi: string) => void;
export declare const apiToSpec: (obj: unknown | Record<string, unknown>) => void;
export declare const getSchemas: (tsconfigPath: string, modelPath: string, restApi: string) => {
    [key: string]: ModelOptions;
};
/**
 * Extract the class name from the model name (as used in API Gateway and in URLs)
 * @param modelName The name of the model, which will end in "Model"
 */
export declare function modelToClassName(modelName: string): string;
/**
 * Extract the list of values from a map of schemas
 * @param map The source map, where each value is a schema
 */
export declare function mapToSchema(map: JsonSchema['properties'] | undefined): JsonSchema[];
/**
 * Walk the tree of sub-schemas, calling a handler for each reference to another model.
 * @param nodeSchemas     A list of JsonSchema objects to walk
 * @param parentClassName The parent class name
 * @param handler         A function to call for each reference to another model
 */
export declare function actOnModelReferences(nodeSchemas: JsonSchema[], parentClassName: string, handler: (parentClassName: string, childClassName: string) => void): void;
