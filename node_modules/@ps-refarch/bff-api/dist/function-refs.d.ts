import { AwsIntegration, Method, MethodOptions } from 'aws-cdk-lib/aws-apigateway';
import { FunctionOptions, IFunction } from 'aws-cdk-lib/aws-lambda';
import { NodejsFunction, NodejsFunctionProps } from 'aws-cdk-lib/aws-lambda-nodejs';
import { Construct } from 'constructs';
import { LambdaFunctionAlarmConfigCallback } from '.';
import { LambdaFunctionAlarmConfig } from './alarm-configs';
import { SessionTableInfo } from './session-table';
export type AnyFunctionRef = LocalTSFunctionRef | LocalUserDefinedFunctionRef | ProxyFunctionRef | MockFunctionRef;
export type LocalFunctionRef = LocalTSFunctionRef | LocalUserDefinedFunctionRef;
export type OnDefineCallback = (props: {
    functionOptions: FunctionOptions;
    scope: Construct;
}) => IFunction;
export type LocalUserDefinedFunctionRef = FunctionRef & {
    onDefine: OnDefineCallback;
};
export type LocalTSFunctionRef = FunctionRef & {
    /**
     * The path to the function definition file, including the file name (without
     * the .ts extension). Base directory is either the passed in baseDir (if any)
     * or DEFAULT_BASE_DIR.
     */
    path: string;
    /**
     * If isAbsolutePath is true, the path is an absolute pathname. Else, it's relative
     * to the configured base.
     */
    isAbsolutePath?: boolean;
    /**
     * Name of exported function within the file.
     */
    handler: string;
};
export type ProxyFunctionRef = FunctionRef & {
    /**
     * Set the proxy flag if you want it to be a proxy.
     *
     * @deprecated don't use, it doesn't work
     */
    proxy: true;
};
export type MockFunctionRef = FunctionRef & {
    mock: true;
    payload: any;
};
export interface CustomMethodResponse {
    /**
     * HTTP status code for a response.
     */
    statusCode: string;
    /**
     * Response Models for the status code.
     * Ex. 'application/json': 'GetProfileResponse'
     */
    responseModels: {
        [key: string]: string;
    };
}
/**
 * Type of authentication to use if it's not unauthenticated.
 */
export declare enum AuthenticationType {
    Session = "sessionAuthorizer",
    UU = "uuAuthorizer",
    IAM = "iamAuthorizer"
}
export type BaseRef = {
    /**
     * Optional method options for API Gateway.
     */
    methodOptions?: MethodOptions;
    /**
     * If true, set up CORS, especially the preflights, for the method. NOTE: this also requires
     * the handler to add CORS headers since we're using Lambda proxy integration. If your lambda
     * handlers are using middy, you can use the `@middy/http-cors` package to help with this. You
     * can optionally use '*', and then it will return ACAO header = '*' (use with caution!)
     *
     * @default false
     */
    cors?: boolean | '*';
    headers?: string;
    /**
     * If true, this particular method will be unauthenticated. All others go
     * thru the configured Authorizer.
     */
    unauthenticated?: boolean;
    /**
     * If it's not unauthenticated, AND if there are more than one type of authorizer defined,
     * you need to tell it which authorizer to use. If there is only one, it will use that one.
     */
    authenticationType?: AuthenticationType;
    /**
     * If passed, specifically allow credentials in CORS preflight.
     */
    corsAllowPreflightCredentials?: boolean;
    /**
     * If true, this method's function needs to call Unified User APIs.
     */
    callsUnifiedUser?: boolean;
    /**
     * The description of the endpoint.
     * Used for API documenatation.
     */
    description?: string;
    /**
     * A list of header parameters, used in the creation of the OpenApi json.
     */
    headerParameters?: string[];
    /**
     * A list of query parameters, used in the creation of the OpenApi json.
     */
    queryParameters?: string[];
    /**
     * A list of required parameters, used in the creation of the OpenApi json.
     */
    requiredParameters?: string[];
    /**
     * A map of content type and response models that are used in the request.
     * Ex. 'application/json': 'GetProfileRequest'
     * Used in the creation of the OpenApi json.
     */
    requestModels?: {
        [key: string]: string;
    };
    /**
     * A list of method responses, used in the creation of the OpenApi json.
     */
    methodResponses?: CustomMethodResponse[];
    /**
     * Optional override for generating OpenApi documentation. If not passed,
     * it will use the API-wide setting. If you set this to true but you're
     * not generating OpenApi, it will be ignored.
     * @default true
     */
    enableOpenApi?: boolean;
};
export interface SessionAuthorizerInfo extends SessionTableInfo {
    lambdaApiAuthorizer: NodejsFunction;
    identitySources: string[];
}
export interface UUAuthorizerInfo {
    lambdaApiAuthorizer: NodejsFunction;
    identitySources: string[];
}
export type FunctionRef = BaseRef & {
    /**
     * Optional set of props to supplement/override NodejsFunction properties.
     */
    props?: NodejsFunctionProps;
    /**
     * If true, grant the necessary access to the session table. Only valid if you are
     * defining the session authorizer (we can relax this if needed). If you set this,
     * your lambda will receive the following environment variables:
     *
     * * PS_SESSION_TABLE: the name of the session table
     * * PS_SESSION_TABLE_ROLE_ARN: ARN of role your code needs to assume
     */
    accessesSessionTable?: boolean;
    /**
     * If you have monitoring enabled, this is an option override Lambda Function alarm config.
     * Can be overridden per-function, but it seems unlikely you would want this.
     */
    lambdaFunctionAlarmConfig?: LambdaFunctionAlarmConfig | LambdaFunctionAlarmConfigCallback;
    /**
     * An optional per-function alarm latency threshold. This can be used to calculate where various
     * latency alarms should be set. If not set, alerting should use a global value.
     */
    alarmLatencyThreshold?: number;
    /**
     * Allow a "before lambda add" function to be executed. The function can modify the environment if it
     * wants to.
     * @param props various properties that may be set
     * @returns either a boolean saying whether to add the function, or void which is same as returning true
     */
    onBeforeAdd?: (props: {
        env: {
            [key: string]: string;
        };
        sessionAuthorizerInfo?: SessionAuthorizerInfo;
    }) => void | boolean;
    /**
     * If passed, call when the lambda function is added to this resource.
     */
    onAdd?: (fn: NodejsFunction, method: string, path: string, methodLink: Method) => void;
    /**
     * More flexible version of onAdd that deals with IFunctions.
     * @param props
     */
    onAddV2?: (props: {
        fn: IFunction;
        method: string;
        path: string;
        methodLink: Method;
    }) => void;
};
export type AwsIntegrationRef = BaseRef & {
    /**
     * Mandatory AwsIntegration instance to be used with api gateway integration.
     */
    integration: AwsIntegration;
};
export declare function isProxyFunctionRef(ref: any): ref is ProxyFunctionRef;
export declare function isLocalTSFunctionRef(ref: any): ref is LocalTSFunctionRef;
export declare function isLocalUserDefinedFunctionRef(ref: any): ref is LocalUserDefinedFunctionRef;
export declare function isMockFunctionRef(ref: any): ref is MockFunctionRef;
export declare function isAwsIntegrationRef(ref: any): ref is AwsIntegrationRef;
