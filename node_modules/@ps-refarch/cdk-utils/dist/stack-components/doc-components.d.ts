import { Construct } from 'constructs';
import { MFEDomain } from '../mfe-domain';
export interface ApiDocsProps {
    /**
     * Domain name setup for API docs frontend. Note that it should NOT be the same as the one
     * for the "normal" FrontEnd of the project: this one is specifically for the API docs. Thus,
     * normally it should set the `domainNamePrefix` property.
     */
    domain: MFEDomain;
    /**
     * Environment name.
     */
    envName: string;
    /**
     * Path to static API documentation. If not supplied, one included with cdk-utils will be used.
     */
    swaggerPath?: string;
}
export declare class ApiDocs extends Construct {
    /**
     * Public constructor.
     * @param scope parent construct
     * @param id local id
     * @param props specific properties
     */
    constructor(scope: Construct, id: string, props: ApiDocsProps);
    /**
     * Construct a FrontEnd according to props, async version.
     * @param scope parent construct
     * @param id local id
     * @param props specific properties
     * @returns newly constructed object
     * @deprecated use standard `new ApiDocs(...)` instead
     */
    static construct(scope: Construct, id: string, props: ApiDocsProps): Promise<ApiDocs>;
    /**
     * Initialize new FrontEnd object.
     * @param frontend constructed object
     * @param props specific properties
     */
    private static init;
}
