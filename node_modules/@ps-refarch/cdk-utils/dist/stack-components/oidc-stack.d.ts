import { Stack, StackProps } from 'aws-cdk-lib';
import { Construct } from 'constructs';
import { GitLabOidcConstruct, GitLabOidcConstructProps } from './gitlab-oidc-construct';
export interface GitlabOidcStackProps extends StackProps {
    /**
     * Repos to allow to deploy.
     */
    oidcEnabledRepos?: Omit<GitLabOidcConstructProps, 'isProduction'>;
    /**
     * Whether this is a production stack or not. The effect of this is to limit the branches that can be
     * deployed to main only. If you don't set this to true, then all branches can be deployed.
     */
    isProduction?: boolean;
}
/**
 * The basic stack for Gitlab OIDC. If you want additional policies added to the OIDC role, you will need
 * to define your own stack and add them there.
 */
export declare class GitlabOidcStack extends Stack {
    oidc: GitLabOidcConstruct;
    constructor(scope: Construct, id: string, props: GitlabOidcStackProps);
}
