import { PositionalConstraint, Rule, Statement, TextTransformationType } from '@aws-sdk/client-wafv2';
import { CustomResource } from 'aws-cdk-lib';
import { Runtime } from 'aws-cdk-lib/aws-lambda';
import { NodejsFunction } from 'aws-cdk-lib/aws-lambda-nodejs';
import { CfnIPSet } from 'aws-cdk-lib/aws-wafv2';
import { Provider } from 'aws-cdk-lib/custom-resources';
import { Construct } from 'constructs';
export declare const DEFAULT_ROOT_ID = "CDKWAFRules";
export type WebACLType = 'REGIONAL' | 'CLOUDFRONT';
export type RuleSourceConfig = Omit<Rule, 'Name' | 'Priority' | 'VisibilityConfig'>;
export type RuleSource = Record<string, RuleSourceConfig & {
    scope: WebACLType;
}>;
/**
 * Interface for overriding the default WAF settings.
 *
 * This interface allows you to specify custom WebACL names for CloudFront and Regional distributions.
 * Use this if you need to override the default WebACL settings assumed by the construct.
 */
export interface OverrideWAFDefaults {
    /**
     * The name of the CloudFront WebACL to use. If not set, the default CloudFront WebACL
     * according to the naming conventions CloudOps uses will be used.
     */
    cloudFrontWebACLName?: string;
    /**
     * The name of the Regional WebACL to use. If not set, the default Regional WebACL
     * according to the naming conventions CloudOps uses will be used.
     */
    regionalWebACLName?: string;
}
export interface WAFRulesProps {
    /**
     * The class key, which is the second highest level of the hierarchy after the root key.
     * This is typically set by another construct that uses this construct rather than
     * being set directly by the end user stack.
     */
    classKey: string;
    /**
     * The application key, which is the third-highest level of the hierarchy after the root key
     * and the class key. This is typically set by the end user stack, and should be unique
     * for this AWS account. NOTE this CANNOT be the MFE id or equivalent backend service id,
     * because it won't be unique across the account; better is the DNS name itself.
     */
    appKey: string;
    /**
     * A list of IPSet dependencies that this construct has. This is used to ensure that the WAF
     * custom resource is created after all of the dependencies are created. This is typically
     * used if you are creating an IPSet that is used by the WAF rules.
     * @default there is none, you need to pass an empty array if you don't have any; this is
     *   to ensure that you are aware of passing in any dependencies that you have created
     */
    dependencyArns: string[];
    /**
     * "Our" rules. These are the rules that we want to apply to the WAF. When an update
     * is made, we will compare the rules that we have with the rules that are currently
     * applied to the WAF. If there are differences, we will update the WAF to match.
     * You can either add them all at once, or you can add them incrementally.
     */
    rules?: RuleSource;
    /**
     * The root key for the construct. This is used to namespace the WAF rule. You can override
     * this, but it's not recommended. Normally, you should just leave the default.
     * @default CDKWAFRules
     */
    rootKey?: string;
    /**
     * If set, this will override the default WAF settings assumed by the construct. Hopefully
     * you don't need to do this, but if you are getting failures in deployment, you can set this
     * to override the defaults.
     */
    overrideWAFDefaults?: OverrideWAFDefaults;
    /**
     * Optional runtime version of the Lambda function.
     */
    runtime?: Runtime;
}
/**
 * Clean the appKey by taking any characters that are not word characters or dashes and
 * substituting dashes for them. If there are consecutive characters like this, only
 * one dash will be substituted. Also convert it to lowercase.
 * @param appKey
 */
export declare function cleanAppKey(appKey: string): string;
/**
 * Helper function to get the PowerSchool IP set. This can be used to make rules like "must be from a PowerSchool host".
 * @param scope parent scope
 * @param props config props
 * @returns the IPSet
 */
export declare function getPowerSchoolIPSet(scope: Construct, props: {
    /**
     * The scope of the IPSet. This can be either REGIONAL or CLOUDFRONT.
     */
    scope: WebACLType;
    /**
     * The class key, which is the second highest level of the hierarchy after the root key.
     * This is typically set by another construct that uses this construct rather than
     * being set directly by the end user stack.
     */
    classKey: string;
    /**
     * The application key, which is the third-highest level of the hierarchy after the root key
     * and the class key. This is typically set by the end user stack, and should be unique
     * for this AWS account. Typically applications might use the MFE id or equivalent
     * backend service id.
     */
    appKey: string;
    /**
     * Additional IPs to add to the IP set. This can be used to add additional IPs to allow access from
     * in addition to the default PowerSchool IPs. These can be either a plain IP address or an IP range
     * in CIDR notation (e.g. 1.2.3.4/30).
     */
    additionalIPs?: string[];
}): CfnIPSet;
/**
 * Helper function to make an AND statement. This can be used to make rules like "must be from a PowerSchool host and must be a GET request".
 * @param statements
 * @returns
 */
export declare function andStatement(...statements: Statement[]): {
    AndStatement: {
        Statements: Statement[];
    };
};
/**
 * Helper function to make an OR statement. This can be used to make rules like "must be from a PowerSchool host or must be a GET request".
 * @param statements
 * @returns
 */
export declare function orStatement(...statements: Statement[]): {
    OrStatement: {
        Statements: Statement[];
    };
};
/**
 * Helper function to make a NOT statement. This can be used to make rules like "must not be from a PowerSchool host".
 * @param statement
 * @returns
 */
export declare function notStatement(statement: Statement): {
    NotStatement: {
        Statement: Statement;
    };
};
/**
 * Helper function to make an IPSet statement. This can be used to make rules like "must be from a PowerSchool host".
 * @param ipSet the IPSet to check against
 * @param matches true if the IPSet should match, false if it should not match
 * @returns the WAF rule statement
 */
export declare function ipSetStatement(ipSet: CfnIPSet, matches: boolean): {
    IPSetReferenceStatement: {
        ARN: string;
    };
} | {
    NotStatement: {
        Statement: Statement;
    };
};
/**
 * Helper function to make a host match statement. This can be used to make rules comparing the HTTP host.
 * @param domain
 * @returns the WAF rule statement
 */
export declare function hostMatchStatement(domain: string): {
    ByteMatchStatement: {
        PositionalConstraint: PositionalConstraint;
        SearchString: Uint8Array;
        FieldToMatch: {
            SingleHeader: {
                Name: string;
            };
        };
        TextTransformations: {
            Priority: number;
            Type: TextTransformationType;
        }[];
    };
};
/**
 * Helper function to make a URI path match statement. This can be used to make rules comparing
 * the URI path.
 * @param path the path to match
 * @param position the positional constraint (optional, default is 'EXACTLY')
 * @returns the WAF rule statement
 */
export declare function uriPathMatchStatement(path: string, position?: PositionalConstraint): {
    ByteMatchStatement: {
        PositionalConstraint: PositionalConstraint;
        SearchString: Uint8Array;
        FieldToMatch: {
            UriPath: Record<string, never>;
        };
        TextTransformations: {
            Priority: number;
            Type: TextTransformationType;
        }[];
    };
};
export declare function allowPathsStatement(props: {
    /** The set of paths to allow */
    paths: string[];
    /** The positional constraint to use for the paths, default is 'STARTS_WITH' */
    positionalConstraint?: PositionalConstraint;
    /** The text transformation type to use for the paths, default is 'NONE' */
    textTransformationType?: TextTransformationType;
}): {
    NotStatement: {
        Statement: {
            ByteMatchStatement: {
                FieldToMatch: {
                    UriPath: Record<string, never>;
                };
                PositionalConstraint: PositionalConstraint;
                SearchString: Uint8Array;
                TextTransformations: {
                    Priority: number;
                    Type: TextTransformationType;
                }[];
            };
        };
    };
}[];
/**
 * The construct that manages the WAF rules. This construct is responsible for managing the rules
 * attached to WebACLs in the account as created by CloudOps.
 */
export declare class WAFRules extends Construct {
    private scope;
    private id;
    private props;
    onEventHandlerFunction: NodejsFunction;
    customResourceProvider: Provider;
    resource: CustomResource;
    rules: RuleSource;
    constructor(scope: Construct, id: string, props: WAFRulesProps);
}
