import { Logger } from '@aws-lambda-powertools/logger';
import { WebACL } from '@aws-sdk/client-wafv2';
import middy from '@middy/core';
import { CloudFormationCustomResourceEvent, CloudFormationCustomResourceResponse } from 'aws-lambda';
import { WAFRulesProps, WebACLType } from '.';
export declare const serviceName = "waf-rules-custom-resource";
export declare const logger: Logger;
export interface WAFCustomResourceProps extends WAFRulesProps {
    ServiceToken: string;
}
export interface WebACLInfo {
    webACL: WebACL;
    lockToken: string;
}
export declare function findWebACL(fnProps: {
    scope: WebACLType;
    props: {
        overrideWAFDefaults?: {
            regionalWebACLName?: string;
            cloudFrontWebACLName?: string;
        };
    };
}): Promise<WebACLInfo>;
export declare const customResource: (event: CloudFormationCustomResourceEvent) => Promise<CloudFormationCustomResourceResponse>;
export declare const handler: middy.MiddyfiedHandler<CloudFormationCustomResourceEvent, CloudFormationCustomResourceResponse, Error, import("aws-lambda").Context>;
