import { IRole } from 'aws-cdk-lib/aws-iam';
import { IFunction } from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';
export interface SISReferenceProps {
    scope: string;
    namespace: string;
    sisReferenceAccount: string;
}
export type AccessRequest = 'integration-ro' | 'credentials-rw';
export declare class SISReference extends Construct {
    credentialRWRole: IRole;
    integrationsRORole: IRole;
    constructor(scope: Construct, id: string, props: SISReferenceProps);
    /**
     * Grant a lambda function access to the SIS reference roles. By default
     * R/W access to the credentials and R/O to the districts table are granted.
     * @param fn the function
     */
    grantCredentialAccess(fn: IFunction): void;
    /**
     * Grant a lambda function access to the SIS reference roles.
     * @param fn the function
     */
    grantAccess(fn: IFunction, access: AccessRequest[]): void;
}
