import { Construct } from 'constructs';
import { OverrideWAFDefaults } from '../waf-rules';
export interface APIDocsWAFProps {
    /**
     * The application key, which is the third-highest level of the hierarchy after the root key
     * and the class key. This is set by the end user stack, and should be unique
     * for this AWS account. NOTE this CANNOT be the MFE id or equivalent backend service id,
     * because it won't be unique across the account; better is the DNS name itself.
     */
    appKey: string;
    /**
     * The front end domain to restrict access to. If set, only PowerSchool IPs will be allowed to
     * access this domain. Note that this is the basic front end domain, and is assumed by default
     * to start with 'apidocs.'. If you need to specify a different domain, you can do so by passing
     * in `frontEndDomainPrefix` as well.
     */
    frontEndDomain?: string;
    /**
     * If set, this will be prepended to the front end domain. This is useful if you need to specify
     * a different prefix than the default 'apidocs.'. (Note you need to add a dot if you want one.
     * This is not added automatically because you may not want one. An empty string is a valid value.)
     * @default "apidocs."
     */
    frontEndDomainPrefix?: string;
    /**
     * The back end domain to restrict access to. If set, only PowerSchool IPs will be allowed to
     * access this domain.
     */
    backEndDomain?: string;
    /**
     * The URI path to restrict access to on the back end domain. This is useful if you need to
     * specify a different path than the default '/apidocs'.
     * @default "/apidocs"
     */
    backEndDomainSuffix?: string;
    /**
     * Additional IPs to add to the allowed IP set. This can be used to add additional IPs to allow access from
     * in addition to the default PowerSchool IPs. These can be either a plain IP address or an IP range
     * in CIDR notation (e.g. 1.2.3.4/30).
     */
    additionalIPs?: string[];
    /**
     * If set, this will override the default WAF rules.
     */
    overrideWAFDefaults?: OverrideWAFDefaults;
}
export declare const OUR_CLASS_KEY = "APIDocsWAF";
/**
 * A construct that sets up WAF rules for the APIDocs generated by the Api construct. This is used automatically
 * by the Api construct, but can also be used manually if needed.
 */
export declare class APIDocsWAF extends Construct {
    private scope;
    private id;
    private props;
    constructor(scope: Construct, id: string, props: APIDocsWAFProps);
}
