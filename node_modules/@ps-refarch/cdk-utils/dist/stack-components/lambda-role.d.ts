import { Role, RoleProps } from 'aws-cdk-lib/aws-iam';
import { Construct } from 'constructs';
export interface LambdaRoleProps extends Partial<RoleProps> {
    /**
     * Enable VPC access for the Lambda function(s).
     */
    enableVpc?: boolean;
}
/**
 * A basic role for Lambda functions. Simply initializes the role with the
 * normal Lambda presets that NodejsFunction would use. Useful for when you
 * want to share a role across multiple functions to prevent role explosion
 */
export declare class LambdaRole extends Role {
    constructor(scope: Construct, id: string, props: LambdaRoleProps);
}
