import { SISIntegrationAttributes } from './sis-credentials-util';
/**
 * Retrieves district integration information using the SIS hostname.
 *
 * This function queries the District DynamoDB table using the GSI1 global secondary index
 * to look up district information by hostname. It handles STS token expiration and retry logic.
 *
 * @param hostname - The SIS hostname to look up (e.g. "district123.powerschoolsis.com")
 * @param runEnv - Environment configuration
 * @param runEnv.namespace - Environment namespace (e.g. "dev", "int", "prod")
 * @param runEnv.sisReferenceAccount - AWS account ID for the SIS reference account
 * @returns Promise resolving to district integration details if found, undefined if not found
 * @throws Error if the base district record is missing or if AWS credentials cannot be obtained
 */
export declare function getSISIntegrationByHostname(hostname: string, runEnv: {
    namespace: string;
    sisReferenceAccount: string;
}): Promise<SISIntegrationAttributes | undefined>;
