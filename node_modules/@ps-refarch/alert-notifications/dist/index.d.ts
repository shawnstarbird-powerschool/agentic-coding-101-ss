import { Topic, TopicProps } from 'aws-cdk-lib/aws-sns';
import { Construct } from 'constructs';
import { Runtime } from "aws-cdk-lib/aws-lambda";
/**
 * Default Lambda runtime version used for the Slack notification Lambda function.
 * This constant is used when no runtime is explicitly specified in AlertNotifierProps.
 *
 * @default Runtime.NODEJS_22_X
 */
export declare const DEFAULT_LAMBDA_VERSION: Runtime;
/**
 * Configuration properties for the AlertNotifier construct.
 * These properties control how alert notifications are delivered to different channels (Slack, PagerDuty)
 * and customize the behavior of the alert notification system.
 */
export interface AlertNotifierProps {
    /**
     * Allows the App to pass in a Topic they have provisioned themselves to be used for delivering alert notifications.
     * Can be left undefined, in which case this construct will create an SNS Topic itself.
     *
     * Having a custom topic allows for more advanced SNS configurations or sharing a topic across multiple alert systems.
     *
     * @example
     * ```typescript
     * const existingTopic = new Topic(this, 'MyCustomTopic');
     * new AlertNotifier(this, 'Notifier', { topic: existingTopic, namespace: 'prod', region: 'us-east-1' });
     * ```
     */
    topic?: Topic;
    /**
     * Allows passing in of custom topic props for the SNS Topic created by this construct.
     * Only used when no pre-existing topic is provided.
     *
     * These props are passed directly to the SNS Topic constructor and can include custom configurations
     * like display name, subscription policies, or encryption settings.
     *
     * @example
     * ```typescript
     * new AlertNotifier(this, 'Notifier', {
     *   topicProps: {
     *     displayName: 'Critical Alerts',
     *     fifo: false
     *   },
     *   namespace: 'prod',
     *   region: 'us-east-1'
     * });
     * ```
     */
    topicProps?: TopicProps;
    /**
     * The namespace of the application. This construct does NOT enable any alert notifications
     * in the DEV namespace by default unless forceDeploy is set to true.
     *
     * Common namespace values include: 'dev', 'test', 'stage', 'prod'
     *
     * This value is also passed to the Lambda function as an environment variable.
     *
     * @required
     */
    namespace: string;
    /**
     * AWS Region where the resources will be deployed, e.g. "us-east-1"
     * This value is passed to the Lambda function as an environment variable
     * and used for generating CloudWatch console links in alerts.
     *
     * @required
     */
    region: string;
    /**
     * Boolean indicating whether notification delivery to PagerDuty should be enabled.
     *
     * When set to true, this construct will expect to find a secret in SecretsManager
     * at /notifications/pagerduty (or a custom path if logicalClientId is specified)
     * during deployment. The secret should contain a JSON object with a "webhookUrl" property.
     *
     * PagerDuty integration uses direct HTTPS subscription from SNS to the PagerDuty webhook URL.
     *
     * @default false
     * @see https://powerschoolgroup.atlassian.net/wiki/spaces/INTDEVPORTAL/pages/65130497971/Solution+Layer+Path+Enable+Cloudwatch+Alarms+and+Notifications+For+Your+MFE
     */
    enablePagerDuty?: boolean;
    /**
     * By default, PagerDuty notifications are only triggered by Alarm Names that begin with "pd".
     * This allows teams to have a wide array of alarms that trigger Slack notifications,
     * while easily identifying a smaller subset of critical alarms which should trigger PagerDuty notifications.
     *
     * This property allows customizing the prefix used for filtering alarms for PagerDuty.
     *
     * @example
     * ```typescript
     * // This will trigger PagerDuty for alarm names starting with "critical-"
     * new AlertNotifier(this, 'Notifier', {
     *   enablePagerDuty: true,
     *   pagerDutyFilterString: 'critical-',
     *   namespace: 'prod',
     *   region: 'us-east-1'
     * });
     * ```
     *
     * @default "pd"
     */
    pagerDutyFilterString?: string;
    /**
     * Specifying a logical client ID will add a domain-level prefix to the secret locations referenced by this construct.
     *
     * Without a logicalClientId, the construct looks for webhook URLs at:
     * - /notifications/slack
     * - /notifications/pagerduty
     *
     * With logicalClientId "MyMFE", the construct will look for webhook URLs at:
     * - /notifications/MyMFE/slack
     * - /notifications/MyMFE/pagerduty
     *
     * This allows for different teams or services to have dedicated alert channels.
     */
    logicalClientId?: string;
    /**
     * Boolean indicating whether notification delivery to Slack should be enabled.
     *
     * When set to true, this construct will expect to find a secret in SecretsManager
     * at /notifications/slack (or a custom path if logicalClientId is specified)
     * during runtime. The secret should contain a JSON object with a "webhookUrl" property.
     *
     * Slack integration uses a Lambda function to transform and forward CloudWatch alerts to Slack.
     *
     * @default true (if both enableSlack and enablePagerDuty are undefined)
     * @see https://powerschoolgroup.atlassian.net/wiki/spaces/INTDEVPORTAL/pages/65130497971/Solution+Layer+Path+Enable+Cloudwatch+Alarms+and+Notifications+For+Your+MFE
     */
    enableSlack?: boolean;
    /**
     * Optional override of the default secret location for the Slack Webhook URL.
     * This can support extremely fine-grained webhook configurations, but is deprecated
     * in favor of the logicalClientId property.
     *
     * @deprecated Instead of using this as a "full override" we recommend teams use the logicalClientId property moving forward.
     */
    slackWebhookSecret?: string;
    /**
     * Optional force deployment, even if running in a dev environment.
     * By default, this construct will not deploy any notification resources in the 'dev' namespace,
     * but setting this to true will override that behavior.
     *
     * @default false
     */
    forceDeploy?: boolean;
    /**
     * Optional Lambda runtime version for the Slack notification Lambda function.
     * If not provided, defaults to DEFAULT_LAMBDA_VERSION (currently NODEJS_22_X).
     *
     * @default Runtime.NODEJS_22_X
     */
    runtime?: Runtime;
}
/**
 * AlertNotifier is a CDK construct that integrates CloudWatch Alarms with notification channels like Slack and PagerDuty.
 *
 * This construct creates or uses an existing SNS topic that CloudWatch Alarms can target, and configures
 * subscriptions to forward these notifications to Slack and/or PagerDuty based on configuration.
 *
 * Key features:
 * - Automatically creates an SNS topic or uses an existing one
 * - Supports multiple notification channels (Slack and PagerDuty)
 * - Flexible secret management with logical client IDs
 * - Configurable alarm filtering for PagerDuty notifications
 * - Disabled by default in dev environments (can be overridden)
 *
 * The AlertNotifier integrates with AWS Secrets Manager to retrieve webhook URLs securely.
 * Secrets should be JSON objects containing a "webhookUrl" property.
 *
 * For Slack integration, it deploys a Lambda function that formats and forwards alerts to Slack.
 * For PagerDuty integration, it configures a direct HTTPS subscription from SNS to PagerDuty's webhook.
 *
 * @example
 * ```typescript
 * // Basic usage with Slack notifications only
 * new AlertNotifier(this, 'AppAlerts', {
 *   namespace: 'prod',
 *   region: 'us-east-1',
 *   enableSlack: true
 * });
 *
 * // With both Slack and PagerDuty, using a logical client ID
 * new AlertNotifier(this, 'CriticalAlerts', {
 *   namespace: 'prod',
 *   region: 'us-east-1',
 *   enableSlack: true,
 *   enablePagerDuty: true,
 *   logicalClientId: 'MyService',
 *   pagerDutyFilterString: 'critical-'
 * });
 * ```
 */
export declare class AlertNotifier extends Construct {
    /**
     * The SNS topic that CloudWatch Alarms should target.
     * This topic will be created by the construct unless an existing topic is provided in the props.
     * CloudWatch Alarms should be configured to send notifications to this topic when they transition to ALARM state.
     */
    readonly alertTopic: Topic;
    /**
     * Creates a new AlertNotifier construct that integrates CloudWatch Alarms with notification channels.
     *
     * @param scope The construct scope (typically 'this' from a Stack or other construct)
     * @param id A unique identifier for this construct within its scope
     * @param props Configuration properties for this AlertNotifier
     *
     * @throws Error If secrets cannot be accessed or webhook URLs cannot be retrieved
     */
    constructor(scope: Construct, id: string, props: AlertNotifierProps);
}
