import { Options } from '@middy/http-cors';
export type OriginHandler = (incomingOrigin: string | undefined, options: Options) => string;
interface Dict<T> {
    [key: string]: T | undefined;
}
export declare const FORBIDDEN_DOMAIN = "@FORBIDDEN@DOMAIN@";
/**
 * A function to return the origin allowed. Assumes PS_IS_PRODUCTION is set
 * in environment.
 * @returns handler suitable for passing as `getHandler` of `@middy/http-cors`
 */
export declare function getOriginHandler(env: Dict<string>): OriginHandler;
export {};
