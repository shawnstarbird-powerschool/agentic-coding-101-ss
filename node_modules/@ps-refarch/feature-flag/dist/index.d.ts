/**
 * @ps-refarch/feature-flag
 *
 * A comprehensive feature flag management package that integrates with LaunchDarkly.
 * This package provides tools for defining, managing, and accessing feature flags in AWS
 * Lambda environments as well as CDK constructs for infrastructure deployment.
 *
 * The package supports both runtime operations (retrieving flag values in application code)
 * and deployment-time operations (configuring environments with LaunchDarkly).
 */
export * from './cdk/feature-flags';
export * from './process-feature-flags';
