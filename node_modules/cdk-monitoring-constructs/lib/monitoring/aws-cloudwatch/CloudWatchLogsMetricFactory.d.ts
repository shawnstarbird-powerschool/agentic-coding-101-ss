import { BaseMetricFactory, BaseMetricFactoryProps, MetricFactory } from "../../common";
export interface CloudWatchLogsMetricFactoryProps extends BaseMetricFactoryProps {
    /**
     * Name of the log group to monitor.
     */
    readonly logGroupName: string;
}
export declare class CloudWatchLogsMetricFactory extends BaseMetricFactory<CloudWatchLogsMetricFactoryProps> {
    private readonly dimensionsMap;
    constructor(metricFactory: MetricFactory, props: CloudWatchLogsMetricFactoryProps);
    metricIncomingLogEvents(): import("../../common").MetricWithAlarmSupport;
}
